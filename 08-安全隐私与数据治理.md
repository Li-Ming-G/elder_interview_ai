# 08｜安全隐私与数据治理

## 1. 目标

本项目会处理录音、家庭关系、人生经历、地点、时间、可能的健康和财务信息，必须默认按照高敏感度个人资料处理。

本文件定义：

- 授权；
- 权限；
- 传输和存储；
- 日志脱敏；
- 数据保存；
- 导出；
- 删除；
- 境外服务；
- 审计。

## 2. 数据分类

### 2.1 原始证据

- 授权录音；
- 访谈录音；
- 原始转录；
- 供应商原始响应。

### 2.2 修订和标记

- 修订文本；
- 说话人修正；
- 说话人校准 attempt、用户角色确认及跳过/失败；
- 说话人批量修正预览、执行与受影响片段成员关系；
- 重要原话；
- 敏感标记；
- 禁止使用；
- 待确认；
- 删除请求。

### 2.3 AI 生成数据

- 结构化记忆；
- 冲突；
- 问题建议；
- 工作记录；
- 风险判断。

### 2.4 系统数据

- 账号；
- 权限；
- 配置；
- 技术日志；
- 审计日志；
- 供应商调用指标。

## 3. 数据处理原则

1. 最小必要；
2. 目的明确；
3. 充分告知；
4. 可撤回；
5. 可追溯；
6. 原始资料不被覆盖；
7. 受限内容隔离；
8. 不默认用于模型训练；
9. 不跨项目混用；
10. 到期清理。

## 4. 授权

### 4.1 访谈前告知

必须说明：

- 会录音；
- 会转成文字；
- 会交给 AI 分析；
- AI 只向倾听员提供辅助；
- 数据会被保存；
- 谁可以访问；
- 保存目的；
- 可以停止、撤回和提出删除；
- 不会未经确认公开。

### 4.2 MVP 授权方式

支持录制口头授权。

必须保存：

- 授权文本版本；
- 授权方式；
- 授权音频；
- 授权时间；
- 操作人员；
- 授权状态。

### 4.3 正式商业化

正式收费前应升级为书面或电子授权，并由具备相应能力的人员审查实际文本和流程。

### 4.4 撤回

撤回后：

1. 授权状态变为 `revoked`；
2. 项目变为 `restricted`；
3. 停止新的 AI 分析；
4. 停止普通导出；
5. 进入后续处理和删除判断；
6. 保留必要审计记录。

撤回与物理删除不是同一个动作。

### 4.5 结束期间的撤权与原始证据保全

assignment 撤销、授权撤回、登录 session 过期或登出后，系统必须立即停止新 PCM、新录音对象、新 AI/ASR 任务和普通业务访问，但不得因此覆盖或静默丢弃撤销前已经产生的原始分片。已有 final drain 只允许处理 stop 前已接受的 PCM并固化确定态证据，不得扩大输入或派生新的 AI 任务。

服务端已接受 stop snapshot 时，冻结的 audio object、expected count 与逐片 commitment 构成唯一的 evidence-finalization 允许列表：

- 原操作者账号仍为 active 且重新认证后，即使 assignment 或授权随后失效，也只能补传允许列表中尚缺的分片、完成该 audio object 和查询最小结束 snapshot；
- 受限能力不得读取或下载正文，不得增加分片、改变 checksum/时间/MIME、继续 PCM、恢复采集、继续 AI 或访问其他项目资源；
- 每次使用必须审计 actor、session、audio object、sequence/action、request ID 和结果，不记录音频、正文、对象键或 token；
- 登录失效时不得匿名补传，不签发长期浏览器 bearer token；账号停用或无法重新认证时保留已保存事实并进入可观察的 interrupted/failed 处置，不放宽权限；
- 授权撤回不等于删除。已经产生的原始证据按授权/删除治理继续受限保存，物理清理必须走正式 deletion request。

尚未接受 stop snapshot 时，首次 stop 与 `finalize_interrupted` 必须重新验证最新授权仍有效且项目未受限；撤权后不得用客户端新提交的自由分片列表建立 finalization、commitments 或例外权限。即使 assignment 仍有效也必须拒绝，服务端只保留已经可靠接收的证据，并将/保持会话为 `interrupted` 等待有权处置。只有撤权前已经持久化的 stop snapshot 才能启用 commitment 范围内的受限补传。

采集 generation 尚未冻结 stop 时，授权撤回或 assignment 撤销必须在同一 project/session/audio 锁序内把 current `preparing|active` generation 与 session 置为 `interrupted`、停止新 PCM/分片/ASR/AI，并释放 producer。`report_interrupted` 是减权动作：重新认证且账号仍 active 的原操作者在 assignment 失效后可报告已经发生的中断，但不能 resume、创建新 generation、提交自由 commitments 或读取正文；账号 disabled 一律拒绝。浏览器 archive 仍是敏感原始证据，本轮只允许内部虚构数据；真实试点前必须具备可理解的本地查看/删除治理和验证。

## 5. 访问控制

角色：

### 倾听员

- 只能查看被分配项目；
- 可以进行访谈、修正和标记；
- 可以导出有权限项目；
- 不可查看其他倾听员项目；
- 不可查看全局密钥。

### 管理员

- 管理账号和供应商配置；
- 查看系统指标和错误；
- 默认不查看访谈正文。

### 数据管理员

- 处理导出和删除；
- 访问受限内容必须有业务原因；
- 所有访问进入审计。

原则：

- 默认拒绝；
- 最小权限；
- 重要操作二次确认；
- 管理操作强制审计。

### 5.1 身份与会话安全

- 本地账号密码使用 Argon2id，参数下限按 `02`；数据库仅保存密码哈希；
- 浏览器使用服务端不透明会话和 `HttpOnly` Cookie，不在 `localStorage`、URL 或普通响应正文保存访问令牌；
- production 会话 Cookie 使用 `__Host-elder_interview_session`、`Path=/`、`HttpOnly`、`Secure`、`SameSite=Strict` 且无 `Domain`；
- 所有状态变更浏览器请求（包括登录）校验允许的 Origin；安全 GET 在存在 Origin 时也校验；登录使用数据库持久化限流，已认证写操作还必须验证与当前会话绑定的 `X-CSRF-Token`；
- CSRF token 在登录响应中首次签发，已认证的 `/auth/csrf` 用于轮换；token 只保存在前端内存，权限变化时轮换，随会话撤销或过期失效；
- 登出、账号停用、权限安全事件和管理员撤销必须使相关会话失效；
- local/test 虚构身份只能通过显式 seed 创建；production 必须拒绝测试 seed、默认账号和默认密码；
- production 用户只由受控运维 CLI `user:create`、`user:set-password`、`user:disable`、`user:enable` 创建、交互式重置密码、停用或启用；CLI 参数 `--operator-ref` 映射到审计 `actor_reference`；密码隐藏输入并二次确认，相关动作必须审计；启用清空 `disabled_at` 但不得恢复历史会话；MVP 无公开注册和自助找回；
- production 登录限流摘要使用独立 pepper；未批准可信代理前只使用直接对端 IP，不信任客户端提供的转发头；
- 登录失败不得暴露账号是否存在；密码、会话原值、Cookie 和 CSRF token 不得进入技术日志或审计 metadata。

## 6. 通信安全

- 全站 HTTPS；
- WebSocket 使用 WSS；
- API 密钥只在后端；
- 前端不持有长期存储密钥；
- 不在 URL 中放个人内容；
- 上传和下载使用短期签名；
- 管理端和生产接口设置访问限制；
- 禁止明文传输音频和转录。

## 7. 存储安全

- 对象存储默认私有；
- 开启服务端加密；
- 数据库使用磁盘加密或等价措施；
- 备份同样受权限控制；
- 测试、预发布、生产使用不同存储空间；
- 测试环境不得复制真实访谈数据；
- 导出文件设置到期时间；
- 临时文件完成后清理。

## 8. 日志脱敏

日志不得记录：

- 完整录音；
- 完整转录；
- 授权正文音频；
- 完整模型输入输出；
- 身份证号；
- 电话号码；
- 家庭住址；
- API 密钥；
- 长期有效下载地址。

可以记录：

- project_id；
- session_id；
- actor_id；
- 错误码；
- 延迟；
- 字符数；
- token 数；
- 供应商请求 ID；
- 哈希；
- 操作类型。

## 9. AI 数据最小化

发送给 LLM 的内容必须经过上下文构建，不得默认发送完整历史录音或全部转录。

优先发送：

- 最近必要片段；
- 当前相关记忆；
- 问题历史；
- 边界；
- 必要基础信息。

不得发送：

- 其他项目内容；
- 已删除或待删除内容；
- 禁止继续处理的内容；
- 与当前任务无关的完整档案；
- 未经评估的敏感附件。

### 9.0 AI 过程记录访问

- 普通倾听员产品只得到当前安全问题投影；第一版不提供 memory/冲突/置信度或完整 provenance 图 UI；
- `MemoryContextReader` 与 `QuestionEvidenceReader` 只允许服务端在 actor 对项目仍有有效 assignment、授权和业务用途时调用，并且每次动态应用 boundary/deletion policy；
- 过程记录的诊断读取不得成为通用正文导出。被分配倾听员只可在明确复盘流程读取其项目中未受限的最小引用、版本、状态和结果；涉及 `restricted`、活动 deletion scope 或跨范围关系时只返回中性状态；
- 任何诊断、复盘、分页和内部 reader 都先验证 retention root 为 `active` 且未到期；`hidden|purging|cleanup_failed` 在物理清理完成前也不得返回正文或 membership。cleanup operator 只能按最小 root ID/状态/错误分类处理，不获得额外正文读取权；
- `admin` 默认只看运行指标，不看访谈或问题正文；`data_admin` 仅因删除/受限数据处置且填写业务原因时读取最小必要记录，所有读取审计；
- 服务间调用身份、用途、project/session scope 必须显式传递，禁止后台任务借系统身份跨项目扫描后把正文投影给用户。

### 9.1 已展示问题与硬安全边界

已展示问题作为历史展示事实保留，不等于其正文可以永远留在普通 UI：

- 普通说话人/文字/记忆修正、普通冲突或单独 `sensitive` 不要求自动撤下当前问题；旧快照从修正起不得参与未来生成、普通 current memory 或跨会话继承；
- `restricted`、`do_not_ask`、活动中的 project/session/segment deletion scope、授权撤回或资源访问权限失效时，当前问题正文必须立即从普通界面撤下，查询、事件 replay、刷新恢复、展示历史浏览和第二次访谈都不得重新带回；“上一个问题”只能读取通过当前安全投影的历史，不能成为绕过撤下的入口；
- 撤下后只显示“继续倾听”或“AI 暂不可用”，不得为了填补空白自动生成替代问题；
- current/history REST、历史 snapshot 定位、cursor 续页和 suggestion WS notification 都必须重新校验调用时权限与 policy。cursor、snapshot ID、旧幂等响应或 WebSocket replay 不授予持续读取权；actor 已无 session 访问权时返回 401/403，不用中性 DTO 暗示资源正文；
- `suggestion.presentation.changed` 事件和 replay 缓存只保存 session/snapshot ID、presentation revision、kind 与 change classification，不保存问题、原因、证据、withdrawal 细节或模型输出。客户端收到通知后必须重新读取 canonical REST projection；
- 历史页默认 20、最大 50，服务端一次批量重建整页安全裁决与 retention 状态，避免逐条 N+1 查询造成可观察的授权时序差异。撤下 item 只能返回中性占位；服务端内部为相似度比较使用的摘要/安全特征不得投影给调用者；
- 受限审计可以记录曾展示的 suggestion ID、job/模型/提示词/Schema/上下文版本、时间和撤下原因，不得在技术日志复制问题正文、完整模型输入或相关受限转录；
- 倾听员的最高现场判断权不能替代软件的权限、禁问、授权与删除义务。

## 10. 境外模型服务

若使用境外模型：

1. 明确供应商和处理区域；
2. 识别传输数据类型；
3. 减少传输范围；
4. 评估是否需要额外告知和单独同意；
5. 不上传完整原始录音；
6. 优先传输裁剪后的文字；
7. 配置禁止供应商用于训练；
8. 保留供应商和模型版本记录；
9. 允许后续替换为境内服务。

正式上线前应针对实际供应商完成合规审查。

## 11. 受限内容

四类 marker 按不同处理维度执行，不得统称为相同的“受限内容”：

| 类型 | 内容处理 | AI 处理 | 摘要与导出 | 查看与审计 |
|---|---|---|---|---|
| `sensitive` | 可继续最小化使用，但属于高谨慎信息 | 授权有效且任务必要时可进入许可内容上下文；问题风险至少提升一级 | 普通产出默认遮蔽正文，只显示存在和数量 | 被分配倾听员可主动展开；正文展开和导出需审计 |
| `restricted` | 原始证据保留，但停止普通后续使用 | 正文及派生记忆不得进入普通 AI 内容上下文 | 完全排除普通摘要和普通导出 | MVP 仅 `data_admin` 基于明确业务原因查看；不自动生成受限 AI 摘要 |
| `do_not_ask` | 约束未来追问，不自动删除或禁止使用既有内容 | 原文不发送；只发送人工确认的最小抽象禁区标签，并在输出后服务端过滤 | 既有内容是否导出由其他 marker 决定；抽象标签可进入“边界”区 | 原始证据沿用其自身敏感/受限等级；变更必须审计 |
| `deletion_request` | 删除工作流信号，不代表已经删除 | 立即停止相关内容的新 AI 处理；项目级请求停止整个项目 AI | 请求处理中拒绝普通导出；仅数据管理员可生成不含申请范围正文的删除处理证据包 | 仅数据管理员处理；完成后只保留最小必要删除审计 |

多个 marker 重叠时取限制并集，优先级为：

```text
deletion_request > restricted > do_not_ask > sensitive > ordinary
```

边界信息发送给模型时必须遵循 `07` 的三层上下文规则。权限无法确认、删除状态不明、边界服务故障或禁区标签不安全时，使用更严格的失败安全结果，不得通过发送原文绕过限制。

## 12. 数据保存期限

以下期限必须分别配置：

- 授权录音；
- 原始访谈录音；
- 原始转录；
- 修订转录；
- AI 建议；
- 结构化记忆；
- 导出文件；
- 技术日志；
- 审计日志；
- 备份。
- AI 过程记录、实际问题目录和 suggestion 展示/换题/撤下历史。

要求：

1. 项目创建时记录适用策略版本。
2. 到期任务可重试。
3. 清理失败需告警。
4. 审计日志按必要范围保留。
5. 具体期限正式上线前确定。

AI 过程记录和会后问题整理按项目的 `ai_retention_policy_version` 与 retention root 执行，不要求每个 child 自带 `expires_at`：

| retention root | owned 生命周期 | 不属于该 root 的内容 |
|---|---|---|
| `ai_job` | job scope/input membership、provider call、逐业务输出 derived row/dependency；automatic claim/resolution、question attempt/candidate、boundary candidate、generated session note、actual-question analysis/catalog/evidence/outcome、context snapshot/membership | 已展示 snapshot；人工/迁移记忆 |
| `question_display_snapshot` | 展示时复制的 question/reason、展示归属的 display/replace/withdraw event | 原 candidate future eligibility；actual-question catalog |
| `memory_retention_root` | 不隶属 AI job 的 human-confirmed/system-migration claim、resolution/member | automatic memory |

`actual_question_analysis` 及整版 catalog 由创建它的 `ai_job` 作为等价保留根；`question_evidence_event` 必须明确且唯一归属于产生它的 job 或当时 display snapshot。root 保存 `retention_policy_version`、`expires_at`、`retention_state`、hidden/cleanup attempt 状态；child 继承 root 生命周期，不保存会漂移的 policy/deadline。`boundary_candidate.expires_at` 仅是临时阻断的业务到期，不是 retention deadline。

root 的 `expires_at` 创建后取以下最早值：该业务类别由策略版本解析的期限、全部可识别来源证据的适用期限、被引用其他 retention root 的期限。具体天数仍在正式上线前由配置确定。若来源期限不能证明，普通读取与 future eligibility 失败关闭；技术日志使用独立且更短的期限，审计日志只保留合规所需最小事实。

到期/清理顺序固定如下：

1. 使用 `(root_kind, root_id, expires_at, retention_policy_version)` 作为 cleanup identity，原子将 root `active -> hidden`；普通查询、cursor、恢复和 replay 立即不可见；
2. 断开 current memory、current published catalog、current display 等 projection；反向查找跨 root dependency，把依赖输出/root 先置 hidden 或 future ineligible；
3. `hidden -> purging`，先删除或去标识化派生正文与跨 root membership，再按 owner 顺序清理 evidence/membership/event、业务输出、derived output/provider call/input scope，最后删除 root；
4. owned child 使用 `ON DELETE CASCADE` 或相同顺序的显式幂等删除；跨 root current/display/published 指针使用 `ON DELETE SET NULL` 或先显式 detach，禁止 `RESTRICT` 阻塞清理；expected dependency count/manifest 必须使缺行、空 FK 或剩余子集持续 ineligible；
5. 中断重试用同一 cleanup identity 从持久状态继续。失败置 `cleanup_failed`、告警并持续隐藏；不得回到 active 或恢复 eligibility；
6. 完成后只允许保留独立 pepper/HMAC 生成的不可逆 root scope 摘要、root kind、策略版本、到期/完成时间、cleanup request ID 与结果分类；不得保留可反查的 segment/question/memory membership、正文、memory value、对象键或 provider payload。

保存期内只保存复盘必要的 job/provider call 版本、状态、错误分类、耗时、token、membership 引用、digest/manifest、display/replace/withdraw 事件、actual-question 证据关系和 context snapshot；不复制完整转录、完整模型输入、供应商原始输出或受限正文。deletion scope 和授权/访问失效优先于保留期。active deletion scope 命中任一 `ai_job` input membership 时，shared provider call/scope 无法按单一 output 证明隔离，必须隐藏并清理整个 job root；correction/policy 失效仍可按逐业务输出最小单元执行。candidate job 清理只 detach snapshot FK；只有 deletion 也命中 snapshot 自身复制正文/证据时才另行隐藏并清理 snapshot root。actual-question dependency 命中时撤下整版 analysis catalog，不做部分保留。

## 13. 导出安全

- 导出前校验权限；
- 普通和受限内容分开；
- 下载地址短期有效；
- 导出文件私有；
- 记录请求人、时间和项目；
- 导出完成后按期限清理；
- 撤回授权后阻止普通导出；
- 删除请求处于 `pending_verification`、`verified`、`processing` 时拒绝普通导出；仅 `data_admin` 可生成不含申请范围正文的删除处理证据包，并只在该证据包 manifest 中标记请求状态与范围标识。`rejected`、`withdrawn` 后按其他限制重算；`completed` 后已删除内容不可恢复。

## 14. 删除流程

1. 接收长者或客户请求；
2. 创建删除申请；
3. 校验身份和项目；
4. 按 scope 建立限制：`project` 将项目置为 `restricted`，`session` 或 `segment_range` 只限制对应内容和派生记忆；
5. 按相同 scope 停止新的 AI 调用，并对所有 scope 阻止项目级普通回顾和普通导出；
   - 停止包含入队前、供应商调用前、结果持久化前和展示前的 scope 重检；命中的排队任务取消，无法取消的在途调用结果丢弃且不得写入任何 AI 派生表；
6. 请求进入 `processing` 后清理范围内在线对象存储；
7. 清理范围内数据库业务数据；
8. 清理相关临时导出；
9. 登记不可逆清理清单哈希和备份延迟清理时间；
10. 上述在线清理全部成功后才将请求置为 `completed`；project scope 同时把项目置为不可恢复的 `deleted`；
11. 保留最小必要删除审计并反馈完成状态。

删除操作必须幂等。

project scope 进入 `restricted` 时必须保留原项目状态；申请 `rejected` 或经核验 `withdrawn` 后，仅在授权撤回和其他正式限制原因都不存在时恢复原状态。`completed` 不恢复，项目已经是 `deleted`。session/segment scope 不改变项目状态。

不得用简单软删除冒充已完成物理清理。

普通项目软删除只设置 `deleted_at`，可在未进入隐私删除流程时恢复；它不代表物理清理。completed project scope 删除请求及其 `status=deleted` 不可通过普通 restore 恢复。

最小 tombstone 规则：project scope 只保留无身份业务字段的项目 ID/删除状态/最小时间；session/segment FK 在 completed 清理事务中置空，只保留使用独立 pepper 生成的不可逆 scope 摘要、transition 和最小删除审计。不得保留 marker note、片段关系、正文、对象键或可恢复派生内容。

segment_range 申请创建时必须冻结 marker 当时的 start/end segment ID；后续 marker 修改或软删除不能缩小 AI 阻断和清理范围。处理完成后清空端点快照，只保留覆盖原始端点的不可逆 scope 摘要。

AI 与删除并发固定四次检查：输入冻结前、供应商调用前、结果写回前、每次展示/查询前。冻结和写回与 deletion producer 使用相同 project/session 锁序；供应商调用不持锁。删除先提交时，AI 取消或撤下；AI 先冻结但删除随后提交时，写回必须发现 revision/scope 漂移并丢弃结果；AI 已写回后删除提交时，动态查询立即隐藏相应 retention root/业务输出并由删除 worker 按本节顺序清理派生关系。

当前 runtime 尚无 deletion request producer 和统一 `DeletionScopeReader`。CON-023 在 DEV-008 实现 producer/read model、C2 接入及上述并发/清理测试前保持 `OPEN / NOT IMPLEMENTED / NOT VERIFIED`；禁止用永远返回“无活动 scope”的 no-op guard 声称已满足本节。

## 15. 审计事件

至少记录：

- 登录和权限失败；
- 项目访问；
- 授权创建和撤回；
- 服务条款修改；
- 原始转录修正；
- 说话人修正；
- 正式流说话人校准开始、确认、失败、跳过与重试；
- 单段/批量说话人修正预览和执行，以及由此产生的派生失效 revision；
- 受限标记；
- 受限内容访问；
- 导出创建和下载；
- 删除申请和完成；
- 管理员配置变更；
- 供应商密钥轮换。
- 会话 stop/recover、结束状态转换和撤权后的受限 evidence-finalization 补传。
- question generation/manual-next 的 request/attempt/publication 分类、presentation revision/display sequence 与 hard withdrawal；不记录问题正文、reason、内部 selection/similarity score、cursor 或完整响应。

## 16. 安全事件处理

发生以下情况时需登记事件：

- 录音公开访问；
- 密钥泄露；
- 越权访问；
- 错误导出；
- 删除未完成；
- 供应商异常保存数据；
- 日志写入个人正文；
- 测试环境使用真实数据。

处理流程：

1. 立即限制访问；
2. 保留必要证据；
3. 识别受影响范围；
4. 修复配置或代码；
5. 轮换密钥；
6. 记录处置；
7. 增加回归测试。

## 17. 上线安全门禁

正式试点前必须确认：

- 无真实密钥进入仓库；
- 对象存储不是公开读；
- 倾听员无法访问未分配项目；
- 撤回后停止 AI；
- 受限内容不进入普通导出；
- 日志不包含完整转录；
- 下载地址会过期；
- 删除流程可验证；
- 备份策略明确；
- 实际授权文本经过审查。
