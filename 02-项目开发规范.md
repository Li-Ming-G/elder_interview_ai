# 02｜项目开发规范

## 1. 规范目标

本文件定义全项目统一的工程基线。

专项业务、数据、接口、音频、AI、安全和测试细节分别由 `03` 至 `09` 规定。本文件不重复专项契约，只规定所有模块共同遵循的实现方式。

## 2. 架构原则

MVP 采用：

> 模块化单体应用

不拆分微服务。

原因：

- 当前业务规模小；
- 产品仍在验证；
- 模块间存在大量事务关联；
- 微服务会增加部署、调试和数据一致性成本；
- 当前重点是验证访谈链路和 AI 问题质量。

## 3. 正式技术基线

以下方案是 MVP 的已批准工程基线。依赖补丁版本由锁文件固定；升级主版本或改变数据访问、认证、测试与运行时边界时，必须单独评估并记录决定。

| 层级 | 方案 |
|---|---|
| 前端 | React + TypeScript + Vite |
| 后端 | NestJS + TypeScript |
| 数据库 | PostgreSQL |
| 缓存和任务 | DEV-001 不启用 Redis/BullMQ；出现首个真实后台任务消费者后再评估 |
| 对象存储 | 腾讯云 COS 或兼容 S3 的对象存储 |
| 部署 | Docker Compose |
| 反向代理 | DEV-001 不启用；进入 staging 部署任务时再加入 Nginx |
| 日志 | JSON 结构化日志 |
| ASR | 供应商适配器 |
| LLM | 供应商适配器 |

### 3.1 运行时与工作区

- Node.js 支持范围：`>=24.11.0 <25`；本地、CI 和开发镜像固定 `24.18.0`；
- pnpm 支持范围：`>=11.15.1 <12`；根 `package.json` 的 `packageManager` 精确固定 `pnpm@11.15.1`；
- 使用 `pnpm-workspace.yaml` 管理 `apps/*` 和 `packages/*`；
- 不引入 Nx、Turborepo 或第二种包管理器；
- TypeScript 使用 ESM、`strict`，稳定补丁版本由锁文件固定。

### 3.2 数据访问与迁移

- 使用 Prisma ORM 7、Prisma Migrate 和 PostgreSQL 驱动适配器；
- Prisma 只能通过后端数据访问服务或模块服务使用，业务模块不得跨模块直接操作对方模型；
- 迁移历史和生成它的 Schema 必须进入 Git；
- PostgreSQL 原生约束、部分索引、触发器等 Prisma Schema 无法完整表达的能力，可以使用经过审查的迁移 SQL；
- 恢复策略以空库前向迁移、备份恢复和修复迁移为准，不宣称依赖自动 down migration；
- 集成测试必须使用 PostgreSQL，不能用 SQLite 替代正式数据库语义。

### 3.3 测试工具

- 单元测试：Vitest；
- API 集成：Vitest + `@nestjs/testing` + Supertest + 隔离 PostgreSQL 测试库；
- 前端组件：Vitest + React Testing Library；
- 浏览器 E2E：Playwright；DEV-001 先用 Chromium 建立主链路烟雾门禁；
- 覆盖率报告必须生成，但 DEV-001 不设无证据的仓库总百分比；配置、会话、密码、权限和资源隔离等安全决策分支必须有自动测试。

### 3.4 CI 与权威命令

- 根 `package.json` 脚本是本地和 CI 的唯一门禁入口，CI 不复制内部命令；
- 默认提供 GitHub Actions 适配器，但在远端托管平台确认前，不宣称远端门禁已经启用；
- 若改用其他 CI，只替换工作流外壳，不改变根脚本和验收门禁；
- 第三方 CI Action 必须固定到不可变提交 SHA。

### 3.5 认证与权限基础

- 同源 Web 使用服务端不透明会话，不把访问令牌保存在浏览器 `localStorage`；
- 本地账号密码使用 Argon2id，最低参数为 19 MiB 内存、2 次迭代、并行度 1，禁止明文或可逆存储；
- 会话 ID 使用至少 256-bit 安全随机值，数据库只保存 SHA-256 哈希；默认空闲过期 30 分钟、绝对过期 12 小时，分别由 `AUTH_SESSION_IDLE_TTL_MINUTES`、`AUTH_SESSION_ABSOLUTE_TTL_HOURS` 配置；
- production Cookie 名为 `__Host-elder_interview_session`，必须设置 `HttpOnly`、`Secure`、`SameSite=Strict`、`Path=/` 且不得设置 `Domain`；local/test HTTP 使用 `elder_interview_session`，其余属性不降低；
- 所有状态变更浏览器请求（包括登录）校验允许的 `Origin`；安全 GET 在存在 `Origin` 时也必须校验，已认证写请求还必须验证与当前会话绑定的 `X-CSRF-Token`；登录另以数据库持久化限流防爆破；
- CSRF token 在登录响应中首次签发，也可通过已认证的 `GET /auth/csrf` 轮换，只保存在前端内存；权限变化时轮换，登出、会话撤销或过期时失效；
- production 必须配置独立的 `AUTH_LOGIN_THROTTLE_PEPPER`；默认只使用直接对端 IP，不信任 `X-Forwarded-For`，直到部署任务明确批准可信代理列表；
- 删除审计范围摘要使用独立 `DELETION_AUDIT_PEPPER` 生成 HMAC-SHA-256；不得与登录限流 pepper、会话值或供应商密钥复用；
- AI retention cleanup 的不可逆 root/request 摘要使用独立 `AI_RETENTION_CLEANUP_PEPPER` 生成 HMAC-SHA-256；不得复用登录限流、删除审计、会话或供应商密钥；
- 角色 Guard 只做粗粒度门禁，项目分配和受限内容仍需服务层逐资源授权；
- local/test 可通过显式 seed 创建虚构测试身份；production 必须拒绝测试 seed 和默认账号；
- production 身份只由受控运维 CLI `user:create`、`user:set-password`、`user:disable`、`user:enable` 创建、重置密码、停用或启用；`user:enable` 清空 `disabled_at` 但不恢复旧会话；调用者显式传入非秘密 CLI 参数 `--operator-ref`，其值写入审计 `actor_reference`；密码必须通过交互式隐藏输入并二次确认，不得经命令参数、环境变量或日志传递；MVP 不提供自助注册和找回；
- 正式身份提供方未来通过认证适配器替换登录入口，不在 MVP 自制 access/refresh JWT 体系。

### 3.6 延后基础设施

- DEV-001 的 Docker Compose 只编排 PostgreSQL 开发库和隔离测试库；
- Redis/BullMQ 在导出、删除或 AI 后处理出现首个真实队列消费者时，通过独立任务和 ADR 引入；
- Nginx 在 staging/TLS/WebSocket/上传限制可以一起验证的部署任务中引入；
- 不创建没有消费者的空队列抽象或空反向代理配置。

MVP 不使用：

- Kubernetes；
- Kafka；
- Elasticsearch；
- 独立向量数据库；
- 服务网格；
- 多数据中心部署。

## 4. 推荐仓库结构

```text
/
├── apps/
│   ├── web/
│   └── api/
├── packages/
│   ├── contracts/
│   ├── config/
│   ├── eslint-config/
│   └── shared/
├── infra/
│   ├── docker/
│   └── nginx/
├── docs/
│   ├── agent/
│   └── contracts/
├── scripts/
├── tests/
├── AGENTS.md
├── 00-项目说明与执行入口.md
├── 01-产品需求文档.md
├── 02-项目开发规范.md
├── 03-业务流程与交互规范.md
├── 04-数据模型规范.md
├── 05-API与实时事件契约.md
├── 06-实时音频与转录规范.md
├── 07-AI访谈引擎规范.md
├── 08-安全隐私与数据治理.md
├── 09-测试与验收规范.md
├── 10-研发协作与交接规范.md
├── .gitignore
├── docker-compose.yml
└── package.json
```

本项目采用单仓库和 pnpm workspace。

## 5. 后端模块边界

```text
AuthModule
ElderProjectModule
ServiceTermModule
ConsentModule
InterviewSessionModule
AudioModule
AsrModule
TranscriptModule
MemoryModule
QuestionEvidenceModule
InterviewCopilotModule
MarkerModule
ExportModule
AuditModule
ProviderConfigModule
```

约束：

1. 业务模块不得直接调用第三方 SDK。
2. 第三方 SDK 必须封装在适配器中。
3. 模块之间通过明确的服务接口协作。
4. 不允许跨模块直接修改对方数据表。
5. 公共 DTO、事件和枚举放入 `packages/contracts`。
6. 模块不能通过复制相同逻辑绕过公共契约。

`MemoryModule` 单一拥有 memory claim/resolution、current reader 与 eligibility；`QuestionEvidenceModule` 单一拥有 generation/display/actual-question 证据历史；`InterviewCopilotModule` 只编排 AI job 与调用二者的正式 service seam。DEV-006/007 不得跨模块直写对方表或复制 question history。

## 6. 三条核心链路解耦

### 6.1 录音链路

负责保存原始音频。

### 6.2 ASR 链路

负责实时转录。

### 6.3 AI 链路

负责结构化记忆和追问建议。

强制规则：

- AI 失败不能中断录音；
- AI 失败不能中断转录；
- ASR 失败不能中断原始录音；
- 数据库故障时优先保护已采集音频；
- 前端必须分别显示录音、ASR、AI 和上传状态。

## 7. 供应商适配器

业务层不得使用供应商原始对象。

推荐接口：

```ts
interface AsrProvider {
  createSession(config: AsrSessionConfig): Promise<AsrStream>;
  closeSession(providerSessionId: string): Promise<void>;
}

interface LlmProvider {
  generateStructured<T>(
    request: StructuredRequest<T>
  ): Promise<T>;
}

interface ObjectStorageProvider {
  createMultipartUpload(objectKey: string): Promise<UploadHandle>;
  uploadPart(
    handle: UploadHandle,
    part: Buffer
  ): Promise<UploadedPart>;
  completeUpload(handle: UploadHandle): Promise<StoredObject>;
}
```

适配器必须负责：

- 供应商签名；
- 原始错误转换；
- 超时和重试；
- 返回对象标准化；
- 供应商请求 ID 记录；
- 敏感配置隔离。

## 8. TypeScript 规范

1. 开启 `strict`。
2. 禁止使用无理由的 `any`。
3. 外部输入先校验再进入业务层。
4. 枚举和状态使用明确联合类型或枚举。
5. 公共函数必须声明返回类型。
6. 禁止静默吞掉异常。
7. 时间统一使用带时区的 ISO 8601。
8. 金额不得使用浮点数直接计算，使用最小货币单位或精确数值类型。
9. 数据库 ID 使用 UUID。
10. WebSocket 事件必须携带 `schema_version`。

## 9. API 规范

1. REST API 统一使用 `/api/v1`。
2. WebSocket 使用独立命名空间。
3. 所有写接口必须进行输入校验。
4. 关键写操作必须支持幂等。
5. 错误返回使用统一格式。
6. 不在错误消息中泄露密钥、供应商原文和个人信息。
7. 分页接口必须明确排序规则。
8. 状态变更接口必须校验前置状态。
9. API 契约变更必须同步更新 `05`。
10. 前后端不得各自维护一套不同枚举。

推荐错误格式：

```json
{
  "code": "CONSENT_REQUIRED",
  "message": "当前项目尚未取得有效授权",
  "request_id": "uuid",
  "details": {}
}
```

## 10. 数据库规范

1. 所有表必须包含明确主键。
2. 重要业务表保留 `created_at` 和 `updated_at`。
3. 软删除使用 `deleted_at`。
4. 状态字段必须受枚举约束。
5. 数据库迁移文件进入 Git。
6. 禁止在生产环境手工修改表结构。
7. 原始数据和修订数据必须分列保存。
8. JSONB 只用于结构确实可能演进的数据。
9. 高频查询字段必须建立索引。
10. 跨表删除必须符合 `08` 的数据治理规则。
11. 业务事务必须明确边界。
12. AI 生成结果不得覆盖人工确认值。

## 11. 配置管理

配置按环境区分：

- local；
- test；
- staging；
- production。

题库受控命令的部署权限只读取启动时校验过的 `APP_ENV`：`local|test` 可承载明确的
`internal_demo` release scope，`staging` 视为正式内部试用环境，`production` 视为生产环境。
命令行参数、CSV 字段、文件名或操作者声明都不能覆盖该部署事实；`staging|production`
必须拒绝 synthetic fixture 的 validate/import/activate/retire 和 runtime reader。

规则：

1. 密钥只通过环境变量或密钥管理服务注入。
2. `.env` 不提交真实密钥。
3. 配置启动时进行完整校验。
4. 关键阈值配置化，不写死在业务代码。
5. ASR、LLM、对象存储必须可替换。
6. 数据保存期限必须配置化。
7. 提示词和 JSON Schema 必须有版本号。

## 12. 日志和审计

### 12.1 技术日志

使用 JSON 结构化日志，至少包含：

- request_id；
- project_id；
- session_id；
- actor_id；
- module；
- event；
- error_code；
- latency_ms；
- provider_request_id；
- timestamp。

不得记录：

- 完整录音；
- 完整转录；
- 完整模型输入；
- API 密钥；
- 长期有效下载地址。

### 12.2 审计日志

授权、撤回、修改、删除、导出、权限变更必须进入审计日志。

审计日志与普通技术日志分开管理。

## 13. 错误与重试

1. 所有外部调用必须设置超时。
2. 只对可重试错误进行重试。
3. 重试必须有限次并采用退避策略。
4. 重复请求必须通过幂等键去重。
5. AI 结构化输出失败最多自动修复或重试一次。
6. 上传分片失败可单独重试。
7. 重试失败必须留下可定位的错误记录。
8. 不得因为后台任务失败而伪造成功状态。

## 14. 前端规范

1. 访谈工作台优先保证稳定和低操作负担。
2. 实时状态必须清晰，不以隐藏 toast 代替持续状态。
3. 中间态和确定态转录必须视觉区分。
4. 原始文本和修订文本不能混为一个字段。
5. 危险操作需要二次确认。
6. 长时间页面必须避免内存持续增长。
7. 浏览器刷新和断线需要恢复提示。
8. 不在前端保存供应商密钥。
9. 关键操作按钮必须防重复提交。
10. 可访问性至少满足键盘操作和清晰状态文本。

## 15. Git 与分支

推荐：

- `main`：可发布代码；
- `develop`：集成分支；
- `feature/<task-id>-<name>`：功能分支；
- `fix/<task-id>-<name>`：修复分支。

提交要求：

1. 一个提交只解决一个清晰问题。
2. 提交消息包含任务编号。
3. 公共契约变更必须单独提交。
4. 禁止提交真实密钥和个人访谈数据。
5. 合并前必须通过格式检查、类型检查和相关测试。
6. 未完成代码不得通过注释伪装为已完成。

## 16. 代码审查

必须检查：

- 是否超出当前范围；
- 是否违反数据不可覆盖原则；
- 是否处理失败和重试；
- 是否存在密钥泄露；
- 是否补充测试；
- 是否破坏接口契约；
- 是否引入无必要依赖；
- 是否更新文档和任务状态。

## 17. 测试基线

每个任务至少包含：

- 正常路径；
- 权限失败；
- 状态前置失败；
- 外部服务失败；
- 幂等或重复提交；
- 数据保存和审计检查。

详细发布门禁见 `09-测试与验收规范.md`。

## 18. 文档同步规则

| 变更 | 必须更新 |
|---|---|
| 产品范围、页面或业务规则 | `01`、`03` |
| 技术栈、模块或工程约束 | `02` |
| 表、字段、状态枚举 | `04` |
| REST、WebSocket、幂等与导出契约 | `05` |
| 音频、ASR、说话人规则 | `06` |
| AI 上下文、提示词、Schema、评测 | `07` |
| 授权、权限、保存、删除 | `08` |
| 验收指标和发布门禁 | `09` |
| 协作规则 | `10` |
| 实际任务状态与交接 | `docs/agent/` |

代码与文档冲突时，任务不得标记完成。

## 19. LLM Provider Runtime V1 技术基线

1. TypeScript 应用内多供应商适配层采用 Vercel AI SDK Core；供应商通过各自 provider package 或经单独审查的 provider-compatible adapter 直接连接厂商 endpoint。不得注册 `gateway`，不得把模型 ID 字符串交给 AI Gateway，也不部署 LiteLLM。
2. `createProviderRegistry` 只承担进程内 provider/model 查找；正式访谈的 coordinator 在冻结 attempt 时解析唯一 active binding。一次 attempt 不得调用第二 provider/model；不存在自动 fallback、provider order 或 shadow call。
3. AI SDK 每次调用必须显式设置 `maxRetries: 0`。SDK 默认重试不得与项目重试叠加；项目既有共享 8 秒绝对 deadline、primary/最多一次完全同输入 `same_input_retry` 是唯一重试真相。每次调用接收由剩余 deadline 派生的 `AbortSignal`，超时或取消后 late result 无写回资格。
4. structured output 使用正式 Output Schema；SDK 的 `Output.object`/JSON Schema 只负责供应商结构化输出接缝，既有服务端 Schema、ID/subset、重复、安全和 writeback 校验继续是发布门禁。Prompt 不复制或扩展 Schema 字段；需要新字段/枚举时先变更正式 Context/Output Schema。
5. registry、endpoint origin、data region、provider/model/model-config、secret reference 与 data-class allowlist 只由服务端配置注入并启动时验证。JSON Schema 通过后还必须执行 `llm-provider-registry-semantics-v1`：provider/model/config identity 唯一，active binding 对每层 exactly-one 解析，endpoint/region/secret/environment/data-class membership 全成立，canonical config digest 重算一致；任一缺失、重复、歧义或不匹配 fail closed。`local|test` deterministic fake 不得回落到 production。
6. 截至 2026-08-14 的官方核验基线为 `ai@7.0.65`、Node `>=22`、Apache-2.0；本 SPEC 不增加依赖。后续实现必须在独立 runtime PR 中重新核验并对 `ai` 和实际选定的 provider package 使用精确版本（无 `^`/`~`），由 `pnpm-lock.yaml` 固定完整传递依赖；升级、provider package 增删或许可证变化均需显式复审。
7. invocation/receipt 必须拆分 requested binding model、observed response model+source、provider request ID+`provider|unavailable`、SDK response ID+`provider_origin|sdk_generated|unknown|unavailable`，不得共用模糊 model/ID 字段。model-config 使用 `llm-model-config-v1` 完整 manifest 与 canonical digest；adapter receipt 还记录 package versions、token、latency、region、direct mode、sanitized warning 和 `as_requested|diverged|unknown`。warning/unknown 的横评结果不得标为相同有效配置。
