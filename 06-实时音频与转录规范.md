# 06｜实时音频与转录规范

## 1. 目标

建立一条可恢复、可追溯、互相解耦的实时音频链路：

```text
原始录音保存
+
实时 ASR 转录
+
说话人角色映射
```

首要原则：

> 原始录音优先于实时转录，实时转录优先于 AI 辅助。

## 2. 总体架构

前端从同一个麦克风 `MediaStream` 生成两条链路。

```text
麦克风
 ├─ AudioWorklet
 │    └─ PCM 音频帧
 │         └─ 业务 WebSocket
 │              └─ 后端 ASR Adapter
 │                   └─ ASR 供应商 WebSocket
 │
 └─ MediaRecorder
      └─ WebM/Opus 分片
           └─ HTTPS 上传
                └─ 对象存储
```

## 3. 录音链路

### 3.0 授权音频与访谈音频

- 口头授权音频在正式访谈 start 之前采集，归属项目级 `audio_object(purpose=consent)`，不要求已有有效授权，也不得混入正式访谈 manifest；
- 正式访谈音频归属 `audio_object(purpose=interview)`，必须绑定同项目的 session，并且只能由原子 start 创建；session 处于允许录音/收束状态时才可上传，独立 audio init 仅用于 consent 等非 interview 用途；
- 两种用途共享不可变分片、checksum、幂等上传、私有保存和 manifest 校验能力，但授权对象不能冒充访谈对象，访谈对象也不能作为口头授权证据；
- 内部原型可用人工生成的虚构音频和本地私有存储适配器验证。真实试点前仍须验证对象存储私有性、加密、签名上传、保留期限和真实口头授权交互。

### 3.1 浏览器采集

推荐：

- `getUserMedia` 获取单一麦克风流；
- `MediaRecorder` 负责归档录音；
- 固定时间切片；
- 每片上传后释放不再需要的内存；
- 不把完整会话长期保存在浏览器内存。

正式访谈只获取一条 MediaStream：MediaRecorder 负责原始 archive，`createMediaStreamSource` 把同一流送入 AudioWorklet；不得为实时转录再次调用 `getUserMedia`。原始录音先启动并写 active checkpoint，再启动 AudioWorklet/业务 WebSocket；后两者失败不得停止 MediaRecorder。

session 级 `InterviewCaptureController` 必须位于准备页、工作台和结束状态的路由组件之上，拥有正式流、recorder、archive、delivery、upload pump、realtime transport 和所有稳定 ID。页面卸载或同 URL 状态切换不得销毁仍需收束的 controller。

### 3.2 分片要求

每个分片包含：

- 音频对象 ID；
- 项目 ID，以及访谈用途下的会话 ID；
- 顺序号；
- 起止时间；
- MIME 类型；
- 文件大小；
- 校验值；
- 上传状态；
- 重试次数；
- 对象存储路径。

分片顺序号在同一会话内唯一。

### 3.3 上传要求

- HTTPS 上传；
- 支持幂等；
- 单片失败可独立重试；
- 重试不产生重复对象；
- 上传完成后回写校验值；
- 完成授权音频或结束访谈音频对象时生成 manifest；
- 检查分片是否连续。

服务端 ACK 只能在二进制已可靠写入私有存储、checksum/size 已核对且数据库记录为 `uploaded` 后返回。complete 必须重新读取存储对象验证，不得只相信客户端或数据库元数据。

### 3.4 本地缓冲

网络短时中断时：

- 暂存尚未上传的分片；
- 网络恢复后按顺序补传；
- 设置最大本地缓冲限制；
- 达到限制时提示倾听员安全结束；
- 不得静默丢弃。

优先使用浏览器可持久化能力保存待传分片，具体实现由研发评估，但必须经过崩溃恢复测试。

本浏览器 archive 与 delivery queue 是两个生命周期：每个 Blob 先以不可变元数据/checksum 写入 archive 一次，delivery 只引用它。服务端 ACK 只能删除待交付标记，不能删除 archive Blob。“本浏览器备份完整”表示 `0..N-1` archive Blob 与元数据、checksum、时间连续性全部存在；它不是永久备份声明。

IndexedDB canary 失败不得开始。可读取 storage estimate 时，内部 MVP 建议至少 64 MiB 可用；剩余估算不高于 16 MiB、达到会话配置上限或实际写失败时触发 `local_archive_failed` 安全中断。阈值必须可配置，不得作为永久产品承诺。

### 3.5 持久化上传作业

可靠补传必须把上传过程作为独立于页面实例的持久化作业，不能只保存 Blob。每个作业对应一个 audio object，并至少保存：

- 本地稳定作业 ID、project、purpose、session 和 MIME；
- init request ID，以及成功后的服务端 audio object ID；
- 每个 sequence 的稳定 upload request ID；
- complete request ID；
- 录制停止时冻结的 expected chunk count；
- `recording|uploading|completing|complete|failed` 作业状态和最小错误码。

网络请求前先持久化对应 request ID；刷新后把遗留的 `uploading|completing` 视为可安全重试，复用原 request ID。服务端 chunk ACK 必须逐字段匹配 audio object、sequence、起止时间、size、checksum、MIME 和 uploaded 状态，全部一致后才可删除 delivery pending；本浏览器 archive Blob 保留。expected chunk count 由录制结束时的序号高水位冻结，不得从 ACK 后剩余 delivery 数量推导。

探索期只实现有限重试、网络恢复或显式恢复触发，不做不可观察的无限后台重试，不引入 Service Worker 或新队列依赖。IndexedDB 升级必须使用前向迁移并保留现有分片、sequence 和 timeline 高水位。

正式 interview upload job 在 start 前由浏览器持久化，在原子 start 响应后绑定服务端创建的唯一 audio object；不得再调用 interview audio init。start/chunk/complete/stop 的 request ID 均在请求前持久化；只有一个顺序 upload pump。响应丢失、刷新或网络边缘恢复时复用同一 session/job/object/request ID。init request ID 只属于 consent 等允许独立初始化的非 interview 对象。

浏览器使用 session 级锁保证同一时刻只有一个 tab 拥有 controller。当前 MVP 不承诺跨浏览器或跨设备接管。刷新/崩溃后不自动请求麦克风；恢复同一 job、object、archive high-water 和 request ID，先报告中断，再由用户选择下一 generation 或安全结束。

### 3.6 实时 PCM 与原始时间轴

AudioWorklet 输出单声道 16 kHz、signed 16-bit little-endian、每帧 100 ms/3200 bytes PCM；不使用 ScriptProcessor，也不在主线程持续重采样。同一 generation 的 PCM sequence 从 0 单调递增；显式 resume 使用新 audio stream ID 和新 sequence，并把服务端 generation `timeline_offset_ms` 带入事件映射。原始 MediaRecorder archive sequence 和 timeline 跨 generation 延续高水位，不插入伪造静音。

实时 transport 最多保留 20 个未 ACK 帧（约 2 秒）。超出只降级实时链路，不把短 PCM 缓冲当作原始录音或崩溃恢复。中断 gap 通过 generation 边界和时间偏移明确表达。

## 4. ASR 音频链路

### 4.0 供应商中立适配器与探索期夹具

业务层只接收标准化 ASR 结果，不直接依赖供应商 SDK 或 payload。适配器输出至少包含：

- `interim|final` 类型；
- 会话内稳定且包含供应商流命名空间的 `ingest_key`；
- 可选 provider segment ID 与 speaker ID；
- 非负整数起止毫秒；
- 文本；
- `realtime|backfill|fixture` 来源；
- 受大小限制的最小 provider payload。

DEV-004A 使用只存在于 local/test 组合根的确定性 fake adapter 验证 final 落库、interim 不落库、幂等重放和故障隔离。fixture 不得暴露为 HTTP/WebSocket 写接口，不接真实供应商、不读取真实密钥或真实访谈资料。最终供应商未选择不阻塞该 seam；真实 provider、音频格式和重连行为由后续任务接入。

### 4.1 AudioWorklet

职责：

- 从麦克风流获取音频；
- 重采样到供应商要求；
- 转换为单声道；
- 转换为 16-bit PCM；
- 按固定帧长发送；
- 控制发送节奏；
- 监测音量和静音。

不得在主线程执行持续高成本音频处理。

### 4.2 业务 WebSocket

浏览器不直接持有 ASR 密钥。

链路：

```text
浏览器 WebSocket
  ↓
业务后端
  ↓
供应商 WebSocket
```

后端负责：

- 鉴权；
- 校验会话状态；
- 创建供应商会话；
- 签名；
- 转发音频帧；
- 标准化供应商结果；
- 保存供应商请求 ID；
- 重连和错误转换。

DEV-004B1 先验证内部合成数据的服务端协议核心，DEV-004B2 再接真实 Chromium 客户端。B1/B2 固定使用：

- 静态 `/ws/interviews`，upgrade 校验 Origin 与 session Cookie；
- 5 秒内首个 `session.join` 完成 session-bound CSRF、assignment、项目/授权/会话状态和单 producer 门禁；
- JSON + base64 的 16 kHz、单声道、16-bit little-endian PCM，每帧 100 ms / 3200 raw bytes；
- audio sequence 从 0 连续递增，相同帧重放幂等、冲突或跳号失败关闭；
- 客户端最多 20 个未 ACK 帧或 64000 raw bytes，背压只暂停 ASR 帧发送，不停止 MediaRecorder 或原始音频上传；
- 服务端事件在 runtime stream 内严格递增，final 先落库再发布；
- 最近 512 个事件或 5 分钟的进程内恢复窗口；不承诺跨进程恢复。

所有 B1 消息均为不超过 8192 bytes 的严格 UTF-8 JSON，未知字段和非文本帧拒绝。客户端每 15 秒 heartbeat，45 秒无有效消息关闭；首个 join 仍必须在 5 秒内完成。

进入 B2 后，heartbeat 与 event ACK 也必须重新验证当前 assignment、项目、授权和 session 模式，不能只验证登录 session；撤权后必须关闭旧连接并释放 producer。未识别的内部或持久化异常以不泄密的 `REALTIME_UNAVAILABLE` 失败关闭，不得伪装成权限拒绝。runtime 帧证据和 final ID 的长时清理仍在长时访谈测试前处理，不属于 B2 内部短链路结论。

内部 streaming fake 由服务端 local/test 组合根注入，根据 PCM 帧序号确定性地产生 interim、final 或故障。客户端不得提供“希望输出的文本”、segment ID 或 provider payload，禁止重新形成公开转录注入入口。production 没有真实 provider 时必须以 `ASR_UNAVAILABLE` 失败关闭，不得自动启用 fake。

DEV-004B1 不实现 AudioWorklet、真实麦克风、真实供应商 SDK、校准/remap、session stop/completion、持久事件 outbox、故障区间或离线补录。JSON/base64 的传输开销、TLS/Nginx、跨进程恢复和长时负载属于后续加固，不阻塞内部 MVP。

## 5. 转录状态

ASR 返回结果分为：

- `interim`：中间态，可变化；
- `final`：确定态，不再变化。

规则：

1. 中间态只用于实时显示。
2. 确定态才写入正式转录。
3. 只有确定态可进入 AI 和长期记忆。
4. 中间态不得生成正式内容标记。
5. 页面必须明确区分两种状态。
6. 供应商原始响应需保留用于排查。

正式持久层只保存 final。相同 `(session_id, ingest_key)` 的完全相同 final 是幂等重放；原始文本、时间、provider segment/speaker 或来源不同必须失败关闭，禁止覆盖。interim 可以在内存中发布给实时事件消费者，但数据库中不得创建 `transcript_segment`。

## 6. 转录片段

标准字段见 `04-数据模型规范.md`，事件结构见 `05-API与实时事件契约.md`。

显示文本规则：

```text
corrected_text ?? original_text
```

原始文本不可覆盖。

原始说话人角色与修正后角色分列保存：首次 final 落库时快照当前会话映射为 `original_speaker_role`，没有映射时写 `unknown`；人工修正只写 `corrected_speaker_role`，不得修改原始快照。

人工修正必须记录：

- 修正人；
- 修正时间；
- 修正前原文；
- 修正后文本。

## 7. 说话人分离与校准

### 7.1 供应商说话人

供应商通常返回临时 `speaker_id`，该值不等于业务角色。

业务角色：

- `elder`
- `interviewer`
- `unknown`

### 7.2 校准流程

1. 倾听员说固定句；
2. 长者说固定句；
3. 记录对应供应商 ID；
4. 建立会话内映射；
5. 倾听员确认或修正；
6. 开始正式访谈。

### 7.3 修正

支持：

- 单片段修正；
- 同一供应商 ID 批量修正；
- 保留修正前角色；
- 修正后重新触发受影响的记忆分析；
- 不跨会话自动沿用供应商 ID。

## 8. 状态与心跳

前端持续显示：

- 麦克风状态；
- 录音状态；
- 分片上传状态；
- ASR 连接状态；
- 最近一次服务器心跳；
- 当前网络状态。

推荐状态：

### 录音

- `idle`
- `recording`
- `stopping`
- `stopped`
- `failed`

### ASR

- `connecting`
- `connected`
- `reconnecting`
- `unavailable`
- `closed`

### 上传

- `healthy`
- `delayed`
- `retrying`
- `failed`

## 9. 重连策略

### 9.1 业务 WebSocket 断开

- 检测断开；
- 进入 `reconnecting`；
- 使用有限次数退避重连；
- 重连时发送最后已确认事件位置；
- 服务端返回恢复状态；
- 防止重复落库。

DEV-004B1 的“恢复”只指同一服务进程内、同一 session runtime event stream 的短时恢复。客户端重连时必须复用 `audio_stream_id`，并携带服务端 `event_stream_id` 和最后连续 ACK 的 `server_sequence`。窗口有效时先按原序 replay，再发送新的 ready；窗口过期、stream ID 不匹配或进程重启时明确返回 `RESUME_WINDOW_EXPIRED/RESET_REQUIRED`。不得从数据库 final 片段反推并伪装完整实时事件历史。

### 9.2 ASR 供应商断开

- 原始录音继续；
- 页面显示转录不可用；
- 后端尝试重建供应商会话；
- 记录故障起止时间；
- 无法恢复的区间在会话结束后补转录。

### 9.3 无法恢复

- 不伪造转录；
- 标记缺失时间范围；
- 允许上传完整录音进行离线补转录；
- 补转录结果保留来源标记。

### 9.4 session recover 与实时 replay 的边界

- WebSocket replay 只恢复同一进程 runtime 中的短时事件展示；它不证明 MediaRecorder 已停止、原始分片完整、manifest 完成或 ASR 已 drain；
- session recover 读取 `session_finalization`、冻结 chunk commitments、audio manifest 和持久 final 转录事实，重驱结束 runner；不使用 `event_stream_id` 或页面 cursor 作为完成依据；
- 服务进程重启可以丢失 runner 和 WebSocket replay，但不得丢失 stop snapshot。`stopping|processing` 必须可由 recover 或服务启动扫描安全重驱；
- 内部 MVP 允许 runner 为进程内有界任务；未来队列/outbox 只替换触发与重试机制，不改变 finalization 聚合、状态门禁、幂等和公共 snapshot。

## 10. 结束会话顺序

1. 前端停止发送新 PCM并释放 producer；服务端一旦接受 stop 即拒绝后续 PCM；
2. 前端调用 `MediaRecorder.stop()`，等待最终 `dataavailable` 后停止创建新原始分片；
3. 冻结唯一 interview audio object 的 expected count 和全部不可变 chunk commitment，先持久化稳定 stop request ID 再请求 stop；
4. 服务端原子记录采集截止、服务端时长和 finalization，session 进入 `stopping`；
5. stop 前已产生且在 commitment 内的分片可以继续补传；stop 后不得新增 object、sequence 或改变 commitment；
6. 全部分片到达后 complete audio object，重新读取私有存储校验并生成 canonical manifest；
7. manifest 完整后 session 进入 `processing`，向 ASR adapter 请求 final drain；
8. 已返回 final 先落库，adapter 终止为 `drained`；不可用/超时则持久化 `degraded` 或 `not_started` 及最小错误码；
9. 关闭供应商会话和业务 WebSocket；
10. 原始录音完整且转录达到上述终态后进入 `completed`；AI 和 session note 可独立继续或失败。

不得点击结束后立即把会话标记 `completed`。不得从最后一条 final、WebSocket close、短时 replay、页面计时或本地 Blob 数量推断 drain/manifest。

stop 时提交的 chunk commitments 既是撤权后受限补传的允许列表，也是拒绝 stop 后新增原始字节的边界。commitment 不替代存储复核；checksum/size/MIME/时间/sequence 任一冲突均失败关闭，不覆盖既有分片。

## 11. 故障降级

### 麦克风权限拒绝

不得开始正式会话。

### ASR 故障

录音期间原始录音继续，AI 自动建议暂停。结束期间若原始 manifest 已完整，ASR 无法 drain 不得阻塞 session 合法完成；服务端持久化 `transcript_status=degraded|not_started` 和不泄密错误码，后续补转录另行处理。

DEV-004B1/B2 内部原型尚无 AI 自动建议，因此 fake streaming ASR 故障时只执行：

- 发布 `asr.status=unavailable` 和最小错误码；
- 不 ACK 未被 adapter 接受的后续 ASR 帧，触发客户端有界背压；
- 不修改 interview session 状态，不修改 audio object/chunk/manifest/upload job；
- MediaRecorder 和原始音频上传继续；
- 不伪造 final，也不在本任务内实现故障区间或离线补录。

### AI 故障

音频和 ASR 不受影响；AI、memory、suggestion 与 session note 永远不是 session `completed` 的门禁。

### 对象存储故障

进入延迟上传，达到本地缓冲阈值后提示安全结束。

### 数据库故障

优先继续本地录音，停止新的 AI 任务，恢复后进行一致性检查。

### 浏览器崩溃

已上传分片保留；尚无 stop snapshot 时会话可标记 `interrupted`，重进页面在门禁有效时选择恢复采集，或提交已有分片 commitments 安全结束。已有 stop snapshot 时保持 `stopping|processing`，使用 session recover 重驱，不倒退为 recording。

### Android Chrome 页面生命周期

首轮正式移动平台是 Android Chrome，手机必须能够完成准备、正常录制、实时转录、中断恢复与安全结束；iPhone Safari 不在本轮承诺内。页面隐藏、切后台、锁屏、旋转和音频设备中断不能由 UI 猜测为“仍正常”或“已失败”：DEV-005R2 必须在目标 Android Chrome 真机上分别验证浏览器是否能继续可靠采集；能够继续时必须保持 archive、时间轴和单一 controller，不得因旋转刷新或重新请求麦克风；无法证明继续可靠采集时必须持久报告 `interrupted`，不得静默丢失后仍显示正常。

现有 interruption reason 是否足以准确承载上述平台事实，必须以真机证据核对。若不足，先同步 `03/04/05/06/09` 与共享契约再扩枚举；R2/R3 不得以页面文案或私有字符串绕过契约。Android 后台/锁屏行为在证据冻结前是 DEV-005R2 的开放技术项，但不阻塞 R1 服务端和 R2C 纯浏览器核心继续开发。

DEV-005R2 已在 OnePlus GM1900、Android 12、Chrome 150 上冻结首个目标设备基线：旋转、约 20 秒切后台和约 20 秒锁屏期间，只要麦克风 track、MediaRecorder、archive 写入和 controller identity 持续健康，允许同一 generation 继续采集，不因 `visibility` 或屏幕方向事件本身创建新 capture、重新申请麦克风或报告中断。页面刷新必须保留同一 local job/archive/request ID，禁止自动请求麦克风，并以 `page_recovery_detected` 持久报告 `interrupted`；运行中撤销麦克风权限导致 track ended 时，以 `microphone_ended` 持久报告 `interrupted`。该结论是首轮目标设备证据，不是所有 Android 厂商、Chrome 版本或 iPhone Safari 的无条件保证；R4 仍须复验显式 resume、下一 generation、累计时间轴和安全结束全链路。

## 12. 性能目标

| 指标 | 目标 |
|---|---:|
| 中间态上屏 | 正常网络下不高于 2 秒 |
| 确定态落库 | 收到后 1 秒内 |
| 连续录音 | 支持 30 至 180 分钟 |
| 浏览器内存 | 60 分钟内不持续线性增长 |
| MVP 并发会话 | 5 |
| 分片丢失 | 正常链路为 0 |

## 13. 安全要求

- 使用 HTTPS/WSS；
- ASR 密钥只在后端；
- 音频对象默认私有；
- 上传地址短时有效；
- 不在 URL 中放转录正文；
- 日志不记录完整音频地址；
- 不同环境使用不同存储桶。

## 14. 验收场景

1. 连续录音 60 分钟；
2. ASR 中途断开后录音完整；
3. 网络断开后分片补传；
4. 同一分片重复上传不重复保存；
5. 浏览器刷新后识别会话中断；
6. 说话人校准和批量修正；
7. 中间态不进入数据库正式记录；
8. 结束时最后一段确定态不丢失；
9. manifest 可准确列出全部分片；
10. 缺失分片能够被检测并明确显示。
