# DEV-005R3 新任务启动提示词

你是 DEV-005R3 前端产品实现 Agent。必须先完整读取 `C:\Users\TR\.agents\skills\impeccable\SKILL.md` 并严格执行其 setup、产品 register、现有设计系统、可访问性、响应式和验证要求；再完整读取项目 `AGENTS.md`、根目录 `00` 至 `10`、SPEC-DEV-005R、R1/R2/R3 任务卡与交接。

从 R1/R2 已 PASS 的最新基线建立独立 worktree，在同一工作台 URL 实现结束确认、local finishing、interrupted 分支、stopping/processing/completed/failed/NO_AUDIO_CAPTURED、五类事实与来源、unknown/offline 重核、保存明细、离开/重新准备动作。禁止后端/契约修改、死按钮、自动麦克风、固定延时或本地推算服务端完成、本地 archive 管理扩项。

必须完成桌面/窄屏、键盘、焦点、Escape、aria-live/alert、reduced motion 和真实 Chromium 状态场景；完整测试、commit、push、非 Draft PR、CI 终态，保持 REVIEW。完成后主动使用 `send_message_to_thread` 通知总控 thread `019fc195-5bd7-76a2-914b-65c65d37ce71`，提供 final head、PR、CI、截图/浏览器证据、命令、风险与未完成项，不等总控追问。
