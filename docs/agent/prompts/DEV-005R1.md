# DEV-005R1 新任务启动提示词

你是拾光长者传记项目的 DEV-005R1 后端实现 Agent。请在 Codex 为你创建的独立 worktree 中完成“服务端采集生命周期与原子开始”，不得切换或修改总控工作区。

## 先读与事实边界

1. 依次完整读取 `AGENTS.md`、根目录 `00` 至 `10`、`docs/agent/README.md`、任务板。
2. 完整读取 `docs/agent/tasks/SPEC-DEV-005R.md`、`DEV-005R1.md`、SPEC-SESSION-END-001、DEV-005C、ADR-022/023、CON-020，以及相关最新交接。
3. 检查 Git 基线、当前 diff、现有 Prisma/contracts/project-foundation/audio/realtime/finalization 实现和测试后再改。
4. 当前是基于已批准产品决定的 stacked implementation candidate；在 SPEC-DEV-005R 获项目负责人 GitHub PASS 前不得合入 main或自称契约通过。

## 必须完成

- start 请求加入稳定 `request_id + mime_type + audio_stream_id`；同事务创建唯一 interview audio object、generation 0 preparing、session recording 和幂等/审计事实；
- 前向 migration 与 `session_capture_generation` 完整约束；
- confirm-active、report-interrupted、resume-capture、abandon-empty API、验证、service、mapper、公共 contracts；
- 公共 capture snapshot 白名单与 `NO_AUDIO_CAPTURED`；
- request→project→session→audio 固定锁序；stop/report/revoke/resume/upload 竞态不产生双对象、双 current generation、双 finalization或状态倒退；
- report-interrupted 的减权权限与账号停用失败关闭；
- PostgreSQL integration barrier、幂等响应丢失、同 key 异 payload、零证据/有证据负向、跨 generation offset、撤权竞态和审计脱敏测试。

## 修改边界

只修改 `apps/api/**`、`apps/api/prisma/**`、`packages/contracts/**`、对应测试、DEV-005R1 专属 task/handoff。禁止修改 `apps/web/**`、中央任务板/追踪/冲突/ADR、旧 DEV-005A/B/C 历史、AI/云存储/队列/真实供应商。

## 交付纪律

执行适用 format/lint/typecheck/unit/migration/PostgreSQL integration/auth/build/smoke。完成后：

1. 只把 DEV-005R1 专属任务记录为 `REVIEW` 候选，不得标 `DONE` 或自宣 `PASS`；
2. commit、push `codex/` 分支并创建非 Draft PR；
3. 不要等用户或总控来追问。完成后必须主动使用 `send_message_to_thread` 向总控 thread `019fc195-5bd7-76a2-914b-65c65d37ce71` 发送：最终 head、PR URL、CI run、修改文件、实际命令与结果、风险、未完成项、需要总控重点复核的内容；
4. 若 CI 仍在跑，持续等待到终态；失败就修复并更新 final head，不要把“已发 PR”当完成。
