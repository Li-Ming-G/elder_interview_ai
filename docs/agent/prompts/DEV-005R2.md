# DEV-005R2 新任务启动提示词

在 DEV-005R1 与 DEV-005R2C 获 GitHub PASS 后，从两者已合并的最新基线创建独立 worktree，完成正式 session-scoped `InterviewCaptureController` 集成。完整读取 `AGENTS.md`、根目录 `00` 至 `10`、SPEC-DEV-005R、R1/R2C/R2 任务卡和最新交接。只修改 `apps/web` 的 controller/API 绑定与必要正式路由 seam；不得改后端或公共 DTO，不完成 R3 最终 UI。

交付必须覆盖 start 前持久 local job/IDs、原子 start 绑定 object、confirm active、同一流接入、upload pump、刷新 dirty checkpoint、report/resume、stop handoff；完整测试、commit、push、非 Draft PR、CI 终态。保持 REVIEW。完成后主动使用 `send_message_to_thread` 通知总控 thread `019fc195-5bd7-76a2-914b-65c65d37ce71`，提供 final head、PR、CI、命令、风险和未完成项，不等总控追问。
