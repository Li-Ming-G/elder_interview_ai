# DEV-006｜后台结构化长期记忆最小纵向链路

## 基本信息

- 状态：`REVIEW`
- 负责人：独立实现任务 `019fe9e5-2a04-79e0-9e49-5f99ca21f648`
- 前置依赖：DISC-006 DONE、DEV-004C1/C2 DONE、SPEC-DEV-006 项目负责人 PASS
- 输入依据：`01/03/04/05/07/08/09/10`、ADR-025/026、SPEC-DEV-006、DEV-004C1/C2
- 允许修改：`apps/api` 中 MemoryModule、QuestionEvidenceModule、AI job coordinator/policy ports，现有 project/assignment/consent producer 的最小 `ai_policy_revision` 递增接缝，`prisma` 的正式前向 migration，必要 shared contracts 与定向测试；具体表/状态/服务以 `04` §4.9/§§4.28-4.43、`05` §§3.9-3.10 为准
- 禁止修改：DEV-007 当前问题 UI/换题实现、完整回顾/导出/删除 UI、真实供应商、向量库、自动传记、录音/ASR/说话人生产者语义
- 交接对象：SPEC-AI-QUESTION-001、DEV-007、会后实际问题整理任务、项目负责人 GitHub 审查

## 目标

使用虚构/脱敏数据和 fake structured provider，建立“可信 final → 后台 current memory → 跨会话 current view → 下一问上下文 seam”的最小可追溯链路，为当前问题和第二次访谈开场提供可靠输入。

## 预期范围

- 只消费 trusted elder conversation final；unknown、校准控制内容、restricted 和 deletion scope 命中证据失败关闭；
- 最小记忆类型、证据回链、冲突/明确更正、范围/unknown、跨 session current view；
- 逐 session role revision watermark、job/segment/output provenance、future eligibility、范围化失效与失败状态；
- 模型/提示词/Schema/上下文版本和最小过程记录；
- 为 DEV-007 提供稳定 current memory 与边界读取 seam；
- 建立 `QuestionEvidenceModule` 共享数据/service seam，并实现会后 actual-question analysis/catalog/evidence、可靠版本发布与跨会话 actual-asked reader；问题生成/display/replace orchestration 留给 DEV-007；
- 实现两阶段 job freeze/call/recheck、零 eligible session scope、segment text/role provenance、memory membership、derived-output eligibility 和过程记录；
- 每个独立业务输出一条 derived row，含跨表一对一/type/project/job 约束和三类 dependency expected count/manifest；五条 claim 必须是五条资格记录，actual-question analysis 以整版 catalog 一条资格记录；
- 实现 `ai_job|question_display_snapshot|memory_retention_root` 三类 retention root、child 继承、到期先隐藏后清理、跨 root detach、CASCADE/显式幂等清理和失败续跑测试；
- legacy 缺 provenance 数据默认 `review_required|unjudged|future-ineligible`，不得推断 displayed、actual asked 或 trusted authority。

## 禁止范围

- 不做记忆列表、人工确认、完整回顾、项目树、向量数据库或自动写书；
- 不接真实 LLM、真实个人资料或公网服务；
- 不实现当前问题展示/换题 UI，不复制 DEV-007 question history；
- 不把问题质量百分比作为首个内部通过门槛；
- 不绕过 CON-023 或声称完整 deletion runtime 已实现。
- 不实现 suggestion REST/WS、自动替换/手动下一个问题/展示历史导航/节流/相似度、问题生成文案和工作台 UI；这些等待 SPEC-AI-QUESTION-001/DEV-007；
- 不创建生产 no-op `DeletionScopeReader`。正式 port 必须失败关闭；在 DEV-008 完成前 deletion coverage 明示 `NOT IMPLEMENTED / NOT VERIFIED`。

## 交付与验收

- 精确交付物、数据库/API/状态机和测试矩阵以 SPEC-DEV-006 项目负责人 PASS 的 final head `4759633ed1e3d9031c8bbe32892d61293f9ec01c` 为准；
- 至少用两次虚构访谈证明 current memory、明确更正、冲突澄清输入、跨会话继承和角色修正后 future eligibility 失效；
- 完整记录输入证据、版本、输出、错误与过程事实，技术日志不含完整正文；
- 项目负责人绑定非 Draft PR exact final head 明确 PASS 后才能 `DONE`。
- 自动测试必须逐项覆盖 `09` §7.5，并提交 format/lint/typecheck/unit/migration/integration/auth/build/smoke/e2e 结果；CON-023 不得以 fake/no-op 冒充覆盖。

## 项目负责人首轮审查与定向修复

- 项目负责人在 PR #22 exact head `d5073501b170c7e11f2bc3e00395fb8fdf794480`（CI `31357613683` SUCCESS）给出正式 `REQUEST_CHANGES`：P0=0、P1=8；该历史永久保留，任务继续 `REVIEW`。
- 定向修复候选逐项补齐：每个 session 的 final 高水位和零 eligible 区分；request/trigger/retry/input identity；漂移后的持久化 cancelled；boundary producer 缺失时生产 `AI_POLICY_UNAVAILABLE`；`question-sim-v1`；context actual-question 同事务冻结与重检；正式导出的 `QuestionEvidenceWriter` seam；retention root 活跃/期限校验与独立 cleanup pepper。
- `QuestionEvidenceWriter` 仅冻结并导出 `beginGenerationAttempt`、`publishAttemptResult`、`withdrawPresentation` 的模块所有权；在 DEV-007 到来前显式返回 `DEV_007_QUESTION_ORCHESTRATION_NOT_IMPLEMENTED`，不得以 no-op 提前伪造 generation/display/replace 事实。
- 当前生产 boundary producer/read model 尚不存在，生产 reader 必须 `AI_POLICY_UNAVAILABLE` fail-closed；local/test 只允许显式 fixture 验证 consumer。不得新增 `content_marker`/deletion 半模型或固定空集合。
- CON-023 保持 `OPEN / NOT IMPLEMENTED / NOT VERIFIED`；修复候选仍须由项目负责人对新 exact head 与 CI 在 GitHub 手动定向复审，不得由开发 Agent 宣布 PASS/DONE 或合并。
