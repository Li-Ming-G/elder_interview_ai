# DEV-005R3｜正式工作台采集、恢复与安全结束体验

## 基本信息

- 状态：`DONE`
- 负责人：前端产品实现 Agent（`codex/dev-005r3-workbench`）
- 输入依据：`SPEC-DEV-005R`、DEV-005R1/2 PASS、`03`、`05`、`09`、ADR-023/024、CON-021
- UI 技能：必须完整遵循 `C:\Users\TR\.agents\skills\impeccable\SKILL.md`
- 前置依赖：DEV-005R1、DEV-005R2 GitHub 审查 PASS；DISC-005R-UI 已完成写回
- 交接对象：DEV-005R4、总控 Agent

REV-024 已确认 DEV-005R2 的 Android controller 生命周期事实门禁通过。R3 开工时必须把两条真机反例作为直接验收输入：服务端/capture 已为 `interrupted` 时不得继续显示“服务端进行中”；同一 URL 必须按真实条件提供“继续同一次访谈”或“安全结束已有音频”，不得因当前旧页面没有动作而修改 R2 生命周期规则。准备页低音量输入易误判的问题按 CON-022 作为 P2 同轮改善，但不得取消输入检测门禁。

## 目标

把准备页、工作台、中断选择和正常结束接到同一个真实 controller 与服务端 snapshot，在同一工作台 URL 中清楚解释本浏览器、服务端录音、转录和 session 的独立事实。

## 允许修改

- `apps/web/src/interview/**`、`apps/web/src/app.tsx`、`apps/web/src/styles.css`；
- 对应单元、Chromium 与无障碍测试；
- 必要的 controller 消费 seam。

## 禁止修改

- 后端、Prisma、公共契约；
- 本地 archive 下载/播放/删除、完整记录页、项目列表、AI 建议实现；
- 使用固定延时、localStorage、URL、最后 final 或本地分片数推算服务端完成。

## 交付与验收

- 结束确认、single-flight、本地 final write、stop/recover/finalize/abandon；
- recording/interrupted/stopping/processing/completed/failed 全状态；
- 五类事实和来源标记、unknown/offline 重核、登录返回；
- 正常桌面/手机按已批准比例护栏；interrupted 与结束态按业务任务重分配重心，只有结束确认使用 modal；
- 顶部最多两行且不铺五 chip；保存状态 disclosure、关键异常提升、stopping 最小化灰置、processing/completed 可收起为持续状态条；
- 所有设备采用左侧说话人/时间、右侧正文的高密度转录；手机元数据栏 52–64px，确定态不重复标签；
- 建议区只实现一个问题/原因、继续倾听、正在更换和一层撤销的布局/状态容器，不实现 DEV-007 业务；
- 1440×900、1024×768、768×1024、390×844、320×568 截图/断言；无横向滚动，触控目标不小于 44×44px，安全区、字体缩放、键盘、焦点、live region、错误 alert、reduced motion；
- 无死按钮、无自动请求麦克风、无伪造成功；
- 完整 verify 通过，状态只能到 `REVIEW`，等待项目负责人 GitHub PASS。

## 实现候选（2026-08-08）

- 工作台改为持续消费 controller 的完整服务端 session/finalization、本浏览器 archive/delivery、持久结束 handoff 与 realtime projection；初次 load、URL、计时器和 UI 状态均不再宣布采集或完成。
- 同一正式 URL 已覆盖 recording、interrupted、stopping、processing、completed、failed、`NO_AUDIO_CAPTURED`；interrupted 初始为原地状态面板，显式 resume 才申请麦克风，显式安全结束才打开唯一 modal。
- 结束链路复用稳定 handoff 调用既有 stop/`finalize_interrupted`/complete/reconcile/abandon seam；本地结束边界冻结后禁止恢复录音，页面只呈现持久 snapshot。
- Chromium 覆盖五视口、七状态共 35 张截图及比例、页面/横向滚动、转录主滚动、移动元数据栏和触控断言；单元覆盖 snapshot 更新、动作 single-flight、modal 焦点/Escape、离线事实与低音量反馈。
- 本节记录实现候选当时只到 `REVIEW`；后续正式 PASS 与合并结论见文末。Android 真机完整 resume/安全结束与 CON-020/021 关闭仍属于 DEV-005R4。

## 总控内部预检清单定向修复候选（2026-08-09）

- 总控交付完整性/内部预检清单绑定 PR #15 head `db9579c4b2ee95370c188c452c81dde5501e4b40`、base `main@bc7ea83fe042b3d12a90b3787323166f0f0308e2`、CI `31258505938` SUCCESS，并提出下列定向修复项；这是实现变更依据，不是项目负责人的正式 GitHub 审查结论，不登记 REV。本轮未改后端、Prisma、公共契约或既有产品结构。
- `reconcile` request ID 现按一次业务尝试管理：响应未知/网络失败继续复用；收到权威成功 snapshot 后释放，下一次用户主动收尾使用新 ID，避免服务端永久重放旧 `stopping` snapshot。
- 初次 load 401 与运行中 verify 401 现先经 controller 权威核验清理并失败关闭，再提供真正“返回登录”；App 清空内存 user、CSRF 与 controller registry。403/授权失效不承诺重新登录可恢复，只提供只读事实与安全离开。
- 结束 modal 记录 normal/interrupted/empty 的实际触发按钮，按钮取消与 Escape 都恢复到本次入口；failed/blocked 不再使用完成语义，completed 才保留“完成并离开”。
- 单纯 delivery 失败在顶部和五事实明细明确“本浏览器仍在保存/录音仍保留，管理服务交付暂不可用，等待重试”；本地 archive 失败与 authority failure 继续按更高优先级失败关闭。两个告警已移除 3 px 侧边强调线，改为完整边框与现有软背景。
- 修复实现提交：`433e098a19787bc24c4f2832f395eaf7e295f9d0`。本地 full unit 32 files / 212 tests、PostgreSQL integration 41、auth 13、普通 Chromium 8/8、auth Chromium 4/4、五视口 × 七状态截图矩阵与完整静态/build/smoke/migration 门禁通过；该候选当时保持 `REVIEW`，随后由文末 REV-025 正式收口。

## 正式审查与收口（2026-08-09）

- 项目负责人在 GitHub 手动复核 PR #15 final head `481ee2593f27c62e3d137842edfd15fe11ad157c`，确认 head 未漂移、CI `31289795181` 完整 verify PASS，并给出 REV-025 `PASS`；P0=0、P1=0。
- reconcile request ID、401 返回真实登录、三类结束入口焦点恢复、真假完成文案、delivery 与本地 archive 事实分离、错误提示层级六项均关闭。
- PR #15 以 merge commit `8d5c4c5fda34a7e80e9c170aba289c3568332a07` 合入 `main`；DEV-005R3 改为 `DONE`，DEV-005R4 解锁为 `READY`。
- 本结论只覆盖 R3 工作台与安全结束实现。CON-020/021/022 继续 `OPEN`，Android 完整 resume、安全结束、manifest 与普通音量复验仍由 R4 负责。
