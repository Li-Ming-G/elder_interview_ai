# DEV-005R3｜正式工作台采集、恢复与安全结束体验

## 基本信息

- 状态：`READY`
- 负责人：新的前端产品实现任务
- 输入依据：`SPEC-DEV-005R`、DEV-005R1/2 PASS、`03`、`05`、`09`、ADR-023/024、CON-021
- UI 技能：必须完整遵循 `C:\Users\TR\.agents\skills\impeccable\SKILL.md`
- 前置依赖：DEV-005R1、DEV-005R2 GitHub 审查 PASS；DISC-005R-UI 已完成写回
- 交接对象：DEV-005R4、总控 Agent

REV-024 已确认 DEV-005R2 的 Android controller 生命周期事实门禁通过。R3 开工时必须把两条真机反例作为直接验收输入：服务端/capture 已为 `interrupted` 时不得继续显示“服务端进行中”；同一 URL 必须按真实条件提供“继续同一次访谈”或“安全结束已有音频”，不得因当前旧页面没有动作而修改 R2 生命周期规则。准备页低音量输入易误判的问题按 CON-022 作为 P2 同轮改善，但不得取消输入检测门禁。

## 目标

把准备页、工作台、中断选择和正常结束接到同一个真实 controller 与服务端 snapshot，在同一工作台 URL 中清楚解释本浏览器、服务端录音、转录和 session 的独立事实。

## 允许修改

- `apps/web/src/interview/**`、`apps/web/src/app.tsx`、`apps/web/src/styles.css`；
- 对应单元、Chromium 与无障碍测试；
- 必要的 controller 消费 seam。

## 禁止修改

- 后端、Prisma、公共契约；
- 本地 archive 下载/播放/删除、完整记录页、项目列表、AI 建议实现；
- 使用固定延时、localStorage、URL、最后 final 或本地分片数推算服务端完成。

## 交付与验收

- 结束确认、single-flight、本地 final write、stop/recover/finalize/abandon；
- recording/interrupted/stopping/processing/completed/failed 全状态；
- 五类事实和来源标记、unknown/offline 重核、登录返回；
- 正常桌面/手机按已批准比例护栏；interrupted 与结束态按业务任务重分配重心，只有结束确认使用 modal；
- 顶部最多两行且不铺五 chip；保存状态 disclosure、关键异常提升、stopping 最小化灰置、processing/completed 可收起为持续状态条；
- 所有设备采用左侧说话人/时间、右侧正文的高密度转录；手机元数据栏 52–64px，确定态不重复标签；
- 建议区只实现一个问题/原因、继续倾听、正在更换和一层撤销的布局/状态容器，不实现 DEV-007 业务；
- 1440×900、1024×768、768×1024、390×844、320×568 截图/断言；无横向滚动，触控目标不小于 44×44px，安全区、字体缩放、键盘、焦点、live region、错误 alert、reduced motion；
- 无死按钮、无自动请求麦克风、无伪造成功；
- 完整 verify 通过，状态只能到 `REVIEW`，等待项目负责人 GitHub PASS。
