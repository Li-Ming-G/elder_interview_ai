# DEV-008A2｜新建访谈完整纵向入口

## 基本信息

- 状态：`REVIEW`
- 负责人：独立 DEV-008A2 实现 Agent
- 前置：SPEC-DEV-008A 与 DEV-008A1 均已 exact-head PASS/merge；A1 merge `29e3f993a65afd08cd301563d94e40cfc66076a8` / main CI `31593387265` SUCCESS
- 输入：`00/01/02/03/04/05/08/09/10`；ADR-034/035；SPEC-DEV-008A、DEV-008A/A1 任务与交接；REV-041/042/043；A1 唯一 shell/routes/read model；既有 project/service-term/consent/session/device-check/start 契约
- 分支：`codex/dev-008a2-new-interview`，基线 `origin/main@51e2337ea86739e209ad696804de7decbcf7a9df`
- 提交：[非 Draft PR #39](https://github.com/Li-Ming-G/elder_interview_ai/pull/39) 已创建；最终 exact head 与 CI 以主动回传审查包为准
- 风险：授权和开始门禁、四 create 权威幂等、未知响应恢复、共享 shell 所有权；需要项目负责人 exact-head 手动审查

## 目标与输出

在 A1 已接收的唯一 authenticated shell 内完成 project → service term → 正式口头 consent → session → prepare/device-check → start。四个 create 的稳定 request ID 必须在首次网络请求前持久化；响应未知、刷新或重开只做 authoritative replay，收到首次/replay 结果并持久化资源身份后才推进。

输出包括 A2 范围的前后端实现、共享契约内的必要接线、自动测试、真实 Chromium/三视口证据、任务/追踪/交接同步，以及非 Draft PR exact-head 审查包。

## 范围与禁止事项

- 复用 A1 的 shell、routes、tokens、shared components、状态词汇和 formal project/session read model，不建立第二套首页或导航。
- 只实现新建访谈纵向入口；不得只建 draft 冒充可开始，不得绕过授权或把 local/test fixture 暴露给普通 UI。
- 不实现 A3 回顾、播放、IndexedDB 删除、容量投影；不实现 DEV-008D、deletion request、服务器删除、导出、ASR/007、PWA/App。
- 如需改变公共 DTO、状态、API、数据模型或授权语义，先登记冲突并走正式契约变更，不在实现中自行扩张。

## 验收标准

- `09` §10.3 的 A2 行全部通过；actor/action/target-or-project-create-identity/canonical payload hash 绑定，同 ID 异绑定冲突。
- project/service term/consent/session 四步在响应丢失、超时、刷新、重开和双击下不重复业务记录、历史或审计；GET 不猜测未知 POST 是否成功。
- 未说明、授权缺失/撤回/版本漂移、无 assignment 或状态不允许时服务端 start 失败关闭；普通 UI 只走正式 MVP 口头授权证据。
- 复用唯一 shell/routes/styles/status vocabulary，并完成 1440×900、390×844、320×568、键盘、focus、live region、44px 和 reduced-motion 验证。
- 完整 CI SUCCESS；非 Draft PR final exact head 获项目负责人手动 PASS 后方可进入 `DONE`。

## 交接说明

执行前必须读取 [DEV-008A1 最终交接](../handoffs/DEV-008A1.md) 和 REV-043 final PASS/merge 记录，确认任务启动时 main CI 仍成功、工作区干净。完成后更新任务板、需求追踪、审查索引和交接；不得改变 DEV-008A3、DEV-008D 或 CON-023 状态。

## 实现候选

- 在 A1 唯一 Home shell 中实现 project → service term → 正式口头 consent 录音与完整上传 → consent record → session → prepare/device-check → start；普通 UI 不暴露 `electronic/written` fixture。
- project/service-term/consent/session 四个 create 在 IndexedDB 首次联网前持久化稳定 request ID、冻结 payload 与步骤；超时/刷新/重开只重放原请求。服务端按 ADR-034 的 actor/action/target-or-create-identity/RFC 8785 payload hash 权威返回首次响应，同 ID 异绑定 409。
- start 在事务锁内要求当前 assignment、可开始项目、device-check、当前服务说明、最新有效且版本为 `mvp-v1` 的 consent；版本漂移、撤权和 assignment drift 均失败关闭且不创建音频对象。
- fresh PostgreSQL 14 migrations 后 integration 80/80；unit 320/320；真实 auth Chromium 5/5；新入口 Chromium 5/5。P1 定向修复新增可等待、幂等的 consent capture dispose；adjacent 修复在每次 effect setup 恢复 mounted guard，React StrictMode setup→cleanup→setup 后 workflow、busy 与录音状态消息仍正常。已有分片与同一未冻结 job 保持可恢复；三视口截图已目视检查，静态格式、lint、typecheck、build 均通过。
- 当前仍为 `REVIEW`。REV-045 只登记实现候选与审查请求；执行 Agent 不宣布 PASS/DONE，不合并。
