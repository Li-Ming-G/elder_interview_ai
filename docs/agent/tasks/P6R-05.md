# P6R-05

Status: `DEFERRED`

## Architecture Mapping (P1-P6/T0-T27)

| Mapping | State |
| --- | --- |
| T18–T24 / P6 runtime integration closeout | `IN SCOPE` |
| Foundation Decision Trace | `IN SCOPE FOR COMPLETENESS` |
| P6R-01..04 | `REQUIRED PREDECESSORS` |
| P4/P5 | `REQUIRED INPUT / UNCHANGED` |
| T25 Prompt | `DEFERRED` |
| T26–T27 Evaluation | `DEFERRED` |
| Real provider/model/data/deployment | `OUT OF SCOPE` |

## Goal

Close T18–T24 with end-to-end synthetic runtime verification and complete Decision Trace coverage without activating a new prompt or real provider.

## Scope

No new architecture is introduced in this task. Only wire or repair the remaining seams required to prove the accepted P6R-01..04 runtime path works together.

## Allowed Files / Areas

- focused integration/wiring across already accepted P6R-01..04 seams;
- existing question-orchestration, presentation, ai-runtime/trace, P4/P5 evidence seams only as required by predecessor contracts;
- focused synthetic/integration tests;
- `docs/agent/tasks/P6R-05.md` and PR documentation.

Do not introduce new semantic contracts, provider/model choices, prompt activation or product behavior.

## Inputs

- exact accepted P6R-01 through P6R-04 heads/PRs, fresh-read and recorded in PR review context;
- accepted P4 Context V2 and P5 Evidence/Gate/Correction path;
- existing Foundation Decision Trace authority.

## Required Behavior

The integrated synthetic path must demonstrate:

1. finalized transcript arrives through the accepted trigger surface;
2. bounded automatic trigger/gate occurs without per-segment Director calls;
3. frozen generation/P4/P5 authorities remain reproducible;
4. optional one-round P5 evidence drill-down still obeys its accepted one-round/fail-closed rules;
5. manual-next can take accepted priority without bypassing authority checks;
6. stale generation cannot publish;
7. deadline/runtime failure becomes `SYSTEM_ERROR`/unavailable;
8. genuine `CONTINUE_LISTENING` remains semantically distinct from failure;
9. P2 background work does not block the live interview lane;
10. current suggestion publication remains CAS/fence safe under races/replay;
11. Decision Trace can answer what triggered, what frozen input authority was used, whether Director/evidence ran, latency/error and publication outcome using safe references;
12. recording/transcription completion remains unaffected by background memory/question failures.

## Explicit Non-Goals

- no T25 prompt update or runtime activation;
- no T26–T27 evaluation corpus/taxonomy work;
- no real provider/model/embedding/tokenizer/budget selection;
- no real data/deployment;
- no new agent framework or second Director;
- no second semantic/presentation authority.

## Tests

At minimum one fixed synthetic integration matrix must cover:

- automatic successful question;
- automatic `CONTINUE_LISTENING`;
- manual-next bypass;
- manual-next supersedes a late automatic generation;
- one evidence drill-down;
- attempted second evidence round rejected;
- deadline timeout;
- P3/P4/evidence failure;
- P2 delayed/failure while live lane continues;
- replay/restart/idempotency;
- recording/transcription remains healthy.

Run full relevant unit/integration/auth/build/smoke/E2E suites, API typecheck, ESLint, Prettier and `git diff --check`.

## Completion Criteria

- T18–T24 runtime behavior is integrated, deterministic and synthetically reproducible;
- relevant repository regressions are green;
- no T25/T26–T27/provider work introduced;
- one PR with exact `ARCHITECT_REVIEW_CONTEXT_V1`;
- Worker stops at `REVIEW`.

## Review Gate

External Architect PR review; merge and successful main verification close this P6 runtime pack.

## Next Task

`null`
