# DEV-002｜最小项目、服务信息、授权与会话

## 基本信息

- 状态：`IN_PROGRESS`
- 负责人：后端业务 Agent（dev002_min_project_consent）
- 前置依赖：DEV-001A；DEV-001B 内部候选 seam `ab9628b`
- 分支：`codex/mvp-v01-vertical-slice`
- 交接对象：总控 Agent、DEV-003B 与后续工作台 Agent

## 目标

在正式需求和契约内实现虚构项目的最小创建/读取、服务信息确认、授权记录与撤回、访谈会话创建及启动门禁，为 MVP-V01 提供稳定 `project_id`、`session_id` 和授权判定 seam。

## 输入依据

`00`、`01`、`02`、`03`、`04`、`05`、`08`、`09`、`10`、ADR-009/013、CON-008、HO-006。

## 允许修改

- `apps/api` 中独立 project/consent/session 业务模块；
- 与正式 `04` 一致的 Prisma schema 和单一前向迁移；
- 对应 REST DTO、模块测试、集成测试和内部虚构 seed/fixture；
- 必要的机器可读契约同步，但不得改变 Markdown 正式语义；
- 本任务直接相关的协作文档。

## 禁止修改

- 音频采集、分片存储、ASR、AI、导出和删除实现；
- DEV-001B 身份安全协议或 CON-008 数据模型候选；
- 真实授权录音、真实个人信息、公网部署、生产运维能力；
- 未经登记批准改变表字段、状态枚举、REST 路径或业务规则。

## 交付物

- 正式数据模型对应的最小 project、assignment、service_term/confirmation、consent_record、interview_session 实现；
- 当前用户只能访问获分配项目；未满足服务确认与有效授权时不能创建/启动会话；
- 授权撤回后禁止新会话并触发正式限制状态；
- 虚构数据的 API 集成测试与给 DEV-003B 的稳定 session seam 交接。

## 验证方式与验收标准

- format、lint、typecheck、相关 unit/integration；
- 空库迁移、重复 deploy、权限 A/B、授权缺失/撤回、幂等与非法状态测试；
- 原始授权记录只追加不覆盖，关键变化有符合现有契约的审计；
- 不依赖真实身份、真实录音或公网环境即可重复验证；
- 高风险数据/授权实现完成后进入 `REVIEW/VERIFY`，不得由实现者宣布最终通过。
