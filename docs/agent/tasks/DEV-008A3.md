# DEV-008A3｜已结束访谈回顾与本机副本管理

## 基本信息

- 状态：`REVIEW`
- 负责人：独立实现分支 `codex/dev-008a3-local-review`
- 前置：SPEC-DEV-008A、DEV-008A1 与 SPEC-DEV-008A3-PREFLIGHT 均已 exact-head PASS/merge；A1 merge `29e3f993a65afd08cd301563d94e40cfc66076a8` / main CI `31593387265` SUCCESS；preflight merge `60f60cb6b5c8f70c9fca9840aa6c495f6e2318d8` / main CI `31598183784` SUCCESS；DEV-003/005 archive/manifest/transcript seam 保持有效
- 输入：`00/01/02/03/04/05/06/08/09/10`；ADR-034/035/036；SPEC-DEV-008A、SPEC-DEV-008A3-PREFLIGHT、DEV-008A/A1 任务与交接；REV-041/042/043/044；A1 唯一 shell/routes/read model；formal `local-audio-archive-v1` Schema 与 fixtures；DEV-003C/005R2C/005R2/005R4 最新交接
- 分支：`codex/dev-008a3-local-review`，base `origin/main@4fc46456869ab01d9880d1aa92e7cd838bf920a8`；base main CI `31599184357` SUCCESS
- 提交：[PR #40](https://github.com/Li-Ming-G/elder_interview_ai/pull/40) 非 Draft 候选；final exact head/CI 在最终审查包绑定
- 风险：本机唯一副本误删、capture 并发、IndexedDB legacy 残留、server preflight 过期、finalization total bytes 缺失/null/unsafe/mismatch、将 local deletion 误报为服务器隐私删除；需要项目负责人 exact-head 手动审查

## 已解除的前置阻塞与实现输入

开工前唯一 iteration-coach Correction 已在零改动阶段确认：`05` §3.6.1 要求 total bytes 与 session finalization 对照，但公共 `SessionFinalizationSnapshot` 缺少该字段。总控已选择方案 A，由 [SPEC-DEV-008A3-PREFLIGHT](SPEC-DEV-008A3-PREFLIGHT.md) 先冻结 `AudioObject.totalSizeBytes` 的 additive nullable 投影、普通/list/evidence 白名单与 fail-closed 测试矩阵。

REV-044 已对该 SPEC final exact head `70167688202117364e5cab74c9a320e0a7d76742` / CI `31597563095` 给出项目负责人 PASS，PR merge `60f60cb6b5c8f70c9fca9840aa6c495f6e2318d8` 且 main CI `31598183784` SUCCESS，故本任务恢复 `READY`。实现必须把缺键、null、unsafe 或 fresh manifest/local archive mismatch 全部投影为 `blocked_server_unverified`，零回执、零部分删除；不得自行重开产品决定或把字段加入首页/evidence seam。

## 目标与输出

在 A1 已接收的唯一 shell 内实现已结束访谈的本机完整录音播放、原始/修订转录只读、本机副本与容量投影、原子本机删除，以及刷新/清站后一致且诚实的投影。

输出包括前向 IndexedDB upgrade、复用 capture Web Lock、fresh server preflight、覆盖 current/legacy/all reports 的单事务删除与最小回执、formal projection/Schema 校验、自动测试、真实 Chromium/三视口证据、任务/追踪/交接同步，以及非 Draft PR exact-head 审查包。

## 范围与禁止事项

- 复用 A1 的 shell、routes、tokens、shared components、状态词汇和服务端权威 action/review access；不建立第二套首页或导航。
- projection 必须使用唯一优先级；Schema v1 的 state/count/pending/playback/deleted_at 交叉条件与正反 fixture 必须机械通过。
- 不实现服务端音频下载、文本/说话人编辑、题库/AI 问题历史、记忆/工作记录、导出、deletion_request、server delete、A2 新建流程、ASR/007 或 PWA/App。
- local delete 必须明确保留服务器录音、转录和记忆，不关闭 CON-023；如需改变公共状态/API/数据模型或删除语义，先登记冲突并走正式契约变更。

## 验收标准

- `09` §10.4 全矩阵通过；processing/completed/failed 分流与 A1 权威动作一致，failed complete manifest 只读回顾/播放但不删除。
- 完整 archive 才可播放；缺片/Blob 不可读不得播放部分录音；不请求不存在的 server download API。
- active capture、Web Locks 不可用、pending delivery、server preflight stale/离线/不一致、session/capture 门禁失败均零写入；任一点 transaction 失败全部回滚。
- 成功删除在一个 transaction 清 current/legacy/session payload、delivery、formal job、all reports/checkpoint 并写稳定最小回执；重复调用和刷新/清站投影符合 Schema。
- 真实 Chromium 覆盖刷新、两个 tab 并发、legacy upgrade、页面卸载；1440×900、390×844、320×568 与无障碍门禁通过。
- 完整 CI SUCCESS；非 Draft PR final exact head 获项目负责人手动 PASS 后方可进入 `DONE`。

## 交接说明

执行前必须读取 [DEV-008A1 最终交接](../handoffs/DEV-008A1.md)、[SPEC-DEV-008A3-PREFLIGHT 最终交接](../handoffs/SPEC-DEV-008A3-PREFLIGHT.md)、REV-043/044 final PASS/merge，以及 DEV-003/005 对应最新交接，确认任务启动时 main CI 仍成功、工作区干净。完成后更新任务板、需求追踪、审查索引和交接；不得改变 DEV-008A2、DEV-008D 或 CON-023 状态。

## 实现候选与本地证据

- ordinary canonical session mapper 显式输出 `total_size_bytes`；仅关联 audio object 已 complete、manifest checksum 存在且 BigInt 为非负 safe integer 时输出 exact number，否则输出 null。首页 list 与 evidence seam 白名单不扩字段。
- 新增只读回顾页面，复用 A1 HomeFrame/routes/styles/StatusBadge 与服务端 `primary_action/review_access`；展示原始/修订转录、本机完整录音、分片/容量近似与明确本机删除边界，无编辑、导出、题库或 AI history。
- 新增 `local-audio-archive-v1` runtime：fresh ordinary session + manifest，逐项验证 identity/count/bytes/chunk sum/checksum/逐片 metadata/Blob SHA-256；缺片或 Blob 不可读不创建部分播放；没有请求 server audio download/delete API。
- IndexedDB 前向升到 v5；复用 capture Web Lock；单事务重新核验后清 current、legacy、delivery、session state、formal job、全部 interruption reports、checkpoint 并写最小回执。事务任一点失败显式 abort；重复删除、刷新和清站投影稳定且诚实。
- 门禁：lint/typecheck/format/build 通过；unit 329/329；fresh PostgreSQL 13 migrations 后 integration 79/79、auth 23/23；真实 Chromium 5/5 覆盖 1440×900、390×844、320×568、双 tab、legacy upgrade、刷新、清站与卸载。
- 当前只形成 `REVIEW` 候选；reviewed head `70b8fe8` 的 P1 隐私文案与阻塞接收 P2 焦点管理已定向修复，component 5/5、unit 330/330、真实 Chromium 6/6。当前中间 head/CI 不构成最终复审包；等待 A2 PASS/merge/closeout 后对齐最新 main、解决共享代码/治理冲突、分配唯一新 REV-ID 并重跑完整门禁。不自行 PASS/DONE/merge。
