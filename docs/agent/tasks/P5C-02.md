# P5C-02

Status: `DEFERRED`

## Architecture Mapping (P1-P6/T0-T27)

| Mapping | State |
| --- | --- |
| T16–T17 / P5 Gate & Correction runtime | `IN SCOPE` |
| P5C-01 contract | `REQUIRED PREDECESSOR` |
| P1/P2 candidate + persistence paths | `NARROW INTEGRATION / AUTHORITY UNCHANGED` |
| P5 Evidence | `UNCHANGED` |
| P4/P6/T25–T27 | `OUT OF SCOPE / DEFERRED` |

## Goal

Implement the accepted memory Gate/Correction V1 contract mechanically on the existing MemoryClaim/MemoryResolution authority, including non-destructive revision/correction behavior and Boundary gating.

## Scope

Add validators/services and the narrow persistence integration needed to enforce accepted Episode/Fact/Boundary evidence gates and correction transitions on existing candidate-operation paths. Reuse existing persistence/CAS/revision/evidence transactions rather than adding a new semantic state store.

## Allowed Files / Areas

- narrow memory gate/validation/persistence code under `apps/api/src/memory/**`;
- existing P1/P2 candidate application seams only where necessary to call the gate;
- Prisma schema/migration only if P5C-01 explicitly accepted a required forward-only storage change; otherwise no migration;
- focused tests and synthetic fixtures;
- `docs/agent/tasks/P5C-02.md` and PR documentation.

## Inputs

- P5C-01 accepted contract, exact head/PR fresh-read and recorded in PR context;
- accepted P2 semantic/persistence runtime;
- accepted P5 Evidence implementation for read-only evidence resolution.

## Accepted Contracts — exact identities

- `memory-gate-correction-v1` exact accepted head/PR from P5C-01.
- P2-A1 semantic envelope `dbb0cc76f582997a6a647781007648c6937a8992`.
- P2-B persistence contract `717c5ca39e678c6f953d0430768ae715ef0feef2`.
- P2-C main completion `b0d8c49c5cdd83b808c0bb2e411b759c024b40c0` and runtime binding `279fe3d142bd87fc0ab215994ad8ab5dc6a2ee4a`.
- P5 Evidence accepted predecessor identities from P5E-01 through P5E-03.

## Required Behavior

1. Candidate operations cannot mutate durable semantic truth until the accepted P5C-01 gate validates semantic kind, authority, evidence eligibility, revision and legal transition.
2. Fact acceptance requires accepted explicit-elder-evidence proof. Inferred/implicit facts fail closed or remain proposal/unaccepted according to the contract.
3. Episode updates preserve evidence membership and uncertainty semantics; no hidden Fact promotion.
4. Boundary activation/revocation/supersession follows the accepted explicit-intent rules. Silence or unrelated later content cannot revoke an active boundary.
5. Corrections are non-destructive. Existing prior revisions/resolutions/evidence remain auditable; successor state uses existing revision/CAS/transaction mechanisms.
6. Conflicting evidence maps to accepted UNCERTAIN/DISPUTED/review-required semantics rather than last-write-wins overwrite.
7. Revision mismatch, stale/deleted/retention-ineligible evidence, illegal transition or persistence race fails closed with zero partial semantic writes.
8. Program code owns durable mutation; LLM/provider output remains proposal-only.
9. No transcript/audio mutation, no second memory authority, no P4 membership rewrite, no Director behavior change.
10. Existing recording/transcription completion must not be blocked by background memory correction failure beyond already accepted memory-lane behavior.

## Explicit Non-Goals

- no P6 trigger/fence/deadline work;
- no Director prompt/tool change;
- no provider/model/tokenizer/embedding/budget choice;
- no P2-D real provider work;
- no real data/deployment;
- no generic workflow/agent framework.

## Tests

Focused tests must prove:

- explicit Fact acceptance and inferred Fact rejection;
- Episode update retains evidence and revision lineage;
- Boundary explicit activation, no silent revocation, and explicit withdrawal path;
- correction creates successor/supersession without destructive overwrite;
- conflicting evidence produces accepted uncertainty/dispute behavior;
- revision/CAS race produces zero partial writes;
- deleted/retention-ineligible evidence blocks mutation;
- P1/P2 existing accepted regression suites remain green.

Run targeted tests, relevant full memory suite, API typecheck, ESLint, Prettier and `git diff --check`.

## Completion Criteria

- accepted P5 Gate/Correction behavior is enforced on existing runtime paths;
- no adjacent P6/provider/prompt work is introduced;
- one PR with `ARCHITECT_REVIEW_CONTEXT_V1` and exact current head is opened;
- stop at `REVIEW`.

## Review Gate

External Architect PR review; merge/main verification closes the P5 stage.

## Next Task

`null`
