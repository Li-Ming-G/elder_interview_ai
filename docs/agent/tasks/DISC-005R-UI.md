# DISC-005R-UI｜页面内容占比与注意力层级

## 基本信息

- 状态：`DONE`
- 负责人：独立讨论任务 `019fdee4-c4b0-7073-b652-0f0caea99cdc`
- 输入依据：`01`、`03`、`05`、SPEC-DEV-005R、DEV-005R3、现有 workbench 实现
- 交接对象：总控 Agent、DEV-005R3

## 目标

在 UI 实现前，与项目负责人冻结准备页、正常工作台、中断和结束状态的内容占比与注意力层级，使倾听员主要关注长者和转录，同时能一眼判断原始录音是否可靠保存；异常和结束时，安全处置必须取得视觉主导。

## 必须讨论

- 准备页桌面/窄屏的说明、状态和主动作占比；
- 正常录制时顶部、转录、单建议区域的视口口径与目标比例；
- 采集、本浏览器 archive、管理服务录音/manifest、转录、session 五类事实的常驻、折叠和异常提升；
- `interrupted|stopping|processing|completed|failed` 状态下的比例重分配；
- 笔记本、平板和手机的结构变化与验收视口；
- DEV-005R3 只实现建议容器，不提前实现 DEV-007 业务的边界。

## 允许与禁止

- 允许：读取正式文档和现有页面，使用 `impeccable` 给出候选方案并与项目负责人逐项讨论。
- 禁止：修改代码或正式文档、创建提交/PR、实现 AI 建议或扩大 DEV-005R3 范围。

## 完成条件

项目负责人明确表示讨论完成，并输出：已确认决定、未决项、正式文档与 DEV-005R3 任务卡的精确修改建议。讨论任务必须主动通知总控；由总控验收和写回，讨论任务不得自行宣布正式生效。

## 完成记录

- 项目负责人已逐项确认，讨论任务主动回传总控。
- 总控已把 Android Chrome 完整主设备、五视口、分状态比例、五事实分区、高密度转录、建议撤销语义与结束面板规则写回正式依据和任务卡。
- Android Chrome 后台/锁屏等行为仍需真机证据，登记为 CON-021；这不阻塞 R1/R2C，阻塞 R2 对该行为自作解释。
