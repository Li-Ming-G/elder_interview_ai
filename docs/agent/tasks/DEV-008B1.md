# DEV-008B1｜同项目下一次访谈入口与权威动作

## 基本信息

- 状态：`DONE`（REV-050 accepted exact head 已获项目负责人 PASS，PR #51 已合并且 main CI 成功；治理 closeout 已完成）
- 前置：SPEC-REPEAT-INTERVIEW-001、DEV-008A4、SPEC-CONTINUING-CONSENT-001 均已 exact-head PASS/merge/治理收口；可实现 fail-closed runtime。真实 `covered` 端到端完成仍必须等待 BLOCKED 的 SPEC-CONSENT-TEXT-POLICY-001 正式接收
- 输入：`01` §§14-15、`03` §§21-22、`04` §§8.1/10、`05` §§11.1-11.2/12、`06` §17、`08` §§19-20、`09` §§17-18、ADR-038/039
- 交接：DEV-008B2、项目负责人 GitHub 审查

## 目标

在 A1/A4 唯一响应式 HomeShell 内实现 project 卡“开始下一次访谈”和 `POST /projects/:id/next-session`，同 project 只创建 sequence+1 session，并在未知响应、刷新、双击和并发下保持稳定幂等。

## 实现范围

- ordinary project mapper 对每行显式投影 `repeat_interview`；restricted/deleted/assignment invalid 与 reader unavailable 失败关闭；
- project action 与 session row/global new-project 动作分离；desktop/mobile/compact 复用同一 HomeShell/routes/tokens；
- request/payload 首次联网前持久化，actor/action/project/payload binding，request→project→session 稳定锁序；
- basis same-project/latest-completed 校验、非终态 session fence、不同 request 并发至多一个 seq+1；
- 新 session 复用既有 device-check/start 端点但建立新 capture/audio/ASR/speaker/calibration/local runtime；
- 普通后续 session 复用仍适用的同 project consent，不新建 consent record/audio/full-consent page；所有正式 start 前显示版本化轻提醒并显式点击“开始访谈”，该点击不是新 consent；
- 接入正式重授权入口与 consent create 的跨 `consent_text_version` 音频拒绝；若实现需要数据库唯一约束或 migration，必须先暂停并另行审查；
- 权限/consent compatibility/reminder version/restricted/deleted/deletion drift 零业务副作用。

## 禁止

- 不实现 post-analysis、opening Director、memory/QuestionEvidence runtime；
- 不改 A4 completion/review/unknown-response UI，不重写 Home/routes/styles；
- 不新增 Prisma 模型/migration、第二 shell、memory UI、真实 provider 或新 project 语义。

## 验收

`09` §§17-18 中 Home/action、sequence/idempotency、跨项目、新采集、持续授权/轻提醒、安全、响应式和 A4 分界行；完整 CI 与项目负责人 exact-head 审查。REV-050 已接收本任务的 fail-closed runtime 范围。SPEC-CONSENT-TEXT-POLICY-001 接收前真实路径继续失败关闭；本任务 `DONE` 不代表真实持续授权正文、真实 `covered` happy path 或真实试点可用。

## 实现候选（2026-08-14）

- 已从 `origin/main@6f6363f517a6588ff4eb31aee7996b7116092c03` 建立独立 worktree 与分支 `codex/dev-008b1-repeat-interview-runtime`；开工基线 main CI `31765760508` SUCCESS；non-Draft [PR #51](https://github.com/Li-Ming-G/elder_interview_ai/pull/51) 已建立。
- 单一服务端 repeat/continuation current-facts owner、ordinary 显式 DTO、next-session 锁序/幂等/并发、start reminder version gate、capture current gate、Home IndexedDB unknown replay 与三视口 UI 已实现；无 Prisma/migration。
- 默认/生产 policy 保持 unavailable；synthetic fictional policy 只允许显式测试 DI。正式重授权 seam 在正文/policy 缺失时停止且不创建 consent/audio。
- 项目负责人已对 old exact head `efac390d5c3a4586c5f5da464490bf8ae7fb9e68`（CI `31769522565` SUCCESS）正式 `REQUEST_CHANGES`，P0=0/P1=3/P2=0；旧 head/CI/结论永久保留。定向修复限定为：旧 `/sessions` 仅首场、repeat `created` 当前页 mic/device 链路、unknown loser 仅使用服务端安全 session pointer。
- 修复候选本地证据：fresh PostgreSQL 14 migrations deploy/status；integration 84/84、auth 23/23、unit 362/362、ordinary Chromium 27/27、auth Chromium 5/5，以及 format/lint/typecheck/build/smoke/diff-check PASS。项目负责人已对 accepted exact head `30975626f00be0526da2d17d200fd1744b9a721c` / CI `31781577641` 正式 PASS，P0/P1/P2=0；PR #51 已以 `6e546853672c687c70a4112bf07d1dfe1763c75f` 合入 main，main CI `31785578105` attempt 2 SUCCESS。attempt 1 ordinary Chromium unknown-project 时序 flake与旧 REQUEST_CHANGES 永久保留。
- 详细范围、失败历史、风险与人工审查重点见 [`DEV-008B1` 候选交接](../handoffs/DEV-008B1.md) 与 [`REV-050`](../reviews/REV-050.md)。联合治理收口完成后本任务 `REVIEW→DONE`；SPEC-CONSENT-TEXT-POLICY-001 继续 `BLOCKED`，故真实 covered 仍不可用。DEV-008B2 仅按既有任务契约转为 `READY`，本 closeout 未启动其实现。
