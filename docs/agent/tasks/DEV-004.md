# DEV-004｜实时 ASR 与说话人映射（父任务）

## 基本信息

- 状态：`DONE`
- 负责人：总控 Agent
- 前置依赖：DEV-003
- 交接对象：DEV-005、DEV-006、项目负责人（GitHub 审查）

## 目标

在不影响原始录音可靠保存的前提下，建立供应商中立、可降级、可追溯的实时转录与会话内说话人映射链路，为访谈工作台和确定态证据消费提供稳定 seam。

## 输入依据

`00` 至 `10`、MVP-V01、ADR-002/006/013/018/019、REV-011/012、HO-016/020/021、CON-014 至 CON-016。

## 子任务顺序

1. DEV-004A：确定态转录证据核心与供应商中立适配器（`DONE`）；
2. DEV-004B1：业务 WebSocket 服务端协议核心、合成 PCM 与短时恢复；
3. DEV-004B2：真实 Chromium 合成 PCM、interim/final 展示、背压与短时重连（`DONE`）；
4. SPEC-DEV-004C：冻结 speaker stream、可信角色、校准、修正和下游失效契约（`DONE`）；
5. DEV-004C1：正式流说话人校准、控制内容标识、unknown 门禁和移动端低负担状态（`DONE`）；
6. DEV-004C2：单段/批量说话人修正、历史/审计与 correction revision/membership seam（`DONE`）；
7. 真实供应商、重连故障区间和离线补转录在供应商/协议明确后另行拆分。

父任务不得因 DEV-004A 或 DEV-004B1 完成而提前 `DONE`。每个后续子任务开工前必须补齐其跨模块契约。

## 全局边界

- 只使用虚构文本、合成音频和隔离数据库；
- ASR 故障不得改变或损害原始录音、audio manifest 和安全结束能力；
- interim 不得进入正式数据库、长期记忆或 AI；
- 原始转录文字与原始说话人角色不可覆盖；
- 不选择真实供应商、不提交密钥、不对公网开放内部夹具；
- 生产 WSS、真实麦克风、长时性能、真实数据合规与 CON-013 仍为真实试点门禁。

## 父任务验收

- 浏览器 PCM 经业务 WebSocket 到供应商中立 adapter，并能实时展示 interim/final；
- final 在 1 秒目标内幂等落库，原文和原角色不可覆盖；
- 会话内说话人校准、未知回退、单段/批量修正与历史保留可验证；
- provider speaker namespace 以持久 speaker stream 隔离；只有用户确认角色进入角色相关下游消费，控制句与 unknown 不污染长期记忆/已问问题；
- ASR/WebSocket 故障时录音继续，原始 manifest 与安全结束不受影响；已收到的 final 不丢失，无法完整转录时明确显示并持久化 `degraded|not_started`，不伪造 final；
- 项目负责人按最终 GitHub head 给出 PASS 后才可 `DONE`。

## 最终收口（2026-08-09）

- A/B/C 子任务已全部完成，C2 由 REV-029 PASS 并合入 main。
- 项目负责人明确决定当前阶段不考虑补转录，优先尽快形成可用产品。该决定只把故障区间持久化、真实供应商重连和离线补转录后置到 `HARDEN-ASR-001`，不降低原始录音、manifest、安全结束、降级可见和不伪造转录的硬门禁。
- REV-030 依据 A/B/C 已通过的 GitHub 审查、main `004dacc` 与 CI `31311278529`，在上述内部 MVP 边界下完成父任务聚合验收；DEV-004 `DONE`。真实供应商与补转录仍须在完整 MVP/真实试点前另行验收。
