# DEV-005R4｜首次访谈真实采集纵向验收与收口

## 基本信息

- 状态：`REVIEW`（候选；仅项目负责人可在 GitHub 对 final head 给出 PASS）
- 负责人：独立任务 `019fe468-6cb3-7cf0-b327-4a46e2d7aae9`
- 输入依据：`SPEC-DEV-005R`、DEV-005R1/2/3 PASS、`06`、`09`、ADR-023/024、CON-020/021/022
- 前置依赖：DEV-005R1、DEV-005R2、DEV-005R3 GitHub 审查 PASS
- 交接对象：项目负责人、总控 Agent、DEV-006/007 后续任务

REV-025 已确认 DEV-005R3 / PR #15 final head `481ee259` 正式 PASS，并以 merge `8d5c4c5` 合入 `main`；本任务全部开发前置已满足。进入 R4 不代表父 DEV-005 或 CON-020/021/022 已关闭。

2026-08-09 已在独立 worktree 启动正式验收任务。实施顺序固定为：环境与证据表核对、桌面自动化、Android 单步人工协作、任务内最小缺陷修复、完整门禁与非 Draft PR；设备证据必须按操作前后资源 identity 和高水位记录，不能以页面观感替代。

## 目标

证明正式路由上的真实浏览器链路从准备、采集、归档、上传、实时转录、中断恢复到安全结束可重复执行，复验普通音量麦克风检测，并收口 CON-020、CON-021、CON-022 与父 DEV-005。

## 允许修改

- 跨模块集成/E2E/故障注入测试与最小缺陷修复；
- 任务板、追踪、冲突、审查、交接和验收证据。

## 禁止修改

- 扩展 AI、项目管理、回顾、导出、删除、真实供应商或生产基础设施；
- 使用真实长者数据；
- 在项目负责人审查前自行关闭 DEV-005 或 CON-020。

## 验收

- 桌面真实 Chromium 与至少一台目标 Android Chrome 真机各完成 5–10 分钟虚构内容；单流、唯一对象、archive/ACK、上传、PCM、stop manifest 全链路；桌面模拟视口不能替代手机采集可靠性；
- 刷新、崩溃检测、网络、响应丢失、撤权、assignment、麦克风、容量、ASR 故障矩阵；
- Android Chrome 旋转、切后台/锁屏、权限/麦克风中断与刷新恢复；不能继续可靠采集时必须显式 interrupted；
- 在 R2 已验证的 OnePlus/Chrome 基线上，从 `page_recovery_detected` 显式 resume 到下一 generation，证明复用同一 session/audio object/local job、archive sequence/timeline 累计延续、PCM sequence 重置并使用服务端 offset，随后完成安全结束、manifest 和真实终态；
- 五目标视口、全 session/capture 状态、页面比例、五类事实、无障碍和窄屏验收；
- 完整 CI 全绿、非 Draft PR、项目负责人绑定 final head PASS 后方可将 DEV-005/CON-020/CON-021/CON-022 收口。

## REVIEW 候选证据（2026-08-09）

- 最小修复：iteration-coach 的独立只读复核发现恢复代 `timeline_offset_ms` 只持久化、未映射到服务端转录事件/落库/drain；现统一在服务端可信边界映射，wire PCM 仍保持 generation-relative。未修改公共 API、WebSocket schema、数据库字段、状态枚举或 interruption reason。
- 桌面正式路由：真实 Chromium 151 使用浏览器原生 MediaStream/MediaRecorder/AudioContext 与内置虚构音源运行 `300000ms`；刷新前 archive high-water `146`、timeline `149942ms`，显式恢复后同 session/object/job、generation `0→1`、新 stream、首帧 PCM `0..100ms`，最终 296 段、timeline `301315ms`、manifest complete、ASR drained、session completed。
- Android 正式路由：OnePlus GM1900、Android 12 / SDK 31、Chrome `150.0.7871.188`，室内约 30–50cm 普通说话由用户多次确认可用；安静/无输入正式判定失败并禁用开始。正式无 query 路由累计 `501485ms`（约 8分21秒），唯一 MediaRecorder 与 AudioContext source 共用同一 stream/track。
- Android 恢复不变量：刷新后稳定页 gUM/MediaRecorder 均为 0，服务端 `interrupted/page_recovery_detected`；显式恢复保持同 session `b09…`、audio object `c9a…`、local job `interview-capture:b09…`，generation `0→1`、stream 更新，archive high-water `426→490`，generation 1 首个 wire PCM 为 sequence `0` / `0..100ms`，服务端 offset 与首个 generation 1 转录 start 均为 `436604ms`。
- Android 安全结束：最终 `dataavailable` 先于 stop；491/491 分片、delivery 0、archive 491 段仍保留，唯一 audio object manifest checksum `2b4870…5631`，ASR `drained`、无 transcript error，session 真实终态 `completed`。未提交 audio Blob、转录正文、Cookie、令牌、数据库凭据或设备序列号。
- 生命周期与界面：R2 的旋转/后台/锁屏/撤权正式证据继续有效；R4 在 R3 正式工作台额外复验旋转与后台返回均不刷新、不重新取麦、不创建新 capture。五视口 × 七状态、触控/焦点/滚动/对比度/reduced motion 沿用 R3 门禁，并在当前基线复跑 3 项正式 Playwright 验收。
- 本地门禁：unit 32 files / 212 tests、integration 7 files / 42 tests、R4 targeted unit 21、targeted integration 15、桌面 R4 5 分钟 E2E 1/1、普通 Playwright 3/3、typecheck 均通过；提交前仍执行最终 format/lint/typecheck/build/diff 检查。CON-020/021/022 与父 DEV-005 在项目负责人 PASS 前继续 `OPEN/IN_PROGRESS`。
