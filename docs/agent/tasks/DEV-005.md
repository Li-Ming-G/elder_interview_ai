# DEV-005｜首次访谈页面闭环（父任务）

## 基本信息

- 状态：`IN_PROGRESS`
- 负责人：总控 Agent
- 前置依赖：DEV-002、DEV-003、DEV-004B2、SPEC-FE-001；安全结束子链另依赖 SPEC-SESSION-END-001
- 交接对象：DEV-007A、纵向集成/产品验证角色

## 目标

用预创建虚构项目交付“准备页 → 转录优先工作台 → 安全结束页”，把现有登录、录音和实时转录 seam 组合为首个正式业务页面闭环。

## 子任务

1. DEV-005A：准备页与正式页面路由外壳；
2. DEV-005B：转录优先工作台与浏览器实时 seam 集成；
3. DEV-005C：按 SPEC-SESSION-END-001 实现服务端会话安全结束编排；
4. DEV-005D：消费 DEV-005C 服务端事实的安全结束页薄集成；
5. AI 建议卡的真实服务接入归 DEV-007A，DEV-005 只预留稳定展示边界。

上述 A/B/C 保留各自历史验收事实；旧 DEV-005D 未实施并已由 `SPEC-DEV-005R` 重构链路取代。当前正式收口子链为：DEV-005R1、DEV-005R2C、DEV-005R2、DEV-005R3 已 `DONE`，DEV-005R4 已 `READY`，负责最后真机纵向验收与父任务关闭。

## 全局边界

- 不实现项目列表、多项目管理、完整回顾编辑、复杂标记、导出/删除 UI；
- 不把内部 audio/realtime harness 当作正式页面；
- 不用前端状态绕过 assignment、正式授权或服务端 session 门禁；
- 原始录音、上传和 ASR/AI 故障保持解耦；
- 页面不能在 `stopping/processing` 时提前显示完成。
- stop/recover 契约未通过和 DEV-005C 未完成前，不得用前端 fixture、固定延时或本地状态伪造安全结束。

## 父任务验收

- 本地 Chromium 可重复走通三页最小路径；
- 转录区、建议卡和安全结束行为符合 `01/03/09`；
- 真实 AI 建议未接入时明确使用 fixture/占位 seam，不宣称 DEV-007 已完成；
- 子任务完成且项目负责人审查通过后才可 `DONE`。
