# CKPT-A-LEGACY-PREPARE-BRIDGE-PACK

Owner-authorized bounded repair pack for the final Checkpoint A retest blocker found on the legacy `/prepare` route.

Sequence:

`CKPT-A-LEGACY-PREPARE-BRIDGE-01 -> null`

The task must only bridge the frontend Start enablement to the already accepted PR #122 backend legacy first-session self-heal path. No backend authority broadening, repeat-session policy change, database/browser reset, P1-P6 semantic change, provider/ASR/Director change, schema migration, deployment work, or unrelated UI redesign is authorized.

Canonical Task Card:

- `docs/agent/tasks/CKPT-A-LEGACY-PREPARE-BRIDGE-01.md`
