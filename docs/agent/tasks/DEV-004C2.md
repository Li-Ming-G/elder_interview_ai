# DEV-004C2｜单段与批量说话人修正及失效证据

## 基本信息

- 状态：`IN_PROGRESS`
- 负责人：独立实现任务 `019fe614-9503-7891-a1d3-8708c60166e0`
- 前置依赖：SPEC-DEV-004C PASS、DEV-004C1 PASS
- 输入依据：`03/04/05/06/07/09`、ADR-025、SPEC-DEV-004C、DEV-004C1
- 交接对象：DEV-006/007、完整回顾切片、项目负责人 GitHub 审查

## 目标

实现默认单段角色修正、稳定批量预览与原子执行、追加式历史/审计，以及供 DEV-006/007 消费的 correction revision 和受影响 segment membership；不实现尚不存在的 AI 派生重算。

## 允许修改

- C2 所需前向 migration、共享角色修正 DTO、持久 preview/membership、服务/API 和直接测试；
- 首次工作台的低负担单段修正；
- 批量预览/执行服务端契约与内部测试 seam；
- correction operation、membership、revision 和审计；
- 本任务 task/handoff 和治理记录。

## 禁止修改

- 在首次工作台加入复杂批量历史编辑器；
- 覆盖 original role/text、content kind 或映射历史；
- 直接实现/伪造 memory、asked-question、suggestion 或 session-note 重算；
- 自动覆盖人工确认事实或解除正式边界；
- 真实供应商、声纹、多人或跨会话身份。

## 交付与验收

- 单段默认且使用乐观版本门禁；
- 批量端点必须是同一 session/stream/label 且未被默认排除的 final；按 `(start_ms,id)` 闭区间形成候选后排除内部单段人工修正，preview 稳定并展示候选/排除/最终数量；
- 任一 preview 成员漂移时整批失败，无半成功；
- 成功修正写 operation/membership、一次 revision 和审计；
- DEV-006/007 可据 revision/membership 使旧派生失效；人工事实/边界保持；
- 完整 CI 与项目负责人 GitHub PASS 后才可 DONE。

## 当前 deletion scope 集成边界（CON-023）

- 正式 `05` 要求 project/session/segment_range 删除 scope 命中时拒绝角色修正；该产品与安全语义保持不变。
- 当前代码基线尚无 `content_marker`、`deletion_request`、transition 或统一 scope 查询，且删除子系统归 DEV-008；CON-006/007 仍阻塞其安全实现。
- 本任务不得为通过 C2 测试新增没有合法 producer/状态机的 deletion 半模型或 no-op guard。当前只实现并验证真实存在的 assignment、最新授权、project `restricted|deleted` 门禁。
- session/segment deletion scope 命中明确记为本任务未验证、非阻塞延期集成项。DEV-008 将来实现正式 producer/read model 后，必须在 C2 单段、preview、execute 的锁后复核中接入统一 scope guard，并补 scope 与并发测试，随后关闭 CON-023。
