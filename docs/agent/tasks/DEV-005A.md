# DEV-005A｜首次访谈准备页与安全结束页

## 基本信息

- 状态：`BLOCKED`
- 负责人：待创建的前端实现任务对话
- 前置依赖：DEV-002、DEV-003、SPEC-FE-001 审查通过
- 交接对象：总控 Agent、DEV-005B

## 目标

使用已分配、已预创建的单个虚构项目，建立正式业务页面路由外壳、首次访谈准备页和安全结束页；不依赖项目列表。

## 允许修改

- `apps/web` 中正式页面路由、页面组件、局部样式和直接测试；
- 调用现有项目/授权/session/audio 状态 seam 的最小客户端层；
- 对应 Chromium 虚构数据场景；
- 如发现缺少只读聚合接口，只能登记并提请总控冻结契约，不能自行扩 API。

## 禁止修改

- 不实现项目列表/新建项目、完整工作台、AI 引擎、回顾/导出/删除 UI；
- 不改 Prisma、WebSocket wire contract、录音可靠性语义或授权规则；
- 不用 query 参数 harness 作为正式入口；
- 不声称真实麦克风、真实试点或生产完成。

## 交付物

- 准备页：长者、预计时长、简短说明、正式授权状态、麦克风/输入状态、开始操作；
- 开始门禁错误的明确显示；
- 结束页：长者、实际时长、各链路处理状态、查看记录、完成离开；
- 页面路由与测试交接。

## 验证与验收

- 未分配、无有效授权、错误 session 状态和麦克风失败不能开始；
- 页面勾选不替代授权证据；
- `stopping/processing/failed/completed` 显示与服务端事实一致；
- unit、typecheck、build 和适用 Chromium 场景通过；
- 完成后提交 GitHub，由项目负责人审查。
