# DEV-005R2｜浏览器单流采集、归档与交付控制器

## 基本信息

- 状态：`DONE`
- 负责人：独立任务 `019fe054-0dcf-7c40-b4fa-121863a9d69c`
- 输入依据：`SPEC-DEV-005R`、DEV-005R1/DEV-005R2C PASS、`05`、`06`、`09`、ADR-023/024、CON-021
- 前置依赖：DEV-005R1、DEV-005R2C GitHub 审查 PASS
- 交接对象：DEV-005R3、DEV-005R4、总控 Agent

前置依赖已于 REV-020、REV-022 和 PR #13/#12 合并后满足；独立项目任务已启动。

正式 controller、直接 unit/Chromium 覆盖与全仓门禁已经完成并合入 main，交接见 [`DEV-005R2`](../handoffs/DEV-005R2.md)。REV-024 进一步完成目标 Android Chrome 真机生命周期验收；DEV-005R2 已 `DONE`，DEV-005R3 可以解锁。CON-021 继续 `OPEN`，由 R4 完成显式恢复与安全结束全链路后关闭。

## PR #14 REQUEST_CHANGES 定向修复

- 总控对 head `c8aa8e9` 定向预审发现三项 P1：start 前置失败覆盖首因并泄漏锁、resume 新 generation 麦克风失败泄漏锁、local job 丢失时未持久报告服务端 active/preparing capture。
- start/resume 现显式跟踪 controller/runtime lock owner 与 server/runtime 接管点；cleanup 不再依赖 `requiredJob()`，临时 stream、runtime interrupt、report 与 release 均不覆盖 primary error。
- 经总控授权，在 IndexedDB v4 既有 `upload-jobs` store 增加独立 `CaptureInterruptionReportRecord`；使用 `capture-interruption-report:v1:${sessionId}:${generationNo}:${audioStreamId}` key、事务级 get-or-create 与严格 runtime discriminant，不含 MIME/archive/localJobId 或 resume job 语义。
- orphan report 每次发送前重新读取服务端 capture；pending/acknowledged 都不能跳过服务端 snapshot。响应丢失、report 失败或 acknowledged 写失败均保留同 request ID；新 generation/stream 使用新 key，损坏/冲突记录失败关闭。
- workbench 仅用既有失败 seam 捕获 `recover()` rejection，未扩展 R3 视觉或 capture phase UI。
- 定向 unit 4 files / 40 tests、全量 unit 32 files / 182 tests、integration 41、auth 13、普通 Chromium 7/7、auth Chromium 4/4 及其余完整本地门禁已通过。REV-023 当时已对 final head `829adf8` 确认三项 P1 关闭、P0/P1=0，PR #14 以 merge `5527af2` 合入 main；当时因无 Android 设备而保持 `REVIEW`，该历史门禁随后由 REV-024 真机证据解除。

## REV-024 Android Chrome 真机验收收口

- 设备：OnePlus GM1900，Android 12 / SDK 31，Chrome `150.0.7871.188`，视口约 384×731、DPR 2.8125。
- 正式路由连续采集约 6 分 20 秒；本浏览器 archive `0..371` 共 372 片、无序号缺口，服务端最终收到 372 片。旋转、约 20 秒切后台和约 20 秒锁屏均保持同一 session、audio object、audio stream 和 generation 0，未刷新、未重新请求麦克风、未创建新 capture。
- 刷新后同一 local job、archive、高水位和稳定 request ID 保留；服务端 session/capture 进入 `interrupted`，原因 `page_recovery_detected`，页面没有自动请求麦克风。
- 第二条正式会话撤销麦克风权限后，本地 archive `0..70` 共 71 片且无缺口；服务端收到 70 片并明确进入 `interrupted`，原因 `microphone_ended`，未继续伪装采集正常。
- 独立只读验收认为 R2 的 controller 事实门禁已经满足，P0/P1=0；旧 workbench 在 interrupted 后仍显示“服务端进行中”以及缺少恢复/安全结束动作属于 R3 的明确输入，不反向阻塞 R2。低音量检测偏严登记为 CON-022 P2。
- DEV-005R2 `DONE`，DEV-005R3 `READY`。CON-021 保持 `OPEN`，R4 必须复验显式 resume 的下一 generation、同一 session/object/job、累计 archive 时间轴和安全结束 manifest。

## 目标

把 DEV-005R2C 浏览器核心与 DEV-005R1 公共契约接成位于路由之上的 session-scoped `InterviewCaptureController`，可靠绑定同一 session/job/object/generation。

## 允许修改

- `apps/web/src/audio/**`、`apps/web/src/realtime-transcription/**`；
- 新增独立 controller/worklet/store 文件和对应单元/Chromium 测试；
- 只为挂接 controller 做最小 `apps/web/src/interview/**` seam 变更。

## 禁止修改

- 后端、Prisma、`packages/contracts`；
- 结束状态页面的最终视觉与文案；
- AI、真实供应商、云存储、跨设备接管。

## 交付与验收

- 单次正式 getUserMedia、MediaRecorder + AudioWorklet；
- archive 单写与 delivery 引用，ACK 不删 archive；
- 稳定 request ID、顺序上传泵、浏览器锁、容量门槛、dirty checkpoint；
- 网络、刷新、track ended、recorder/archive/auth 故障按规范降级；
- 目标 Android Chrome 真机验证 visibility、切后台、锁屏、旋转和音频设备中断；能够继续时保持 archive/时间轴/单 controller，不能证明可靠继续时持久报告 interrupted；
- 旋转只重排，不刷新、不重新申请麦克风、不创建新 capture；现有 interruption reason 不足时先升级正式契约，不得由页面私有解释；
- 真实 Chromium 虚构内容 5–10 分钟基础证据；
- 完整 verify 通过，状态只能到 `REVIEW`，等待项目负责人 GitHub PASS。
