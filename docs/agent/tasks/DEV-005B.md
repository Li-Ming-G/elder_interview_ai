# DEV-005B｜转录优先访谈工作台

## 基本信息

- 状态：`BLOCKED`
- 负责人：待创建的前端实现任务对话
- 前置依赖：DEV-004B2 合并、DEV-005A 页面外壳
- 交接对象：总控 Agent、DEV-005D、DEV-007A、纵向集成角色

## 目标

把已验证的浏览器实时转录 seam 接入正式工作台，完成顶部状态、转录主体、回看与新内容提示、底部单问题卡占位边界，并为 DEV-005D 保留明确的结束动作挂载位置。

## 输入依据

`AGENTS.md`、`00` 至 `10`、MVP-V01、SPEC-FE-001、DEV-004B2、DEV-005A、ADR-019/020/021、CON-018/019 及最新相关交接。

## 允许修改

- `apps/web` 正式工作台页面、实时客户端适配、局部样式和直接测试；
- 对现有 B2 transport 做不改变 wire contract 的复用/抽取；
- 适用 Chromium E2E。

## 禁止修改

- 不实现真实 LLM、建议持久化或“换一个”后端契约；
- 不接真实麦克风、AudioWorklet 或真实 ASR；
- 不实现项目管理、完整回顾、批量修正、复杂标记和导出；
- 不调用 stop/recover，不根据本地 transport 状态显示 session 已完成；
- 不破坏 MediaRecorder/原始上传独立性，不把 ASR 失败映射为录音失败。

## 交付物

- 顶部窄状态栏；
- interim/final、长者/倾听员/unknown 可辨的转录主体；
- 自动跟随、用户回看暂停、新内容计数和“回到最新”；
- 单个建议/继续倾听的稳定展示接口，真实数据由 DEV-007A 接入；
- ASR/AI 降级和结束动作挂载位置；本任务不调用 stop/recover，也不显示结束完成状态。

## 验证与验收

- 工作台主要空间用于转录，移动/窄屏仍可完成核心操作；
- 回看期间不强制跳到底部，返回最新后恢复跟随；
- 重连/replay 不重复 final；
- ASR/AI 失败不停止原始录音；
- unit、typecheck、build 和真实 Chromium 合成数据场景通过；
- 完成后提交 GitHub，由项目负责人审查。

## Agent 决策权限与 Git 责任

- 可在已冻结 wire contract 内复用/抽取 B2 transport，并决定工作台组件和滚动状态组织；
- 若需改变 WebSocket 字段、恢复语义、公共 API、依赖或建议契约，停止并反馈总控；
- 实现 Agent 不修改后端和治理文档，不代替审查者宣布 PASS；总控负责 Git 与收口。
