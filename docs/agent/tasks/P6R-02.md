# P6R-02

Status: `DEFERRED`

## Architecture Mapping (P1-P6/T0-T27)

| Mapping | State |
| --- | --- |
| T18–T20 / P6 trigger + gate runtime | `IN SCOPE` |
| P1 finalized/buffer trigger | `NARROW INTEGRATION` |
| P6R-01 Runtime Orchestration V1 contract | `REQUIRED PREDECESSOR` |
| T21 generation fencing | `DEFERRED TO P6R-03` |
| T22–T24 deadline/error closeout | `DEFERRED TO P6R-04` |
| P5/P4 semantic authorities | `UNCHANGED` |
| T25–T27 | `DEFERRED` |

## Goal

Implement the accepted finalized-transcript buffer and automatic/manual generation gate without changing Director semantics.

## Scope

Integrate the accepted Runtime Orchestration V1 trigger rules into the existing question-generation runtime and existing finalized-ASR/presentation seams.

## Allowed Files / Areas

- `apps/api/src/question-orchestration/**`;
- narrow finalized-event integration under `apps/api/src/realtime-transcription/**` where mechanically required;
- narrow QuestionPresentation read/gate seam where required;
- focused tests;
- `docs/agent/tasks/P6R-02.md` and PR documentation.

Do not redesign P1/P2 memory semantics, P5 evidence/gate semantics, prompts or providers.

## Inputs

- P6R-01 exact accepted contract head/PR, fresh-read and recorded in PR review context;
- existing finalized transcript event surface;
- existing QuestionOrchestrationService compatibility behavior;
- existing QuestionPresentation/AiJob idempotency authority.

## Required Behavior

1. Only finalized transcript events participate in automatic eligibility; interim/untrusted content does not trigger Director.
2. Rapid finalized segments converge through the accepted bounded debounce/buffer behavior.
3. Automatic generation respects the accepted minimum-interval gate and does not create one Director attempt per segment.
4. Automatic attempts are deterministic and deduplicated/idempotent for the same accepted trigger identity.
5. Manual-next starts immediately after existing availability/authority checks and bypasses automatic debounce/minimum-interval waiting.
6. Manual-next does not bypass presentation revision, snapshot, authorization, retention/deletion, idempotency or publication checks.
7. Automatic and manual triggers feed the same canonical generation authority rather than parallel independent publication paths.
8. Recording/finalized-ASR completion must not synchronously wait for Director completion.
9. Existing P4 frozen context and P5 evidence behavior remain unchanged.
10. Do not implement the full generation/publication fence beyond the minimum accepted interfaces required for P6R-03.

## Explicit Non-Goals

- no generation fence redesign beyond P6R-01-defined seam;
- no deadline/error redesign;
- no prompt/provider/model changes;
- no new semantic memory state;
- no new workflow/agent framework;
- no product tuning of numeric interval/debounce values unless already mechanically frozen by P6R-01.

## Tests

Focused tests must prove:

- rapid finalized segments converge according to the accepted trigger rule;
- interim content does not generate;
- automatic minimum interval is respected;
- manual-next bypasses automatic waiting;
- duplicate trigger identity does not duplicate durable generation;
- recording/finalized-ASR path returns without waiting for generation completion;
- existing P5 evidence-round and P4 consumer regressions remain green.

Run targeted suites, relevant question-orchestration/realtime suites, API typecheck, ESLint, Prettier and `git diff --check`.

## Completion Criteria

- trigger/gate runtime matches the accepted P6R-01 contract;
- no adjacent fence/deadline/prompt/provider expansion;
- one PR with exact `ARCHITECT_REVIEW_CONTEXT_V1`;
- Worker stops at `REVIEW`.

## Review Gate

External Architect PR review; `PASS` + merge + successful main verification required before P6R-03.

## Next Task

`P6R-03`
