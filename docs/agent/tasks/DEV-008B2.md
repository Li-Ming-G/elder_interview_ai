# DEV-008B2｜会后分析与第二次访谈开场建议

## 基本信息

- 状态：`REVIEW`（独立执行分支 `codex/dev-008b2-post-session-opening`，基线 `origin/main@1eb26b2`；non-Draft [PR #56](https://github.com/Li-Ming-G/elder_interview_ai/pull/56)；old exact head `0e703af` / CI `31800324817` 获 [REV-052](../reviews/REV-052.md) REQUEST_CHANGES，P0=0/P1=3/P2=0，正在定向修复）
- 前置：SPEC-REPEAT-INTERVIEW-001、DEV-008A4、SPEC-CONTINUING-CONSENT-001 与 DEV-008B1 fail-closed runtime 均已 exact-head PASS/merge/治理收口
- 真实 provider 门禁：deterministic fake/unavailable seam 可在上述依赖关闭后实现；真实 LLM 质量/试点结论继续依赖真实 ASR provider PASS、真实 LLM 选择与数据治理门禁
- 输入：`03` §§21.3/22.3、`04` §§8.2-8.3/10、`05` §§11.3-11.4/12、`07` §18、`08` §§19-20、`09` §§17-18、ADR-027-031/038/039

## 目标

把 canonical completed 接入既有 Memory/QuestionEvidence 生产调用链；第二次 session calibration gate 可以先进入 waiting，但只有 basis session 两个 post-analysis lane 均 terminal 后才恰好触发一次基于同 project eligible facts 的 opening suggestion。降级 lane/gate 不伪造当前 speaker membership，AI 等待/失败不影响录音、completed 或回顾。

## 实现范围

- durable/stable post-session trigger root 与两 lane job identity；完成提交后非阻塞触发，GET/render/WS replay 不触发；
- trusted elder final→`MemoryService.extract` current resolution；trusted interviewer final→`QuestionEvidenceService.reconcileActualQuestions`，degraded/unknown 为 unjudged；
- `post_session_analysis` 内容无关状态、attempt/retry evidence，失败不回退 completed；
- calibration confirmed/failed/skipped 或 no-attempt provider-unavailable 的稳定 gate identity；basis lane 非 terminal 时只 waiting，不创建 opening job/attempt/Context；
- 两 lane terminal 后绑定 basis analysis trigger + calibration gate，`second_session_opening` attempt exact once；成功 lane 必须纳入，unjudged/failed/cancelled/unavailable 诚实降级；
- same-project current/eligible memory、published/judgeable/eligible actual asked、boundary/policy/trusted membership 的 Context snapshot；
- 复用唯一 Director/Prompt/Schema/QuestionEvidence writer/current/history/REST/WS，provider unavailable 诚实降级。

## 禁止

- 不新增第二 AI、planner/critic、question history、memory/summary/pending UI；
- 不把 displayed/manual-next/replaced/not-observed 当 actual asked；
- 不用 deterministic fake 宣称真实 LLM/provider 或真实试点通过；
- 不改变 DB ownership、privacy/deletion/retention、A4 UI 或每 session raw-data 隔离。

## 验收

`09` §§17-18 中 post-analysis success/failure、analysis-first、calibration-first、单 lane degraded、刷新/重放并发、Context membership、opening exact once、持续授权动态门禁、provider unavailable、安全/retention/drift/UI scope 行；完整 CI 与项目负责人 exact-head 审查。实现候选已完成并保持 `REVIEW`，详见[交接](../handoffs/DEV-008B2.md)。真实 LLM/provider、正式持续授权正文/真实 `covered` 与真实试点仍受 DEV-LLM-PROVIDER-001、SPEC-CONSENT-TEXT-POLICY-001 等独立门禁约束，均尚未实现或验收。

## 实现候选摘要

- canonical completed commit 后以稳定 trigger root 非阻塞协调 memory/actual 两 lane；启动与周期 reconciliation 只读取持久事实，GET/list 仅投影。
- calibration-first 严格投影 `waiting_basis_analysis` 且 request/attempt/Context/job 均为空；两 lane terminal 后才以稳定 gate identity 创建一次 opening。
- Context 复用既有 scope/membership/derived-output seam，以 trigger、session scope 与 context builder process version 固化 basis、analysis identity 和两 lane outcome；成功 lane 只消费 same-project eligible current memory 与 published judgeable actual asked。
- 生产默认继续 fail closed；测试只使用显式 fictional consent policy 与 local deterministic provider seam，不构成真实 LLM、真实 `covered` 或真实试点证据。
- 无 Prisma schema/migration、无新 UI、无第二 AI/history owner；QuestionEvidence 仍是展示/attempt/event 唯一 writer。

## 本地验收候选

- format/lint/typecheck/build/smoke/diff-check PASS；unit 62 files / 375 tests，fresh PostgreSQL integration 15 files / 86 tests，auth 4 files / 23 tests。
- ordinary Chromium 27/27、auth Chromium 5/5 PASS；fresh database 14 migrations deploy/status PASS。
- 首个实现 head `c00a5a42d8c32f0d4b23edd009291fb25ccea6ea` 的完整 CI `31799994616` 全步骤 SUCCESS；最终治理 head 仍须独立 exact-head CI。
- 旧失败与修复过程完整保存在[交接](../handoffs/DEV-008B2.md)；当前结论仅为实现候选，不代表 PASS/DONE。

## REV-052 定向修复状态

- 三项 P1 已形成新实现候选：current stream/capture gate + consumer-session fence；pending-no-attempt/running-attempt 两类恢复；持久 Context provenance 与 Director exact frozen membership。
- 新增最小 Prisma migration 与 4 项定向 integration 回归；fresh PostgreSQL 15 migrations deploy/status、完整 integration 15 files / 88 tests PASS，完整 exact-head CI 待 GitHub 验证。
- 状态继续 `REVIEW`；old `0e703af` / `31800324817` / REQUEST_CHANGES 永久保留，不自行 PASS/DONE/merge。

### 第二次定向复审

- `996994f508b4c7833410e6b9c0a6073dd7ca486e` / CI `31812498482` SUCCESS 获项目负责人 `REQUEST_CHANGES` P0=0/P1=1/P2=0；与 old `0e703af` P1=3 一并永久保留。
- 已关闭 current-stream gate、原两个 opening crash windows、Context provenance 主体。唯一 P1 为无 snapshot 的 stale context job：同一 30 秒 grace 后 terminal、重复 reconcile 幂等、late writeback 整体回滚、consumer attempt≤1。
- 先本地修复验证；等待 PR #55 merge 后人工整合 latest main，再形成唯一最终复审 head。
