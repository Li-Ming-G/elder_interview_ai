# DEV-008B2｜会后分析与第二次访谈开场建议

## 基本信息

- 状态：`BLOCKED`
- 前置：SPEC-REPEAT-INTERVIEW-001 项目负责人 exact-head PASS/merge；DEV-008A4 / PR #44 新 exact-head PASS/merge；DEV-008B1 的 session/action seam 可用
- 真实 provider 门禁：deterministic fake/unavailable seam 可在上述依赖关闭后实现；真实 LLM 质量/试点结论继续依赖真实 ASR provider PASS、真实 LLM 选择与数据治理门禁
- 输入：`03` §21.3、`04` §§8.2-8.3、`05` §§11.3-11.4、`07` §18、`08` §19、`09` §17、ADR-027-031/038

## 目标

把 canonical completed 接入既有 Memory/QuestionEvidence 生产调用链，并在第二次 session calibration gate 终结后恰好触发一次基于同 project eligible facts 的 opening suggestion；降级 gate 不伪造当前 speaker membership，AI 失败不影响录音、completed 或回顾。

## 实现范围

- durable/stable post-session trigger root 与两 lane job identity；完成提交后非阻塞触发，GET/render/WS replay 不触发；
- trusted elder final→`MemoryService.extract` current resolution；trusted interviewer final→`QuestionEvidenceService.reconcileActualQuestions`，degraded/unknown 为 unjudged；
- `post_session_analysis` 内容无关状态、attempt/retry evidence，失败不回退 completed；
- calibration confirmed/failed/skipped 或 no-attempt provider-unavailable 的稳定 gate identity 与 `second_session_opening` attempt exact once；
- same-project current/eligible memory、published/judgeable/eligible actual asked、boundary/policy/trusted membership 的 Context snapshot；
- 复用唯一 Director/Prompt/Schema/QuestionEvidence writer/current/history/REST/WS，provider unavailable 诚实降级。

## 禁止

- 不新增第二 AI、planner/critic、question history、memory/summary/pending UI；
- 不把 displayed/manual-next/replaced/not-observed 当 actual asked；
- 不用 deterministic fake 宣称真实 LLM/provider 或真实试点通过；
- 不改变 DB ownership、privacy/deletion/retention、A4 UI 或每 session raw-data 隔离。

## 验收

`09` §17 中 post-analysis success/failure、Context membership、opening exact once、provider unavailable、安全/retention/drift/UI scope 行；完整 CI 与项目负责人 exact-head 审查。当前依赖未关闭，任务保持 BLOCKED。
