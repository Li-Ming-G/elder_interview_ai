# DEV-008A4｜首次访谈主链路整合修复

- 状态：`REVIEW`
- 负责人：独立执行窗口 `codex/dev-008a4-first-interview-flow`
- 基线：`origin/main@2f29cc7ef66563aebd2cd3d293606a5de6c20ca6`
- 审查：REV-047；项目负责人 GitHub exact-head 手动审查；执行 Agent 不得 PASS/DONE/merge
- iteration-coach：本轮唯一独立只读复核为 `Correction / NO-PAUSE`；确认无需新 Prisma 模型或新 CON

## 用户结果

普通倾听员从最低项目信息开始，在同一响应式网页中先确认当前页麦克风，再录制正式口头授权；授权后建立正式录音/ASR并完成独立校准，进入普通工作台；结束后系统自动安全收尾，原始录音完整时即使 ASR 降级也能进入回顾、播放或只删除当前浏览器副本。

## 实现边界

- ServiceTerm UI/gate dormant，保留 API/表/历史且不创建伪数据。
- early session + current-page mic + verbal consent + formal start。
- dedicated formal-stream calibration；ordinary calibration transcript/review/AI filtering。
- 已存在 collecting attempt 的 ASR 降级必须以稳定幂等 request ID 调用 server `skip`；只有无 attempt 且 provider unavailable 才本地越过，权限错误不提供降级。
- WS 1.1 interim 以服务端持久 attempt 时间边界发布 `content_kind`；普通 UI 只显示 conversation interim/final，迟到更高 revision 校准内容仍被排除。
- exact complete ACK→formal local job complete；自动 closeout 与有界 review re-projection。
- canonical completed 在同 URL 切换为独立完成页，从 DOM 移除 transcript/suggestion/工作台操作，只呈现保存完成事实、查看回顾和返回工作区。
- 复用 A1 HomeShell/tokens/buttons/focus/44px/reduced-motion；仅响应式 Web。
- 不做真实 ASR/LLM、题库、server deletion、导出、部署、PWA/App；CON-023/DEV-008D 不变。

## 必须验证

以 `09` §16 为准，另完成 format/lint/typecheck/unit/fresh migrations/integration/auth/build/smoke/Chromium/auth Chromium，保存 1440×900、390×844、320×568 真实浏览器截图，建立 non-Draft PR 并等待 exact-head CI SUCCESS。

## 当前验证候选

- 本地 format/lint/typecheck/build 通过；full unit `56 files / 339 tests` 通过。
- 本地 Chromium 在 `browserType.launch: spawn EPERM` 失败，未进入页面断言；未修改测试目标或用 fake 代替。
- 本地 Docker API 与测试数据库环境变量不可用；fresh migration/integration/auth/smoke/auth Chromium 必须由 non-Draft PR exact-head CI 完整覆盖。
