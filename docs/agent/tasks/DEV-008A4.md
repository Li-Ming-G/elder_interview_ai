# DEV-008A4｜首次访谈主链路整合修复

- 状态：`REVIEW`
- 负责人：接续执行窗口 `codex/dev-008a4-first-interview-flow-continuation`
- 基线：`origin/main@2f29cc7ef66563aebd2cd3d293606a5de6c20ca6`
- PR：[PR #43](https://github.com/Li-Ming-G/elder_interview_ai/pull/43)，non-Draft；实现候选提交 `e003c519835f7cadaa4b7edc89b65760a00bc783`
- 审查：REV-047；项目负责人 GitHub exact-head 手动审查；执行 Agent 不得 PASS/DONE/merge
- iteration-coach：本轮唯一独立只读复核为 `Correction / NO-PAUSE`；确认无需新 Prisma 模型或新 CON

## 用户结果

普通倾听员从最低项目信息开始，在同一响应式网页中先确认当前页麦克风，再录制正式口头授权；授权后建立正式录音/ASR并完成独立校准，进入普通工作台；结束后系统自动安全收尾，原始录音完整时即使 ASR 降级也能进入回顾、播放或只删除当前浏览器副本。

## 实现边界

- ServiceTerm UI/gate dormant，保留 API/表/历史且不创建伪数据。
- early session + current-page mic + verbal consent + formal start。
- dedicated formal-stream calibration；ordinary calibration transcript/review/AI filtering。
- exact complete ACK→formal local job complete；自动 closeout 与有界 review re-projection。
- 复用 A1 HomeShell/tokens/buttons/focus/44px/reduced-motion；仅响应式 Web。
- 不做真实 ASR/LLM、题库、server deletion、导出、部署、PWA/App；CON-023/DEV-008D 不变。

## 必须验证

以 `09` §16 为准，另完成 format/lint/typecheck/unit/fresh migrations/integration/auth/build/smoke/Chromium/auth Chromium，保存 1440×900、390×844、320×568 真实浏览器截图，建立 non-Draft PR 并等待 exact-head CI SUCCESS。

## 接续实现补强

- ASR 降级放行：server 已有 collecting attempt 时 resolve `skip`；仅 ASR unavailable 且无 attempt 才允许 local bypass。
- Interim fence：resolved attempt 的 `end_timeline_ms` 与当时 `hypothesis_id` 双重过滤迟到校准 revision，新 conversation hypothesis 正常显示。
- Completed：canonical verify 到 `completed` 后同 URL 自动替换为独立完成页；普通 transcript、suggestion、工作台动作不渲染。
- 浏览器证据：Chromium 27/27，覆盖三视口、44px、焦点、aria-live、reduced motion、overflow；ASR degraded + raw complete 可载入播放/删除；dirty/pending/checksum mismatch 灰置。
- 本地环境：format/lint/typecheck/build 通过；unit 339/339（首轮高负载时 AI retry 时限用例 1 项偶发失败，单独复现与完整重跑均通过且未改测试）；Docker Desktop 进程可启动但 Linux engine pipe 未就绪，因此 fresh PostgreSQL migrations/integration/auth/smoke/auth Chromium 以 exact-head GitHub CI 为权威门禁。
- CI attempt 1：`7e1fae8d` / `31654793944` 在 integration 暴露旧测试仍把校准控制片段当 ordinary transcript；按正式隔离契约改为 API 只见 conversation、数据库仍保留 calibration 证据，等待新 exact head 全门禁。
- CI attempt 2：`6ae8d2cb` / `31655022427` 已通过 integration/auth/build/smoke/普通 Chromium，最后 auth Chromium 暴露旧 fixture 仍造 0 元 ServiceTerm 与旧错误优先级；已删除伪条款并断言数据库为 0，等待新 exact head 全门禁。
