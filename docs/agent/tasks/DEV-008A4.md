# DEV-008A4｜首次访谈主链路整合修复

- 状态：`DONE`
- 负责人：独立执行窗口 `codex/dev-008a4-first-interview-flow`
- 基线：`origin/main@2f29cc7ef66563aebd2cd3d293606a5de6c20ca6`
- 审查：REV-047；项目负责人已对 accepted exact head `3824da7c48f9f63b4ca71b0fb56f459d8c24fa7d` / CI `31711325876` 手动审查 [PASS](https://github.com/Li-Ming-G/elder_interview_ai/pull/44#issuecomment-5281992260)，P0/P1/P2=0；merge `175e92e3bda76f4b180e85519e3bf8e62c356311` / main CI `31712044809` SUCCESS
- iteration-coach：本轮唯一独立只读复核为 `Correction / NO-PAUSE`；确认无需新 Prisma 模型或新 CON
- 用户实测历史候选：`f1eea3c38bef6742fdb53e6070b99ecaa0bb380c` / exact-head CI `31666579181` SUCCESS；该候选仍被真实使用发现恢复与同源回顾问题，不得把曾经全绿解释为产品验收通过

## 用户结果

普通倾听员从最低项目信息开始，在同一响应式网页中先确认当前页麦克风，再录制正式口头授权；授权后建立正式录音/ASR并完成独立校准，进入普通工作台；结束后系统自动安全收尾，原始录音完整时即使 ASR 降级也能进入回顾、播放或只删除当前浏览器副本。

## 实现边界

- ServiceTerm UI/gate dormant，保留 API/表/历史且不创建伪数据。
- early session + current-page mic + verbal consent + formal start。
- dedicated formal-stream calibration；ordinary calibration transcript/review/AI filtering。
- 已存在 collecting attempt 的 ASR 降级必须以稳定幂等 request ID 调用 server `skip`；只有无 attempt 且 provider unavailable 才本地越过，权限错误不提供降级。
- WS 1.1 interim 以服务端持久 attempt 时间边界发布 `content_kind`；普通 UI 只显示 conversation interim/final，迟到更高 revision 校准内容仍被排除。
- exact complete ACK→formal local job complete；自动 closeout 与有界 review re-projection。
- canonical completed 在同 URL 切换为独立完成页，从 DOM 移除 transcript/suggestion/工作台操作，只呈现保存完成事实、查看回顾和返回工作区。
- 复用 A1 HomeShell/tokens/buttons/focus/44px/reduced-motion；仅响应式 Web。
- 不做真实 ASR/LLM、题库、server deletion、导出、部署、PWA/App；CON-023/DEV-008D 不变。

## 用户实测后的限域修复

- canonical session 已为 `stopping/processing` 时，即使刷新导致 React 内存中的 `endHandoff` 丢失，也会自动以稳定幂等 reconcile request 恢复并继续 verify；有 handoff 时先重建 exact complete ACK，再核对 canonical 状态。只有确认失败才显示一个通俗动作“重新核对保存状态”。
- 回顾页在同一 `location.origin` 上持续约 15 秒有界重投影 fresh manifest 与本机 IndexedDB；完整、无 dirty/pending 且 identity/count/bytes/checksum 全匹配时播放和本机删除可用。门禁不匹配继续灰置，并显示当前 origin 及跨主机名/端口诊断。
- 新建操作的 unknown response 自动以原 request ID/payload 在当前页、联网恢复、重新可见与刷新/重开时重放；移除会把服务端已提交响应误判为 status=0 的固定 15 秒客户端超时，不向普通用户暴露 request ID/payload。
- 删除继续复用既有 `alertdialog`：默认焦点取消、ESC/取消无副作用、确认后才原子删除当前浏览器副本，服务器录音/转录/记忆/审计不变。
- 普通身份显示继续只读 `AuthUser.display_name`。本地项目负责人受控身份使用既有交互式 CLI 新建独立账号，例如 `pnpm --filter @elder-interview/api user:create -- --email owner-local@example.invalid --display-name 黎明 --role interviewer --operator-ref local-owner-setup`；密码交互输入，需设置本地 `DATABASE_URL`，不得复用或修改提交中的 `listener-a@example.test` 测试 fixture，也不得提交本地凭据/数据库。

## 必须验证

以 `09` §16 为准，另完成 format/lint/typecheck/unit/fresh migrations/integration/auth/build/smoke/Chromium/auth Chromium，保存 1440×900、390×844、320×568 真实浏览器截图，建立 non-Draft PR 并等待 exact-head CI SUCCESS。

## 最终验收与接收

- non-Draft [PR #44](https://github.com/Li-Ming-G/elder_interview_ai/pull/44) accepted exact head `3824da7c48f9f63b4ca71b0fb56f459d8c24fa7d`，tree `c1445127bedc9d42fc44ed236d98aceac6c534ba`。
- exact-head CI [31711325876](https://github.com/Li-Ming-G/elder_interview_ai/actions/runs/31711325876) completed / SUCCESS；verify 覆盖 format、lint、typecheck、unit、fresh PostgreSQL migration deploy/status、integration、auth、build、smoke、ordinary Chromium 与 auth Chromium。
- 项目负责人正式手动审查结论为 PASS，P0/P1/P2=0，严格绑定上述 exact head；旧候选和失败历史不因本结论改写。
- PR #44 以 merge commit `175e92e3bda76f4b180e85519e3bf8e62c356311` 合入 `main`；main CI [31712044809](https://github.com/Li-Ming-G/elder_interview_ai/actions/runs/31712044809) completed / SUCCESS，verify 3m28s，全链路通过。
- 本任务仅在 DEV-008A4 响应式网页整合修复范围进入 `DONE`；真实 ASR/provider、真实 LLM、题库、server deletion、导出、部署、PWA/App、DEV-008D/CON-023 与真实试点不由本结论完成。

## 最终验证记录

- 本地 final `format:check`、lint、typecheck、build、`git diff --check` 通过；full unit `56 files / 339 tests` 通过。
- 本地 Chromium 在 `browserType.launch: spawn EPERM` 失败，未进入页面断言；未修改测试目标或用 fake 代替。该本地限制由 accepted exact-head GitHub CI 的 ordinary/auth Chromium 全矩阵成功覆盖。
- 本地 Docker API 与测试数据库环境变量不可用；accepted exact-head CI 已用 fresh PostgreSQL 完成 migration deploy/status、integration、auth、smoke 与 auth Chromium。该记录只说明验证环境分工，不删除本地失败证据。
