# REV-065｜MEMORY-T5-T8-P2-C-RUNTIME-001

## 当前状态

`REVIEW / REQUEST_CHANGES HISTORY PRESERVED / CORRECTED HEAD NOT RE-REVIEWED`。pre-integration dirty snapshot `c3eaa4ae…` 已获正式 `REQUEST_CHANGES`；当前 integration correction 尚未独立复审，不包含 PASS/DONE。

## 待审范围

- 任务：`MEMORY-T5-T8-P2-C-RUNTIME-001`；
- 映射：`T5-T8/P2`、`T18-T19/P6`、`T0/Foundation-Observability`；
- 前置：A1 PR #71 accepted/merge/main 门禁、P2-B PR #72 accepted/merge/main 门禁与 PR #73 governance closeout 已登记；
- 当前对象：治理、任务拆分和 `04/07/08/09/10` 正式 P2-C contract-first 前置；
- 后续对象：forward-only Prisma/migration/repository/runtime exact head，须另行绑定 commit、PR、CI 和审查结论。

## 必审不变量

1. `MemoryClaim/MemoryResolution` 是唯一 semantic authority；Proposal/Plan 不持久，checkpoint/layer/Long/Trace 只保存 typed refs、revision、digest、manifest 和状态。
2. A1 stable authority CAS 与现有 append-only Resolution row history 通过 `authority_id + resolution_revision` 明确分层，不原地覆盖旧 row。
3. freeze/call/validate/plan/CAS/atomic commit 分段正确，供应商调用不持 DB 锁；任何失败没有半提交。
4. duplicate、concurrent、replay、retry、rebase、crash、restart、late callback、final-tail 均满足 one-winner/exactly-once 和零复活写入。
5. typed retention、Decision Trace 和 evidence refs 可重算且 reference-only；deletion/retention/policy/Boundary/human authority drift 失败关闭。
6. deterministic fake 只在 local/test，unavailable seam 诚实终结；两者不得冒充 P2-D 真实 provider/model/data PASS。
7. P3/pgvector、P4 budget、UI、真实 provider/model/secret/data 均不进入 P2-C。
8. checkpoint严格分离真实P1 terminal与P2 producer；online/final source必填矩阵、v1.1历史/v1.2当前producer兼容可机械验证。
9. 现有AiJobCoordinator与Int policy/retention只做兼容扩展；自动P2继承AiJob root，MemoryRetentionRoot ownership与RetentionState枚举不变，不出现第二coordinator、隐式列改型或第二root状态机。
10. pending/running/所有非success job不预留target revision/new identity；successful existing-slot新row复用stable authority并正确supersedes；final Mid不可用时Long诚实terminal unavailable且不阻塞completed/opening。

## Contract-first correction history

对治理 exact `0e58d4f` 的只读复核未形成 PASS；发现 retention `long_projection`/CASCADE 与 P2-B accepted policy 冲突、stable authority 缺少可落 FK registry、`origin_thread_id` nullable、claim-scoped evidence link 被误当共享 authority，以及 deletion digest、Trace non-null sentinel、physical FK/error registry不足。随后建立 `memory-persistence-p2c-compatibility-v1.md` 与 `memory-persistence-p2c-physical-fk-v1.json` 做窄幅 forward-only 修复；对 exact `545df8d` 的后续 P1 又补齐 digest 非 FK自校验、Trace typed source columns/FK/CHECK与当前 migration hash verification。本段是早期 placeholder 历史；后续 pre-integration 正式 `REQUEST_CHANGES` 见下文，任何 Correction 都不等于 PASS。

## 待补证据

- implementation branch、exact commit、PR、CI；
- fresh/current-main/repeat migration 证据；
- targeted 与全量验证；
- 独立审查结论及 P0/P1/P2；
- merge/main 与 main CI（仅在项目负责人授权并实际发生后登记）。

## 2026-08-20｜exact 6de1d96 定向 P1 correction

复审指出 3 项 P1：authority FK 必须保留 legacy NULL 但禁止新 P2 NULL；retention P2-B string policy 不得从 legacy Int 隐式 cast；migration manifest 必须机械冻结 26 条 expected count、实际 migration SHA 与 canonical predecessor fingerprint。本轮仅更新 contracts/governance，写入实际 `92a8faae0197f0390c3547664872a4a0ad167f9a139daea6c44c1393e31d049e` 与 fingerprint `2b1a4ba4a0a20f2e986cec7de2c9863dd7a67673abb033406374517e4bafcea6`。状态仍 `PENDING / NOT REVIEWED`，旧 FAIL 历史不覆盖，待新 exact commit 独立复审。

## 2026-08-20｜integration typed source nullable correction

总控机器检查新增一个窄缺口：`decision_trace_memory_source_input_segment_fk nullable=false` 与五类 exactly-one typed union 冲突。当前候选已改为五个 typed FK 全部物理 nullable，由 source-kind CHECK + `num_nonnulls(...)=1` 决定有效性，并保留 `trace_id` 等 row authority 列非空；JSON semantic cases 覆盖五类单 target 正例与 zero/multiple/kind-mismatch/missing-required-column 反例。此记录不是独立审查结论；后续正式 verdict 如下。

## 2026-08-20｜pre-integration dirty snapshot `c3eaa4ae…` formal REQUEST_CHANGES

独立 pre-integration reviewer 对审查方提供的 dirty snapshot `c3eaa4ae…` 给出正式 `REQUEST_CHANGES`：`P0=0 / P1=3 / P2=0`。仓库与本地 refs 未提供该缩写对应的完整 40 位 SHA，因此本记录按审查方原文保留缩写，不臆造完整值。

- `P1-01 Trace`：五个 typed FK 均 nullable 之外，`source_kind` 还必须闭合于 `checkpoint|job|input_segment|evidence|resolution`；现有 `decision_trace` parent 的 `decision_outcome/director_invoked/status/stage/error_code/context_revision/stage_timings_json` 等字段不得误写入 `decision_trace_memory_semantic` child，parent 的 non-null/sentinel/lifecycle 与 child digest 字段必须逐列冻结并有正反 JSON 自检。
- `P1-02 Evidence`：`memory_evidence_authority.authority_revision` 是唯一 evidence revision owner；`memory_claim_evidence`/bridge 只承担 `(claim_id,evidence_id,authority_revision)` pair/parity，不得再称为 authority。
- `P1-03 Policy`：P2 string `p2_policy_revision/p2_retention_policy_version` 只存在于 `memory_p2_job_projection`；checkpoint 只保留 legacy Int snapshot 与独立 contract identity，不得成为第二 policy source，不得 cast/替代。

该轮已通过或未提出缺陷并继续保留的检查项：P0/P2 均为0；legacy authority NULL/P2-only non-null gate；26 migrations 与目标 SHA `92a8faae0197f0390c3547664872a4a0ad167f9a139daea6c44c1393e31d049e`；canonical fingerprint `2b1a4ba4a0a20f2e986cec7de2c9863dd7a67673abb033406374517e4bafcea6`；62 FK 与唯一 cleanup `SET_NULL`；P2-B accepted artifacts immutable；P1-P6/T mapping、A1/P2-B accepted 与旧 FAIL/REQUEST_CHANGES 历史未被覆盖。

当前 integration candidate 已针对三项 P1 做 docs/machine-contract correction，并新增 source-domain、Trace placement、Evidence ownership、policy source 的 positive/negative semantic checks；这只是待复审候选，不是 reviewer CLOSED/PASS。状态保持 `REVIEW`，等待新 exact head 独立复审。

本地 semantic 自检历史永久保留：首次命中 `terminal_error_on_parent` 正例缺少 parent-present/sentinel-valid 两个 fixture facts，已修 fixture而未放宽规则；两个后续压缩 PowerShell command 又分别因 canonical function 被误解析为 `return{+`、`Where-Object` operator 语法无效而在执行前失败（另有一次 `foreach` 缺空格 parser error），均未改文件。最终用等价显式只读脚本通过 26 migrations/hash/fingerprint、62 FK/唯一 SET_NULL、typed nullable/domain、Trace/Evidence/policy 正反 cases 与 P2-B artifact drift 检查；未运行CI/全量测试。
