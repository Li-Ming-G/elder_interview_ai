# REV-063｜MEMORY-T5-T8-P2-A1-SEMANTIC-ENVELOPE-001

## 当前状态

`REVIEW / THIRD TARGETED RE-REVIEW LOCAL PASS / NOT ACCEPTED`。

## 唯一前置独立结论

唯一 iteration-coach 结论为 `Correction`：P2-B persistence 之前必须先建立 A1 semantic envelope，明确 transient Context、LLM SemanticProposal、程序验证的 non-persistent plan 与 committed-authority bridge，避免 proposal/plan/layer/Trace 形成第二 semantic authority。

该 Correction 只纠正路线，不是对当前实现候选的独立验收，也不允许标记 PASS/DONE。

## 审查对象

- task：`MEMORY-T5-T8-P2-A1-SEMANTIC-ENVELOPE-001`；
- branch：`codex/memory-p2-a1-semantic-envelope-001`；
- base/main：`00953acadb8edabefe0e59a9c570af745d22100b`；
- base tree：`033d3a9b2d905c8c758e6784063eae0da405b3bb`；
- candidate scope：五个 semantic envelope Schema 及其后续 docs/fixtures/pure validators/loaders/tests；
- exclusions：Prisma、migration、runtime persistence、provider、P2-B/C/D、P3/P4/P5、真实数据与部署。

## 必审不变量

1. `MemoryClaim/Resolution` 仍是唯一 semantic authority；proposal/plan/Long/layer/Trace/log 全部不可成为 value truth source。
2. Context、Proposal 与 ValidatedMemoryMutationPlan 均为 transient；A1 不持久化 plan，也不实现 commit。
3. committed bridge 只能引用未来事务实际提交的 Resolution/Evidence/layer authority，不得让未提交 proposal 冒充 committed state。
4. source/claim/evidence/target references、revision、count/order/digest/manifest 与 policy/deletion/retention 必须严格失败关闭。
5. Long/layer/Trace/log reference-only，不复制 semantic value、Transcript、Prompt、Context 或 provider payload。
6. A1 只覆盖 T5-T8/P2 contract seam；P1 只作 accepted upstream，P3/P4/P5 不启动，P6 runtime 后置。
7. 推进顺序必须保持 `A1 accepted -> P2-B persistence -> P2-C -> P2-D`。
8. 同一 evidence 可以支持多个 claim；proposal/commit evidence membership 的唯一键是 `(claim_ref, evidence_ref)` pair。Context 中 transcript segment membership 仍必须唯一，不因跨 claim 复用 evidence 而复制 segment membership。

## 接收门禁

- contract docs/Schema/fixtures/pure validators/loaders/tests 全部形成 exact head；
- docs-only link/table/Prettier/diff-check 与适用 contract tests 通过；
- 独立 reviewer 对 exact head 给出可追溯结论；
- 项目负责人明确接收后才可把 A1 标记 PASS/DONE 并解锁 P2-B。

## 首次独立 implementation review

结论：`FAIL / P0=0/P1=3/P2=2`。该结论绑定 `main@00953ac` 上的 dirty contract-only candidate；未运行远端 CI、未发布 GitHub 评论。

### P1

1. **source / claim / evidence provenance 子图未闭合**：proposal 声明的 source members 与实际 claim/evidence 未做逐 proposal 归属闭环；committed evidence 只校验 allowed pairs 子集，未要求与 proposal evidence 完全相等，且重复 transcript segment identity 可通过。
2. **authority revision、slot identity 与 semantic state parity 不完整**：`new_slot` 未强制初始 revision 1；committed status 可偏离 proposal；P1 v1.2 的 value/resolution/status dialect 未闭合；existing target 未完整证明属于 proposal source set、同一 semantic slot 与预期 CAS authority。
3. **`session_end_to_long` 未证明完整 final Mid/current 输入**：只限制 source kind，未证明 final Mid manifest、显式 current set、mode-specific checkpoint parity，也未要求 trigger session 属于 source session set。

### P2

1. **Canonical/digest gate 覆盖不足**：golden 只覆盖局部空值，尚缺 content/evidence/plan/commit 全域 canonical golden、array-order 与逐字段 tamper；committed projection 也缺完整对象级 digest 证明。
2. **任务范围自相矛盾**：首次候选实际按总控委派同步 `04/05/07/08/09/10`，但当时 task card 写成禁止修改正式 `04`-`10`。本治理 correction 已按最新更具体授权修正为“仅允许必要的 A1 契约语义同步 `04/05/07/08/09/10`”；不新增产品含义且无需 conflict log。该 finding 与首次 FAIL 仍永久保留。

首次 review 同时确认：定向测试 46/46、API typecheck、新增 TS lint、Prettier 与 diff-check 通过；accepted P2-A、P1/DecisionTrace artifacts 与 migrations 字节不变；无 Prisma、migration、provider activation、P2-B/C/D、P3/P4 或 UI 漂移。这些通过项不覆盖五项 finding，也不构成 PASS。

## 第二轮定向 re-review

结论：`FAIL / P0=0/P1=2/P2=1`。首次 review 历史保持不变；第二轮只记录修复候选仍未关闭的定向问题，状态继续 `REVIEW`。

### P1

1. **duplicate durable resolution source 未失败关闭**：Context 仍可用不同 source ref 重复引用同一 durable Resolution authority；source identity、resolution ID/revision 与 source manifest 的唯一性没有形成同一闭环。
2. **proposal/commit evidence uniqueness 语义矛盾**：实现把 evidence 当成跨 claim 全局唯一，与“同一 evidence 可以支持多个 claim”的业务语义冲突。总控裁决：同一 evidence 可支持多个 claim；唯一性按 `(claim_ref, evidence_ref)` pair；proposal 与 commit 必须对 pair 集合做完整 parity。Context transcript segment membership 仍全局唯一，不允许借跨 claim 复用复制 segment membership。

### P2

1. **剩余 tamper test gap**：修复后的 durable-source uniqueness 与 evidence pair/parity 语义仍缺对应的正反 tamper 回归，尚不能机械证明 duplicate、missing、extra、cross-claim reuse 与 segment-membership duplication 均按上述裁决处理。

第二轮结论不改变 A1 contract-only 范围，不授权 core/runtime、Prisma、migration、provider activation、P2-B/C/D、P3/P4 或 UI，也不覆盖 T4/PR #70 与 P2-A/PR #69 历史。

## 第三次审查（第二次定向 re-review）

结论：`FAIL / P0=0/P1=2/P2=0`。首次与第二轮 FAIL 历史保持不变；本轮没有 P2 finding，状态继续 `REVIEW`。

### P1

1. **projection-wide target/CAS/Resolution ID 未唯一**：校验仍只在单 entry 内约束 target authority；不同 committed entries 可以竞争或重复声明同一 durable target/CAS authority，或复用 committed Resolution ID，projection 级 one-winner/one-authority 不变量未闭合。
2. **MemoryEvidence ID 仅 entry-local 唯一**：`memory_evidence_id` 的重复检查只覆盖单 entry；同一 committed projection 的不同 entries 可复用同一 durable MemoryEvidence ID，无法证明 evidence authority 在 projection 范围内唯一。

修复必须在整个 committed projection 上统一建立 target/CAS/Resolution ID 与 MemoryEvidence ID 唯一集合，并增加跨 entry 正反例。本轮结论不构成 PASS，也不改变 evidence 可跨 claim 复用、`(claim_ref, evidence_ref)` pair 唯一及 Context segment membership 唯一的既有裁决。

## 第四次审查（第三次定向 re-review）

本地结论：`PASS / P0=0/P1=0/P2=0`。定向本地矩阵 `138/138` 通过，适用 static checks 通过。

该结论只绑定当前 dirty contract candidate：没有 commit、PR、GitHub exact head、远端 CI、正式评论、merge 或项目负责人 acceptance。因此它只说明前三轮 finding 在当前本地候选上已关闭，不能写成 GitHub/CI PASS，不能把 A1 标记 DONE，也不能解锁 P2-B/C/D。

前三轮 `FAIL / P0=0/P1=3/P2=2`、`FAIL / P0=0/P1=2/P2=1`、`FAIL / P0=0/P1=2/P2=0` 及其全部 finding 永久保留；本地 PASS 不覆盖、删除或改写这些历史。

当前仍无 exact-head independent PASS、远端 CI 或正式 acceptance；状态保持 `REVIEW`。

## 永久前置历史

- P2-A/PR #69 的 lint failure、两轮 REQUEST_CHANGES、治理 P2、accepted head/CI/PASS/merge/main CI 继续由 REV-061 永久保留；A1 不替代或改写该历史。
- T4/PR #70 的四轮 REQUEST_CHANGES 与最终 v1.2 accepted scope 继续由 REV-062 永久保留；A1 不扩大其 PASS 范围。
