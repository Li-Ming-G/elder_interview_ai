# REV-055｜DEV-ASR-PROVIDER-001 latest-main integration 窄复审包

## 当前结论

- 状态：`REVIEW`。不得把本包写成 PASS/DONE，不得由执行 Agent 合并 PR #57。
- PR：[PR #57](https://github.com/Li-Ming-G/elder_interview_ai/pull/57)，必须保持 OPEN、non-Draft。
- accepted ASR content：`5271b52bc7149a5b716d97df0dc6a5204aae397c` / CI `31800257197` / 项目负责人手动 PASS / P0/P1/P2=0；永久保留，不因 integration head 改写。
- integration base：`f2707ff09be2723812aff1a6bc4cfd0e33ab841b` / CI `31823015620` SUCCESS；new exact head/CI 待提交与验证。

## latest-main 治理保全

- PR #54 / REV-052、PR #55 / REV-053、PR #56 / REV-054（branch-local REV-052）全部保留；#58 accepted `e17a1410b1f3ae2c5cd9dff5f17650152e54cdef` / CI `31819220314`，#59 head `5e9d6eda` / CI `31822686412`，final main `f2707ff` / CI `31823015620` 均永久记录。
- SEC-AUTH-PUBLIC-001 仍只接收应用身份/会话和 direct-peer fail-closed seam；trusted ingress/proxy/header/origin 防绕过、公网部署与真实数据许可均未完成。
- DEV-008B2 仍只在 REV-054 的 fail-closed/provider-neutral 范围 DONE；真实 LLM、正式 continuing-consent covered、真实数据/试点未完成。
- SPEC-STAGING-DEPLOY-001 只在 docs/machine contract 范围 DONE；DEV-STAGING-DEPLOY-001 继续 BLOCKED，`data_mode=synthetic_only` 不变。

## 迁移与语义复核范围

- main 原 17 个 migration 不改写：SEC migration #15/#16、B2 migration #17 保留。ASR `20260815120000_drop_legacy_speaker_mapping_session_unique` 必须是第 18 个，fresh deploy、repeat deploy 与 status 均须通过。
- accepted ASR runtime/tests 相对 `5271b52` 只允许 latest-main 组合与 migration 文件名机械顺延，不得改变 adapter、gateway/runtime、stream-scoped speaker mapping、PCM evidence、sticky completeness、final/drain 或安全诊断语义。
- 重点回归：录音 manifest；同 provider label 在 reconnect/new speaker stream 合法复用且每 stream current 唯一；旧 stream trusted mapping 不跨 stream；sticky degraded 不被后续成功覆盖；final/drain receipt；direct-peer auth 对伪造 forwarded headers 不降级。
- main auth/B2/staging 内容相对 `f2707ff` 不得被覆盖；session finalization 必须同时保留 B2 post-session notification 与 ASR receipt/completeness drain 判定。

## 外部证据与禁止外推

- 本轮不新增腾讯连接、PCM 上传或费用。provider/replay/desktop/Android 真实外部实证仍严格绑定旧 head `af99d9129c74e7db5b877aeef43f6d99f248b50c`，不能冒充 fresh integration evidence。
- actual billing/SKU 仍为 unknown；CON-027 OPEN；真实长者/PII、真实数据、生产部署与真实试点禁止。
- ASR accepted PASS 只解除真实 LLM runtime 的工程依赖，不代表真实 LLM、数据治理、持续授权或生产试点通过。

## iteration-coach 记录

- DEV-ASR-PROVIDER-001-CLOSEOUT 开工前已执行恰好一次独立只读复核；latest-main integration 属同一任务，不重跑。
- 一次误启动的第二复核在用户纠正后立即中断；未取得或采纳其结论，未据此修改实现。该纠正永久保留在任务交接。

## 待审查证据

- 完整本地矩阵已通过：format/lint/typecheck/build/diff；unit 408；fresh/repeat/status 18 migrations；integration 94；auth 26；smoke；ordinary/auth Chromium 27/5；正式 300000ms R4 1/1。
- accepted 非重叠 ASR 代码/测试 33 blobs 与 `5271b52` 完全一致；main 非重叠内容 66 blobs 与 `f2707ff` 完全一致。两个重叠运行时/测试文件经双向 diff 证明只组合双方已接收语义。
- 待补 new exact head 与 exact-head CI SUCCESS。
- 项目负责人临时授权的独立窄复审结论。结论给出前保持 `REVIEW`。
