# REV-054（branch-local REV-052）｜DEV-008B2 实现候选正式审查

## 绑定对象与结论

- PR：[PR #56](https://github.com/Li-Ming-G/elder_interview_ai/pull/56)。
- old exact head：`0e703af722134f98c9e11659d6fa8472eade5846`。
- exact-head CI：`31800324817`，SUCCESS。
- 项目负责人正式结论：`REQUEST_CHANGES`；P0=0、P1=3、P2=0。
- 状态：DEV-008B2 保持 `REVIEW`；旧 head、CI 和本结论永久保留。执行 Agent 不得自行 PASS/DONE/merge。

## P1-1｜current speaker stream gate 与 consumer exact-once

- calibration gate 必须绑定 consumer session 当前 active speaker stream/capture generation；旧 stream terminal 不得满足新 stream。
- 反例必须覆盖：stream A confirmed 后 rotate/reconnect 到 B，B 未 terminal 时投影 `waiting_calibration` 且 opening count=0；B terminal 后该 consumer session 整体最多一个 opening attempt。
- 必须有 consumer-session 级 opening consumed/exact-once fence，不能因 calibration gate identity 改变产生第二 opening。

## P1-2｜crash/restart reconciliation

- 必须覆盖 question_generate job 已 freeze 为 pending、attempt 尚未创建的崩溃窗口，以及 attempt running、complete 尚未终结的崩溃窗口。
- restart/reconcile 不得因 `job.replayed` 抛错或因已有 attempt直接 return而永久卡死。
- 可确定性终结 orphan 为 failed/unavailable，或安全接管；不要求重跑 LLM，但最终必须 terminal，且同一 consumer 最多一个 committed attempt。
- 增加进程启动、ACK 丢失与并发通知回归。

## P1-3｜权威 Context provenance

- opening Director 真正消费的 frozen Context 与持久 `InterviewContextSnapshot` 必须是同一个权威输入事实；禁止 `create()` 抓集合 A、`prepare()` 再抓集合 B。
- 持久绑定 basis session、basis analysis trigger、memory lane terminal outcome+job identity、actual lane terminal outcome+job identity，以及实际 membership；不能只把 outcome 塞进 `contextBuilderVersion`。
- 不增加第二 AI、记忆 UI 或新 owner；若复用既有字段/metadata即可则不要求 migration，但 provenance 必须可机械验证。

## 已通过且不得重做

- completed 后 AI 非阻塞；`waiting_basis_analysis` 零 job/attempt/Context/provider side effect。
- same-project eligible current memory、published judgeable actual asked；`displayed != actual asked`。
- QuestionEvidence 单一 owner；无第二 AI、记忆 UI。
- local/test deterministic seam 不冒充真实 LLM；正式 continuing-consent covered 继续被 `SPEC-CONSENT-TEXT-POLICY-001` 阻塞。

## 定向修复候选

- P1-1：current active stream + current capture generation gate、consumer-session consumed fence 与 gate 轮换稳定投影已实现并回归。
- P1-2：stale frozen job/no attempt、stale running attempt、startup reconciliation、ACK 丢失与并发通知已实现并回归为 terminal + 单 attempt。
- P1-3：Context snapshot 新增 basis/analysis/calibration/lane job+outcome provenance；opening job 只能从同一 snapshot 的 exact membership freeze，attempt 持久绑定 snapshot。
- 仍是 `REVIEW / REQUEST_CHANGES` 下的修复候选；须等待 new exact-head CI SUCCESS 与项目负责人定向复审，不代表 PASS。

## 第二次项目负责人定向复审（永久保留）

- 绑定 new exact head：`996994f508b4c7833410e6b9c0a6073dd7ca486e`。
- exact-head CI：`31812498482`，SUCCESS。
- 正式结论：`REQUEST_CHANGES`；P0=0、P1=1、P2=0。
- 已关闭且不得推翻：current-stream gate、原两个 opening crash windows、权威 Context provenance 主体。
- 唯一相邻 P1：`InterviewContextService.create()` 在 context job freeze 已提交而 snapshot writeback 尚未发生时崩溃，会留下无 `InterviewContextSnapshot` 的 pending/running `context_snapshot` job；replay 抛错且现有 scanner 只从 snapshot/attempt 反查，导致永久非终态。
- 窄修复只允许：30 秒 grace 后确定性终结无 snapshot 的 stale context job；restart/ACK 丢失/并发 reconcile 幂等；late writeback 失去资格；同 consumer opening committed attempt 仍最多一个。
- latest-main integration：唯一 P1 候选已人工整合 `origin/main@c4a2b2d55ae281d8be9ff799dba73d12ab29acf1`；保留 PR #54/#55 的 REV-052/053、ADR-041/042 与 DONE/BLOCKED 边界。PR #58 accepted `e17a1410b1f3ae2c5cd9dff5f17650152e54cdef` / CI `31819220314` SUCCESS、final main `c4a2b2d55ae281d8be9ff799dba73d12ab29acf1` / CI `31819542394` SUCCESS 为本轮集成基线事实。
- 本地 integration 矩阵已 PASS：17 migrations fresh/repeat/status、unit 385、integration 89、auth 26、ordinary/auth Chromium 27/5、format/lint/typecheck/build/smoke；new exact-head CI 仍待完成，不得把 integration 候选视为 PASS。
- 旧 `0e703af722134f98c9e11659d6fa8472eade5846` / CI `31800324817` / P1=3 与本 head/CI/P1=1 两轮历史均永久保留；状态继续 `REVIEW`，不得自行 PASS/DONE/merge。
