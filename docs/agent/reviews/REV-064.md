# REV-064｜MEMORY-T5-T8-P2-B-PERSISTENCE-CONTRACT-001

## 范围

- 基线：`main@7d02fa65e283472f87c52fcae12b332d18e85ec4`。
- 状态：`DONE / PASS / CONTRACT ONLY`。
- 映射：`T5-T8/P2`、`T18-T19/P6`、`T0/Foundation-Observability`。

## 历史

初始独立 adversarial 结论永久保留：`P0=0/P1=6/P2=1`，涉及 Schema-first 校验、manifest/parity、predecessor/identity/layer/cycle、duplicate membership、FK delete policy、A1 evidence provenance 和 Claim/Resolution reference completeness。该历史不因后续修复覆盖。

## 当前候选证据

PR #72 已接收该 persistence Schema/fixtures/pure validator，exact head 为 `717c5ca39e678c6f953d0430768ae715ef0feef2`，exact-head CI `32245656541` SUCCESS；merge/main 为 `8bbb2cc24dcea9cfdb556a44e86ce037cfa90b89`，main CI `32254759316` SUCCESS。已补 Schema-first AJV gate、checkpoint/source job 与 Long job parity、identity+revision unique invariant、Claim evidence IDs/authority scope/canonical manifest digest/evidence bridge 完整性。

本地验证：定向 `34/34`；API typecheck、目标 ESLint、Prettier、JSON parse、diff-check 通过。全量 unit `78 files / 800 passed / 1 failed`，失败为既有 web focus timing assertion，未改产品或测试目标；exact-head CI 与 main CI 均已完成全步骤 SUCCESS。

## 当前结论

独立 exact-head 复核结论为 `PASS`：`P0=0/P1=0/P2=0`。最后 retry predecessor lifecycle P2 已按 `failed|cancelled|unavailable` gate 修正，并由定向 `34/34` 覆盖。该 PASS 仅接收数据库无关 contract-only 范围；后续 Prisma/runtime 必须保持 typed retention target 与 `RESTRICT` 语义。P2-C adversarial runtime、Prisma/migration、真实 provider/model/secret/data、P3/P4/UI 均未在本任务范围。
