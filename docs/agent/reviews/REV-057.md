# REV-057｜MEMORY-T0-TRACE / PR #63

- 架构映射：`T0 / Foundation-Observability`；为后续 P1–P6 提供 reference-only Decision Trace，不实现任何 P1–P6 producer。
- 状态：`REVIEW / REQUEST_CHANGES 修复候选`；执行 Agent 不自行 PASS、DONE 或 merge。
- PR：[#63](https://github.com/Li-Ming-G/elder_interview_ai/pull/63)。

## 永久审查历史

1. exact head `b34000a6f21b5f941144e808fbfd2027a8418988` / CI `31922715469` SUCCESS：项目负责人 `REQUEST_CHANGES`，P0=0 / P1=5 / P2=0。
2. exact head `b137b44eee6954d0cdd20f252081517811f82541` / CI `31924593302` SUCCESS：项目负责人第二次定向 `REQUEST_CHANGES`，P0=0 / P1=5 / P2=0。ADR 编号冲突已 CLOSED；其余四项仍未闭环，并新增 migration-history P1。

以上 head、CI、结论与问题清单永久保留，不由后续候选覆盖。

## 本轮仅修复的五项 P1

1. `director_invoked` 改为由持久 `AiProviderCall` 是否存在机械派生；Trace 持久保存全部 publication outcome，包括 `published`、`not_better`、`duplicate_filtered`、`stale_basis`、`superseded_by_manual`、`policy_blocked`、`not_applicable`。
2. attempt 与初始 Trace 在同一事务创建；missing/running Trace 由 request advisory lock、30 秒 grace、startup + periodic reconciler 收束。恢复从冻结 job memberships 重建 transcript/memory/actual-question/session/question-bank/P4 与 context digest；终态 Trace 不被 replay 改写。
3. P4 Schema 与数据库 CHECK 锁定 `available ↔ revision != null`、`unavailable ↔ revision == null`。display snapshot 保存 presentation revision + normalized digest；question bank 保存 opaque `source_version` + content digest。
4. Reader 从 `AiJobSessionScope` 取得全部 basis/consumer session scope，统一复用 `AiPolicyService` 与 `AiOutputEligibilityService` 的 memory/actual-question authority；任一 scope deletion、consent/project/source retention 或 derived-output invalidation均 fail closed。
5. `20260816100000` 与 `20260816101000` 已恢复为 `b34000a6` 原字节；全部新增只在 forward migration `20260816102000`。旧 20-migration review-head 数据库到 21 的升级已实测，含旧 `revision=7` P4 row 回填为 `revision_status=available`。

## 本地验证

- old migration byte identity：PASS。
- unit：69 files / 427 tests PASS。
- fresh PostgreSQL：21/21 deploy、status、repeat deploy PASS。
- fresh full integration：15 files / 97 tests PASS；auth：4 files / 26 tests PASS。
- targeted question-presentation + post-session：2 files / 11 tests PASS。
- final targeted 首轮曾在 attempt 业务事务已 `succeeded`、紧随其后的 Trace finalize 尚未提交的合法短窗口读到 `trace=running`；未改产品门禁或放宽终态目标，测试改为显式等待 Trace 终态，原样重跑 10/10 PASS。该 crash seam 由 startup + periodic deterministic recovery 覆盖。
- 新增 concurrent missing-Trace repair 反例首轮触发 PostgreSQL Serializable stale-snapshot/P2034；根因是等待 advisory lock 的事务保留了 winner 提交前快照。修复为 request advisory lock + Read Committed 后锁内重读 authoritative attempt/Trace；同一反例重跑 6/6 PASS，attach-vs-finalize 仍保持独立 Serializable transaction。
- 复用已运行多轮的隔离库做 full integration 时，既有固定 question-bank version 残留导致 `QUESTION_BANK_VERSION_EXISTS`，结果 96/97；未改测试或题库逻辑，换全新空库后 97/97 PASS。
- typecheck、任务范围 lint、build、smoke、diff-check：PASS。
- 全仓 lint 的本地首次尝试被用户工作区既有 `tmp/dev008a4-sandbox` 复制树触发 233 个 parser-project 误报；本任务源码另有 1 个 timer lint 问题并已修复。未删除或修改该临时树，干净 exact-head CI 仍为最终全仓证据。

## 保留边界

- ADR-043/044 继续 `Proposed / REVIEW`；已关闭的 ADR-033/034 编号冲突不重开。
- Trace 不保存完整 prompt、Context、transcript 或 provider 原文；只保存 ID/revision/version/digest/membership reference。
- 无真实 provider、密钥、真实长者/PII、真实数据、公网部署、formal v2 或 P1–P6 producer。
