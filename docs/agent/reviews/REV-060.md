# REV-060｜MEMORY-T2-T4-RUNTIME-001 / PR #68

## 正式审查历史

- 对象：[PR #68](https://github.com/Li-Ming-G/elder_interview_ai/pull/68) old exact head `4bda58c22cc99cd2339767d2ceccfddc45176256`；exact-head CI `32017818045` SUCCESS。
- 结论：`REQUEST_CHANGES / REVIEW`，正式合计 P0=0/P1=4/P2=0。
- 正式记录：[P1-1 至 P1-3 comment 5314799838](https://github.com/Li-Ming-G/elder_interview_ai/pull/68#issuecomment-5314799838)、[P1-4 comment 5314826620](https://github.com/Li-Ming-G/elder_interview_ai/pull/68#issuecomment-5314826620)。old head、CI、两条结论永久保留，不被后续候选或复审覆盖。

## 四项 P1 集中修复候选

- P1-1：新增且仅新增 forward migration；official old all-or-none CHECK 被确定性替换为显式 provenance lifecycle，session/thread parent-delete trigger 在 FK `SET NULL` 前原子标记 detach。fresh、current-main legacy sentinel 与 exact old-head upgrade 均已验证；exact old-head 迁移前枚举到两个正式旧 CHECK，迁移后只剩 exactly-one-root 与新 lifecycle CHECK，实际 thread/session delete 通过。
- P1-2：semantic validator 与 runtime CAS 双层冻结 target canonical key、memory type、semantic kind、resolution revision、thread/anchor identity；normal/disputed 恶意反例均失败关闭并保持零 business write。
- P1-3：`AI_MEMORY_INPUT_DRIFT` cancelled/no-consumption 后，从稳定 base trigger、cancelled predecessor ID 与 current authority digest 派生 deterministic rebase identity。failed retry 规则不变；并发 notify/startup/scanner 最终只有一个 succeeded consumption，旧 late callback 仍零写。
- P1-4：legacy profile 继续严格接受旧 `memory_extract` + lane identity；P1 profile 严格接受同 project/basis session 的合法 `working_memory_maintain` terminal，并区分 committed snapshot、`MEMORY_UNJUDGED` 与其他诚实 terminal。persisted succeeded/unjudged 均进入唯一 second-session opening attempt。

## 当前结论与边界

- 当前仍是 `REVIEW`。新 exact head 与 CI 在本次集中提交后绑定到 PR #68；只有项目负责人后续正式复审可以改变结论。
- 未接真实 provider/secret/data、Context V2、P2/P3/P4/UI；未修改 `.codex/iteration-learning.md`；不宣称 PASS/DONE/merge。
- PR #65/#66/#67 的 old/REQUEST_CHANGES/PASS/merge/main CI 历史继续按 REV-059、ADR-046/047 与任务卡永久保留。
