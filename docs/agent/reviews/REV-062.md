# REV-062｜MEMORY-T4-P1-SEMANTIC-TRIGGER-001

## 当前状态

`REQUEST_CHANGES FIXED LOCALLY / RE-REVIEW PENDING / REVIEW`。本文件不构成 PASS。

## 审查范围

- T2 / Foundation：Episode/Fact/Boundary 核心与 optional memory tag；
- T4 / P1：v1.2 contract、selected-new-batch 累计字符门禁与 candidate validation；
- T18-T19 / P6：forward migration、v1.2 durable namespace、replay/recovery/late fence；
- T0 / Foundation-Observability：reference-only trigger/provenance，不复制正文。

明确排除 P2-B/C/D、P3/P4 新功能、真实 provider/secret/data、UI 和部署。

## 前置独立路线审查

独立只读审查基于 `main@d50e56886723de41f3fccf38a9d76b5b70541b32` 返回 `P0=0/P1=2/P2=0`：

1. v1.1 强制 `memory_type` 并把它用于 slot/CAS/DB identity，必须以 forward v1.2 和 nullable/tag-aware migration 修正，不能原地改写历史；
2. runtime 的单 segment `some(length >= threshold)` 会被 ASR 碎片化饿死，必须在 selected new batch 上累计后再判 trigger。

本任务在实现后仍需独立 code review、exact-head CI 与正式审查结论。

## 首轮独立实现审查

独立只读审查针对完整未提交候选返回 `REQUEST_CHANGES / P0=0/P1=3/P2=3`：

1. **P1｜post-session contract regression**：新 producer 已生成 v1.2，但 integration 仍强制 v1.1，full integration 为 `134/136`。
2. **P1｜namespace fail-closed 不完整**：pure validator 允许 `working_memory_maintain + trigger_identity=null`。
3. **P1｜T0 trigger observation 未持久化**：阈值、累计值与 selected-new membership 只存在于临时 Context，Decision Trace 不能凭 typed references 重建触发事实。
4. **P2｜historical reader test gap**：没有真实持久化 v1.1 terminal job/snapshot 的兼容读取用例。
5. **P2｜final-low-content recovery test gap**：没有覆盖重复、并发、restart 后仍恰好一个 `MEMORY_UNJUDGED` job 且 provider/business writes 为 0。
6. **P2｜governance drift**：contracts README 仍把已接收合并的 v1.1 写成 REVIEW。

该结论及 `134/136` 失败必须永久保留，不被后续修复或 PASS 覆盖。

## 本地定向修复

- post-session current producer 明确断言 v1.2；另以真实数据库 row 保留 v1.1 terminal job/snapshot reader 兼容用例。
- namespace validator 对 null identity 返回稳定错误码 `AI_JOB_MAINTAINER_TRIGGER_NAMESPACE_REQUIRED`。
- 新增 forward Decision Trace trigger observation root 与 ordered membership；normal/unjudged trace 只保存 source job、trigger identity/kind、segment ID/revision/digest/order/useful-count、累计值、阈值和 manifest。root+members 同事务写入，Reader 重算 source revision/digest/count/manifest 并失败关闭；不保存正文、Prompt、Context 或 provider payload。
- final-low-content 覆盖同 runtime 重复、两个 runtime 并发、restart/reconcile 与 missing-trace repair；最终 job 恰好一个，provider/Context/snapshot/claim/resolution/consumption 均为 0。
- README 已把 v1.1 标为 accepted/merged historical runtime；v1.2 保持 REVIEW。
- 最小阈值同时由 service、Reader 和数据库 CHECK 强制为正整数。

## 本地验证证据

- contract unit `43/43`；定向 unit `5 files / 76 tests`；full unit `75 files / 604 tests`。
- fresh PostgreSQL `26/26` migrations；v1.1 exact state → 两条 forward migration `1/1`；targeted PostgreSQL `3 files / 39 tests`；repeat deploy/status up-to-date。
- full integration `17 files / 137 tests`；auth `4 files / 26 tests`。
- workspace typecheck、lint、build、smoke PASS；最终 format/diff-check 在治理同步后重跑。
- 永久失败/告警：修复前 full integration `134/136`；根级新增测试曾分别触发一次 Prettier 与 ESLint 失败，原样修复后相关 full checks PASS；既有 PostgreSQL `client.query` deprecation warning 未隐藏。

当前仅说明六项 finding 已形成可复审候选。独立 re-review、commit/push、exact-head CI、PR PASS/merge 均尚未发生。

## 第二次定向复审

独立审查锁定 base `d50e56886723de41f3fccf38a9d76b5b70541b32` 与当时 binary diff hash `0c84dac7be47fdaa5bdf63495a5ac6ab728f1c36`，结论仍为 `REQUEST_CHANGES / P0=0/P1=2/P2=0`：

1. **P1｜AiJob → Decision Trace crash durability**：normal freeze 与 final-low rejected job 都先提交 AiJob、后在另一事务建立 Trace；startup scanner 不扫描 memory job missing trace，normal replay 又直接返回，因此 restart-only 不能保证 one-job→one-trace。现有测试在 `reconcilePersistedState()` 后手工调用 `requestFinalFlush()`，未证明纯启动恢复。
2. **P1｜final-low source provenance**：`MEMORY_UNJUDGED` job 没有 durable source input membership，`final-unjudged:<digest>` suffix 也未与 observation manifest 机械绑定；已有 job + 当前 selected-new 为空时可补出空 observation，无法证明原始触发集合。

本轮同时确认旧六项中的五项和 final-low duplicate/concurrent 子项已关闭；Trace root+members 同事务、正阈值门禁、v1/v1.1 immutable/reader compatibility、无 P2-B 漂移也已关闭。当前正在以同一 job transaction 建立 trace、持久 source membership、canonical suffix parity 与 restart-only repair；未形成第三次复审结论。

## 第三次独立聊天窗口复审

独立可见复审窗口再次返回 `REQUEST_CHANGES / P0=0/P1=2/P2=0`：

1. **P1｜final-low manifest parity 未闭环**：`AiJobSessionScope.segmentManifestHash`、`AiJob.inputHash` 与 Decision Trace selected-new manifest 仍由不同 canonical 结构计算；Reader 不校验 scope count/manifest，也不校验 Trace inputHash 与 source AiJob/input source parity。已落库的 scope/inputHash 漂移仍可能被读为可用 Trace。
2. **P1｜fresh pending/running orphan 未确定性收束**：scanner 找到 v1.2 missing-trace job 后，对 pending/running 直接跳过；`terminalizeStaleJobs()` 只处理超过 30 秒的 job。启动/即时 reconcile 仍可能留下刚提交的 orphan pending/running，现有测试只覆盖推进时钟后的 stale 情形。

本轮再次确认两条新路径的 AiJob、source rows 与 Trace 创建均处于同一 `afterFreeze` transaction；上述两项是当前唯一未关闭意见，正在另一个独立实现窗口修复，尚未形成新的 PASS。

## 第四次独立聊天窗口复审

独立复审确认第三轮的 scope/inputHash parity 与 fresh orphan CAS 已关闭，但仍返回 `REQUEST_CHANGES / P0=0/P1=1/P2=0`：

**P1｜final-low source set 未完整失败关闭**：Service/Reader 只查询 `membershipKind=new` 并比较该子集；若持久层追加 overlap maintenance membership、未被 new membership 引用的 `AiJobInputSegment`，或同一 job 追加另一个 session scope，仍可能返回可读 Trace。final-low 必须证明完整 source set 恰好是单 session、selected-new、无 overlap/额外 input 的 gate 事实。

正常/low afterFreeze 原子性、parity/orphan 两项、历史 succeeded/provider-called 保护、v1/v1.1 immutable 与无 P2-B 漂移均已确认；当前只修上述完整 source-set invariant。
