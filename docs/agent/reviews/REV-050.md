# REV-050｜DEV-008B1 项目负责人 GitHub 人工审查

- 当前结论：`REQUEST_CHANGES 后定向修复候选 / REVIEW`。项目负责人已对 old exact head 正式给出 `REQUEST_CHANGES`；本轮实现 Agent 只整理替换候选证据，不是复审结论，不得据此 PASS、DONE 或 merge。
- 审查对象：non-Draft [PR #51](https://github.com/Li-Ming-G/elder_interview_ai/pull/51)，分支 `codex/dev-008b1-repeat-interview-runtime`，base `origin/main@6f6363f517a6588ff4eb31aee7996b7116092c03`；final exact head 与 exact-head CI 以 GitHub 为准。
- 永久旧结论：old exact head `efac390d5c3a4586c5f5da464490bf8ae7fb9e68`，exact-head CI `31769522565` SUCCESS；项目负责人正式 `REQUEST_CHANGES`，P0=0 / P1=3 / P2=0。锁定时 PR OPEN、非 Draft、未合并、mergeable、head 未漂移。三项 P1 为：旧 `/projects/:id/sessions` 可绕过 repeat 权威流程；seq2 创建后 preparation 未执行当前页设备检查而卡在 `created`；unknown/concurrent loser 丢弃服务端 session 指针并按本地 basis+1 猜测。
- 定向修复候选：旧 session endpoint 仅首场且保留 winner exact replay；repeat preparation 接通 `created → 当前页 mic/input → device-check → reminder → 显式 start`；`InterviewApiError` 安全保留 plain-object details，Home 只接受服务端精确 `session_id/sequence_no`，非法/扩展结构失败关闭且不展示。
- 本地候选证据：format/lint/typecheck/build/smoke/diff-check PASS；fresh PostgreSQL 14 migrations deploy/status、integration 84/84、auth 23/23；unit 362/362；ordinary Chromium 27/27；auth Chromium 5/5。替换 exact head 与 exact-head CI 待提交/推送后绑定。
- 审查范围：单一 repeat/continuation decision owner、ordinary DTO 完整投影、next-session 锁序/幂等/并发、安全新 session、production policy unavailable、test-only synthetic DI、reauthorization fail-closed seam、recording reminder start gate、唯一 HomeShell 三视口与恢复行为。
- 强制负向边界：`SPEC-CONSENT-TEXT-POLICY-001` 继续 `BLOCKED`；真实路径不得 covered；DEV-008B2/post-analysis/opening/Memory/QuestionEvidence caller、真实 ASR/LLM、server deletion/008D、ServiceTerm 新语义、App/PWA/导出不在本候选中。
- 审查重点与失败历史：见 [`../handoffs/DEV-008B1.md`](../handoffs/DEV-008B1.md)。尤其核对 old Home/cache/idempotency 不恢复权限、并发 loser 只指向安全 seq+1、reminder 漂移零 audio/capture 副作用，以及 test fixture 双门槛。
- 允许的下一状态：只有项目负责人对新 exact head 与完整 CI 完成定向复审并明确给出结论后才能变更；old REQUEST_CHANGES 不得覆盖。当前 DEV-008B1 保持 `REVIEW`、REV-050 不得自宣 PASS，PR 不得合并。
