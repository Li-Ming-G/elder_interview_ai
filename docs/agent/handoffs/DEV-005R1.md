# HO-039｜DEV-005R1 服务端采集生命周期 REVIEW 候选

- 日期：2026-08-08
- 分支：`codex/dev-005r1-capture-lifecycle`
- 状态：`REVIEW` 候选；已 rebase 到 SPEC 收口后的 `main`，本任务新 head 未获项目负责人 GitHub 审查，不得登记 DONE 或合并
- 接收对象：总控 Agent、项目负责人、DEV-005R2/R3/R4

## 实现结果

- 前向 migration 新增 `session_capture_generation`、状态/时间/跨 session 外键、单 current generation、generation 0 offset、顶层 `capture_failure_code` 终态约束；修复与既有 finalization migration 重复创建 interview object 唯一索引的问题。
- start 使用稳定 `request_id + mime_type + audio_stream_id`，在 request→project→session 锁内原子创建唯一 interview audio object、generation 0 preparing、recording session、审计与幂等响应；interview audio init 改为拒绝，避免第二对象入口。
- 新增 confirm-active、report-interrupted、resume-capture、abandon-empty 的验证、路由、service、contracts 与统一公共 snapshot；capture snapshot 只暴露白名单字段。
- 采用已批准契约修正：无 finalization 的空采集将 session 置 `failed`、顶层 `capture_failure_code=NO_AUDIO_CAPTURED`，finalization 保持 `null`；未扩大失败枚举。
- 第一帧 PCM 由 adapter 成功接受与 `first_pcm_accepted_at` 持久化在 project→session→audio 临界区内串行；持久化失败映射为脱敏 `REALTIME_UNAVAILABLE`，不记录 frame、不 ACK。
- report-interrupted 只允许 active 原 actor 在 assignment 失效后减权报告，disabled actor 失败关闭；stop/finalization 或终态只返回稳定 snapshot，不倒退。
- resume 复用唯一对象、递增 generation，并校验 local archive count、server chunk high-water、前代 timeline offset；新 stream 可安全替换已释放的旧 runtime。
- revoke 与 start/stop/upload/capture 共用固定资源锁序；提交后释放 producer，幂等重放可依据脱敏审计中的首次 session ID 补做清理，不重复业务写入或审计。
- normal stop 将当前 preparing/active generation 置 `stopped` 并统一使用 `SessionSnapshotService`；撤权先到时转为 `auth_lost/interrupted`，不会创建双 finalization 或状态倒退。

## 实际命令与结果

- `pnpm.cmd db:generate`：PASS。
- 全新 PostgreSQL 18.3 隔离库执行 `pnpm.cmd db:migrate:deploy`、`pnpm.cmd db:migrate:status`：PASS，7 个 migration 全链部署，schema up to date。
- `pnpm.cmd format:check`：PASS。
- `pnpm.cmd lint`：PASS。
- `pnpm.cmd typecheck`：PASS。
- `pnpm.cmd test:unit`：PASS，25 个文件、130 项测试。
- `pnpm.cmd test:integration`：PASS，7 个文件、36 项测试；包含真实 advisory-lock barrier、atomic start、同 key 异 payload、PCM 证据、跨 generation timeline、空证据负向、终态、撤权与 start/stop/upload/capture 五路竞态。
- `pnpm.cmd test:auth`：PASS，3 个文件、13 项测试。
- `pnpm.cmd build`：PASS。
- `pnpm.cmd test:smoke`：PASS，Web/API/PostgreSQL smoke 抓取 2 个构建资产。
- `git diff --check`：PASS。

## 未完成与风险

- SPEC-DEV-005R 已经 REV-021 PASS 并合入 `main`；本分支现以 `main` 为交付 base，但 DEV-005R1 新 head 仍须独立复审，不得据此合并或宣称本任务通过。
- 未接真实 ASR/云存储/队列，也未执行 R4 的真实 Chromium 5–10 分钟虚构试录；这些不属于 R1，但仍是后续发布门禁。
- 为保证 adapter 成功接受与首 PCM 证据在撤权/stop/abandon 竞态中严格串行，当前本地/测试 adapter 调用位于短数据库事务内；接真实 provider 前需由 R4 重点复核延迟与事务超时边界，不得直接沿用为长网络调用。
- 已运行浏览器 `pnpm.cmd test:e2e:auth -- --project=chromium`：PASS，4/4。夹具复用 atomic start 返回的固定 `audio_stream_id`，覆盖合成 PCM、interim/final、重连与 ASR 失败分类；R4 的 5–10 分钟真实生命周期验收仍不在 R1 范围。

## 重点复核

- 复核 migration 的复合外键、状态 CHECK、partial unique 与既有 finalization index 的前向兼容。
- 复核 `CapturePcmEvidenceService` 的锁内 adapter 调用、网关无 ACK 失败关闭，以及 revoke replay 的 runtime 补清理语义。
- 复核 report-interrupted 的原 actor/disabled 边界、terminal stable snapshot、capture API 的审计元数据幂等 payload 比对。
- 复核 stop/revoke/upload/capture barrier 断言只允许单 object、单 current generation、单 finalization且无状态倒退。

## 2026-08-08 P1 并发边界修正

- `CapturePcmEvidenceService` 只在首帧证据未知时进入按 `audio_stream_id` single-flight 的 project→session→audio 临界区；adapter 接受有 250 ms deadline 与 `AbortSignal`，超时沿既有 `ASR_UNAVAILABLE` 失败关闭，事务回滚且不写 `first_pcm_accepted_at`。首证据持久后，后续帧只调用有界 adapter，不再打开 Prisma transaction 或取得业务 advisory lock；进程重启后的首次调用从数据库重新 hydrate，并使用 1024 项有界 LRU 提示缓存。
- realtime runtime 新增 producer lease。Gateway 在 adapter 返回、每次 ingestion 后和最终 `recordFrame/audio.ack` 前复核同一 lease；stop/revoke/report 提交后的 runtime 失效会丢弃迟到结果，不记录、不 ACK。
- consent revoke 审计改为脱敏的 `interrupted_captures[{session_id,generation_no,audio_stream_id}]`。首次 post-commit cleanup 与幂等 replay 都只用 `session_id + audio_stream_id` 原子条件清理；旧 revoke 不会误杀重新授权后 resume 的新 generation producer，同时旧 generation 的响应丢失/崩溃窗口仍可补清理。
- `reportInterrupted` 的首次调用和幂等 replay 同样只条件清理请求绑定的原 audio stream，避免 gen0 请求重放误杀 gen1。
- 新增 unit：首证据后多帧仅一次 transaction、重启 hydrate、adapter timeout、证据写失败、producer lease 迟到 ACK、条件 runtime cleanup。新增 PostgreSQL：首帧 timeout 与 stop、首帧成功与 stop 两种线性结果、首证据后 adapter 阻塞不阻塞 stop/revoke、revoke/report replay 的旧/新 generation 隔离与崩溃窗口补偿。

### 修正后完整验证

- `pnpm.cmd db:generate`：PASS。
- `pnpm.cmd db:migrate:deploy`、`pnpm.cmd db:migrate:status`（PostgreSQL 18.3 隔离测试库）：PASS，7 migrations，schema up to date。
- `pnpm.cmd format:check`、`pnpm.cmd lint`、`pnpm.cmd typecheck`：PASS。
- `pnpm.cmd test:unit`：PASS，26 files / 136 tests。
- `pnpm.cmd test:integration -- --run`：PASS，7 files / 40 tests。
- `pnpm.cmd test:auth -- --run`：PASS，3 files / 13 tests。
- `pnpm.cmd build`、`pnpm.cmd test:smoke`、`git diff --check`：PASS。
- `pnpm.cmd test:e2e:auth -- --project=chromium`：PASS，4/4；关闭了 CI 首次 run `31239201568` 暴露的旧 harness stream 漂移。

### 修正后风险

- 250 ms deadline 保证内部 fake/未来 adapter 挂起不会无限持有首帧业务锁；未来真实 provider 必须实际响应 `AbortSignal`，否则本服务会按时失败关闭，但 provider 自身可能仍有迟到的外部副作用。真实 provider、跨进程 producer lease 与 R4 真实 Chromium 长录音仍不在 R1 范围。
- 本交付仍只是 `REVIEW` 候选；SPEC 已独立收口不等于 DEV-005R1 通过，不得据此宣布 DEV-005R1 DONE 或合并。

## 2026-08-08 REV-020 supersede：跨 generation PCM 证据

- REV-020 对 head `6847dc2` 的原 PASS 已被本次 P1 supersede；DEV-005R1 重新维持 `REVIEW`，等待 PR #13 新 head 的项目负责人复审。
- `SessionCaptureService.abandonEmpty()` 在既有 request→project→session→audio 锁内新增 session 全代际存在性查询：任一 `session_capture_generation.first_pcm_accepted_at` 非空即返回 `CAPTURE_EVIDENCE_EXISTS`。不依赖 runtime，不改零证据成功路径、finalization、schema 或公共 snapshot。
- PostgreSQL 新反例：gen0 接受 PCM 后中断，零 archive resume gen1，gen1 未接受 PCM再次中断；abandon gen1 返回 409。失败后 session 仍 `interrupted/capture_failure_code=null`，audio 状态不变、finalization 仍空，gen0/gen1 均保持 `interrupted`，首 PCM 证据分别为非空/空。
- 保留原 gen0 零证据成功与同 generation 有证据拒绝用例；定向 `session-capture` 当前 10/10 通过。
- 契约基线 `80ab84f` 已把 `04`、`05` 与 SPEC 同步为 session 全 generations 聚合规则；本 R1 定向修复与最新正式依据一致。
- 最终门禁：`db:generate`、`db:migrate:deploy/status`、`format:check`、`lint`、`typecheck`、`build`、`test:smoke`、`git diff --check` 均 PASS；unit 26 files / 136 tests，PostgreSQL integration 7 files / 41 tests，auth 3 files / 13 tests，Chromium auth E2E 4/4。
