# HO-039｜DEV-005R1 暂停检查点

- 日期：2026-08-08
- 分支：`codex/dev-005r1-capture-lifecycle`
- 状态：`IN_PROGRESS`，不是审查候选，不得登记 PASS/DONE
- 接收对象：总控 Agent 后续续做

## 已完成

- 新增采集代次、采集中断原因与空采集失败事实的数据模型及迁移候选。
- 增加公开 session/capture snapshot、采集确认、中断、空采集放弃与恢复服务骨架。
- 正式 start 请求已接收 `mime_type` 与 `audio_stream_id`，并在事务中创建唯一 interview audio object 与 generation 0。
- 增加运行时 producer 释放入口，recover 的 `resume_capture` 已分流到采集服务。

## 实际验证

- `pnpm db:generate`：PASS。
- `pnpm typecheck`：PASS。
- `pnpm test:unit`：PASS，24 个文件、127 项测试。
- `git diff --check`：PASS。

## 尚未完成

- start 的 PostgreSQL 并发、重放与 payload mismatch 集成测试。
- 授权撤回与当前 capture/session 的同锁中断联动。
- ASR 首次接受 PCM 后持久化 `first_pcm_accepted_at` 的网关接线。
- stop/finalization 与 capture `stopped` 状态、统一公开快照的接线。
- R1 任务卡要求的数据库迁移、权限、竞态与完整回归验证。

## 风险与恢复说明

- 当前代码仅达到可编译检查点，未完成 PostgreSQL 集成验证，不能提交项目负责人审查。
- 下一次从本文件的“尚未完成”顺序继续；完成前不得启动依赖 R1 公共契约的 DEV-005R2 正式集成。
