# HO-026｜DEV-004B2 浏览器实时纵向链路启动

- 任务编号：`DEV-004B2`、父 `DEV-004`、REQ-003、ADR-019
- 交出角色：总控 Agent / iteration-coach 独立只读预审
- 接收角色：浏览器实时转录实现 Agent（`dev004b2_browser_realtime_impl`）
- 时间：2026-08-07
- 分支与提交：`codex/dev004b2-browser-realtime`；本交接随契约基线提交固化，具体 SHA 由后续实现交接回填
- 已完成：完整读取正式依据和 HO-024；盘点 B1 contracts、服务端 gateway、前端登录/音频 harness 与 Chromium 测试 seam；独立预审采用 Correction mode，确认 B2 启动并纳入 heartbeat/event ACK 资源权限复核及 `REALTIME_UNAVAILABLE/4500`，长时 runtime 清理延期。
- 允许修改：任务卡声明的 Web realtime 模块、薄 harness、必要 `/ws` 代理、两项最小 API 加固及直接相关 unit/integration/Chromium 测试。
- 禁止修改：Prisma、真实麦克风/AudioWorklet/供应商、正式工作台、校准/remap、stop/completion、持久恢复、AI/记忆、原始录音上传语义和生产部署。
- 决策权限：局部实现和测试组织可自主决定；wire 字段、路径、PCM、恢复窗口、权限语义或依赖变化必须先报告总控。
- Git 责任：实现 Agent 不 commit/push/PR，不修改治理文档；总控负责验证、收口和 GitHub。
- 下一步：总控提交契约基线后派发实现 Agent；完成后进入 REVIEW，等待项目负责人按最终 GitHub head 审查。
- 必须先读取：AGENTS、`00` 至 `10`、协作 README、任务板、DEV-004/004B1/004B2、ADR-019、REV-013、HO-024/026。

## HO-028｜DEV-004B2 实现候选交项目负责人审查

- 交出角色：独立实现任务对话 / 总控 Agent
- 接收角色：项目负责人（GitHub 审查）
- 实现证据：独立 worktree `b3d1678`；纯 B2 审查分支实现 `87dd225`；总控严格 ACK 与 E2E 证据补强 `ce67549`。
- GitHub：PR #5 `https://github.com/Li-Ming-G/elder_interview_ai/pull/5`；最终审查 head 以 CI 完成后的 GitHub 值为准。
- 修改范围：API realtime access/gateway、Web realtime transport/harness、Vite `/ws` proxy、直接 unit/integration/普通与 auth Chromium 测试；无 Prisma、依赖、真实 LLM/ASR、正式工作台或治理外功能。
- 总控复核：heartbeat/event ACK 重验资源权限；撤权关闭并释放 producer；未知内部错误使用 `REALTIME_UNAVAILABLE/4500`；客户端独立 audio/event cursor、20 帧/64000 bytes 背压、interim revision、final 去重、短时重连；ready/audio ACK 必须匹配本地 audio stream。
- 本地命令：`pnpm format:check` PASS；`pnpm lint` PASS；`pnpm typecheck` PASS；`pnpm test:unit -- --run` PASS（19 files / 103 tests）；`pnpm build` PASS；`pnpm test:e2e` PASS（Chromium 4/4）；`git diff --check` PASS。
- 环境未验证：`TEST_DATABASE_URL` 未设置、PostgreSQL `127.0.0.1:5433` 不可达、Docker daemon 不可用；`pnpm test:smoke` 因 API/database 未 ready 失败。migration、PostgreSQL integration、auth、auth Chromium 与 smoke 必须由 GitHub CI 补齐。
- 当前状态：`REVIEW`，REV-014 PENDING；未宣称 PASS/DONE。
- 审查边界：仅内部虚构/合成 PCM；真实麦克风/ASR、AudioWorklet、校准/remap、长时、刷新/进程/跨进程恢复、持久 outbox、正式工作台与生产部署不在本次范围。
- 合并后下一步：用户 PASS 后先合并 B2，再让 DEV-006A 基于最新 main 重放 migration、PostgreSQL integration 与全 CI；DEV-004C 仍需先解决 CON-014。

## HO-029｜REV-014 两项 P1 定向修复交接

- 审查证据：项目负责人锁定 PR #5 head `70b8f2d`、CI `31140269703`，结论 `NEEDS_CHANGES`。
- 修复提交：`6fd228f`。
- P1-1 修复：服务端在格式有效的 join 进入鉴权前保存请求 session，权限/CSRF/session 错误信封不再使用 NIL UUID；客户端错误信封和 close code 均可稳定分类 auth/permission/session 并停止重连。
- P1-2 修复：客户端 `apply` 明确成功/失败，只有成功事件推进 event cursor、发送 ACK 或触发 PCM 重发；terminal/reset 关闭 socket、停止 heartbeat/reconnect，后续 synthetic frame 返回 false。
- 修改文件：`realtime.gateway.ts/spec.ts`、`realtime-transport.ts/spec.ts`，无契约、Prisma、依赖、正式 UI 或范围外改动。
- 本地验证：定向 unit `33/33`；全仓 unit `19 files / 109 tests`；format、lint、typecheck、build、普通 Chromium `4/4`、diff check 全部 PASS。
- 未验证：本机仍无 PostgreSQL/Docker，migration、PostgreSQL integration、auth、auth Chromium 和 smoke 由 GitHub 新 head CI 执行。
- 接收对象：项目负责人；CI PASS 后仅需定向复审两项 P1，不需要推翻整体 B2。
