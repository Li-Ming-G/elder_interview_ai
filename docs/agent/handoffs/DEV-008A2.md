# DEV-008A2 实现交接｜新建访谈完整纵向入口

## 身份与状态

- base：`origin/main@51e2337ea86739e209ad696804de7decbcf7a9df`；该点包含 A1 merge `29e3f993a65afd08cd301563d94e40cfc66076a8`，main CI `31594298585` SUCCESS。
- branch：`codex/dev-008a2-new-interview`
- PR：[非 Draft PR #39](https://github.com/Li-Ming-G/elder_interview_ai/pull/39)；accepted exact head `1ad334de678b242fa0eb3e399af9138053ac251f` / CI `31608031668` SUCCESS。
- 当前状态：`DONE`；REV-045 获授权总控 exact-head 手动审查 PASS（P0/P1/P2=0），[正式评论](https://github.com/Li-Ming-G/elder_interview_ai/pull/39#issuecomment-5268364704)。merge `7c32760fd9a128ece2e7ecffd35d2941a6ccfece` / main CI `31609156286` SUCCESS。
- iteration-coach：本轮恰好一次独立只读 Learning 复核，结论 `NO-PAUSE`；确认四 create 和 `mvp-v1` start gate 均为已接收契约的实现缺口，不需新增公共设计。

## 已实现

1. A1 唯一 `/interviews/new` shell 内形成 project → service term → 正式口头 consent 音频 → consent record → session → prepare/device-check → start 的完整入口；创建 draft 后仍明确提示未就绪。
2. 普通 UI 固定逐项展示 `03` §6.1 告知内容，只创建新鲜 `purpose=consent` 音频并在完整 manifest 后提交 `consent_method=recorded_verbal`、`consent_text_version=mvp-v1`；不暴露 `electronic/written`。
3. 四个 create 的 request ID、canonical payload、attempt state 与服务端响应在首次 fetch 前进入 actor-bound IndexedDB workflow；双击在持久化前即加锁。超时/网络中断标为 unknown，刷新/重开锁定表单并只重放原 ID；ACK 的 project/target/payload/version/status 不匹配失败关闭。
4. 服务端统一保存 actor/action/target-or-project-create-identity/RFC 8785 payload SHA-256 与首次响应快照。首次业务记录、assignment/历史、审计和 idempotency record 同事务；同 ID 异 actor/action/target/create identity/payload 返回 `IDEMPOTENCY_KEY_REUSED`。
5. Prisma migration 为旧记录兼容地增加 nullable `create_identity/request_payload_hash` 并允许 project create 无 target；新写入受 target/create identity 二选一约束。
6. 正式 consent 录音复用既有可靠 chunk/archive/upload job；init/chunk/complete 各自稳定 request identity。刷新可恢复未冻结 job，冻结后禁止重新追加，保存失败只恢复同一 job。
7. start 在 project/session 事务锁内验证当前 assignment、project ready/active、session device_check、当前 service term、最新有效 consent 和 `mvp-v1` 版本；失败不创建 audio/generation/audit/idempotency 副作用。
8. preparation 的 detached session create 同样在 IndexedDB 首次联网前持久稳定 ID，收到权威响应后才清除。
9. REV-045 P1 修复为 consent capture 增加幂等、可等待的 `dispose()`：显式“返回工作区”先停止 MediaRecorder/全部 stream track 再 SPA 导航，路由卸载仍执行同一防线并立即阻断 listener/state 更新。该动作不 freeze/upload/complete、不删除已暂存分片，也不替换原 job/ID；重进只继续同一未冻结 job。
10. adjacent P1 修复在 lifecycle effect 每次 setup 显式设置 `mounted.current=true`；React StrictMode 的 setup→cleanup→setup 后，workflow `save`、busy `endAction` 与录音 `showMessage` 不再被第一次 cleanup 永久抑制。cleanup 的 dispose/退订语义保持不变。

## 验证证据

- 静态：Prisma format、`pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm build`、`git diff --check` 通过。
- unit：52 files / 320 tests；包含 dispose 幂等/不可复用、显式返回等待释放、unmount dispose/退订及 StrictMode 初始化后 workflow/busy/message 正常反例；既有回归保持全绿。
- latest-main exact candidate fresh PostgreSQL：新建 `dev008a2_exact_367d343`，从空库应用 14 migrations，`prisma migrate status` up to date；integration 14 files / 80 tests 全绿。
- 数据库反例：四 create exact replay 无重复业务/历史/审计；同 ID 异 actor/action/target/payload 409；口头授权音频不完整拒绝；版本漂移/撤权/assignment drift 的 start 均拒绝且 audio object 为 0。
- 真实认证浏览器：HttpOnly cookie/Origin/CSRF、合成音频 IndexedDB→真实 API 完整上传、实时通道回归及本轮三种 start 反例，5/5。
- 新入口 Chromium：5/5；新增真实 fake-device media stream 的录制中 SPA 返回，直接断言全部原生 MediaRecorder inactive、全部 track ended、麦克风释放；IndexedDB 保留已产生分片、仅一个 `expectedChunkCount=null` 的原 job，重进“继续录制授权”仍使用同一 job ID。
- adjacent regression：普通 Chromium 全套 18/18，隔离 Web/API/PostgreSQL smoke 继续通过并抓取 2 个 dist assets。
- 三视口：`1440×900`、`390×844`、`320×568` 无页面级横向溢出；按钮至少 44px，键盘 focus、live region、reduced motion 均验证。
- 本地 gitignored 截图：`output/playwright/dev-008a2-verbal-consent-desktop-1440x900.png`、`dev-008a2-verbal-consent-mobile-390x844.png`、`dev-008a2-verbal-consent-compact-320x568.png`；已目视检查。

## 失败历史与处置

- 单元测试首次误用 Vitest 不支持的 `--runInBand`；改为正式 `--run`，316/316 通过。
- 只构建 web 时未先构建 workspace contracts，模块解析失败；改跑根 `pnpm build`，全 workspace 构建通过。
- 首次 integration 未提供 `TEST_DATABASE_URL`，测试编排失败；设置显式测试库后定向套件通过。
- 复用旧测试库的全量 integration 两次分别暴露旧 question-bank version 残留和旧 session-capture `capture-v1/v2` fixture 与新 `mvp-v1` 门禁不符。只修正当前 start fixture 版本；最终以全新数据库从零迁移，80/80 通过。
- 首次普通 Playwright 被已占用的 4173 端口阻断；改用隔离端口 4273 后通过。
- 最终 smoke 默认 4173 同样被既有进程阻断；改用 Web 4573 / API 3300 后真实 Web/API/PostgreSQL smoke 通过并抓取 2 个 dist assets。
- 认证浏览器首次覆盖 API 端口为 3202，但 Vite proxy 固定 3101，未形成有效登录测试；恢复 API 3101、隔离 Web 端口后通过。
- 完整 auth Chromium 首轮 4/5，既有合成上传 harness 的 project create 漏 `request_id`，API 正确 422；补齐正式请求后 5/5。
- 首次按文件批量运行 Prettier 时 PowerShell 将数组折叠为单参数，随后 Prisma 文件又不能由 Prettier 推断 parser；改用 PowerShell argument splat 和 `prisma format`，最终格式门禁通过。
- ESLint 首轮发现 40 项显式返回类型/事件类型/测试 helper 问题；未降低规则，逐项修正后 lint 全绿。
- latest-main fresh DB 复验首次从 `apps/api` 目录调用了仅在 workspace 根定义的 `db:migrate:deploy`，pnpm 在执行迁移前即报脚本不存在；改用该包正式 `prisma:migrate:deploy/status` 后 14 migrations、status 与 integration 80/80 全绿，无代码或数据库回滚。
- PR #39 old exact head `d240afd3` / CI `31600521245` 虽自动门禁全绿，独立审查仍发现 P1：录制中 SPA 离开没有 dispose seam，可能让麦克风继续采集。该事实保留为 REV-045 REQUEST_CHANGES；本次只定向关闭此项并跑 adjacent regression。
- 托管 PowerShell 一度把 `pnpm` 解析到被执行策略禁止的 `pnpm.ps1`，改用已安装的 `pnpm.cmd`/本地 `.cmd` binary；沙箱内 Chromium 启动返回 `spawn EPERM`，权限环境恢复后同一测试进入断言。首个真实断言用了不存在的首页标题，页面实际已成功导航；修正为既有 A1 标题后新增 Chromium 1/1、全文件 5/5。
- 全量 unit 首轮 318/319 仅既有 `ai-job-coordinator.retry` 绝对 deadline 用例受调度抖动，在 retry 前已耗尽；该既有用例立即复跑 2/2，随后未改目标的全量 319/319 通过。
- 中间修复 head `cce98c8f1be3e92cd6c776d49c5cc747252b7579` / CI `31606714871` SUCCESS 关闭了麦克风离页释放，但定向复审发现 adjacent P1：`<StrictMode>` 第一次 cleanup 把 `mounted` 置 false，第二次 setup 未恢复，开发模式状态更新会永久被抑制。以每次 setup 显式置 true 的一行修复关闭，并新增 StrictMode 包裹回归；中间 head 不作为 PASS 候选。
- StrictMode 修复 head `ef85c3b81a24ead486457659a82809244b3953c4` 的首个 CI `31607585915` 在既有 `audio-buffer.spec.ts` 原生 MediaRecorder 用例读取到 0 个分片，普通 E2E 17/18，auth-e2e 因 fail-fast 未执行；同一 CI 的 format/lint/typecheck/unit 320、migration/integration/auth/build/smoke 均成功，本次新增用例未失败。本地对该既有 audio-buffer 文件以双 worker 重复 3 轮为 9/9；未修改测试目标或产品代码，保留失败历史并以新 exact head 重跑完整 CI。

## 明确边界

- 不实现 A3 回顾、播放、容量投影、IndexedDB 删除或本机副本回执。
- 不实现 DEV-008D、deletion request、服务器删除、导出、ASR/LLM/DEV-007、PWA/App；不建立第二 shell/token/navigation。
- internal/test API 用例仍可用 electronic/written fixture 验证旧兼容门禁，但普通产品 UI 绝不暴露或消费它们。
- `CON-012` 仅以“一次正式授权使用一个新 consent audio object”规避，不裁决跨版本复用；`CON-023` 不变。
- `08` §17 的真实试点授权文本、真实长者/PII、对象存储/供应商/删除等门禁未关闭；本候选只证明内部 MVP runtime。
- main 在实现期间最终前进到 `4fc46456869ab01d9880d1aa92e7cd838bf920a8`，对应 main CI `31599184357` SUCCESS；A1 与 REV-044 final closeout 均在祖先链。SPEC-DEV-008A3-PREFLIGHT 已 DONE、ADR-036 Accepted、CON-029 RESOLVED、A3 READY；本候选占 REV-045，并已 rebase 到该 exact main，未回退并行治理。
- 没有形成超出 ADR-034/035 的新架构决定，也没有新文档冲突，因此未修改冲突日志或新增 ADR。

## 项目负责人 / 获授权总控审查重点

1. 四 create 在 request lock、project lock与同事务 snapshot 中是否完整绑定并安全兼容旧 idempotency 行；
2. IndexedDB 是否确实先持久后联网，未知响应、刷新、重开和双击是否只能使用原 ID/payload；
3. 普通 UI 是否只有 fresh recorded-verbal 路径，授权音频是否必须 complete 且冻结后不可追加；
4. start 在版本漂移、撤权、assignment drift 下是否持续零 audio/generation/audit/idempotency 副作用；
5. 三视口、键盘/focus/live/reduced-motion/44px 与 A1 唯一视觉/导航是否保持一致。

## 最终接收与下游

- PR #39 在合并前复核为 OPEN、非 Draft、base `main`、exact head `1ad334de678b242fa0eb3e399af9138053ac251f`，CI `31608031668` SUCCESS，PASS 评论严格绑定同一 head；随后以 merge commit 合入，没有 squash 审查绑定历史。
- `d240afd31bc94015e10b01b179550088ed85083d` 的 REQUEST_CHANGES/麦克风释放 P1、`cce98c8f1be3e92cd6c776d49c5cc747252b7579` 的 StrictMode adjacent P1、`ef85c3b81a24ead486457659a82809244b3953c4` / CI `31607585915` 的 audio-buffer 时序 flake 继续永久保留，最终 PASS 不覆盖这些事实。
- DEV-008A2 由 `REVIEW→DONE`；父 DEV-008A 保持 `IN_PROGRESS`。DEV-008A3 保持 `REVIEW`，分支临时 REV-045 的编号冲突在主线修正为 REV-046，须基于 A2 merge 后的新 main 重新整合并形成 exact-head 包；本接收不替 A3 PASS/DONE。
- DEV-008D 保持 `BLOCKED`，CON-023 继续 `OPEN / NOT IMPLEMENTED / NOT VERIFIED`；无业务代码、Prisma、migration、A3、ASR、DEV-007 或 008D 的 closeout 追加改动。
