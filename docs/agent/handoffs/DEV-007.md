# DEV-007｜父任务聚合验收交接

## 状态与接收对象

- 状态：`VERIFY`
- 接收对象：项目负责人聚合验收
- 集成点：`main@3bb80df36d484779761cf6bb6d45c302fa8d32d7`
- main 集成 CI：`31468031796` completed / success
- 边界：父任务无新增实现窗口；本交接只汇总已通过的专项契约、A/B 实现和 main 集成证据。

## 已通过组成

1. SPEC-AI-QUESTION-001：REV-032 PASS，merge `10fcc5c`。
2. DEV-006：REV-033 PASS，merge `28fb22d`；QuestionEvidence/current-memory/actual-asked seam 单一所有权保持不变。
3. SPEC-QUESTION-JOURNEY-001：REV-034 PASS，merge `f0bff3f`。
4. DEV-007A：REV-035 PASS，merge `7f9a173`；不可变题库版本、许可/fixture 门禁、journey stage seam 已交付。
5. SPEC-QUESTION-DIRECTOR-001：REV-037 PASS，merge `d320f642`；自由生成、唯一 Schema、同输入 retry、seen/declared 分离已冻结。
6. DEV-007B：REV-038 PASS，final head `0f03c270b7022ce8dbbce75028afe7e9f3e12cf3`，CI `31465809589`，merge `3bb80df`；P0/P1/P2=0。

main merge `3bb80df` 的 push CI `31468031796` 已完成且成功，完整 verify 通过。

## 聚合验收建议范围

- A 的 active/eligible/journey seam 是否由 B 只读消费，未改写 release membership、未复制 history；
- 可信 Context → 一个 Director → 基础硬校验 → QuestionEvidence candidate/current/history → REST/无正文 WS → 工作台是否在 main 集成点共同闭合；
- continue listening、manual next、automatic replacement、displayed != actual asked、历史 cursor/anchor、hard withdrawal 与 AI unavailable 降级是否保持既有契约；
- old exact-head REQUEST_CHANGES 历史、旧 PR #25 与 CON-023/生产缺口是否继续被清楚隔离。

## 不属于本次通过范围

- 正式 14 列题库内容、来源许可和内容质量；
- 真实 LLM/embedding、生产 boundary/deletion reader、生产部署；
- 真实长者数据、正式内部试用或真实试点质量结论。

## 下一步

项目负责人需对父 DEV-007 给出明确聚合 `PASS` 或 `REQUEST_CHANGES`。在此之前父任务保持 `VERIFY`，不得标记 `DONE`，DEV-008 继续 `BLOCKED`。
