# DEV-006 最终交接

## 状态与审查绑定

- 状态为 `DONE`；项目负责人已完成最终定向复审并合并。
- 分支 `codex/dev-006-memory`；非 Draft [PR #22](https://github.com/Li-Ming-G/elder_interview_ai/pull/22)。
- 项目负责人已对 old exact head `d5073501b170c7e11f2bc3e00395fb8fdf794480`、CI `31357613683` SUCCESS 正式 `REQUEST_CHANGES`：P0=0、P1=8。该历史永久保留。
- 最终 exact head `07d5ce1c75ce31e2265e78559545373ce216edb1`，CI `31363920049` SUCCESS；项目负责人定向复审 PASS，P0/P1=0；merge commit `28fb22dede07d5d64589a30b67128f16c311f360`。

## 八项关闭证据

1. `ai_job_session_scope` 新增 `max_segment_start_ms/max_segment_id/scope_reason`，每个被评估 session 保存真实最新 final 高水位；`no_final` 与 `final_present` + `eligible_segment_count=0` 可区分。deferred PostgreSQL constraint trigger 拒绝伪造高水位，真实反例已覆盖。
2. `ai_job` 新增 `trigger_dedupe_key/retry_of_job_id/request_identity_hash/input_hash`；request 与 trigger advisory lock 后权威重放首次结果，retry 显式绑定前序 job，冻结完成后才保存 input identity 并进入 running。相同 request 响应未知不会再次调用 provider。
3. freeze-call-recheck 对 assignment/consent/project policy、session watermark、segment text/role/content/digest、memory 与 actual-question membership 漂移返回取消结果，先提交 `job=cancelled` 再向调用方抛错；真实 provider-succeeded 后漂移测试覆盖 policy/text/role/memory。
4. 生产 `BoundaryPolicyReader` 在正式 producer/read model 缺失时返回 `AI_POLICY_UNAVAILABLE` fail-closed；local/test 仅用显式 `LocalTestBoundaryPolicyFixtureReader` 验证 consumer。没有新增 `content_marker`/deletion 半模型，也没有固定空集合/no-op 冒充覆盖。
5. `question-sim-v1` 实现 NFKC、大小写、全半角/Unicode 标点与空白规范化；供应商中立 matcher port + deterministic local/test fake 覆盖标点、全半角、同义改写，以及否定、人物、时间槽差异不会错误标成 `actual_asked`。
6. context snapshot 的 memory 与 current published eligible actual-question 在同一冻结事务中写 membership；snapshot 保存双 count/manifest，writeback 同锁序重检。catalog 在两事务间 supersede 会持久化 job cancelled，不写旧 question。
7. `QuestionEvidenceModule` 正式导出唯一 owner 的 `QuestionEvidenceWriter`，方法名冻结为 `beginGenerationAttempt/publishAttemptResult/withdrawPresentation`；DEV-007 可依赖该 port，不需直接接触底层表或另建 history。DEV-006 不提前实现 generation/display/replace 编排，当前调用显式返回 `DEV_007_QUESTION_ORCHESTRATION_NOT_IMPLEMENTED`，不是 no-op。
8. display snapshot 与 evidence event dependency 同时验证 retention root `active` 且 `expires_at>now`；hidden/purging/cleanup_failed/expired 全部普通消费失败。cleanup HMAC 改用独立必填 `AI_RETENTION_CLEANUP_PEPPER`，不再复用登录限流 pepper。

## Migration 与主要文件

- 新 migration：`apps/api/prisma/migrations/20260810062000_dev006_review_invariants/migration.sql`；schema 同步新增上述 job/scope/context identity 与约束字段。
- AI runtime：`ai-job-coordinator.service.ts`、`ai-output-eligibility.service.ts`、`ai-policy.service.ts`、`ai-retention.service.ts`、runtime module 与测试。
- Memory/context：`memory.service.ts`、`interview-context.service.ts`。
- Question evidence：module/service、`question-similarity.matcher.ts` 与 unit test。
- 配置/门禁：`packages/config`、`.env.example`、CI、smoke、Playwright auth/R4 fixtures 与 PostgreSQL integration/auth tests。

## 本地验证

- `pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`git diff --check`：PASS（治理文档更新后会再次运行静态门禁）。
- unit：35 files / 232 tests PASS。
- PostgreSQL integration：11 files / 65 tests PASS；其中 DEV-006 定向 8/8。
- auth integration：3 files / 13 tests PASS。
- build：PASS。
- smoke：标准脚本增加可覆盖端口、默认仍为 3100/4173；以隔离端口 3190/4193 对 11-migration 数据库运行，Web/API/PostgreSQL PASS（2 assets）。
- E2E：普通 Chromium 9/9 PASS；真实 Web/API auth Chromium 4/4 PASS（只覆盖本任务适用既有链路，没有 DEV-007 UI）。
- 空库：11 migrations 全量 deploy PASS。
- legacy：先部署前 9 migrations，插入虚构 legacy user/project/completed session，再部署 DEV-006 两个 migrations；11/11 applied，旧 project/session 原值保留。

## 未实现与风险

- CON-023 继续 `OPEN / NOT IMPLEMENTED / NOT VERIFIED`：deletion producer/read model、统一 reader、C2 回接、传播/并发与安全验收均未完成；test fixture 绝不计作覆盖。
- 正式 boundary producer/read model 同样尚未实现；生产明确 `AI_POLICY_UNAVAILABLE`，因此相关 AI 普通消费 fail-closed。这是总控已批准的限域修复，不是产品范围扩张。
- 未实现 DEV-007 LLM 问题生成、自动替换、manual next、suggestion REST/WS、history/workbench UI；未选择真实 matcher/LLM 供应商。
- DEV-007 原技术前置已满足，但项目负责人要求先处理“当前项目与预期不一致”的产品对齐；CON-025 解决前不得创建 DEV-007 实现任务或分支。
