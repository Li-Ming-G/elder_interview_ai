# DEV-008A3 最终交接

## 状态

- 当前：`DONE`
- base：`origin/main@5035c119fa5a3eeb7999d305f5c052672dc50d25`；main CI `31610391026` SUCCESS
- branch：`codex/dev-008a3-local-review`
- review：[PR #40](https://github.com/Li-Ming-G/elder_interview_ai/pull/40) 非 Draft；主线唯一编号 REV-046。accepted exact head `93be9a27b93e763e56457668c78b5ac2a332bab4` / CI `31612276827` 获授权总控 PASS（P0/P1/P2=0），[正式评论](https://github.com/Li-Ming-G/elder_interview_ai/pull/40#issuecomment-5268932084)
- merge：merge commit `d2a911d3fd4362a84653c1401c4c23b8c5b4aafe`（parents `5035c119fa5a3eeb7999d305f5c052672dc50d25` / `93be9a27b93e763e56457668c78b5ac2a332bab4`）非 squash 合入；main CI `31613083916` SUCCESS
- 边界：reviewed head `70b8fe8` 的 REQUEST_CHANGES（P1=1/P2=1）与中间修复 `f491d99` / CI `31606148505` 永久保留；父 DEV-008A 仅在响应式网页 A 范围 DONE，DEV-008D BLOCKED、CON-023 OPEN / NOT IMPLEMENTED / NOT VERIFIED

## 实现范围

- API runtime：普通 canonical `GET /sessions/:id` 的 finalization 始终显式包含 `total_size_bytes`；complete audio object 才从既有 `AudioObject.totalSizeBytes` 精确投影，缺 manifest/null/unsafe 或非 complete 均为 null。list/evidence DTO 不扩宽。
- 回顾：A1 唯一 Home shell/route 内呈现原始与修订转录，只读且无编辑/导出/问题历史；failed complete session 可回顾与播放，但删除关闭。
- 播放与投影：只拼接本 origin 的完整 IndexedDB archive；fresh session+manifest 对照 session/audio identity、count、total bytes、chunk sum、manifest checksum、sequence/key/size/MIME/timeline/checksum 与 Blob 实际 SHA-256。缺片、不可读或任何不一致不部分播放。
- 删除：IDB v5 前向 upgrade 新增最小 `local-deletion-receipts` 与查询索引；复用 capture 同名 Web Lock；成功路径在单个 readwrite transaction 清 current+legacy+delivery+state+formal job+all interruption reports+checkpoint 并写回执，保留其他用途 job。同步/异步错误均 abort 并回滚。
- 诚实边界：所有按钮、确认框、结果和回执均说明只删除此浏览器/此设备副本，服务器录音、转录、记忆和审计仍保留；确认层说明正式隐私删除需走独立申请且本页不提供该流程，没有新增死链或调用 server audio download/delete/deletion_request API。

## 验证证据

- `pnpm lint`、`pnpm typecheck`、`pnpm format:check`、`pnpm build`：通过。
- `pnpm test:unit --run`：56 files / 341 tests 通过；formal `local-audio-archive-v1` Schema 1/1。
- 全新隔离 PostgreSQL：14 migrations deploy/status 成功；`pnpm test:integration --run` 14 files / 80 tests，`pnpm test:auth --run` 4 files / 23 tests；测试库已强制关闭连接并删除。
- 普通真实 Chromium 24/24：A2 `new-interview.spec.ts` 5/5、A3 `review-local-archive.spec.ts` 6/6；另以全新 PostgreSQL 跑 auth Chromium 5/5。覆盖三视口、44px、visible focus、aria-live、reduced motion、overflow、双 tab Web Lock、v4→v5、完整事务清理、刷新、清站、卸载原子性，以及非模态 danger confirmation 的默认安全焦点、Tab 循环、Escape/取消恢复和成功结果焦点。
- 截图：`output/playwright/dev-008a3-review-desktop-1440x900.png`、`dev-008a3-review-mobile-390x844.png`、`dev-008a3-review-compact-320x568.png`；已目视检查布局、focus 与无横向溢出。

## 失败历史

- 首次 Playwright 默认端口 4173 被已有进程占用，测试未启动；改用隔离 4293/4294/4295 后 5/5。
- 首次 PostgreSQL 诊断只设置 `TEST_DATABASE_URL`，Prisma 因缺 `DATABASE_URL` 落到错误空库并报告缺表；临时库删除后同时设置两变量，以全新库完成 13 migrations 与全部测试。
- 初次严格 lint 报告测试回调显式类型/无 await/unbound method 等问题；未放宽规则，逐项修正后仓库 lint 清零。事务 rollback 测试曾揭示同步 `DataError` 可能让早期删除提交，因此生产实现增加显式 abort 并由回归证明 archive/回执全回滚。
- final main 整合后的首次 typecheck 因 Prisma client 未按 A2 schema 重新生成失败；运行正式 `db:generate` 后通过。首次 lint 捕获 A2 新入口替代后失效的 `ComingSoonRoute` import，删除该失效 import 后通过。
- fresh DB 编排先后因外层超时过短、PowerShell 数据库名插值、误用密码而在有效迁移前失败；每个明确命名临时库均删除，随后按 `.env.example` 用新库完整通过。首次 auth Chromium 把 API 改到 3106，但 Vite 正式代理固定 3101，登录前 5/5 失败；删除临时库后恢复 3101，在另一新库 5/5 通过。

## 审查重点与未实现边界

- 审查重点：ordinary key lifecycle/safe integer、fresh preflight 全字段闭合、active/pending/lock/offline/mismatch 的零写入、事务失败全回滚、legacy/all reports 清理、failed complete 的播放/删除分离，以及文案不得冒充 server privacy deletion。
- 未实现：server delete/008D/deletion_request、导出、文本/说话人编辑、题库/AI history/007、ASR/LLM、PWA/App、第二套 shell/token/navigation。A2 新建入口仅作为已合入 main 的相邻回归保留，不属于 A3 新增范围。
- A3 本身没有新增 Prisma schema/migration；本分支整合进来的 migration 属于已接收 A2。真实 PostgreSQL/API 只验证服务器权威 preflight、权限与 A2 相邻回归，不能证明任何服务器资料已被删除。

## Final main 整合候选历史（接收前）

- 已合并 final main `5035c119`，逐块保留 `NewInterviewApi + ReviewApi`、`/interviews/new + review route` 与两套样式/运行时语义，没有选择单侧整文件覆盖。
- 治理冲突按 main 解决：A2 DONE/REV-045；A3 REVIEW/REV-046；父 A IN_PROGRESS。旧 A3 审查与中间修复历史均保留。
- 本地完整矩阵已重跑；下一门禁仅为推送 final exact head、等待同 head CI SUCCESS 并请求项目负责人复审。任务继续 REVIEW、不合并。

## 最终接收与下游

- PR #40 在合并前复核为 OPEN、非 Draft、base `main@5035c119fa5a3eeb7999d305f5c052672dc50d25`、CLEAN/MERGEABLE，head 未漂移；exact-head CI `31612276827` 与 PASS 评论均严格绑定 accepted head `93be9a27b93e763e56457668c78b5ac2a332bab4`。
- PR 按仓库 merge-commit 策略非 squash 合入；merge `d2a911d3fd4362a84653c1401c4c23b8c5b4aafe` 保留 prior main 与 accepted head 双 parent，main CI `31613083916` SUCCESS。
- DEV-008A3 `REVIEW→DONE`；A1/A2/A3 均 DONE 后，父 DEV-008A 只在统一倾听员响应式网页 A 范围 `IN_PROGRESS→DONE`。
- `70b8fe89be9830cae5c3b493a88900eef881456e` REQUEST_CHANGES/P1=1/P2=1 与 `f491d99` 中间修复 / CI `31606148505` 永久保留；final PASS 不覆盖失败和修复历史。
- DEV-008D 保持 `BLOCKED`，CON-023 继续 `OPEN / NOT IMPLEMENTED / NOT VERIFIED`。本接收没有新增 server deletion/deletion_request、导出、PWA/App、ASR/LLM、DEV-007、正式题库或真实试点能力。
