# DEV-005R3｜正式工作台与安全结束 UI 交接

## 候选身份

- 分支：`codex/dev-005r3-workbench`
- 基线：`main@bc7ea83fe042b3d12a90b3787323166f0f0308e2`
- 实现提交：事实 projection `a3c3ba4189267b7f70ef3e3d007d3cc65af8d4d4`；工作台/UI/Chromium `aa3efb419e09818f89f1ce7d08cf7c88aef1d9c4`；首轮治理交接 `8160a6797b0be3634200be287e6d24a16ca761b1`。
- 非 Draft PR：[GitHub #15](https://github.com/Li-Ming-G/elder_interview_ai/pull/15)；最终 head 以动态 `refs/pull/15/head` 与总控锁定通知为准，[PR Checks](https://github.com/Li-Ming-G/elder_interview_ai/pull/15/checks) 绑定该 head，避免自引用文档提交产生伪“final hash”。
- 当前状态：`DONE`。项目负责人 REV-025 PASS，PR #15 已以 merge `8d5c4c5` 合入 `main`；详细收口见文末。

## 总控内部预检后的定向修复候选（2026-08-09）

- 检查基线：总控交付完整性/内部预检清单绑定 PR #15 head `db9579c4b2ee95370c188c452c81dde5501e4b40`、base `main@bc7ea83fe042b3d12a90b3787323166f0f0308e2`、CI `31258505938` SUCCESS，并提出本节定向修复项。该清单是实现变更依据，不是项目负责人的正式 GitHub 审查结论，不登记 REV，也不构成 PASS/REQUEST_CHANGES。
- 修复实现提交：`433e098a19787bc24c4f2832f395eaf7e295f9d0`；分支仍为 `codex/dev-005r3-workbench`，PR 仍为非 Draft #15。治理交接提交后以动态 `refs/pull/15/head` 作为新 final head；总控只整理审查包，正式 PASS/REQUEST_CHANGES 仅由项目负责人在 GitHub 手动给出。
- reconcile：一次用户业务尝试内，网络/未知失败保留 request ID；权威成功响应投影后释放 ID，下一次用户主动 reconcile 使用新 ID。回归直接覆盖 stopping 首次成功、manifest complete、第二次新 ID 到 processing，以及失败重试同 ID。
- auth/authority：load 401 会先调用 controller 权威核验清理潜在既有 runtime，再显示可执行“返回登录”；运行 verify 401 由 controller 中断采集/释放所有权后显示相同动作。App 清空 user、CSRF、controller registry 并 replace 到真正登录页。403/授权失效继续失败关闭、无 resume/finalize，只提供“离开工作台”，不声称重新登录可恢复权限。
- modal/copy：normal、interrupted、empty 三个入口都把真实触发按钮交给唯一结束 modal；按钮取消和 Escape 均恢复本次入口。processing 为“安全离开”，failed 为“保留现状并离开”，blocked 为“离开工作台”，只有 completed 保留“完成并离开”。
- delivery/UI：非 authority 的 deliveryError 且本地 archive 正常时，顶部写“本浏览器仍在保存 · 管理服务交付暂不可用”，明细写“本浏览器录音仍保留 · 管理服务交付暂不可用，等待重试”，不再展示旧 uploaded count 作为当前交付事实。本地 archive 失败和 authority failure 仍优先失败关闭。`.transcript-notice`/`.workbench-action-error` 改为完整边框，不再使用 3 px `border-left`。

### 修复候选验证

- `pnpm.cmd format:check`、`lint`、`typecheck`、`build`、`git diff --check`：PASS。
- full unit：32 files / 212 tests PASS；本轮直接相关的 App、workbench、controller 定向集为 3 files / 60 tests PASS。
- PostgreSQL `127.0.0.1:5433`：migration deploy/status PASS，7 migrations，无待执行；integration 7 files / 41 tests、auth 3 files / 13 tests PASS。
- smoke：机器上的另一个工作区继续占用正式 4173；未终止该进程，本轮在内存中把同一脚本的 Web 端口等价替换为 4174，当前构建/API/PostgreSQL PASS，抓取 2 个资产，未修改 smoke 脚本。
- 普通 Chromium 8/8 PASS；auth Chromium 4/4 PASS。均只用临时配置把 Web 改到 4174/4175；auth 临时白名单同时保留测试固有 4173 Origin，配置跑完已删除。
- `workbench-states` 1/1 PASS，重新生成 35 张五视口 × 七状态截图；新增真实 Chromium 断言确认 interrupted 点击“安全结束已有音频”→默认焦点“继续访谈”→Escape→焦点精确返回原按钮。截图人工复核未发现布局、横向滚动或视觉层级回退。
- 未修改 `apps/api`、Prisma、`packages/contracts`；CON-020/021/022 均保持 `OPEN`。Android 最终纵向链路仍属于 R4，本轮桌面 Chromium 不冒充真机证据。

## 实现事实

- `InterviewCaptureController` projection 新增完整 `serverSession`/finalization、最近核验时间/错误与持久 `endHandoff`。工作台先 recover 同一 local job，再持续 GET/focus/online 核验；unknown、未开始、登录/权限/授权或网络异常均失败关闭或保留上次事实，不用 URL、首次 load、计时器或 UI 推断完成。
- 同一工作台 URL 原地呈现 recording/interrupted/stopping/processing/completed/failed/`NO_AUDIO_CAPTURED`。服务端变为 terminal/interrupted 时 controller 采信持久 snapshot，并停止仍在运行的本地 runtime、收最后 archive、释放锁；挂载/刷新/旋转不会自动申请麦克风、resume、创建 session/audio/generation。
- interrupted 保留只读转录，按真实 local job/archive/capture/授权条件提供“继续同一次访谈”“安全结束已有音频”或“结束无音频会话”；resume 只在用户点击后复用同一 session/audio/local job 并请求麦克风。持久结束 handoff 一旦冻结，优先级高于旧 interrupted/recording snapshot，永久禁止继续追加。
- 唯一结束 modal 使用冻结标题/说明/按钮、默认焦点、Escape 和焦点恢复；single-flight 依次完成本地最终写入、stable stop 或 `finalize_interrupted`、delivery flush、audio complete 与服务端复核。stopping 不可收起，processing/completed 可收起，刷新默认展开；没有延时模拟和死的完整回顾入口。
- 顶部只保留长者称呼、时长、安全摘要、结束动作；五类事实进入来源明确的保存明细。转录是唯一主要纵向滚动区，final 不重复标签，interim 仅显示“识别中”；手机始终为 52–64 px 左元数据、右正文。建议区只保留“继续倾听”布局空态。
- 准备页麦克风门禁改为 3.6 秒采样、600 ms 噪声基线、动态阈值与连续帧判定；区分静音/太弱并给出重试说明，但两者均不能通过。非法长者称呼使用“这位长者”，预计时长继续只读正式服务条款，不偷加编辑字段。

## 主要文件

- `apps/web/src/interview/interview-capture-controller.ts` / `.spec.ts`：统一事实 projection、只读核验、终态 runtime 收尾与 frozen handoff 防恢复。
- `apps/web/src/interview/interview-api.ts`：只补既有 abandon/stop/complete 前端调用 seam，不改公共契约或后端。
- `apps/web/src/interview/workbench-shell.tsx` / `.spec.tsx`、`apps/web/src/styles.css`、`apps/web/src/app.tsx`：全状态工作台、安全结束、响应式与无障碍。
- `apps/web/src/interview/microphone-check.ts`、`preparation-page.tsx` 及 specs：低音量 P2 与通俗缺省称呼。
- `tests/e2e/workbench-states.spec.ts`、`tests/e2e/preparation.spec.ts`：真实 controller seam、同 URL 状态切换、结束链与五视口截图/尺寸矩阵。

## 验证证据

- `pnpm.cmd format:check`、`pnpm.cmd lint`、`pnpm.cmd typecheck`、`pnpm.cmd build`、`git diff --check`：PASS。
- PostgreSQL `127.0.0.1:5433`：migration deploy/status PASS，7 migrations，无待执行。
- `pnpm.cmd test:unit`：32 files / 197 tests PASS。
- `pnpm.cmd test:integration`：7 files / 41 tests PASS；`pnpm.cmd test:auth`：3 files / 13 tests PASS。
- `pnpm.cmd test:smoke`：等价隔离 Web 端口 4174 PASS，2 assets fetched；正式 4173 当时由 `C:\Users\TR\Documents\elder_interview_ai` 的 preview 占用，未终止其他工作区进程。
- 普通 Chromium：8/8 PASS。正式配置等价改用临时 4174，覆盖原全集与新增 workbench matrix；临时配置已删除。
- auth Chromium：4/4 PASS。Vite proxy 仍使用正式 API 3101，仅临时把 Web 改到 4175 并允许测试 Origin；临时配置已删除。
- 截图：`test-results/dev-005r3/` 下保存 recording/interrupted/stopping/processing/completed/failed/no-audio × 1440×900、1024×768、768×1024、390×844、320×568 共 35 张。断言覆盖页面无横/纵滚动、转录主滚动、44 px 触控、手机元数据宽度、比例护栏和同 URL 状态切换；桌面 Chromium 不冒充 Android 真机。

## 风险与后续

- CON-020/021 保持 `OPEN`：R3 证明桌面正式 controller/UI 纵向链路，但只有 R4 在目标 Android Chrome 复验显式 resume 到下一 generation、同一 session/object/job、累计 timeline 和安全结束 manifest 后才能关闭。
- CON-022 保持 `OPEN`：算法、文案、unit/合成 Chromium 已改善；普通音量是否在 OnePlus/Android Chrome 稳定通过仍需 R4 真机复验。
- 本任务未实现 archive 下载/播放/删除、完整回顾、项目列表、AI 追问、真实 ASR/LLM、云存储/队列或生产设施；未修改 `apps/api`、Prisma、`packages/contracts`。

## 项目负责人正式审查与合并收口（2026-08-09）

- 正式审查：项目负责人手动 GitHub 复核，REV-025 `PASS`，严格绑定 PR #15 final head `481ee2593f27c62e3d137842edfd15fe11ad157c`；P0=0、P1=0。
- 自动证据：CI `31289795181` 完整 verify PASS；审查确认 PR open、非 Draft、可合并、base `main@bc7ea83fe042b3d12a90b3787323166f0f0308e2` 且 head 未漂移。
- 关闭内容：attempt-scoped reconcile ID、401 登录返回 seam、三类 modal 焦点恢复、终态文案、delivery/archive 事实分离与 impeccable 告警层级六项全部关闭。
- 合并：PR #15 以 merge commit `8d5c4c5fda34a7e80e9c170aba289c3568332a07` 合入 `main`；DEV-005R3 `DONE`，交接 DEV-005R4 `READY`。
- 保留边界：CON-020/021/022 仍为 `OPEN`；R4 必须在目标 Android Chrome 完成显式 resume、同一 session/object/job、累计 archive、PCM offset、安全结束 manifest、终态与普通音量复验。
