# DEV-008B2 最终交接

## 状态与基线

- 状态：`DONE`；[PR #56](https://github.com/Li-Ming-G/elder_interview_ai/pull/56) accepted exact head `90ce5b35ebe032931860045da5b63e97b2df3674` / CI `31820768146` SUCCESS 获项目负责人临时授权的总控独立[正式 PASS](https://github.com/Li-Ming-G/elder_interview_ai/pull/56#issuecomment-5295947362)（P0/P1/P2=0），merge/main `4b59c4d351d82e84ed88610cd0678b4882d84dab` / CI `31821662034` SUCCESS。old `0e703af` P1=3、`996994f` P1=1 与全部失败历史永久保留。
- 分支：`codex/dev-008b2-post-session-opening`。
- base：`origin/main@1eb26b2f0f6f56d72b9646f3c5e876ad4cbb4228`；提交前再次 fetch，origin/main 未漂移。
- iteration-coach：开工前恰好一次独立只读复核为 `Clear`；未启动第二次复核。复核要求的 durable recovery、waiting 零副作用、terminal 降级、固定 unavailable gate 与 QuestionEvidence 单一 owner 均纳入实现审计。
- 正式门禁：生产 continuing-consent policy 仍 unavailable；`SPEC-CONSENT-TEXT-POLICY-001` 继续 BLOCKED。真实 LLM runtime/provider 亦未接入；所有工程验证只使用既有 local/test deterministic 或 provider-neutral unavailable seam。

## 实现范围

1. canonical completed 提交后只发非阻塞通知；稳定 `post-session-analysis-v1` root 派生 memory extract 与 actual-question reconcile 两条有界 trigger/request identity。启动和 5 秒周期 reconciliation 从持久事实恢复，GET/list/WS replay 不触发业务作业。
2. 两 lane 复用 `MemoryService`、`QuestionEvidenceService`、`AiJobCoordinator` freeze/call/writeback、retention 与 eligibility；unknown/degraded 为 unjudged，失败/取消/provider unavailable 为 terminal，不回退 completed、录音或回顾。
3. sequence>=2 consumer 同时等待 calibration terminal gate 与 basis 两 lane terminal。calibration-first 只投影 `waiting_basis_analysis`，request/attempt 均 null，且零 opening job、attempt、Context/provider side effect。
4. 两前置 terminal 后以 consumer + basis analysis trigger + calibration gate 生成稳定 opening request/trigger；重复通知、并发与周期恢复汇聚同一 identity。confirmed gate 的 job scope含 basis+consumer；failed/skipped/unavailable 只含 basis，绝不伪造当前 stream trusted membership。
5. Context 复用现有 current eligible memory、current published/judgeable actual asked、scope/dependency/derived-output owner。basis session 由 scope、analysis identity 由 trigger、lane terminal outcome 由既有 context-builder process version 固化；成功 job 不借用 failure code。
6. opening 复用唯一 Director/Prompt/Schema、QuestionPresentation/QuestionEvidence writer、current/history/REST/WS。policy/provider 前置不可用时写诚实 terminal unavailable attempt，不新增 fallback 基础题、第二 AI、memory UI 或 question history。
7. 系统 trigger 的 rejected/orphaned 作业保留持久 terminal evidence；跨实例只在 30 秒宽限后把无进展 pending/running 判为 restart orphan，避免误杀另一实例正在执行的 provider call。

## 定向证据

- identity unit：稳定 root/lane/opening/request UUID、键长边界与固定 unavailable gate identity。
- integration：calibration-first waiting 的 request/attempt/Context 零副作用；completed 两 lane成功；same-project current memory；published judgeable actual asked；Context membership/process outcome；重复 completion/calibration 通知仍仅一个 opening attempt；confirmed scope含 basis+consumer。
- 回归修复：旧测试直接构造 finalization/calibration service 时协调器依赖保持可选；通知异常不阻断原 single-flight map 清理。双 trusted-role replay identity 与 context process metadata 已按正式语义修正。

## 完整本地验证

- `pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm build`、`pnpm test:smoke`、`git diff --check`：PASS。
- `pnpm test:unit`：62 files / 375 tests PASS。
- fresh PostgreSQL：14 migrations deploy/status PASS；无新增 schema 或 migration。
- `pnpm test:integration -- --run`：15 files / 86 tests PASS。
- `pnpm test:auth -- --run`：4 files / 23 tests PASS。
- ordinary Chromium：27/27 PASS；auth Chromium：5/5 PASS。

## 永久失败与修复历史

- 首次 `pnpm test:unit -- --runInBand` 因 Vitest 不支持该 CLI 参数失败；改用正式脚本后全绿，未改测试目标。
- 首次在共享 `elder_interview_test` 跑全量 integration，被先前残留 AI job/input FK 与固定 fixture 污染而多项失败；保留失败，改用新建隔离数据库验证。
- 第一个 fresh DB 排除污染后真实暴露 5 项回归：3 项旧 speaker-calibration 测试直接构造服务缺新依赖，2 项 finalization 因通知抛错导致 single-flight map 未清理。修复为 Nest optional/guarded notification 后定向 12/12、全量 86/86。
- 一次 fresh migration+integration 组合命令被工具 3 秒超时中断，迁移已完成；重新执行 status 与完整 integration 后通过。
- ordinary Chromium 首次调用被工具 5 秒超时中断；重新执行正式命令后 27/27。
- auth Chromium 首次因另一个并行 worktree 的测试 API 正占用硬编码 3101 而启动失败。确认其路径属于并行任务后未终止对方进程；端口自然释放后原命令 5/5 通过。
- 当前 pg 客户端仍输出既有 `client.query while already executing` 弃用警告；未扩大范围处理，不影响断言结果。

## GitHub 审查候选

- non-Draft PR：[PR #56](https://github.com/Li-Ming-G/elder_interview_ai/pull/56)。
- 首个实现 exact head：`c00a5a42d8c32f0d4b23edd009291fb25ccea6ea`；完整 CI `31799994616` 全步骤 SUCCESS。
- 本段为候选阶段治理追记；其后 accepted exact head、完整 CI 与授权独立结论已在顶部及收口段登记。

## 人工审查重点

1. completion/calibration 通知是否始终在业务 commit 后且不被 await；GET/list 是否确实只读。
2. stable identity、DB trigger dedupe、跨实例 orphan grace 与 QuestionEvidence attempt request unique 是否共同保证 exact-once 和恢复。
3. `waiting_basis_analysis` 是否在所有路径保持 request/attempt null，并且没有 job/Context/provider side effect。
4. degraded lane 是否诚实 terminal，成功 lane membership 是否仍全部进入 Context；display snapshot/manual-next/replaced/not-observed 是否绝不冒充 actual asked。
5. confirmed 与 failed/skipped/no-attempt unavailable gate 的 current-session speaker scope 是否严格分离。
6. context-builder process version、trigger 与 session scope 是否充分绑定 basis analysis identity和两 lane outcome，而未滥用 succeeded job failure code。
7. policy/deletion/retention/assignment/provider 不可用时是否留下 terminal evidence且不阻塞创建、录音、ASR、安全结束、completed 或回顾。
8. 是否继续只有一套 Director/QuestionEvidence/current/history/UI；没有第二 AI、memory/summary/pending UI 或真实 provider 冒充。

## 未实现与阻塞

- 未接入真实 LLM SDK/provider/model/region/secret，不形成真实继承质量或真实试点结论。
- 未提供正式持续授权正文/machine policy；生产真实 `covered` 仍失败关闭，`SPEC-CONSENT-TEXT-POLICY-001` 保持 BLOCKED。
- 为权威 provenance 与 attempt 绑定新增最小 Prisma migration；未改变 retention/deletion ownership、A4 UI、每 session raw/runtime 隔离、ServiceTerm、导出、PWA/App 或 server deletion。
- 无新产品/契约冲突，无新 ADR；实现沿用已接收 ADR-038/039 与现有所有权。

## REV-054（branch-local REV-052）定向修复候选（2026-08-14 至 2026-08-15）

- P1-1：gate 只接受 consumer 当前 active speaker stream 且必须绑定当前 `preparing/active` capture generation；A confirmed 后 rotate 到 collecting B 时为 `waiting_calibration`、opening count=0。QuestionEvidence writer 在 session advisory lock 内设置 consumer-session consumed fence；后续 gate identity 再变化也不产生第二 attempt。
- P1-2：启动/周期 reconciliation 覆盖 stale `question_generate` job 无 attempt 与 stale running attempt。两者均确定性终结为 failed/unavailable；既有 attempt 不再阻止 stale sibling job 清理，30 秒 grace 保留跨实例安全余量。
- P1-3：migration `20260814210000_dev008b2_review_invariants` 持久记录 basis session、analysis trigger、calibration gate/confirmed、memory/actual lane outcome+job identity，并让 opening attempt 绑定 Context snapshot。Director 只从同一 snapshot 的 exact scope/segment/memory/actual membership freeze；读取时机械校验 basis finalization、lane type/trigger/project/terminal status、revision 与 digest。
- 保留边界：completed/录音/ASR/回顾仍不等待 AI；`waiting_basis_analysis` 仍零副作用；同项目 eligible current memory、published judgeable actual asked、`displayed != actual asked`、QuestionEvidence 单一 owner、无第二 AI/记忆 UI 均未改变。真实 LLM 未接入；正式 continuing-consent `covered` 仍被 `SPEC-CONSENT-TEXT-POLICY-001` 阻塞。

### 本轮验证与永久失败历史

- PASS：API typecheck、仓库 lint、定向 PostgreSQL 4/4、fresh PostgreSQL 15 migrations deploy/status、完整 integration 15 files / 88 tests、workbench unit 定向 39/39。
- 首次 migration deploy 在含旧 opening attempt 的共享测试库触发新 CHECK；修复为 `NOT VALID`，保留旧历史同时继续约束新写入。第一次失败遗留的本任务新 DDL 已在明确测试库中精确回滚后重新 deploy 成功。
- 首次完整 unit 为 374/375，一项既有 workbench 401 异步测试超时；原测试定向复跑 39/39，未修改测试或产品目标，完整 unit 交 exact-head CI 复核。
- 首次共享 PostgreSQL 完整 integration 被旧 AI input/question-bank 残留污染而失败；未修改旧测试目标，改用全新隔离库 `elder_dev008b2_review`，从零 deploy 后 88/88。
- 旧 exact head `0e703af722134f98c9e11659d6fa8472eade5846`、CI `31800324817` SUCCESS、REQUEST_CHANGES P0=0/P1=3/P2=0 永久保留。本段记录当时的 `REVIEW` 修复候选；最终 accepted head/CI/PASS 不覆盖该历史。

## `996994f` 定向复审后的唯一相邻 P1

- 项目负责人绑定 `996994f508b4c7833410e6b9c0a6073dd7ca486e` / CI `31812498482` SUCCESS，正式 `REQUEST_CHANGES` P0=0/P1=1/P2=0。旧 `0e703af` P1=3 与本轮 P1=1 均永久保留。
- 已关闭且不得重写：current-stream gate、原两个 opening crash windows、Context provenance 主体。
- 唯一待修：context job freeze 成功但 snapshot writeback 前 crash，留下无 snapshot 的 stale `context_snapshot` job；必须纳入同一 30 秒 grace terminalization，并覆盖 restart、ACK 丢失、并发 reconcile、late writeback 失效与 consumer attempt≤1。
- 集成结果：已人工整合 `origin/main@c4a2b2d55ae281d8be9ff799dba73d12ab29acf1`。PR #58 accepted `e17a1410b1f3ae2c5cd9dff5f17650152e54cdef` / CI `31819220314` SUCCESS、final main CI `31819542394` SUCCESS；PR #54/#55 的 REV-052/053、ADR-041/042、SPEC-STAGING/SEC-AUTH DONE、DEV-STAGING BLOCKED 与无公网/真实数据边界全部保留。DEV-008B2 canonical review 改为 REV-054，并永久注明 branch-local REV-052。

### 唯一 P1 本地修复候选

- `reconcileAll()` 在派生 consumer 前先扫描 stale `context_snapshot` pending/running job；若同一 `ai_job_id` 无 `InterviewContextSnapshot`，超过 30 秒 grace 后复用 `failOrphanedSystemJob()` 收束为 `failed/SYSTEM_COORDINATOR_RESTARTED`。重复 startup/notify/reconcile 只执行条件更新，幂等且不调用 provider。
- `AiJobCoordinator.writeBack()` 的 drift terminalization 与最终 succeeded commit 都改为 `status=running` CAS。scanner 已将 job 终结时，late writeback 的 callback 不执行；若 scanner 在 callback 事务期间胜出，最终 CAS 失败会回滚整个 snapshot/membership 事务，不能复活 succeeded。
- persisted-state integration 新增 context freeze→无 snapshot crash→重复 startup/notify reconcile→context job failed、opening unavailable attempt恰好一个、late writeback 失去资格；原 4 项 current-stream/question-orphan/running-attempt/provenance 回归保持不变，定向共 5/5。
- P1-only checkpoint 本地 PASS：format、lint、typecheck、build；unit 62 files / 375 tests；fresh PostgreSQL `elder_dev008b2_context` 从零 deploy/status 15 migrations；完整 integration 15 files / 89 tests。
- 永久失败：在上轮已执行过完整 integration 的 `elder_dev008b2_review` 重复跑全量时，固定 question-bank version 残留导致 1/89 `QUESTION_BANK_VERSION_EXISTS`；未修改测试目标，改用全新数据库后 89/89。既有 pg `client.query while already executing` 弃用警告继续保留。
- 唯一 P1 已在 checkpoint `59c9434` 固化，并于 merge checkpoint `6158815` 人工整合 latest main。
- integration 候选本地完整矩阵 PASS：Prisma generate；format、lint、typecheck、build；unit 65 files / 385 tests；隔离库 `elder_dev008b2_integrated` fresh 17 migrations deploy/status、repeat deploy 无 pending、repeat status up-to-date；integration 15 files / 89 tests；auth 4 files / 26 tests；smoke；ordinary Chromium 27/27；auth Chromium 5/5。
- 本轮首轮 typecheck 因合并后本地 Prisma client 尚未 regenerate，`AuditActorType` 生成物失配失败；执行正式 `pnpm db:generate` 后同一源码 typecheck 及后续全矩阵 PASS。既有 pg 并发 query deprecation warning、ordinary E2E 启动瞬间 `/auth/me` proxy `ECONNREFUSED` 后 27/27 PASS 均保留，不修改测试目标。
- 该候选随后形成唯一 accepted review head `90ce5b35`，exact-head CI `31820768146` SUCCESS；最终结论见治理收口段。

## 治理收口（2026-08-15）

- REV-054 使用 canonical 编号并永久保留 branch-local REV-052 alias；accepted head/CI、PASS 评论、merge SHA 与 main CI 均已机械核对。
- DEV-008B2 仅在已接收的 fail-closed/provider-neutral/local-test seam 范围 `REVIEW→DONE`。真实 LLM/runtime/provider、正式 continuing-consent 正文/machine policy/`covered`、真实数据与真实试点不在 DONE 范围。
- SPEC-CONSENT-TEXT-POLICY-001 继续 `BLOCKED`；仓库无 canonical DEV-008B 父项，历史 DEV-008 继续 `CANCELLED`，DEV-008A 仍为网页 A 范围 `DONE`，DEV-008D 继续 `BLOCKED`。
- 本 closeout 只修改治理文档与 journal，不改 apps/packages/contracts/schema/migrations/dependencies，不启动 #57。
- 命令历史：一次只读 parent/status/handoff 机械核对组合命令已输出证据但返回 exit 1；未修改文件。后续窄化只读核对确认仓库无 DEV-008B 父项、DEV-008 仍 CANCELLED、DEV-008A DONE、DEV-008D BLOCKED，未扩大状态范围。
