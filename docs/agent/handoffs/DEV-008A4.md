# DEV-008A4 首次访谈主链路整合候选交接

## 状态与审查边界

- 状态：`REVIEW`；绑定 REV-047 与 non-Draft [PR #44](https://github.com/Li-Ming-G/elder_interview_ai/pull/44)，等待 exact-head CI 与项目负责人 GitHub 手动审查。
- 分支：`codex/dev-008a4-first-interview-flow`；base `origin/main@2f29cc7ef66563aebd2cd3d293606a5de6c20ca6`。
- iteration-coach：本任务恰好一次独立只读复核，结论 `Correction / NO-PAUSE`；后续未重复启动。
- 本交接不是 PASS/DONE/merge 结论。

## 已完成

- 普通新建不展示/创建 ServiceTerm，ready/start/capture current gate 不再依赖 ServiceTerm；API/表/历史保留 dormant，无 0 元或伪时长记录。
- 链路改为最低项目信息、early session、当前页 mic/input、口头授权录音/consent、formal capture/ASR、专属校准、工作台；刷新必须重做当前页 mic。
- ordinary transcript REST 只返回 conversation；WS 1.1 interim 增加权威 `content_kind`，后端按同 speaker stream 持久 attempt 时间重叠分类，工作台只显示 conversation interim/final。迟到更高 revision 校准内容不泄入，后续 conversation interim 正常恢复。
- collecting calibration 的 ASR 降级以稳定幂等 request ID 调用唯一 `resolveSpeakerCalibration(action=skip)`；无 attempt + provider unavailable 才允许本地 bypass，auth/permission 错误不显示该按钮，provider 恢复不会在已显示工作台后启动隐藏 attempt。
- happy path 自动 flush/stop-or-finalize/complete/reconcile/verify；仅失败才显示重试，稳定 request ID/水位防止无限循环和迟到回退。4 秒 canonical verify 保留，processing 可自动进入 completed。
- exact complete ACK 精确核对 object identity/count/bytes/checksum/chunk commitments 后，formal local interview job 才落 `complete`；回顾有界重投影，不放宽 fresh manifest + local archive identity/count/bytes/checksum/dirty/pending 门禁。
- completed 为同 URL 独立 `.completion-page` DOM，自动聚焦完成标题，不保留 transcript/suggestion/workbench 结构，只提供已保存事实、查看回顾、返回工作区。
- 已保留 A1 HomeShell/tokens/buttons/focus/44px/reduced-motion/响应式体系；未新增 PWA/App/第二套视觉。

## 验证与失败历史

- PASS：`pnpm.cmd format:check`、`pnpm.cmd lint`、`pnpm.cmd typecheck`、`pnpm.cmd build`、`git diff --check`。
- PASS：full unit `56 files / 339 tests`；包含 collecting degraded server skip/稳定重试 ID、无 attempt local bypass、auth 不越过、迟到 calibration interim 过滤/后续 conversation interim 恢复、completed 独立 DOM，exact complete ACK 后本机 complete 与回顾重投影。
- CI 失败历史：PR #44 initial exact head `a26a5230f9d50d28633aee528fb7eeb79e842528` 的 CI `31665010283` 在 fresh PostgreSQL integration 发现 2 项测试夹具漂移后失败；静态、unit、migration deploy/status 已成功，后续门禁按工作流跳过。迟到 interim 用例曾使用无父记录的随机 generation/audio ID；旧 calibration API 用例仍期望普通正文返回校准原始证据。修复为构造真实 capture generation 外键身份，并同时断言数据库保留 4 条原始证据（2 条 calibration）而 ordinary API 只返回 2 条 conversation；未修改产品代码或降低过滤目标，等待新 exact head 完整 CI。
- CI 二次失败历史：head `13079c6d9b1d700425105ac5188d1350a2478ea7` / CI `31665356601` 再次在 integration 80/82 失败；其余已执行门禁成功。真实 generation 被错误放进全局 `beforeEach`，碰撞既有“每 session 一个 interview audio object”唯一约束；两条相同 `start_ms` 的正文又被测试误按写入顺序断言。修复为只在迟到 interim 用例局部建立 generation，并验证正文集合及正式 `start_ms,id` 排序，不改产品逻辑或安全边界。
- 二次修复后的首次本地 full unit 出现 1 项既有工作台 401 异步等待超时（338/339）；该用例未被修改，定向立即复跑 1/1 PASS，随后 full unit 56 files / 339 tests PASS、build PASS。两次 GitHub CI 的 unit 阶段亦均成功；保留为非阻塞时序失败证据，不以修改测试目标清绿。
- Chromium 失败证据：本地 `pnpm.cmd test:e2e ...` 因根脚本参数转发形式意外启动全部 24 项；24 项均在 1–3ms 的 `browserType.launch: spawn EPERM` 失败，未进入页面断言，外层 120s 超时。错误与安全副本一致；未改浏览器参数、产品代码或测试目标。
- 数据库阻塞证据：`TEST_DATABASE_URL_SET=False`、`DATABASE_URL_SET=False`，Docker API npipe 返回 permission denied；依用 fresh PostgreSQL 的 migration/integration/auth/smoke/auth Chromium 本地未执行。
- 上述本地未执行门禁不降级；GitHub CI `verify` 明确包含 PostgreSQL 18.3 service、migrate deploy/status、integration、auth、smoke、全量 Chromium 与 auth Chromium，必须在 exact head 全绿后才交审。

## 外部阻塞

- 真实 ASR/provider 仍属外部阻塞；本任务只证明 raw audio 与 degraded/fake 路径，不宣称真实 ASR 验收。
- 本地浏览器进程与 Docker API 权限不可用；对应门禁等待 exact-head GitHub CI。
- 项目负责人 exact-head 手动审查尚未进行。

## 真正需项目负责人确认

- 无新的商业/隐私/权限产品决策项。实现均可从已确认决定、ADR-037 与正式契约推出。
- 仅需项目负责人对 non-Draft PR exact head 执行代码/产品手动审查并给出结论；在此前任务保持 REVIEW。

## 未扩大边界

- ServiceTerm API/表/历史保留 dormant；无 Prisma migration。
- 未实现真实 ASR/LLM、题库、server deletion、导出、部署、PWA/Android/App。
- 本机删除仍只删当前 origin/browser/device IndexedDB；服务器录音/转录/记忆/审计保留，DEV-008D/CON-023 不变。
