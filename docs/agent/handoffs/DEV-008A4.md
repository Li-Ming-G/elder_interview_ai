# DEV-008A4 接续实现交接

## 状态与审查边界

- 状态：`REVIEW`；REV-047 `PENDING`。
- 分支：`codex/dev-008a4-first-interview-flow-continuation`；基线 `origin/main@2f29cc7ef66563aebd2cd3d293606a5de6c20ca6`。
- PR / exact head / CI：待建立 non-Draft PR 后由本文件后续补充；项目负责人负责 exact-head GitHub 手动审查。
- 执行 Agent 只交付实现、验证与审查包，不给出 PASS/DONE，不 merge。

## 接续完整性

- 原 worktree：`C:\Users\TR\.codex\worktrees\7a1a\elder_interview_ai`，未修改、未清理。
- 机械接管后原/新 tracked diff SHA-1 均为 `44445294f09eec786fc715b6dae25ed51fce303e`；numstat SHA-1 均为 `90b9c9649dd6c673771ed2c2f38e6d9695144ef1`；name-status SHA-1 均为 `84bd41814bbab297b04496eeec50667bc4c1c752`。
- 原/新 task card SHA-256 均为 `E7E0E68C4C0B01095208D82A53905D796818EE07C66521D86543534C21752DED`；接管时 stat 为 38 files / 1118 insertions / 916 deletions。

## 已完成实现

- ServiceTerm ordinary UI/gate dormant；early session、当前页麦克风、正式口头授权、formal start/stream、独立校准与普通工作台主链路完成。
- complete exact ACK 后 formal local job 落 `complete`；正常 closeout 自动执行，失败保留稳定 ID 定向重试；A3 fresh verifier 不放宽。
- 校准 final 从普通 transcript/review 排除；降级时 collecting attempt 通过 resolve `skip` 收束，仅 ASR unavailable 且无 attempt 可本地放行。
- resolved attempt 的 `end_timeline_ms` 与离开校准时的 `hypothesis_id` 构成 interim fence：迟到同 hypothesis 高 revision 与边界内 interim 不显示，新 conversation hypothesis 正常显示。
- canonical `completed` 在同一 URL 渲染独立完成态，仅显示保存事实、查看回顾、返回工作区；普通 transcript、suggestion 与 workbench 操作不渲染。
- 完成页和校准页使用内部滚动边界；320×568 目视重叠已修复，沿用既有 tokens、按钮、focus 与 reduced-motion 规则。

## 验证证据

- `pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm build`：PASS。
- `pnpm test:unit --run`：56 files / 339 tests PASS。首轮高负载时既有 AI retry deadline 用例 1 次偶发失败；该文件 2/2 与后续完整重跑均通过，未改测试目标。
- `pnpm test:e2e -- --project=chromium`：27/27 PASS。
- Chromium 覆盖：1440×900、390×844、320×568；44px、focus、aria-live、reduced motion、横向/页面级意外 overflow；校准与 recording/interrupted/stopping/processing/completed/failed/no-audio 全状态截图在 `output/playwright/`。
- A3 纵向：`transcript_status=degraded` + raw manifest/archive complete 时明确显示“转录降级保存”，实际创建本机 `<audio>` 并允许删除；dirty、pending delivery、checksum mismatch 三类均同时禁用播放与删除。
- `git diff --check`：PASS。

## 环境限制与 CI 门禁

- Docker Desktop 应用进程可启动，但本机 `dockerDesktopLinuxEngine` pipe 未就绪；没有复用、删除或重置任何既有数据库。
- 因此 fresh PostgreSQL migrations deploy/status、integration、auth、smoke 与 auth Chromium 由 non-Draft PR exact-head GitHub CI 的 PostgreSQL 18.3 service 完整验证；CI 未 SUCCESS 前本任务不能请求项目负责人接收。

## 未改变边界

- 无 Prisma migration、新表、新枚举或新 API；ServiceTerm 表/API/历史保留。
- 不实现真实 ASR/LLM、正式题库、server deletion/deletion_request、导出、部署、PWA/App 或真实长者/PII 试点。
- DEV-008D 继续 `BLOCKED`，CON-023 继续 `OPEN / NOT IMPLEMENTED / NOT VERIFIED`。

## 下一接收对象

项目负责人只需对 non-Draft PR 的 final exact head、CI 与本交接执行 GitHub 手动审查；若提出意见，继续在本任务内定向修复并保持 REVIEW。
