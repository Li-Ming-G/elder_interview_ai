# 10｜研发协作与交接规范

## 1. 目标

本文件用于：

- 拆分研发任务；
- 明确依赖；
- 记录状态；
- 约束多 Agent 并行开发；
- 统一交接和验收。

`00` 只保留当前阶段和入口；实际任务状态维护在 `docs/agent/00-task-board.md`，实际交接记录维护在 `docs/agent/05-handoff-log.md`。本文件只规定协作规则和模板。

### 1.1 探索期任务解锁原则

- 先围绕一个有限纵向里程碑拆分任务，避免同时铺开全部 Epic；
- 依赖按“当前任务实际需要的最小稳定 seam”判断，不要求父任务所有生产门禁都已关闭；
- 最小 seam 必须有正式依据、明确输入输出、责任人、可回退边界和集成验证，不得用口头约定并行；
- 只影响未来生产部署、且不影响本地内部验证或核心数据安全的问题，登记为后续阻塞并继续不受影响的任务；
- 并行任务不得同时修改同一迁移、公共契约或共享核心模块；需要交叉时先串行固定契约，再合并；
- 任务 `DONE`、内部原型可集成、真实试点可发布是三个不同结论，均须按各自证据报告。

### 1.2 GitHub 人工审查流程

- 开发 Agent 完成本地适用验证后先 commit 并 push 到 GitHub，再把任务置为 `REVIEW`；
- 审查由项目负责人在 GitHub 执行并返回意见。push、PR 创建或 CI 通过只代表候选可审查，不代表验收通过；
- 审查记录必须包含仓库、分支、commit SHA、PR/审查链接、结论和未解决意见；修复后继续 push 到同一审查分支，并等待项目负责人复核；
- 未收到项目负责人明确通过意见前，高风险任务不得标记 `DONE`；总控可以执行测试和修复，但不能代替项目负责人给出最终审查结论；
- 若项目负责人明确改派独立审查 Agent，仍遵守实现者与审查者角色分离。
- 当前项目的默认流程固定为项目负责人在 GitHub 手动审查。开发 Agent、交接消息或其他 Agent 写出的“请总控复核”，一律解释为“请总控检查审查包完整性并转交项目负责人”，不得据此触发总控自行审查。只有项目负责人在当前任务中明确要求改派独立审查 Agent 时，才可改变默认审查者。

## 2. 任务状态

- `TODO`：未开始；
- `READY`：依赖满足，可以执行；
- `IN_PROGRESS`：正在执行；
- `BLOCKED`：存在依赖或问题；
- `REVIEW`：等待代码审查；
- `VERIFY`：等待任务要求的独立验收；
- `DONE`：通过适用验证；
- `CANCELLED`：取消。

文档整理、格式修复、简单配置和局部低风险修改可以由总控 Agent 自检后进入 `DONE`，但必须记录验证证据。涉及核心架构、关键业务规则、权限、安全、状态机、核心数据模型、跨模块契约、大规模合并或 MVP 发布时，只有项目负责人或独立审查/验收角色明确通过后才能进入 `DONE`。

## 3. 任务字段

每个正式开发任务至少记录：

```text
任务编号
任务名称
目标
范围
明确不做
依赖
负责人或 Agent
输入文档
输出
验收标准
状态
分支
提交
风险
交接说明
```

## 4. Epic 划分

### Epic A：工程基础

任务：

- 前端初始化；
- 后端初始化；
- pnpm workspace；
- PostgreSQL；
- 数据库迁移；
- 登录；
- 角色权限；
- Docker Compose；
- Nginx；
- 错误码；
- 结构化日志；
- 环境配置；
- CI 基础检查。

### Epic B：项目、服务和授权

任务：

- 长者项目；
- 项目列表和详情；
- 项目分配；
- 服务条款版本；
- 累计时长；
- 超时提醒；
- 授权文本版本；
- 授权录音；
- 授权状态；
- 撤回；
- 启动门禁。

### Epic C：音频和实时转录

任务：

- 麦克风检测；
- AudioWorklet；
- MediaRecorder；
- 音频分片；
- 本地缓冲；
- 分片重试；
- 业务 WebSocket；
- ASR 适配器；
- 中间态和确定态；
- 说话人校准；
- 说话人映射；
- ASR 重连；
- manifest；
- 补转录（`HARDEN-ASR-001`，真实供应商/可靠性阶段后置，不阻塞当前内部产品价值验证）。

### Epic D：访谈工作台

任务：

- 首次访谈准备页；
- 顶部窄状态栏、转录主体和底部建议卡；
- 链路状态；
- 实时转录；
- 自动跟随、回看暂停、新内容计数和回到最新；
- 计时；
- 服务剩余提醒；
- 安全结束；
- 中断恢复。

旧 `DEV-005A/B/C` 已在各自声明的页面外壳、合成 PCM 工作台和服务端结束编排范围通过，历史证据保留。完整纵向链路按 `SPEC-DEV-005R` 重建为 `DEV-005R1`（服务端采集生命周期）、`DEV-005R2`（浏览器单流采集/归档/交付）、`DEV-005R3`（正式工作台恢复与结束体验）和 `DEV-005R4`（跨模块验收）。R1 是共享数据/API 前置；R2 后 R3 才接管工作台，R4 最后收口。文本/说话人修正、复杂内容标记、项目管理、完整回顾和本地备份管理仍按后续切片解锁。

### Epic E：AI 长期记忆

任务：

- `memory_item`；
- 记忆类型和状态；
- 证据关联；
- 冲突组；
- 人工确认；
- 访谈记录员；
- 结构化输出；
- 上下文构建器；
- 受限内容过滤；
- 重新分析任务。
- 后台 current memory 与跨会话 current view；
- 冲突澄清、明确更正和 future eligibility；
- AI 过程记录与会后实际问题整理的稳定 seam。

第一版不实现记忆管理 UI、人工候选确认页、完整知识图谱或向量数据库。自动记忆可直接辅助问题，但证据、版本、冲突、删除和失效关系必须保留。

### Epic F：AI 追问

任务：

- 采访导演；
- 问题 Schema；
- 自动触发；
- 手动触发；
- 节流；
- 单个当前最佳问题；
- 推荐原因；
- 证据片段；
- 继续倾听；
- “没用，换一个”和当前会话相似问题排除；
- 内部建议证据历史；
- 基础问题回退。

DISC-006 已取消第一版 AI unavailable 时的基础问题自动回退：失败只显示不可用，倾听员每次“换一个”触发一次人工重试。基础问题库可以保留为未来人工资源，但不冒充 AI 输出。

倾听员采用、已问、忽略、稍后和改写操作不属于首轮产品行为。`SPEC-AI-QUESTION-001` 是 DEV-007 开工硬前置。

### Epic G：回顾、导出和删除

任务：

- 录音播放器；
- 转录回顾；
- 会话工作记录；
- 项目导出；
- 受限内容隔离；
- 短期下载；
- 撤回限制；
- 删除申请；
- 物理清理；
- 审计。

### Epic H：测试和试点

任务：

- 测试身份；
- 虚构项目数据；
- 预录双人音频；
- ASR stub；
- LLM stub；
- AI 评测集；
- 故障注入；
- 性能测试；
- 权限测试；
- 真实授权访谈；
- 质量报告。

## 5. 依赖关系

```text
A 工程基础
├─ B 项目、服务和授权
├─ C 音频和实时转录
│   ├─ D 访谈工作台
│   └─ E AI 长期记忆
│       └─ F AI 追问
├─ G 回顾、导出和删除（按数据生产者逐步解锁）
└─ H 测试和试点贯穿各阶段
```

更准确的任务依赖：

- B 依赖 A；
- C 依赖 A；
- D 依赖 B、C；
- E 的后端能力依赖 C1 已通过的确定态转录、证据 ID、流级可信角色和控制内容过滤契约，以及独立 `SPEC-DEV-006` 对跨 session revision/provenance/stale 消费模型的项目负责人 PASS；不硬依赖 C2 的复杂批量修正 UI，也不硬依赖 D 的完整工作台 UI；
- F 依赖 E；
- `DEV-005A` 依赖 B 的项目/授权/会话 start 门禁，不依赖项目列表或安全结束能力；
- `DEV-005B` 依赖 DEV-004B2 稳定的浏览器实时转录 seam 与 DEV-005A 页面外壳；
- `DEV-005C` 依赖 `SPEC-SESSION-END-001`、DEV-003 原始音频收束 seam 和 DEV-004B2 实时转录 seam，不依赖 DEV-005A/B 页面完成；
- 旧 `DEV-005D` 未实施并已由 `DEV-005R3` 取代；其“只消费服务端事实”的原则继续有效；
- `SPEC-DEV-005R` 统一承接 A-R/B-R/C-R/D-R 已批准决定；它通过后 `DEV-005R1` 和仅限新浏览器音频模块的 R2-core 可以有限并行，但共享 DTO 只由 R1 修改；
- `DEV-005R2` 的正式集成依赖 R1 PASS；`DEV-005R3` 依赖 R1/R2 PASS；`DEV-005R4` 依赖 R1/R2/R3 PASS；
- `SPEC-DEV-004C` 先冻结正式 `speaker_stream_id`、角色 authority、校准控制内容和修正 revision/membership；其 PASS 后先实施 `DEV-004C1`，再实施 `DEV-004C2`。C1 只承载低负担正式流校准与可信角色门禁；复杂批量修正入口继续归后续完整回顾切片；
- `DEV-007A` 依赖 E 的可用上下文/证据 seam 与 `SPEC-AI-QUESTION-001`；
- G 若保持一个完整任务，依赖 B、C、D、E、F；只有正式拆分后，不含回顾 UI 的导出或删除子任务才可按其实际数据生产者单独解锁；
- H 从 A 开始准备，最终依赖全部功能。

对应当前任务编号：

- `DEV-006` 后端最低依赖 `DEV-004C1` PASS 与 `SPEC-DEV-006` PASS。前者提供可信 effective role、控制内容排除和 session revision，后者冻结项目级跨 session watermark、job/segment/output provenance、stale 状态和查询过滤；不必等待 DEV-004C2 代替它实现 AI 重算，与 `DEV-005` 的展示集成另行验收；
- `SPEC-DEV-006` 还必须在 DEV-006/007 之间冻结单一所有权：DEV-006 负责 current memory、冲突/更正、跨会话 current view、provenance 与过程记录；DEV-007 负责当前问题、换题、已展示快照和未来 eligibility。会后实际问题整理可以拆为独立子任务或指定唯一模块，但不得两边各维护一套 question history；
- `DEV-008` 若不拆分，前置为 `DEV-002`、`DEV-003`、`DEV-004`、`DEV-005`、`DEV-006`、`DEV-007`；
- 后续优先把 `DEV-008` 拆为回顾、导出、删除三个可独立验收的任务，避免不必要串行化。


## 6. Agent 分工建议

### 总控 Agent

重点读取全部根目录规范和 `docs/agent/`。

负责：

- 维护 `00-项目说明与执行入口.md`；
- 维护 `docs/agent/00-task-board.md`；
- 拆分任务和检查依赖；
- 协调公共契约变更；
- 登记冲突和重要决策；
- 不代替项目负责人或独立审查 Agent 宣布通过。

### 前端 Agent

重点读取：

- `01-产品需求文档.md`；
- `02-项目开发规范.md`；
- `03-业务流程与交互规范.md`；
- `05-API与实时事件契约.md`；
- `06-实时音频与转录规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 项目页面；
- 访谈工作台；
- 麦克风和录音；
- WebSocket 客户端；
- 状态展示；
- 恢复交互。

### 后端 Agent

重点读取：

- `02-项目开发规范.md`；
- `03-业务流程与交互规范.md`；
- `04-数据模型规范.md`；
- `05-API与实时事件契约.md`；
- `06-实时音频与转录规范.md`；
- `07-AI访谈引擎规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 数据库；
- REST；
- WebSocket 服务；
- 对象存储；
- ASR 适配器；
- 权限；
- 导出和删除。

### AI Agent

重点读取：

- `01-产品需求文档.md`；
- `03-业务流程与交互规范.md`；
- `04-数据模型规范.md`；
- `05-API与实时事件契约.md`；
- `07-AI访谈引擎规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 记忆模型；
- 上下文构建；
- 提示词；
- JSON Schema；
- 评测集；
- 问题质量。

### 项目负责人或测试与审查 Agent

重点读取全部文档，尤其是 `03` 至 `09`、需求追踪矩阵和最新交接。

负责独立验证，不根据开发者自述直接通过，也不能通过修改验收目标替实现开绿灯。项目负责人通过 GitHub 审查时，必须把明确结论和对应 commit/PR 返回总控登记。

## 7. 公共契约变更

任何 Agent 需要修改以下内容时，必须先提出契约变更：

- 表结构；
- 状态枚举；
- REST 路径；
- WebSocket 事件；
- AI JSON Schema；
- 内容标记类型；
- 授权和删除规则；
- MVP 范围。

契约变更记录格式：

```text
变更编号：
提出人：
影响文档：
当前规则：
建议规则：
变更原因：
兼容性影响：
迁移方案：
测试影响：
批准状态：
```

未经批准，不得先在代码中形成既成事实。

## 8. 任务交接模板

```markdown
# 任务交接

## 基本信息
- 任务编号：
- 分支：
- 提交：
- 当前状态：

## 已完成
-

## 未完成
-

## 关键设计
-

## 修改文件
-

## 数据库或接口变更
-

## 测试结果
-

## 已知问题
-

## 风险
-

## 下一位 Agent 必须先读取
-

## 复现或运行方式
-
```

## 9. 完成定义

任务进入 `REVIEW` 前：

- 代码可运行；
- 类型检查通过；
- 相关测试通过；
- 没有真实密钥；
- 文档同步；
- 提交交接记录。

需要独立验收的任务进入 `VERIFY` 前：

- 项目负责人或独立审查角色已给出代码审查结论；
- 契约测试通过；
- 已知限制写明；
- 测试环境可复现。

任务进入 `DONE` 前：

- 已完成该任务风险等级要求的验证；
- 若任务需要独立验收，项目负责人或独立验收角色已经明确通过；
- 无阻塞缺陷；
- 文档与实现一致；
- `docs/agent/00-task-board.md` 和需求追踪矩阵已经更新。

## 10. 禁止事项

- 未读文档直接开发；
- 擅自扩大 MVP；
- 把后续规划当当前需求；
- 用 AI 生成内容覆盖原始数据；
- 绕过授权门禁；
- 前端保存供应商密钥；
- 后端直接依赖供应商原始对象；
- 修改公共契约不更新文档；
- 自己开发、自己宣布验收通过，或把 GitHub push/CI 成功冒充审查通过；
- 用“后续再补测试”把任务标记完成。

## 11. 每轮开发结束后的总控检查

1. 当前任务是否完成原目标；
2. 是否超出范围；
3. 是否引入新依赖；
4. 是否修改契约；
5. 是否影响其他 Agent；
6. 是否存在安全或数据风险；
7. 是否补充测试；
8. 是否需要更新后续任务状态；
9. 是否具备清晰交接；
10. 是否可以交给独立验收。
