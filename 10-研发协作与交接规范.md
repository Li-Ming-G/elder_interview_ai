# 10｜研发协作与交接规范

## 1. 目标

本文件用于：

- 拆分研发任务；
- 明确依赖；
- 记录状态；
- 约束多 Agent 并行开发；
- 统一交接和验收。

`00` 只保留当前阶段和入口；实际任务状态维护在 `docs/agent/00-task-board.md`，实际交接记录维护在 `docs/agent/05-handoff-log.md`。本文件只规定协作规则和模板。

### 1.1 探索期任务解锁原则

- 先围绕一个有限纵向里程碑拆分任务，避免同时铺开全部 Epic；
- 依赖按“当前任务实际需要的最小稳定 seam”判断，不要求父任务所有生产门禁都已关闭；
- 最小 seam 必须有正式依据、明确输入输出、责任人、可回退边界和集成验证，不得用口头约定并行；
- 只影响未来生产部署、且不影响本地内部验证或核心数据安全的问题，登记为后续阻塞并继续不受影响的任务；
- 并行任务不得同时修改同一迁移、公共契约或共享核心模块；需要交叉时先串行固定契约，再合并；
- 任务 `DONE`、内部原型可集成、真实试点可发布是三个不同结论，均须按各自证据报告。

### 1.2 GitHub 人工审查流程

- 开发 Agent 完成本地适用验证后先 commit 并 push 到 GitHub，再把任务置为 `REVIEW`；
- 审查由项目负责人在 GitHub 执行并返回意见。push、PR 创建或 CI 通过只代表候选可审查，不代表验收通过；
- 审查记录必须包含仓库、分支、commit SHA、PR/审查链接、结论和未解决意见；修复后继续 push 到同一审查分支，并等待项目负责人复核；
- 未收到项目负责人明确通过意见前，高风险任务不得标记 `DONE`；总控可以执行测试和修复，但不能代替项目负责人给出最终审查结论；
- 若项目负责人明确改派独立审查 Agent，仍遵守实现者与审查者角色分离。
- 当前项目的默认流程固定为项目负责人在 GitHub 手动审查。开发 Agent、交接消息或其他 Agent 写出的“请总控复核”，一律解释为“请总控检查审查包完整性并转交项目负责人”，不得据此触发总控自行审查。只有项目负责人在当前任务中明确要求改派独立审查 Agent 时，才可改变默认审查者。

## 2. 任务状态

- `TODO`：未开始；
- `READY`：依赖满足，可以执行；
- `IN_PROGRESS`：正在执行；
- `BLOCKED`：存在依赖或问题；
- `REVIEW`：等待代码审查；
- `VERIFY`：等待任务要求的独立验收；
- `DONE`：通过适用验证；
- `CANCELLED`：取消。

文档整理、格式修复、简单配置和局部低风险修改可以由总控 Agent 自检后进入 `DONE`，但必须记录验证证据。涉及核心架构、关键业务规则、权限、安全、状态机、核心数据模型、跨模块契约、大规模合并或 MVP 发布时，只有项目负责人或独立审查/验收角色明确通过后才能进入 `DONE`。

## 3. 任务字段

每个正式开发任务至少记录：

```text
任务编号
任务名称
目标
范围
明确不做
依赖
负责人或 Agent
输入文档
输出
验收标准
状态
分支
提交
风险
交接说明
```

## 4. Epic 划分

### Epic A：工程基础

任务：

- 前端初始化；
- 后端初始化；
- pnpm workspace；
- PostgreSQL；
- 数据库迁移；
- 登录；
- 角色权限；
- Docker Compose；
- Nginx；
- 错误码；
- 结构化日志；
- 环境配置；
- CI 基础检查。

### Epic B：项目、服务和授权

任务：

- 长者项目；
- 项目列表和详情；
- 项目分配；
- 服务条款版本；
- 累计时长；
- 超时提醒；
- 授权文本版本；
- 授权录音；
- 授权状态；
- 撤回；
- 启动门禁。

### Epic C：音频和实时转录

任务：

- 麦克风检测；
- AudioWorklet；
- MediaRecorder；
- 音频分片；
- 本地缓冲；
- 分片重试；
- 业务 WebSocket；
- ASR 适配器；
- 中间态和确定态；
- 说话人校准；
- 说话人映射；
- ASR 重连；
- manifest；
- 补转录（`HARDEN-ASR-001`，真实供应商/可靠性阶段后置，不阻塞当前内部产品价值验证）。

### Epic D：访谈工作台

任务：

- 首次访谈准备页；
- 顶部窄状态栏、转录主体和底部建议卡；
- 链路状态；
- 实时转录；
- 自动跟随、回看暂停、新内容计数和回到最新；
- 计时；
- 服务剩余提醒；
- 安全结束；
- 中断恢复。

旧 `DEV-005A/B/C` 已在各自声明的页面外壳、合成 PCM 工作台和服务端结束编排范围通过，历史证据保留。完整纵向链路按 `SPEC-DEV-005R` 重建为 `DEV-005R1`（服务端采集生命周期）、`DEV-005R2`（浏览器单流采集/归档/交付）、`DEV-005R3`（正式工作台恢复与结束体验）和 `DEV-005R4`（跨模块验收）。R1 是共享数据/API 前置；R2 后 R3 才接管工作台，R4 最后收口。文本/说话人修正、复杂内容标记、项目管理、完整回顾和本地备份管理仍按后续切片解锁。

### Epic E：AI 长期记忆

任务：

- `memory_claim/evidence + memory_resolution/member`；
- current memory 与动态 future eligibility；
- 证据关联；
- 冲突组；
- 人工 authority 保护；
- 访谈记录员；
- 结构化输出；
- 上下文构建器；
- 受限内容过滤；
- 两阶段 job、重新分析与失败状态；
- 后台 current memory 与跨会话 current view；
- 冲突澄清、明确更正和 future eligibility；
- AI 过程记录与会后实际问题整理的稳定 seam。

第一版不实现记忆管理 UI、人工候选确认页、完整知识图谱或向量数据库。自动记忆可直接辅助问题，但证据、版本、冲突、删除和失效关系必须保留。

### Epic F：AI 追问

任务：

- 题库 CSV 模板、校验、原子导入、版本、启停和许可门禁；
- `basic|deep` 题库的运行时读取；
- `rapport|life_outline|story_depth` 旅程状态与确定性判定；
- 采访导演；
- 问题 Schema；
- 自动触发；
- 手动触发；
- 节流；
- 单个当前最佳问题；
- 推荐原因；
- 证据片段；
- 继续倾听；
- 自动更新当前最佳问题、“下一个问题”和当前会话相似问题排除；
- “上一个问题 / 回到当前问题”的只读展示历史导航；
- 内部建议证据历史；
- `QuestionEvidenceModule` 共享 generation/display/actual-question 证据基座。

DISC-006 已取消第一版 AI unavailable 时的静态基础题自动回退：失败只显示不可用，倾听员每次点击“下一个问题”触发一次人工重试。后续产品纠偏明确：版本化题库是正常问题生成的可选参考，不是正文白名单，也不是 AI unavailable 时的 UI 兜底。零 eligible 参考时仍可依据可信对话自由生成；编排不可用仍返回不可用。

倾听员采用、已问、忽略、稍后和改写操作不属于首轮产品行为。`SPEC-AI-QUESTION-001` 与项目负责人 PASS 的 `SPEC-QUESTION-JOURNEY-001` 是 DEV-007A/B 的共同硬前置。

### Epic G：倾听员工作区、最小回顾和删除治理

任务：

- `DEV-008A1`：共享 authenticated home/app shell、项目/访谈列表和统一 routes；
- `DEV-008A2`：新建访谈纵向入口，完整复用 project/service term/consent/session/device-check/start 门禁；
- `DEV-008A3`：已结束访谈最小回顾、录音播放器、原始/修订转录查看和本机浏览器录音副本管理；
- `DEV-008D`：正式服务器隐私删除、统一 `DeletionScopeReader`、C2/AI 回接、物理清理和审计。

若 A3 开工前发现 fresh delete preflight 所需的普通 session snapshot 缺字段，必须先以独立 contract SPEC 串行补齐公共 DTO、正式 04/05/08/09 与验证矩阵；在该 SPEC exact-head PASS/merge 前 A3 保持 `BLOCKED`。这种 additive contract-first 扩展可以暂时 optional+nullable 兼容旧 producer，但后续 runtime 任务必须用字段白名单测试证明 canonical response 实际显式发出；首页 list 与 restricted evidence-finalization 不因 A3 扩宽。

倾听员导出取消；未来其他角色若需要，必须另立受控任务，不与 A1/A2/A3 或 008D 混合。

### Epic H：测试和试点

任务：

- 测试身份；
- 虚构项目数据；
- 预录双人音频；
- ASR stub；
- LLM stub；
- AI 评测集；
- 故障注入；
- 性能测试；
- 权限测试；
- 真实授权访谈；
- 质量报告。

## 5. 依赖关系

```text
A 工程基础
├─ B 项目、服务和授权
├─ C 音频和实时转录
│   ├─ D 访谈工作台
│   └─ E AI 长期记忆
│       └─ F AI 追问
├─ G 倾听员工作区、最小回顾和删除治理（按正式拆分解锁）
└─ H 测试和试点贯穿各阶段
```

更准确的任务依赖：

- B 依赖 A；
- C 依赖 A；
- D 依赖 B、C；
- E 的后端能力依赖 C1 已通过的确定态转录、证据 ID、流级可信角色和控制内容过滤契约，以及独立 `SPEC-DEV-006` 对跨 session revision/provenance/stale 消费模型的项目负责人 PASS；不硬依赖 C2 的复杂批量修正 UI，也不硬依赖 D 的完整工作台 UI；
- F 依赖 E；
- `DEV-005A` 依赖 B 的项目/授权/会话 start 门禁，不依赖项目列表或安全结束能力；
- `DEV-005B` 依赖 DEV-004B2 稳定的浏览器实时转录 seam 与 DEV-005A 页面外壳；
- `DEV-005C` 依赖 `SPEC-SESSION-END-001`、DEV-003 原始音频收束 seam 和 DEV-004B2 实时转录 seam，不依赖 DEV-005A/B 页面完成；
- 旧 `DEV-005D` 未实施并已由 `DEV-005R3` 取代；其“只消费服务端事实”的原则继续有效；
- `SPEC-DEV-005R` 统一承接 A-R/B-R/C-R/D-R 已批准决定；它通过后 `DEV-005R1` 和仅限新浏览器音频模块的 R2-core 可以有限并行，但共享 DTO 只由 R1 修改；
- `DEV-005R2` 的正式集成依赖 R1 PASS；`DEV-005R3` 依赖 R1/R2 PASS；`DEV-005R4` 依赖 R1/R2/R3 PASS；
- `SPEC-DEV-004C` 先冻结正式 `speaker_stream_id`、角色 authority、校准控制内容和修正 revision/membership；其 PASS 后先实施 `DEV-004C1`，再实施 `DEV-004C2`。C1 只承载低负担正式流校准与可信角色门禁；复杂批量修正入口继续归后续完整回顾切片；
- `SPEC-AI-QUESTION-001` 已由项目负责人 GitHub 手动审查 PASS 并合并；其 current/history/actual-question 前置继续有效，但不自动解除 CON-025；
- `SPEC-QUESTION-JOURNEY-001` 必须在非 Draft PR exact final head/CI 获项目负责人 GitHub 手动审查 PASS 后，才可解除 CON-025 对问题路线的暂停；
- `DEV-007A` 依赖 E 的可用上下文/证据 seam、已 PASS 的 `SPEC-AI-QUESTION-001` 和 `SPEC-QUESTION-JOURNEY-001`；
- `DEV-007B` 依赖 `DEV-007A` PASS，并继续依赖 DEV-006 current-memory/actual-asked/QuestionEvidence seam；
- G 已由 SPEC-DEV-008A 正式拆分。A1 依赖既有认证、project assignment 与项目/session read model；A2 依赖 A1 合并及既有 B/DEV-005 start 门禁；A3 依赖 A1 合并及 DEV-003/DEV-005 的 browser archive、manifest、transcript seam。A2/A3 不依赖 DEV-007、正式题库、问题历史或完整回顾；
- 008D 独立依赖正式 deletion producer/read model、CON-006/007 已解决契约、CON-023、C2/AI consumer 回接和安全验收，不被 local deletion 替代；
- H 从 A 开始准备，最终依赖全部功能。

对应当前任务编号：

- `DEV-006` 后端最低依赖 `DEV-004C1` PASS 与 `SPEC-DEV-006` PASS。前者提供可信 effective role、控制内容排除和 session revision，后者冻结项目级跨 session watermark、job/segment/output provenance、stale 状态和查询过滤；不必等待 DEV-004C2 代替它实现 AI 重算，与 `DEV-005` 的展示集成另行验收；
- `SPEC-DEV-006` 已冻结单一所有权：DEV-006 建立 `QuestionEvidenceModule` 的共享数据/服务基座、actual-question analysis/catalog、current memory、动态 eligibility、provenance 与过程记录；DEV-007 只经该模块实现 generation/display/replace，并消费 actual-asked reader。displayed snapshot、future eligibility 与当前 visibility 分开，但不得各维护一套 question history；每个独立业务输出一条 derived row，actual-question analysis 整版一条，禁止下游改为 output-set 或逐 question 资格表；
- 历史 `DEV-008` 已由 SPEC-DEV-008A 拆分并停止作为可执行聚合任务；旧依赖与审查历史保留，但不得再把 DEV-007 父项聚合验收作为 A1/A2/A3 前置；
- `SPEC-DEV-008A` 已在 REV-041 对 final exact head/CI 获授权手动 PASS 并以 PR #31 合并；DEV-008A1 开工前 Correction 随后发现 CON-028。SPEC-DEV-008A1-ACCESS 又在 REV-042 对 exact head `81f0bba3d30139e458e919da969d40386231cc62` / CI `31586889712` 获项目负责人正式 PASS，并以 merge `18ba7381f7ba747c2fb3beefe28297c6d063a174` / main CI `31587442461` 接收；A1 已恢复 READY。A1 自身 PASS/merge 后 A2/A3 才可独立并行；A1 的 READY 不解锁父 A、A2、A3 或 008D，也不关闭 CON-023；
- A1/A2/A3 共享一套响应式网页 shell、tokens、列表/状态/空错词汇与 routes。A1 是共享组件和公共 DTO/API owner；A2/A3 不得各建第二套 home、导航、token、按钮、卡片、状态色或 session list；
- 当前只做响应式网页与 Android Chrome 等现代浏览器，不做 PWA、Service Worker 安装体验、Capacitor/WebView 或 Android App。未来 Android 软件另立产品阶段。

### 5.1 SPEC-DEV-008A 与实现切片边界

`SPEC-DEV-008A` 是 docs/machine-contract/governance-only 任务，不修改业务代码、Prisma/migration 或测试。其交付冻结：统一工作区、新建纵向入口、session list read model、最小回顾、本机 archive 投影/播放/删除、任务依赖和验收矩阵。

- `DEV-008A1`：唯一 home shell、项目/访谈列表、空/loading/error/restricted、共享 ListRow/StatusBadge 等价语义、统一 routes。允许实现最小 `GET /projects/:id/sessions` read model 和 DTO；服务端必须按 `03` §17.2 返回唯一 home state/action/review access，A1 不以“未完成/已完成”二分自行猜测；restricted+有效 assignment 只返回独立最小 `ProjectListRestrictedProjection`，不复用 `ProjectResponse`、不含 session 行或主动作；session cursor 绑定 project/keyset anchor 并失败关闭；不得实现新建表单、回顾或本机删除；
- `DEV-008A2`：在 A1 shell 内实现“新建访谈”完整流程；创建 draft 后继续完成 service term 与正式口头授权，再创建 session、进入准备页/device check/start。四个 create 首次网络请求前持久稳定 request ID，由 `04` §4.23 权威绑定 actor/action/target-or-project-create-identity/payload hash 并重放；不得用 GET 猜未知 POST、提供 fixture 生产入口或绕过授权；
- `DEV-008A3`：在 A1 shell 内实现 completed/processing 及 failed+complete manifest 的只读回顾、本机完整 archive 播放、原始/修订转录只读、容量事实和本机副本原子删除；failed 只允许回顾/播放而不允许本机删除。projection 使用唯一优先级与 formal Schema `state_basis`/交叉字段/正反 fixture。不得实现文本编辑、问题历史、记忆/工作记录、server download、导出或正式隐私删除；
- `DEV-008D`：实现 `04/05/08/09` 已冻结的 server deletion request producer/read model、统一 scope guard、C2/AI/回顾传播、在线/备份清理和审计；关闭 CON-023 必须以完整 runtime/并发/不泄密证据为准。

UI 共用 `apps/web/src/styles.css` 现有 OKLCH tokens、排版、context-label、primary/secondary/danger、focus-visible、44px 与 reduced-motion。1440×900、390×844、320×568 是 A1/A2/A3 共用硬门禁；实现名称可变，但共享组件语义、状态词汇和导航只能有一套。

DEV-008A1 开工前唯一 iteration-coach Correction 已发现 restricted 项目缺少机器投影，并确认两项既有普通读取绕权。SPEC-DEV-008A1-ACCESS 已 exact-head PASS/merge，CON-028 已 RESOLVED，A1 恢复 `READY`；实现必须同时覆盖：普通 project/service-term/consent/session reader 拒绝 restricted/deleted/软删除与 assignment 失效；session/finalization `created_by` 不替代 assignment；限制前冻结 stop 的原操作者只消费专属 `EvidenceFinalizationResponse`，该 seam 不进入 Home/prepare/workbench/review。不得为恢复实现启动第二次 iteration-coach。

### 5.2 DEV-006 可下发范围

项目负责人对 `SPEC-DEV-006` 明确 PASS 后，DEV-006 只实施：

1. `transcript_segment.text_revision`、`elder_project.ai_policy_revision/ai_retention_policy_version` 与 `04` §§4.28-4.43 的前向 migration、索引和 repository；旧缺 provenance 数据按 review-required/unjudged/future-ineligible 迁移，不推断 display/actual/trust；
2. AI job 两阶段 coordinator、逐 session scope（含 0 eligible）与 segment/memory membership、provider call 记录；实现逐业务输出一对一 derived association、segment/memory/question expected dependency count/manifest、跨表 deferred constraint 和权威 eligibility reader；以 fixture 证明五条 claim 对应五条资格记录；
3. trusted elder memory claim/resolution/current reader、冲突/明确更正/人工 authority 保护和跨会话 context snapshot；
4. `QuestionEvidenceModule` 共享表与服务、会后 actual-question reconcile、整版 catalog 资格/可靠目录发布和 actual-asked reader；显式记录 display/event/actual-question dependencies，任一命中时整版撤下；不实现问题文案生成算法；
5. 过程记录最小化；实现 `ai_job|question_display_snapshot|memory_retention_root` 三类 retention root、child 生命周期继承、先隐藏后清理、CASCADE/显式幂等顺序、跨 root detach、最小清理审计与失败续跑；接入权限/授权/boundary/deletion policy ports，以及现有 project/assignment/consent producer 对 `ai_policy_revision` 的最小事务性递增接缝和 §09 的 unit/integration/auth/migration fixture；不借此改变生产者业务状态机；
6. 对当前不存在的 deletion runtime 只定义并注入失败关闭的正式 port，不允许生产 no-op。能在 DEV-008D 前安全运行的本地 fixture 必须显式标注 `deletion coverage = NOT IMPLEMENTED / NOT VERIFIED`，CON-023 继续 OPEN。

DEV-006 不实施：记忆管理 UI；suggestion REST/WS、自动替换/手动下一个问题/历史导航/节流/相似度；基础题回退；Prisma 之外的 DEV-007 页面；deletion producer/worker；C2 producer 改造；真实供应商或生产部署。任何共享 DTO/枚举变化先改 04/05/07/08/09 契约。

`SPEC-AI-QUESTION-001` 已在上述 generation attempt/display snapshot/state/event seam 上冻结问题 Schema、自动生成与替换触发、排序稳定/防抖、手动“下一个问题”、只读展示历史导航、节流、相似度、REST canonical projection、无正文 WS notification 和错误状态，并已 PASS/合并；不得重定义 actual question、current memory、derived-output cardinality、retention roots 或另建 question history。DEV-007 当前只因 `SPEC-QUESTION-JOURNEY-001`/CON-025 保持 BLOCKED。

### 5.3 DEV-007 重新拆分

`DEV-007` 保留为父任务，不直接开工；第一版拆为两个串行子任务：

#### DEV-007A｜题库基础设施、旅程阶段与确定性选择 seam

- 负责 `07` §10 的 CSV 模板 validator、全量 draft import、不可变版本、原子 activate/retire、来源/许可与 fixture 环境隔离；
- 实现 14 列 `QuestionBankImportService/QuestionBankReader/QuestionJourneyService` 和 deterministic selector/fake；严格校验 purpose、`question_condition_v1`，并按 `journey_policy_v1` 固定优先级输出稳定 stage/reason codes；
- 只构建题库 release/item 与阶段判定事实，不调用 LLM、不生成最终问题正文、不发布 candidate/snapshot、不修改工作台、不实现题库管理 UI；
- 以 `09` §7.7 的 import/license/stage/fixture 反例为硬门禁。SPEC PASS 前保持 `BLOCKED`。

#### DEV-007B｜结构化访谈导演与工作台集成

- 读取 DEV-006 current memory、可靠 actual asked、边界与 A 的 active bank/stage；
- 确定性后端按唯一 Context Schema 构造 frozen `InterviewDirectorContextV1`，把 DEV-006 current memory/actual asked、可信 final、current/recent displayed、阶段与 0..N 个安全题库参考交给一次逻辑生成；模型可采用、广泛改写或不用题库；transport/timeout 或第一次返回未过基础硬校验时最多一次完全同 Prompt/Context/Output Schema 的 retry，不引入修复提示或第二个模型；
- 通过唯一 Output Schema、引用 ID/subset、重复与硬安全等确定性检查，经现有 QuestionEvidence writer 追加 candidate/current/history；单问题和事实贴切性由 Prompt/评测/人工实践评价，所有输入源事实保持只读；
- 通过既有 `QuestionEvidenceWriter` 发布 candidate/current/history，接入既有 REST/无正文 WS 与工作台；不得直写 QuestionEvidence 表、复制 history 或改变 displayed != actual asked；
- 保留自动替换、手动下一问、历史浏览、硬撤下和失败降级契约；首版不选真实生产供应商、不引入第二个 planner agent、不把 fixture 当产品内容；
- 只有 SPEC 与 A 均 PASS 后才可启动；当前保持 `BLOCKED`。

正式题库仍是未来真实试用的重要内容资产，但不再是 B 工程链路的硬前置。A 的虚构 fixture 只证明参考 seam；真实问题质量、许可与试用仍需项目负责人题库和真实倾听实践验收。

数据库协调、上下文构建、权限/安全、事务、幂等与写回由普通后端 application service 负责，不算第二个智能体。第一版只有一个实时 Director，没有 planner/critic；同一逻辑生成最多包含 primary 与一次同输入技术 retry。记忆抽取、实时下一问和会后实际问题整理是三种独立 job/prompt/schema，可以复用同一 provider adapter，但不得让模型直接查询或修改数据库。

### 5.4 SPEC-AI-QUESTION-001 交接边界

- `QuestionEvidenceModule` 单一拥有 attempt/candidate/display snapshot/state/event 与 actual-question catalog。DEV-007 不得直写这些表，也不得复制 current/history repository；
- `InterviewCopilotModule`/DEV-007 只负责自动 trigger、manual-next 编排、版本化 comparator/similarity、调用 `MemoryContextReader` 与 `QuestionEvidenceReader`，并通过 `beginGenerationAttempt/publishAttemptResult/withdrawPresentation` 提交事实；
- 前端只调用 `05` §3.9 的 current/history/next/request-status REST 和消费无正文 `suggestion.presentation.changed`。历史锚点是客户端状态，不建立服务端 browse entity；
- DEV-006 继续拥有 shared schema/repository、actual-question reliable publication、eligibility/retention 与 deletion port；DEV-007 只读 current published actual asked，不把 displayed、manual requested、explicitly replaced、not observed 或 unjudged 写成 actual question；
- shared DTO/枚举以 `04/05` 为准。任何实现发现需要改 QuestionEvidence 所有权、三类 retention root、actual catalog 整版资格、硬撤下范围或 ADR-028 产品行为，必须先登记冲突并回到契约审查，不得在 DEV-007 代码中自行扩展；
- DEV-007 实现任务至少拆出服务端 orchestration/契约测试与工作台薄集成两个可审查提交；两者都不得选择真实模型供应商或宣称 CON-023 deletion runtime 已实现。


## 6. Agent 分工建议

### 总控 Agent

重点读取全部根目录规范和 `docs/agent/`。

负责：

- 维护 `00-项目说明与执行入口.md`；
- 维护 `docs/agent/00-task-board.md`；
- 拆分任务和检查依赖；
- 协调公共契约变更；
- 登记冲突和重要决策；
- 不代替项目负责人或独立审查 Agent 宣布通过。

### 前端 Agent

重点读取：

- `01-产品需求文档.md`；
- `02-项目开发规范.md`；
- `03-业务流程与交互规范.md`；
- `05-API与实时事件契约.md`；
- `06-实时音频与转录规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 项目页面；
- 访谈工作台；
- 麦克风和录音；
- WebSocket 客户端；
- 状态展示；
- 恢复交互。

### 后端 Agent

重点读取：

- `02-项目开发规范.md`；
- `03-业务流程与交互规范.md`；
- `04-数据模型规范.md`；
- `05-API与实时事件契约.md`；
- `06-实时音频与转录规范.md`；
- `07-AI访谈引擎规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 数据库；
- REST；
- WebSocket 服务；
- 对象存储；
- ASR 适配器；
- 权限；
- 导出和删除。

### AI Agent

重点读取：

- `01-产品需求文档.md`；
- `03-业务流程与交互规范.md`；
- `04-数据模型规范.md`；
- `05-API与实时事件契约.md`；
- `07-AI访谈引擎规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 记忆模型；
- 上下文构建；
- 提示词；
- JSON Schema；
- 评测集；
- 问题质量。

### 项目负责人或测试与审查 Agent

重点读取全部文档，尤其是 `03` 至 `09`、需求追踪矩阵和最新交接。

负责独立验证，不根据开发者自述直接通过，也不能通过修改验收目标替实现开绿灯。项目负责人通过 GitHub 审查时，必须把明确结论和对应 commit/PR 返回总控登记。

## 7. 公共契约变更

任何 Agent 需要修改以下内容时，必须先提出契约变更：

- 表结构；
- 状态枚举；
- REST 路径；
- WebSocket 事件；
- AI JSON Schema；
- 内容标记类型；
- 授权和删除规则；
- MVP 范围。

公共 response 新增 nullable 字段时，契约必须同时写明权威来源、缺键/null 的生命周期含义、分阶段兼容、允许读取的端点与角色、不得扩大的最小 DTO，以及 consumer 对 legacy/不一致事实的失败关闭测试。契约阶段不得通过修改业务 mapper 形成既成事实；实现任务只能在契约 exact-head PASS/merge 后落地 producer 与回归矩阵。

契约变更记录格式：

```text
变更编号：
提出人：
影响文档：
当前规则：
建议规则：
变更原因：
兼容性影响：
迁移方案：
测试影响：
批准状态：
```

未经批准，不得先在代码中形成既成事实。

## 8. 任务交接模板

```markdown
# 任务交接

## 基本信息
- 任务编号：
- 分支：
- 提交：
- 当前状态：

## 已完成
-

## 未完成
-

## 关键设计
-

## 修改文件
-

## 数据库或接口变更
-

## 测试结果
-

## 已知问题
-

## 风险
-

## 下一位 Agent 必须先读取
-

## 复现或运行方式
-
```

## 9. 完成定义

任务进入 `REVIEW` 前：

- 代码可运行；
- 类型检查通过；
- 相关测试通过；
- 没有真实密钥；
- 文档同步；
- 提交交接记录。

需要独立验收的任务进入 `VERIFY` 前：

- 项目负责人或独立审查角色已给出代码审查结论；
- 契约测试通过；
- 已知限制写明；
- 测试环境可复现。

任务进入 `DONE` 前：

- 已完成该任务风险等级要求的验证；
- 若任务需要独立验收，项目负责人或独立验收角色已经明确通过；
- 无阻塞缺陷；
- 文档与实现一致；
- `docs/agent/00-task-board.md` 和需求追踪矩阵已经更新。

## 10. 禁止事项

- 未读文档直接开发；
- 擅自扩大 MVP；
- 把后续规划当当前需求；
- 用 AI 生成内容覆盖原始数据；
- 绕过授权门禁；
- 前端保存供应商密钥；
- 后端直接依赖供应商原始对象；
- 修改公共契约不更新文档；
- 自己开发、自己宣布验收通过，或把 GitHub push/CI 成功冒充审查通过；
- 用“后续再补测试”把任务标记完成。

## 11. 每轮开发结束后的总控检查

1. 当前任务是否完成原目标；
2. 是否超出范围；
3. 是否引入新依赖；
4. 是否修改契约；
5. 是否影响其他 Agent；
6. 是否存在安全或数据风险；
7. 是否补充测试；
8. 是否需要更新后续任务状态；
9. 是否具备清晰交接；
10. 是否可以交给独立验收。

## 12. SPEC/DEV-ASR-PROVIDER-001 交接门禁

`SPEC-ASR-PROVIDER-001` 只冻结正式规范与 `docs/contracts/streaming-asr-provider-v2*` machine contract，保持 REVIEW 并等待项目负责人对 non-Draft PR exact head 手动审查；Agent、push、CI 或营销资料均不得自宣 PASS/DONE。

项目负责人 PASS 后才可启动 `DEV-ASR-PROVIDER-001`。DEV 必须在一个原子改动范围内把生产 `StreamingAsrAdapter` v1 迁至 v2 port，并更新 gateway/runtime/finalization/config/metrics/test；不得保留 v1/v2 并行生产 truth source。DEV 不得扩大到 Prisma migration、第二 provider/diarizer、gap/backfill、真实 LLM、真实长者数据或部署；若发现必须新增公共产品状态、改变数据库权威事实、改变校准体验或扩大数据授权，先暂停登记契约变更。

DEV 的完成包必须包含桌面 Chromium、目标 Android、同 PCM 真实 provider 三次 replay、主动断线 lane、exact-head CI、账单/配额核对与数据治理未决项；完整门禁见 `09` §15。

## 13. SPEC-ASR-WIRE-PARAM-001 窄修正门禁

`SPEC-ASR-WIRE-PARAM-001` 只修正腾讯 V2 话者分离 wire 参数及签名覆盖的过时供应商事实，以 Accepted ADR-033 部分取代 ADR-032 的对应事实前提；ADR-032、SPEC-ASR-PROVIDER-001 的既有任务、交接、审查和 PASS 历史不得改写。PR #29 exact head `650f856c918639a7b992294b805873d7052ab44e` 已获项目负责人手动 PASS 并合入 main；后续再改变 wire/signature 规则必须新开契约审查，不能借本次结论扩张。

DEV-ASR-PROVIDER-001 现可按 `09` §15 使用同一虚构 TTS PCM、`reconnect=0` 做恰好一次受控诊断连接；仍失败时停止无界试错并走腾讯支持。该诊断不证明 close 1005 根因，也不替代三次 replay、桌面/Android、主动断线、账单与数据治理的完整验收。

## 14. Repeat Interview 实施拆分与冲突边界

`SPEC-REPEAT-INTERVIEW-001` 只修改正式规范、shared machine DTO、任务/审查/交接治理；不实现页面、API runtime、Prisma migration、真实模型或 provider。其 ADR 在项目负责人 exact-head PASS 前保持 Proposed，任务保持 REVIEW，执行 Agent 不得 PASS/DONE/merge。

实现拆为：

- `DEV-008B1`：同 project next-session endpoint、幂等/并发/权限、普通 project 权威 action projection，以及 A4 接收后复用唯一 HomeShell 的 project 卡入口。不得改 post-analysis/Director，也不得改 A4 completion/review/unknown-response UI。
- `DEV-008B2`：canonical completed 后的 memory/actual-question 生产触发、内容无关状态/retry projection、calibration 与 basis-analysis 双前置协调、terminal 后 opening exact once、Context membership 与唯一 QuestionEvidence publication。必须覆盖 analysis-first/calibration-first/单 lane degraded/刷新重放并发；不得新建第二 AI/history 或记忆 UI。

B1/B2 的 SPEC 前置均已由项目负责人 PASS/merge。DEV-008A4/PR #44 已在 exact head `3824da7` 获项目负责人 PASS 并 merge `175e92e`；B1 又在 REV-050 accepted head `30975626` PASS 并 merge `6e546853`，治理收口后只在 fail-closed runtime 范围 DONE。B2 因此仅转为 implementation-ready 的 READY，后续涉及 Home/routes/styles 的实现仍以 accepted A4/B1 main 为基线且不得改写 A4 completion/review/unknown-response UI。B2 的 deterministic seam 可在既有 fake/unavailable provider 上实现和验收，但真实 LLM 结果门禁继续依赖真实 ASR/provider 和后续真实 LLM 选择；不得用 deterministic fake 宣称真实继承质量通过。

若实施发现必须改变数据库所有权、隐私/删除/retention 语义、已选网页方向或引入另一个 AI/历史，必须停止并新开契约/方向审查。普通 DTO、锁序、错误分类和测试细节在上述边界内由实现任务自行完成。界面身份一律读取 `AuthUser.display_name`，不得硬编码个人名称或修改公共 fixture。

## 15. Continuing Consent 实施门禁

`SPEC-CONTINUING-CONSENT-001` 只允许修改正式规范、additive shared DTO/常量和直接治理记录；不修改业务 runtime、Prisma/migration、页面、ASR/LLM、删除或部署。总控已在本次产品决定前恰好执行一次 iteration-coach Correction；执行任务不得再启动第二次，也不写 legacy learning log。

在本 SPEC non-Draft PR exact-head CI 全绿、项目负责人手动 PASS、merge 与治理收口前，DEV-008B1/B2 均保持 BLOCKED。SPEC 执行 Agent 只能提交 REVIEW 与人工审查包，不得自宣 PASS/DONE 或合并；ADR-039 保持 Proposed，CON-012 保持 DECIDED 门禁。

该门禁已由 PR #49 accepted exact head `1d241a4b8c40827a93eefe1c9825021b6859df74` / CI `31764584701`、项目负责人 PASS、merge `712b4ff46acbff5168453c79b2d02375a84fa017` 与 main CI `31764903272` 满足。B1 随后已在 fail-closed runtime 范围实现、PASS/merge 并治理收口为 DONE；B2 只因 B1 完成而转 implementation-ready 的 READY，当前未启动。两者都不能被模糊写成“真实持续授权 READY”：真实 covered、正式正文与真实试点仍以 BLOCKED 的 SPEC-CONSENT-TEXT-POLICY-001 正式接收为门禁。

B1 已在 REV-050 接收的受控范围同时接入 project `consent_continuation`、session `recording_start_reminder`、start request reminder version、next-session/start 动态重查、完整重授权路由、跨版本音频拒绝，以及 `09` §18 的刷新/幂等/设备/倾听员/跨 session 矩阵；生产 policy 缺失继续失败关闭。repeat action 仍必须逐值遵守 `04` §8.1 唯一优先级与 shared discriminated unions；非终态 session + reauthorization 只能 session conflict/null action。B2 实现不得只加前端文案或只改 DTO，也不得把“开始访谈”写成 consent；在 freeze/call/writeback/display 继续消费相同 current consent gate，不另建授权判断。

真实 `covered` happy path 与 B1 端到端产品交付另依赖 SPEC-CONSENT-TEXT-POLICY-001：由有权主体提供真实正式授权正文，项目负责人/数据治理接收正文版本/digest 与 machine scope。Agent 不得自行撰写、解释为法律意见或批准正文。该任务未接收前真实路径必须失败关闭；任何 local/test fixture 只能显式虚构、隔离使用，不能解锁真实路径或真实试点结论。

若实现证明需要新持久字段、兼容政策存储、数据库唯一约束或 migration，必须先暂停并提交明确 contract/ADR/迁移审查；不得在 B1/B2 中静默加列。ServiceTerm 继续 dormant。

## 16. LLM Provider 与 Prompt 发布门禁

1. `SPEC-LLM-PROVIDER-001` 只交付 docs/shared machine contract、v2 draft workspace 与 synthetic evaluation fixture；不添加 SDK/provider runtime 依赖，不选择第一家真实厂商，不索取密钥。
2. 本轮复用总控已完成的唯一 iteration-coach Correction，不启动第二次复核。Correction 要求的 Prompt lifecycle、SDK no-retry/fallback、真实 provenance、评测隔离、region/secret fail-closed 与 ASR 前置必须逐项进入交接。
3. 后续真实 runtime 必须另立 DEV 任务：先取得 `DEV-ASR-PROVIDER-001` 正式 PASS，再由项目负责人选择 provider/model/region/data policy；更新 contract/profile 与 exact dependency pins 后才能编码。未选择时 registry 保持空 active binding 和真实数据 default deny。
4. Prompt 作者只能编辑 `v2-draft/system.md` 与 `task.md`。冻结 candidate、生成 digest、运行固定 synthetic evaluation、接收 formal v2 与切换 loader 分属显式受审步骤；任何内容变化产生新 candidate，不覆盖 v1/formal artifact。
5. 若 Prompt 需要新 Context/Output 字段、枚举或约束，先提交正式 Schema contract 变更；不得把 Prompt/TypeScript interface 单方面升级为第二事实源。
6. 多模型横评输出只能进入隔离评测工件，禁止调用 QuestionEvidence/current/history writer。真实数据、shadow traffic、自动 winner/fallback、第二 critic、LiteLLM、Gateway 和在线 Prompt 管理面板均需另立方向/契约任务。
7. registry profile 或 active binding 变更必须同时通过 JSON Schema 与 `llm-provider-registry-semantics-v1` fixtures；实现 Agent 不得自行改成 first-match、忽略未激活坏配置或用 provider defaults 补缺。重复 identity 和任一 reference/membership 歧义都使整个 registry fail closed。
8. model-config 内容只能通过新增 immutable `llm-model-config-v1` manifest/version/digest 更新。receipt/持久化必须分离 requested/observed model、provider request/SDK response identity，并保留 sanitized warning/status；横评只有 equal-effective-config contract 通过才可按“相同配置”比较。
9. REV-051（accepted contract 中 branch-local 标记 REV-050）的 old `b7ae9a4` / CI `31769677989` / REQUEST_CHANGES P1=3 永久保留；accepted `77fb3a8` / CI `31783061076` 获内容 PASS，latest-main integration `a324e2b` / CI `31787175381` 又获窄 integration PASS（均 P0/P1/P2=0）。PR #52 merge/main `99ce83d` / main CI `31789810221` attempt 1 SUCCESS 后，SPEC 已治理收口为 DONE、ADR-040 Accepted；branch-local REV-050 alias 历史不改写。
10. 后续真实 runtime 由独立 `DEV-LLM-PROVIDER-001` 承接并保持 BLOCKED，直至 DEV-ASR-PROVIDER-001 正式 PASS、项目负责人选择 provider/model/region/data policy、数据治理接收 DPA/retention/training/跨境与重授权依据。本 SPEC 的 DONE 不允许安装 SDK、实现 runtime、索取密钥、加载 `v2-draft` 或宣称真实 provider 可用。

## 17. Staging Deployment 合同优先门禁

1. `SPEC-STAGING-DEPLOY-001` 只写 docs/contract/governance，不安装 Cloudflare 软件或插件、不请求 token/域名/Access 权限、不改 runtime/CI/Prisma/依赖、不执行公网部署。
2. 本轮恰好一次 iteration-coach 独立只读复核结果为 Correction；网络/数据双轴、Access/app 双身份、受信 proxy、WS 生命周期、Windows 无人值守、恢复演练、外部断电监控和逐层回滚必须保留。
3. `SPEC-STAGING-DEPLOY-001` 已在项目负责人对 non-Draft PR exact head + 完整 CI 明确 PASS 且 merge/main CI 成功后转为 `DONE`，ADR-041 已转为 `Accepted`；该结论只接收 docs/machine contract。`DEV-STAGING-DEPLOY-001` 继续 `BLOCKED`，须等待项目负责人明确实施授权、目标 Windows 主机、固定 hostname/Tunnel/Access 等非秘密资源标识、受控 secret 注入路径以及异机备份与外部监控目标；上述条件齐备仍只是启动实施的前置条件，不是已部署、已开放公网或已完成 trusted proxy/header/origin 防直连的证据。
4. DEV 只允许实现 `named-synthetic`，正式服务端 manifest 的唯一数据许可字段固定为 `data_mode=synthetic_only`。missing/unknown/invalid/其他值及平行许可字段全部失败关闭；真实来源即使去标识/脱敏或 provenance 不明，也必须在 connect/upload/persist 前零业务副作用拒绝。Quick 不能作为稳定入口、回滚或 Named 验收证据；Access 不能接成应用 SSO/自动账号映射。
5. DEV 交接必须包含部署 manifest、服务账户/监听/防火墙/Origin/proxy trust 清单、冷启动/断电/磁盘/备份恢复/监控/WS/回滚实测和所有失败历史；不得把 Dashboard 截图或 Tunnel healthy 单独当安全证据。
6. DEV-STAGING-DEPLOY-001 无权把字段改为 real/true 或新增真实模式。任何真实数据试点必须另立解锁任务，逐项引用身份、正式授权正文、服务器删除与备份传播、ASR/LLM 数据治理、QA 和目标环境 exact-head 结论，并形成独立数据治理决定、新版 machine contract 与项目负责人正式接收；未来真实来源脱敏数据也不得由本 SPEC/DEV 自动放行。

## 18. 公共网络身份与 staging 部署集成边界

`SEC-AUTH-PUBLIC-001` 负责应用身份域：密码、应用账号、匿名认证失败审计、不透明 session、Cookie/Origin/CSRF、会话撤销、默认直接对端限流、Access 不授予应用权限、错误最小化及自动反例。它可以把 `staging|production` 固定为外部 HTTPS Cookie profile，但不得创建公网入口、读取真实凭据或宣称公网可用。

`SPEC-STAGING-DEPLOY-001` 负责部署域：Cloudflare 入口形态、origin 直连阻断、TLS 终止/回源、Access policy、可信代理集合、唯一客户端 IP header、hop 解析、WebSocket/上传代理和运行时 secret 注入。SPEC 的 merge 只接收部署契约；在 `DEV-STAGING-DEPLOY-001` 实现并通过受信入口、origin 防绕过、proxy/header/hop 正反例前，应用默认 resolver 必须继续只认直接对端并忽略所有转发 header。

两任务集成时：

- Access 即使在部署层验证通过，也只表示请求获准到达应用；应用 principal、角色、assignment、审计 actor 和 session 撤销仍以应用数据库为准；
- SEC 只提供窄 `ClientIpResolver` 接口、直接对端实现和伪造 header 反例，不选择 Cloudflare header、CIDR、代理层数或直连策略；
- DEPLOY 不能通过信任边缘 header 自动登录应用，也不能降低 Cookie、Origin、CSRF 或会话门禁；
- 若部署实现最终选择跨源、允许绕过 Cloudflare 直连 origin，或用 Access identity 取代应用账号，必须重开 ADR-009/042 及身份/权限/CSRF/审计契约，SEC runtime 不得自行兼容；
- 发生治理文件冲突时从最新 `origin/main` 人工合并，保留双方任务状态、旧失败、exact head、CI 和审查事实，不用整文件覆盖。

## 19. Memory Maintainer Contract-first 门禁

`MEMORY-T2-T4-CONTRACT-001` 只冻结 T2/Foundation、T4/P1 与 T18-T19/P6 的正式数据职责、Context/Output Schema、Hybrid Trigger、事务与恢复语义；不修改 Prisma、migration 或 runtime producer。其 non-Draft PR exact-head 必须经独立审查 PASS 并 merge，后续 `MEMORY-T2-T4-RUNTIME-001` 才可实现 migration、repository、scanner 和 finalized-batch orchestration。

runtime 不得新增第二套 Working value truth source，不得继续以 post-session claim Schema 或 canonical prefix 猜 Thread 关系，不得依赖进程内 notification 作为持久 authority。每个大块只在最终可审查 head 运行一次完整 CI；中间用定向本地测试。独立 reviewer 绑定 exact head 给出结论后，执行 Agent 才可按授权 strict merge-only，并立即进入下一 T/P 映射任务。

## 20. Memory Maintainer V1.1 Correction 与 runtime cutover 门禁

1. PR #66 的 v1 accepted head/CI/PASS/merge/main CI 是永久历史；Correction 不撤销该审查结论，也不原地修改已接收的 v1 Schema/fixtures。v1 标记 `accepted-history/pre-runtime-superseded`，不得被 runtime 加载。
2. `MEMORY-T2-T4-CONTRACT-002` 只交付 v1.1 docs/machine schemas/fixtures/pure validators/tests，状态保持 REVIEW。已有唯一 iteration-coach/独立审计结论为 Correction，本任务不得再启动第二次。
3. runtime 开工须等待本任务 non-Draft PR exact-head 独立 PASS 且 merge；后续任务只能实现 v1.1，不得回退 v1 或把本 contract Agent 的本地测试当项目负责人 PASS。
4. 后续 migration 必须保持 non-maintainer trigger dedupe 原语义，只为 `working_memory_maintain` 使用 `pending|running|succeeded` partial unique slot，并保存 failed retry predecessor/attempt；不得通过删除失败 row、改 identity 或复活 job 规避。
5. consumption 必须由 transcript lifecycle 持有。AI cleanup 只 detach nullable snapshot/job-input pointer；任何 implementation、retention job 或 recovery scanner 都不得删除 consumption 后重跑已消费 segment。
6. P1 cutover 是单 producer release gate：同一 release 在启用 scanner/final flush 前禁用旧 post-session `MemoryService.extract/memory_extract` 写入。post-session lane 只能委托同一 P1 final flush 或投影同一 P1 terminal outcome；历史 rows 保留，actual-question lane 不变。
7. contract task 形成一个大块后先完成定向本地测试/format/typecheck/lint/diff，再提交并只触发一次 exact-head CI；CI 后如 head 改变，旧 CI 只能保留为历史，必须由总控/项目负责人决定是否需要新的 exact-head 运行。执行 Agent不得 merge。
