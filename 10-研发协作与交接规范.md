# 10｜研发协作与交接规范

## 1. 目标

本文件用于：

- 拆分研发任务；
- 明确依赖；
- 记录状态；
- 约束多 Agent 并行开发；
- 统一交接和验收。

`00` 只保留当前阶段和入口；实际任务状态维护在 `docs/agent/00-task-board.md`，实际交接记录维护在 `docs/agent/05-handoff-log.md`。本文件只规定协作规则和模板。

### 1.1 探索期任务解锁原则

- 先围绕一个有限纵向里程碑拆分任务，避免同时铺开全部 Epic；
- 依赖按“当前任务实际需要的最小稳定 seam”判断，不要求父任务所有生产门禁都已关闭；
- 最小 seam 必须有正式依据、明确输入输出、责任人、可回退边界和集成验证，不得用口头约定并行；
- 只影响未来生产部署、且不影响本地内部验证或核心数据安全的问题，登记为后续阻塞并继续不受影响的任务；
- 并行任务不得同时修改同一迁移、公共契约或共享核心模块；需要交叉时先串行固定契约，再合并；
- 任务 `DONE`、内部原型可集成、真实试点可发布是三个不同结论，均须按各自证据报告。

## 2. 任务状态

- `TODO`：未开始；
- `READY`：依赖满足，可以执行；
- `IN_PROGRESS`：正在执行；
- `BLOCKED`：存在依赖或问题；
- `REVIEW`：等待代码审查；
- `VERIFY`：等待任务要求的独立验收；
- `DONE`：通过适用验证；
- `CANCELLED`：取消。

文档整理、格式修复、简单配置和局部低风险修改可以由总控 Agent 自检后进入 `DONE`，但必须记录验证证据。涉及核心架构、关键业务规则、权限、安全、状态机、核心数据模型、跨模块契约、大规模合并或 MVP 发布时，只有独立审查或验收通过后才能进入 `DONE`。

## 3. 任务字段

每个正式开发任务至少记录：

```text
任务编号
任务名称
目标
范围
明确不做
依赖
负责人或 Agent
输入文档
输出
验收标准
状态
分支
提交
风险
交接说明
```

## 4. Epic 划分

### Epic A：工程基础

任务：

- 前端初始化；
- 后端初始化；
- pnpm workspace；
- PostgreSQL；
- 数据库迁移；
- 登录；
- 角色权限；
- Docker Compose；
- Nginx；
- 错误码；
- 结构化日志；
- 环境配置；
- CI 基础检查。

### Epic B：项目、服务和授权

任务：

- 长者项目；
- 项目列表和详情；
- 项目分配；
- 服务条款版本；
- 累计时长；
- 超时提醒；
- 授权文本版本；
- 授权录音；
- 授权状态；
- 撤回；
- 启动门禁。

### Epic C：音频和实时转录

任务：

- 麦克风检测；
- AudioWorklet；
- MediaRecorder；
- 音频分片；
- 本地缓冲；
- 分片重试；
- 业务 WebSocket；
- ASR 适配器；
- 中间态和确定态；
- 说话人校准；
- 说话人映射；
- ASR 重连；
- manifest；
- 补转录。

### Epic D：访谈工作台

任务：

- 三栏布局；
- 链路状态；
- 实时转录；
- 自动滚动；
- 计时；
- 服务剩余提醒；
- 文本修正；
- 说话人修正；
- 内容标记；
- 安全结束；
- 中断恢复。

### Epic E：AI 长期记忆

任务：

- `memory_item`；
- 记忆类型和状态；
- 证据关联；
- 冲突组；
- 人工确认；
- 访谈记录员；
- 结构化输出；
- 上下文构建器；
- 受限内容过滤；
- 重新分析任务。

### Epic F：AI 追问

任务：

- 采访导演；
- 问题 Schema；
- 自动触发；
- 手动触发；
- 节流；
- 首选和备选问题；
- 推荐原因；
- 证据片段；
- 继续倾听；
- 建议历史；
- 反馈；
- 基础问题回退。

### Epic G：回顾、导出和删除

任务：

- 录音播放器；
- 转录回顾；
- 会话工作记录；
- 项目导出；
- 受限内容隔离；
- 短期下载；
- 撤回限制；
- 删除申请；
- 物理清理；
- 审计。

### Epic H：测试和试点

任务：

- 测试身份；
- 虚构项目数据；
- 预录双人音频；
- ASR stub；
- LLM stub；
- AI 评测集；
- 故障注入；
- 性能测试；
- 权限测试；
- 真实授权访谈；
- 质量报告。

## 5. 依赖关系

```text
A 工程基础
├─ B 项目、服务和授权
├─ C 音频和实时转录
│   ├─ D 访谈工作台
│   └─ E AI 长期记忆
│       └─ F AI 追问
├─ G 回顾、导出和删除（按数据生产者逐步解锁）
└─ H 测试和试点贯穿各阶段
```

更准确的任务依赖：

- B 依赖 A；
- C 依赖 A；
- D 依赖 B、C；
- E 的后端能力依赖 C 的确定态转录、证据 ID、说话人映射和已批准的边界过滤契约，不硬依赖 D 的完整工作台 UI；
- F 依赖 E；
- G 若保持一个完整任务，依赖 B、C、D、E、F；只有正式拆分后，不含回顾 UI 的导出或删除子任务才可按其实际数据生产者单独解锁；
- H 从 A 开始准备，最终依赖全部功能。

对应当前任务编号：

- `DEV-006` 后端最低依赖 `DEV-004` 和已解决的 AI 边界契约；与 `DEV-005` 的展示集成另行验收；
- `DEV-008` 若不拆分，前置为 `DEV-002`、`DEV-003`、`DEV-004`、`DEV-005`、`DEV-006`、`DEV-007`；
- 后续优先把 `DEV-008` 拆为回顾、导出、删除三个可独立验收的任务，避免不必要串行化。


## 6. Agent 分工建议

### 总控 Agent

重点读取全部根目录规范和 `docs/agent/`。

负责：

- 维护 `00-项目说明与执行入口.md`；
- 维护 `docs/agent/00-task-board.md`；
- 拆分任务和检查依赖；
- 协调公共契约变更；
- 登记冲突和重要决策；
- 不代替独立审查 Agent 宣布通过。

### 前端 Agent

重点读取：

- `01-产品需求文档.md`；
- `02-项目开发规范.md`；
- `03-业务流程与交互规范.md`；
- `05-API与实时事件契约.md`；
- `06-实时音频与转录规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 项目页面；
- 访谈工作台；
- 麦克风和录音；
- WebSocket 客户端；
- 状态展示；
- 恢复交互。

### 后端 Agent

重点读取：

- `02-项目开发规范.md`；
- `03-业务流程与交互规范.md`；
- `04-数据模型规范.md`；
- `05-API与实时事件契约.md`；
- `06-实时音频与转录规范.md`；
- `07-AI访谈引擎规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 数据库；
- REST；
- WebSocket 服务；
- 对象存储；
- ASR 适配器；
- 权限；
- 导出和删除。

### AI Agent

重点读取：

- `01-产品需求文档.md`；
- `03-业务流程与交互规范.md`；
- `04-数据模型规范.md`；
- `05-API与实时事件契约.md`；
- `07-AI访谈引擎规范.md`；
- `08-安全隐私与数据治理.md`；
- `09-测试与验收规范.md`。

负责：

- 记忆模型；
- 上下文构建；
- 提示词；
- JSON Schema；
- 评测集；
- 问题质量。

### 测试与审查 Agent

重点读取全部文档，尤其是 `03` 至 `09`、需求追踪矩阵和最新交接。

负责独立验证，不根据开发者自述直接通过，也不能通过修改验收目标替实现开绿灯。

## 7. 公共契约变更

任何 Agent 需要修改以下内容时，必须先提出契约变更：

- 表结构；
- 状态枚举；
- REST 路径；
- WebSocket 事件；
- AI JSON Schema；
- 内容标记类型；
- 授权和删除规则；
- MVP 范围。

契约变更记录格式：

```text
变更编号：
提出人：
影响文档：
当前规则：
建议规则：
变更原因：
兼容性影响：
迁移方案：
测试影响：
批准状态：
```

未经批准，不得先在代码中形成既成事实。

## 8. 任务交接模板

```markdown
# 任务交接

## 基本信息
- 任务编号：
- 分支：
- 提交：
- 当前状态：

## 已完成
-

## 未完成
-

## 关键设计
-

## 修改文件
-

## 数据库或接口变更
-

## 测试结果
-

## 已知问题
-

## 风险
-

## 下一位 Agent 必须先读取
-

## 复现或运行方式
-
```

## 9. 完成定义

任务进入 `REVIEW` 前：

- 代码可运行；
- 类型检查通过；
- 相关测试通过；
- 没有真实密钥；
- 文档同步；
- 提交交接记录。

需要独立验收的任务进入 `VERIFY` 前：

- 代码审查通过；
- 契约测试通过；
- 已知限制写明；
- 测试环境可复现。

任务进入 `DONE` 前：

- 已完成该任务风险等级要求的验证；
- 若任务需要独立验收，独立验收已经通过；
- 无阻塞缺陷；
- 文档与实现一致；
- `docs/agent/00-task-board.md` 和需求追踪矩阵已经更新。

## 10. 禁止事项

- 未读文档直接开发；
- 擅自扩大 MVP；
- 把后续规划当当前需求；
- 用 AI 生成内容覆盖原始数据；
- 绕过授权门禁；
- 前端保存供应商密钥；
- 后端直接依赖供应商原始对象；
- 修改公共契约不更新文档；
- 自己开发、自己宣布验收通过；
- 用“后续再补测试”把任务标记完成。

## 11. 每轮开发结束后的总控检查

1. 当前任务是否完成原目标；
2. 是否超出范围；
3. 是否引入新依赖；
4. 是否修改契约；
5. 是否影响其他 Agent；
6. 是否存在安全或数据风险；
7. 是否补充测试；
8. 是否需要更新后续任务状态；
9. 是否具备清晰交接；
10. 是否可以交给独立验收。
