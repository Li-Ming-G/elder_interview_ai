# 09｜测试与验收规范

## 1. 目标

本文件定义 MVP 的测试范围、验收指标和发布门禁。

开发人员完成代码不代表任务完成。任务必须通过与风险相称的对应验证后才能关闭。

验收责任按风险分级：

- 文档整理、格式修复、简单配置和局部低风险修改，在不改变产品范围、业务规则或公共契约时，可由总控 Agent 自检关闭，但必须记录实际检查和证据；
- 涉及核心架构、关键业务规则、权限、安全与隐私、状态机、核心数据模型、跨模块契约或大规模合并时，必须由项目负责人或独立审查/验收 Agent 给出结论；项目负责人使用 GitHub 审查时，结论必须绑定仓库、分支、commit 和 PR/审查链接；
- MVP 进入真实试点前始终必须经过项目负责人或独立角色验收，并满足第 13 节发布门禁；
- 需要独立审查时，实现者不得代替项目负责人或审查者宣布通过；push、CI 成功和进入 `REVIEW` 都不等于验收通过。

### 1.1 探索期分层门禁

内部核心原型与真实试点使用不同门禁，不能互相冒充：

- 内部原型允许使用虚构/脱敏数据、stub 和明确标记的测试夹具，只在本地或受控内部环境运行；必须可安装、构建、迁移、运行，范围可回退，已知缺口写入任务与交接；
- 内部原型不得包含真实密钥、真实个人信息或真实访谈资料，不得对公网开放；原始录音/转录/授权不可覆盖，录音分片必须具备序号、幂等、校验、manifest 与失败可见性，ASR/AI 失败不得中断或丢失原始录音，AI 派生结论必须回链确定态转录；
- 父任务或相邻模块尚未最终 `DONE` 时，只要后续任务依赖的最小接口已固定、边界清晰且可回退，可启动内部原型子集；未完成项继续保留原状态，不得为解锁下游而伪造 `DONE`；
- 真实身份、真实录音、真实个人信息、对公网开放或付费客户一旦进入范围，私有存储、访问隔离、有效授权、恢复能力、日志脱敏及相应独立审查立即成为开工或发布门禁；
- 第 13 节完整发布门禁只用于真实试点。仅影响未来生产部署的问题登记为后续阻塞项，不阻塞不受影响的本地内部验证。

## 2. 测试层级

- 单元测试；
- 模块集成测试；
- API 契约测试；
- WebSocket 契约测试；
- 端到端测试；
- 长时间录音测试；
- 故障注入测试；
- 安全和权限测试；
- AI 离线评测；
- 真实访谈试点。

工具基线：

- Vitest：单元测试、前端组件测试和 API 测试运行器；
- `@nestjs/testing` + Supertest + PostgreSQL：API 模块集成测试；
- React Testing Library：前端组件交互；
- Playwright：真实浏览器 E2E；DEV-001 先以 Chromium 建立烟雾门禁。

CI 和本地必须调用相同的根脚本。集成测试不得用 SQLite 代替 PostgreSQL。

## 3. 测试数据原则

1. 自动测试使用虚构或脱敏数据。
2. 不把真实访谈数据提交到仓库。
3. 测试录音使用明确授权或人工制作音频。
4. 固定测试数据可重复加载和重置。
5. 测试中的供应商调用应支持 stub。
6. 真实供应商集成测试单独标记。
7. 测试身份至少包含：
   - 倾听员 A；
   - 倾听员 B；
   - 管理员；
   - 数据管理员；
   - 无权限用户。

## 4. 单元测试

必须覆盖：

- 项目状态机；
- 会话状态机；
- 授权门禁；
- 服务时长计算；
- 超时提醒；
- 音频分片幂等；
- WebSocket 事件去重；
- JSON Schema 校验；
- 上下文构建；
- 记忆冲突处理；
- 人工确认优先级；
- speaker stream 作用域、角色 authority 与 trusted effective role；
- 校准控制片段排除；
- 单段/批量角色修正版本冲突与全成全败；
- 重复问题判断；
- 题库 CSV 校验、原子版本激活、许可与 fixture 环境门禁；
- 访谈旅程阶段的确定性、可进可退和 reason code；
- 题库 seen membership、declared attribution、事实 grounding 与运行时资格四类 provenance；
- 权限控制；
- 导出过滤；
- 删除流程幂等。

## 5. 集成测试

### 5.1 主链路

```text
浏览器采集
→ 音频分片上传
→ 业务 WebSocket
→ ASR
→ 确定态转录落库
→ AI 记录员
→ 结构化记忆
→ AI 采访导演
→ 问题展示
→ 会话结束
→ 倾听员工作区中的已结束访谈回顾
```

### 5.2 接口契约

验证：

- REST 路径和状态码；
- DTO 校验；
- 公共错误格式；
- WebSocket 事件字段；
- Schema 版本；
- 幂等请求；
- 枚举一致性；
- 权限失败；
- 状态前置失败。

## 6. 音频和 ASR 测试

场景：

1. 连续录音 60 分钟；
2. 录音 180 分钟边界测试；
3. 麦克风权限拒绝；
4. 无输入设备；
5. ASR 中途断开；
6. 网络中断后恢复；
7. 分片上传失败和重试；
8. 同一分片重复上传；
9. 浏览器刷新；
10. 浏览器崩溃后恢复；
11. 结束时最后确定态；
12. 说话人校准；
13. 单段修正；
14. 批量说话人修正；
15. 补转录（后置 ASR 可靠性任务与完整 MVP/真实试点验收，不属于当前内部可用 MVP 的 DEV-004 关闭门槛）。
16. start 后同一正式 speaker stream 校准；
17. 校准失败、跳过与重试；
18. provider/runtime 重建后的新 speaker stream 重新确认；
19. 校准控制句消费排除；
20. 角色修正后的 revision、受影响 membership 与下游失效 seam。

验收：

- ASR 失败不影响原始录音；
- 正常链路无分片缺失；
- 分片重复上传不重复保存；
- 中间态不进入正式 AI 链路；
- 会话结束能生成完整 manifest；
- 缺失范围能够检测并显示。

当前内部可用 MVP 只要求 ASR 故障不影响原始录音、manifest、安全结束，并把转录明确标为 `degraded|not_started`；不要求故障区间持久化或离线补转录。后两项由 `HARDEN-ASR-001` 在真实供应商/可靠性阶段验收，不阻塞 DEV-006/007 的虚构数据核心价值验证。

### 6.1 说话人校准与修正矩阵

| 场景 | 必须验证 |
|---|---|
| 正常校准 | 原子 start 和原始录音先成功；同一正式 speaker stream 观察两个 label；用户确认后映射为 elder/interviewer，authority 为 user_confirmed |
| begin 前 delayed final | 普通 PCM 已处理、随后 begin，再收到前段 delayed final；依据冻结 timeline 完全落在 start 前，保持 conversation，不进入 attempt membership |
| resolve 后 delayed final | 校准 PCM 已处理、resolve 冻结终点并确认后才收到 final；依据不可变区间仍写 calibration membership/content kind，不因 attempt 已 confirmed 漏标 |
| 跨边界 final | final 与校准半开区间存在非零重叠时整段按 calibration 排除；不得把控制句片段拆漏进普通 AI |
| provider 自动候选 | 值即使为 elder/interviewer，未确认前 `trusted_effective_role=unknown`，不得进入角色相关 AI 消费 |
| 失败/跳过 | 原始录音、archive、上传与 final 转录继续；页面持续待确认；不创建可信映射，可重试 |
| 新 speaker stream | 同一 session/generation 内 provider/runtime 重建，或恢复后创建新 namespace；同名 `speaker_1` 不继承旧映射，重新进入待确认 |
| 短时 transport replay | event stream 变化但 provider namespace 未变时不误建新 speaker stream；`event_stream_id` 不参与 mapping |
| 控制句 | 服务端 attempt 范围写 `speaker_calibration`；客户端不能任意标记；保留原始证据但 memory/asked/suggestion/摘要输入均无该内容 |
| 单段修正 | 只改目标 final 的 corrected role；original role/authority/text/content kind 不变；版本冲突零副作用 |
| 批量预览 | 同一 speaker stream、provider label、明确范围；返回稳定 preview/count；默认排除已有单段人工修正 |
| 批量执行 | preview membership 或任一版本漂移时整批冲突；成功时全部更新、单一 revision、完整 membership 与审计 |
| 下游门禁 | trusted elder 只进入长者事实；trusted interviewer 只进入已问问题/必要上下文；unknown 不进入角色相关 memory/asked/suggestion，不足时继续倾听 |
| 修正后失效 | membership 命中的旧派生结果立即失去未来生成/current memory/跨会话资格；已经展示的普通问题快照可以保留且不自动重算；下一次换题/正常生成必须读取新 revision；重算失败不恢复旧 eligibility |
| 已展示问题硬撤下 | `restricted`、`do_not_ask`、活动 deletion scope、授权或访问权限失效时，当前问题正文立即隐藏，GET/WS replay/刷新不带回；只显示继续倾听或 AI 暂不可用，不自动生成替代问题 |
| Android 小屏 | 明确“正在录音 · 正在确认说话人”、成功/待确认/重试；触控目标 ≥44×44px；失败不误报录音失败 |
| canonical snapshot | GET、begin、resolve、session.ready 与 WS updated 使用相同 DTO；同 request ID 重放首次 snapshot，GET 返回当前 snapshot，WS replay 返回原事件 snapshot |

自动测试使用确定性双 label fake，只证明供应商中立状态、隔离、幂等、门禁和失效语义；没有真实供应商时不得宣称真实说话人分离准确率、阈值、声纹或多人 diarization 已通过。

## 7. AI 评测

### 7.1 评测集

不少于 30 段脱敏或虚构中文访谈。

覆盖：

- 没什么可讲；
- 冷场；
- 重复；
- 时间模糊；
- 人物混乱；
- 转折；
- 冲突；
- 明确拒绝；
- 敏感话题；
- 口音和方言；
- ASR 错字；
- 事实冲突；
- 连续讲述不应打断；
- 已问过类似问题。

### 7.2 人工标注

每段标注：

- 可接受追问；
- 不可接受追问；
- 应继续倾听；
- 风险等级；
- 应提取人物和事件；
- 应保留原话；
- 应识别边界；
- 允许的时间线结论。

### 7.3 指标

| 指标 | 首轮目标 |
|---|---:|
| 有用或修改后可用问题 | ≥ 60% |
| 重复问题率 | < 10% |
| 高风险不当追问率 | < 5% |
| JSON 解析成功率 | ≥ 98% |
| 证据回链正确率 | ≥ 95% |
| 明确拒绝边界识别率 | ≥ 90% |
| AI 目标返回时间 | ≤ 8 秒 |

这些数值在 DISC-006 和首个 DEV-006 内部记忆试用中只作为观察项，不作为开工或通过阈值。首轮使用虚构/脱敏的两次访谈场景、完整过程记录和人工定性复盘验证方向；接入真实模型、真实数据或进入真实试点前，再将适用质量与安全指标恢复为门禁。

### 7.4 DISC-006 内部记忆与实际问题整理矩阵

至少覆盖：

1. 自动后台记忆不显示候选/置信度列表，但能改善当前下一问；
2. 人物/关系/地点/事件/时间/选择/未讲完故事均有 segment provenance，unknown 与校准控制内容不进入；
3. 冲突不覆盖，问题改为自然澄清；明确更正后旧值只保留历史，未来生成与第二次访谈使用新值；仍不确定时使用范围或开放问法；
4. 普通说话人/文字修正后当前已展示问题继续可见，且没有自动 AI 调用；用户下一次换题恰好一次请求并使用新 revision；
5. `restricted|do_not_ask|deletion_request` 或权限失效后当前正文立即撤下，不发生自动替代请求，刷新/replay 不恢复；
6. 会后可信 interviewer final 中提取系统推荐与倾听员自发的实际问题；展示后实际问出、明确换掉、未观察到问出、证据不足/未判断分类可追溯；只有实际问过进入第二次防重复；
7. ASR degraded/not_started、角色 unknown 或会后分析失败时不作否定结论、不更新可靠目录；
8. AI 超时/解析失败显示不可用，不返回基础题、不后台自动重试；每次点击“下一个问题”只有一次幂等 attempt；
9. 两次访谈直接给一个基于当前有效记忆和实际问过问题的开场问题，不展示记忆回顾列表；
10. 过程记录可还原原始片段、当时记忆、问题、实际问法、更正、继承和版本变化；技术日志不含完整转录、完整模型输入或敏感正文。
11. 同一 memory-extract job 生成五条 claim 时恰有五条 derived output；每条 claim 独立失效，未命中的四条保持资格；业务 root/type/project/job 错配在事务提交时被拒绝。
12. actual-question analysis 只绑定一条 catalog derived output；任一 segment、display snapshot、replace/withdraw event 或既有 actual-question dependency 缺失/变更时整版 catalog 撤下，不出现部分发布。
13. `ai_job`、`question_display_snapshot`、`memory_retention_root` 三类 root 的到期先隐藏、projection detach、child 清理、失败重试和最小审计可完整复现；child 不持有独立 retention deadline。

### 7.5 SPEC-DEV-006 实现验收矩阵

DEV-006 后续实现至少以虚构/脱敏的两次访谈 fixture 覆盖下表。首个内部试用不设质量百分比门槛，但安全不变量、过程记录完整性、数据迁移和并发测试是硬门禁。

| 维度 | 必须证明 |
|---|---|
| 跨 session 水位 | 项目 job 对每个评估 session 写 scope，包括 0 eligible segment；实际 membership 与 scope 分开，不能用 trigger session 或单 revision 代替 |
| 输入 provenance | 每个 segment 保存 text/role revision、trusted role/authority、content kind、digest 和 input order；每个 memory 保存 resolution revision |
| derived cardinality | 每个独立 claim/resolution/candidate/boundary candidate/generated note/context snapshot 一条 derived output；5 claims=5 rows；跨表一对一、type/project/job/business ID 错配均被数据库约束拒绝 |
| dependency completeness | segment/memory/question 的 expected count 和 canonical manifest 与实际行完全一致；CASCADE 缺行、跨 root FK 置空、重复/乱序或只剩子集都 future ineligible，不产生 vacuous pass |
| current memory | trusted elder conversation final 可自动 current；unknown/calibration 不进入；conflict 不覆盖；明确更正只切换未来 current，旧证据保留 |
| 权威 eligibility | correction 提交后即使 materialized invalidation 尚未刷新，version/operation anti-join 也排除旧输出；重算失败不恢复 |
| displayed snapshot | 普通修正保持当时正文可见、不自动重算，但 future ineligible；新换题恰好一个新 attempt |
| hard withdrawal | restriction、do-not-ask、活动 deletion scope、授权/assignment 失效立即隐藏正文；GET、恢复和未来 WS replay 均不回流且不自动替代 |
| actual questions | 从全部 trusted interviewer final 提取系统匹配和自发问题；只有 actual question 进入防重复；outcome 不冒充 actual question；analysis 以整版 catalog 一条资格记录发布，任一 dependency 命中即整版撤下 |
| unjudged | degraded/not-started、unknown、证据不足、Schema/过滤失败不产生 not-observed，不覆盖已有可靠目录 |
| 二阶段并发 | freeze 后 correction/deletion/授权变化、调用中变化、写回竞争分别导致取消/丢弃；供应商调用期间不持 DB 锁 |
| 幂等/重试 | 同 request 重放首次结果；同 key 不同绑定冲突；自动 trigger 去重；一次换题一个 attempt；question generation 的 transport/timeout 或第一次返回未过基础硬校验时最多一次 `same_input_retry`，两次 Prompt/Context/Output Schema/model config/version/digest/input hash 逐值相同且第二次不含前次输出或错误；权限/安全/deletion/重复/writeback 漂移不 retry；显式新动作使用新 ID 和 retry link |
| 删除传播 | project/session/range 精确命中；任一 job input 命中时整个 ai_job root 隐藏/清理，correction 仍按业务输出最小失效；candidate job 与独立 display snapshot root 分开判定；FK 不阻塞清理；只保留不可逆 scope 摘要和最小审计 |
| retention roots | ai_job、display snapshot、非 AI memory root 各自计算最早 expires_at；children 继承且无独立 deadline；actual analysis 随 job root，display 事实不随 candidate 普通失效删除 |
| 到期/重试 | 到期先 hidden 并 detach current/published/display，再按顺序 purge；CASCADE 与显式 cleanup 可重复执行；中断后同 cleanup identity 续跑，cleanup_failed 始终不可见且不恢复 eligibility |
| 迁移 | 空库与旧数据升级均通过；旧缺 association/dependency provenance 数据默认为 review-required/unjudged/future-ineligible，不推断 display/actual/trust；无法证明期限的 legacy root 以 migration time 到期并 hidden 排队清理，不伪造保留窗口；原始证据不变 |
| 最小化/保留 | 普通 UI 无 memory 管理；技术日志无完整转录/输入/输出；权限、过期与删除均失败关闭且可审计 |
| 失败降级 | 超时/过滤/Schema 失败显示不可用或继续倾听，不返回基础题、不无限重试、不影响录音/ASR |

CON-023 在 deletion producer、统一 reader、C2 接入和上述删除并发测试前必须报告 `NOT IMPLEMENTED / NOT VERIFIED`；no-op guard 不得让测试通过。DEV-006 的完成报告必须列出每个不变量对应的 unit/integration/auth/migration 测试名和过程记录 fixture。

### 7.6 SPEC-AI-QUESTION-001 契约验收矩阵

DEV-007 必须以 deterministic fake + PostgreSQL/API/WS/component/browser 测试逐项证明；不得用页面本地状态、固定延时或只比较字符串代替服务端契约。

| 维度 | 必须证明 |
|---|---|
| 初次 publication | 有 eligible suggestion 时 attempt/candidate/derived/snapshot/display state/event/幂等响应同一事务；`display_sequence=1`、presentation revision 单调；无合格问题时原子发布 continue 且不造伪 snapshot；任一步失败无半提交 |
| 自动更优 | `question-select-v1` 仅使用冻结 grounding、水位、stage/purpose/risk，按 `07` §9 固定公式分别重算 candidate/current；同阶段新候选 grounding 最近回答、旧 current 只 grounding 更早回答的 fixture 分差 `>=0.12` 并替换；同输入重复计算同分；公共 DTO/Director Output 无 confidence/内部 score |
| 临界防抖 | 分差 0.119、不满 dwell、debounce 内 current/policy 漂移均不发布；同分 tie-break 确定且不绕过最小分差 |
| 相同证据重放 | 相同 trigger/dedupe 只一 attempt；不得重复 snapshot、sequence、event 或 current 跳动 |
| 连续自动竞态 | 两个自动结果乱序完成；只有 basis revision 仍 current 的一个可提交，旧结果 `stale_basis`，无 last-writer-wins |
| 自动中性状态 | 已有 visible suggestion 时，automatic 的 continue/unavailable 不覆盖 current；无 current 时无合格问题可发布 continue；AI unavailable 不产生自动 replacement |
| 手动优先 | manual intent fence 先提交时，更早在途 auto 只能 `superseded_by_manual`；auto 先发布使 revision 改变时，旧 expected revision 的 manual 不创建 attempt并返回 `SUGGESTION_CURRENT_CHANGED` |
| 手动幂等 | 首次请求前持久 request ID；响应丢失以相同 ID 重放得到同 attempt/首次响应；同 key 异 payload、跨 actor/session 冲突；双击只一次业务 attempt |
| 手动单飞/节流 | session 同时一条 manual；新 ID 并发 409 且不创建第二 job；3 秒间隔、60 秒 6 次窗口和 `Retry-After` 可重复；节流 key 重放结果稳定 |
| 手动结果 | 生成期间保留 current；新 suggestion/continue 才写 `manual_next_committed`；失败/超时写 failed/unavailable，不返回基础题、不后台重试，且 requested 事件不冒充 explicitly replaced |
| 排除顺序 | hard policy → current → recent 20 displayed → reliable actual asked → memory/boundary → attempt 去重依次执行；unjudged/not-observed/explicitly-replaced 不进入跨会话已问集合 |
| 相似度 fixture | exact digest、中文同义改写 `>=0.88` 被排除；否定、人物/时间槽改变不误杀；短问句有专门样例；matcher/threshold 版本写入过程记录 |
| 历史稳定性 | `(display_sequence,id)` 总序、同毫秒多 snapshot、并发插入、before/after cursor 均无重复/遗漏；默认 20、最大 50；非法/过期/跨 session cursor 422 而非回首页 |
| 刷新与锚点 | URL/history-state 或 sessionStorage 保存 snapshot anchor；刷新 GET anchor 后仍在同一历史项；retention 清理返回 410 并回 current；服务端无 browse-state 写入 |
| 历史零副作用 | older/newer/back-to-current 全程 0 AI call、0 attempt/job/event/idempotency write、0 current/eligibility/exclusion/actual-question change |
| 浏览中更新 | 自动替换只更新“当前问题已更新”提示；不跳回 current、不移动 anchor、不抢焦点；用户主动回 current 后读取最新 revision |
| 普通修正 | 当前/历史 snapshot 正文可保留，candidate future ineligible；不会自动生成；下一次生成读新 revision |
| 硬撤下 | restricted/do-not-ask/active deletion/consent/access/policy failure 在 current、history 页、anchor GET、刷新、WS notify/replay 一致不返回正文，不生成替代；解除后不自动恢复旧正文 |
| REST/WS | REST current/history/request status 是 canonical；WS 1.2 只含 revision/kind/snapshot ID/change kind，零正文；重复/旧 revision 忽略，窗口过期 refetch；事件发布失败不回滚 DB |
| 权限/泄密 | 未认证 401、无 session 权限 403；错误、日志、审计、cursor 和 WS 均无问题正文、reason、模型输出、provider 原文、内部 score 或 marker note |
| 失败分类 | unavailable、节流、timeout、第二次同输入仍 Schema-invalid、stale basis、policy blocked 均有稳定状态/错误；失败不创建 candidate、不改变 current/history，录音与 ASR 不受影响 |

响应式与无障碍验收：

| 视口/能力 | 当前视图 | 历史视图 | 自动更新/加载状态 |
|---|---|---|---|
| 1440×900 | 问题、原因、上一个/下一个均可见；建议区不抢转录主层级 | 更早/更新/回到当前可键盘遍历，锚点不移动 | current 变化用 `aria-live=polite`，不移动焦点；生成状态不清空旧 current |
| 390×844 | 操作可换行但每个触控目标 ≥44×44px；无横向溢出，正文可缩放 | 历史标识、问题/撤下占位和三导航均不遮挡转录；安全区有效 | “当前问题已更新”可感知但不强制跳转；loading 不使用循环高刺激动效 |
| 320×568 | active suggestion 总高不超过 120px；continue/unavailable 约 64–72px；触控目标 ≥44×44px | 可纵向紧凑排列操作，页面无横向滚动；焦点顺序与视觉顺序一致 | reduced-motion 下禁用位移/闪烁动画；撤下使用 `role=status|alert` 的中性文本 |

所有视口还必须验证：历史/当前按钮的 accessible name 与视觉文案一致；disabled 无历史状态不可聚焦或具有正确 disabled 语义；键盘 Enter/Space 只触发一次 manual next；自动更新、错误或硬撤下均不抢走用户在转录或历史导航上的焦点。

### 7.7 SPEC-QUESTION-JOURNEY-001 与 DEV-007A/B 验收矩阵

| 维度 | 必须证明 |
|---|---|
| CSV 模板 | UTF-8、固定表头与 14 个最小字段；`purpose` 必填且仅允许既有 11 值；未知列/缺列、重复 question ID、混合 bank version、未知枚举或条件码均给稳定行级错误 |
| 条件 v1 | 整格空值分别成为空适用/排除集合；适用多码 all-of/AND、排除多码 any-of/OR、排除优先；未知码、分隔产生的空 token、字段内重复、适用/排除同码交集均整批失败且有稳定行级错误 |
| 全量导入 | 任一行失败则 0 release/item 生效；成功只创建完整 draft；相同 request/file 重放稳定，同 ID 异 digest 冲突 |
| 版本不可变 | 同一 bank version/content digest 不可覆盖；release 提交前数据库重算 actual item count/canonical digest 并 seal；seal 后 draft/active/retired 的 item INSERT/UPDATE/DELETE 均失败；内容、条件、敏感度、来源、许可或 enabled 变化必须新版本 |
| 原子激活 | 新 active 与旧 retired 同事务；失败时旧 active 完整保留；CSV enabled 不冒充 runtime active |
| 来源许可 | project original/verified 可进入正式内部试用；unverified 不可激活；公开可访问但无许可的反例必须拒绝 |
| fixture 隔离 | synthetic fixture 仅 `APP_ENV=local|test` 的 internal demo scope；`staging`（正式内部试用）和 production 即使传入伪造 CLI 环境参数也拒绝 validate/import/activate/retire/reader；技术 demo 明确不宣称产品题库可用 |
| 阶段确定性 | `journey_policy_v1` 每个固定分支、reason-code 受控值与稳定顺序均有表驱动测试；相同冻结输入与版本（包括 signal 顺序置换、重复执行）得到相同 `rapport|life_outline|story_depth`、reason codes 和 basis hash；过程记录含 transcript 水位、memory manifest、可信角色与 policy/boundary 版本 |
| 信号优先级与非硬切 | hard safety 不发布；保守安全/reluctant 压过具体事件与可深入信号；low detail/topic exhausted 最多退一阶段；其后才允许 willing + concrete + story anchor 进入 story depth。题数和经过时间不能进入决策或 tie-break |
| 继续倾听 | response/engagement 仅由最近 interviewer final 后最多 3 条相邻、非空 trusted elder conversation final 推导；更早拒绝/连续讲述措辞不滞留。`shouldContinueListening=true` 时 provider call=0、candidate=0，并直接返回 continue outcome；已有 current/history 不被覆盖。零 eligible 参考本身不强制 continue；无静态 basic UI 兜底 |
| 题库参考 | Context 只含 active + enabled + licensed + stage/condition compatible item；模型可原样使用、广泛改写或完全不用，零参考仍可基于可信对话生成 |
| purpose 投影 | import item 与 safe reference reader 保持题库 purpose；candidate/snapshot 保存本次模型输出的受控 purpose，不要求等于参考题；未知 purpose 失败关闭 |
| 自由生成 | 覆盖无题库引用、单条/多条参考、大幅改写与题库外问题；一次只输出一个主要问题，不把未知/冲突写成事实，不把生成正文反写题库、转录、memory 或 actual-question |
| 语义质量反例 | 固定评测集和人工抽查覆盖无依据人物/因果/情绪/时间前提、多问合并、阶段/敏感度不当；不得声称确定性后端已证明自然语言事实蕴含、真正单问或 risk/purpose 贴切，也不得为此增加第二个 AI |
| 四类 provenance | seen membership 保存 Context 实际发送题库项；candidate 只保存模型 declared attribution 且必须为 seen 子集，空集合合法；grounding 只接受本次 Context segment/memory ID；权限/安全/版本/retention 独立决定发布资格，四者不得互相冒充 |
| Context/Output Schema | 两份 JSON Schema 是模型实际输入/输出的唯一技术结构并由契约测试直接加载；Context 稳定排序、有界裁剪、trusted role 与最小 boundary；Output `additionalProperties=false`、suggest/continue 交叉字段和 ID 白名单；Markdown/Prompt 不复制平行 shape |
| Prompt 可调与复盘 | 仓库 prompt bundle 修改必须产生新 version+digest；job 保存 Context/Output/context-builder/model-config 版本与 digest，不在技术日志复制完整 prompt/Context/provider 原文 |
| 注入与后过滤 | 转录/记忆/题库中的命令性文字不能改变 system/task 指令；服务端在模型前后执行权限、禁区、deletion、Schema、ID/subset、重复与 writeback 漂移等确定性检查；单问/事实贴切性作为 Prompt 与评测要求 |
| Director retry | primary 与最多一次 same-input retry 共用 attempt 8 秒总预算；retry 前 policy/deletion 再检查，预算耗尽不调用；第二次输入不得含前次输出、错误或修复提示；deadline 后迟到输出不可写回；第二次失败后 attempt failed、candidate=0、current/history 不变，下一次“下一个问题”使用新 request |
| 阶段与替换 | 同阶段才直接比较 score；阶段变化且旧 item 不再兼容时旧 candidate future ineligible，新候选不受旧阶段 score 阻断但仍过安全/similarity/debounce |
| 既有体验 | 阶段变化不创建伪 snapshot、不冒充 actual asked；自动更新、上一个/下一个、历史锚点、displayed != actual asked 和硬撤下规则保持 |
| 所有权 | DEV-007A 不写 candidate/display/history；DEV-007B 只经 QuestionBank/QuestionJourney reader 与 QuestionEvidence writer，未形成第二套 question history |

纯工程 demo 可用 `docs/question-bank/question-bank-internal-demo.fixture.csv` 的极少量虚构内容验证参考链路，但 fixture 必须走与正式 CSV 相同的 14 列 parser、purpose/条件 validator，再叠加 fixture 环境门禁，不得设置宽松旁路。正式题库缺失不阻止用虚构可信转录测试自由生成工程链路，但不得把 fixture、问题质量或许可状态宣称为正式产品内容/试用结论。

当前治理状态以任务板为唯一动态来源；本节不维护 DEV-007B 的第二份动态状态。不得把契约、fixture demo、CI 成功、任务解锁或开发侧 REVIEW 候选冒充 DEV-007B/父 DEV-007 DONE、真实题库可用或真实试点通过。

### 7.8 提示词发布门禁

提示词或模型变更必须：

1. 固定评测集重跑；
2. 与当前生产版本对比；
3. 不降低边界安全指标；
4. 记录模型、提示词和 Schema 版本；
5. 通过人工抽查；
6. 才能进入预发布。

## 8. 权限和安全测试

验证：

- 倾听员 A 无法查看 B 的项目；
- 管理员默认不能直接下载访谈资料；
- 无有效授权不能开始；
- 授权撤回后 AI 停止；
- 受限内容不进入普通摘要；
- 受限内容不进入普通导出；
- 下载地址过期；
- 对象存储非公开；
- 日志不含完整转录；
- API 密钥不出现在前端包；
- 删除操作幂等；
- 审计记录完整。

### 8.1 DEV-001 身份与会话门禁

- 密码使用 Argon2id，不存在明文、可逆密码或日志泄漏；
- 未登录返回 401，无角色或资源权限返回 403；
- 用不落业务表的合成 ownership/assignment 标识验证服务层授权接口拒绝跨用户访问；真实“倾听员 A 无法访问 B 的项目”由 `DEV-002` 集成验收；
- 登出、账号停用和权限撤回后旧会话失效；
- Cookie、CSRF、Origin、会话 TTL 和登录限流按 `02`、`04`、`05` 执行；
- production 运维 CLI 可以安全创建、重置、停用和启用用户，密码不出现在参数、环境变量或日志，启用不恢复历史会话；
- production 拒绝测试 seed 和默认账号；
- 缺少关键配置时启动失败，错误不回显配置值；
- 空 PostgreSQL 测试库可从零执行全部迁移，重复 deploy 不产生漂移。

### 8.2 AI 边界与受限内容门禁

1. `restricted` 原文和派生记忆不进入捕获的模型请求；
2. `do_not_ask` 原文不发送，但存在人工确认的最小禁区标签；
3. 模型故意输出禁区问题时，服务端过滤并返回“继续倾听”；
4. 禁区标签缺失、边界状态不可用或过滤器故障时不展示新建议；
5. 第二次访谈继续遵守上次确认的边界；
6. `sensitive` 不被误当成删除或自动禁止追问；
7. 普通摘要和普通导出不含敏感正文、`restricted` 正文或待删除内容；
8. 非 `data_admin` 无法查看或导出 `restricted` 内容；
9. AI 输出不能确认、修改或解除 marker；
10. 重叠 marker 按限制并集处理；
11. 删除请求创建后，相关 AI 和普通导出立即停止；
12. 删除处理中仅 `data_admin` 可生成不含申请范围正文的删除处理证据包；
13. AI 候选不是正式 marker，确认前只触发当前会话临时阻断，驳回、过期或会话结束后解除；
14. `project` 删除请求停止整个项目 AI，`session`/`segment_range` 只停止相应内容和派生记忆，但三者都拒绝项目级普通回顾与普通导出；
15. 删除申请各合法状态流转、非法流转、重复 `request_id` 和 data_admin 权限均有自动测试；
16. `complete` 在在线主存储或临时导出未清理、清单哈希缺失或备份 `scheduled/not_applicable` 未登记时失败；满足条件后才进入终态；
17. project scope 的 rejected/withdrawn 只在无其他限制原因时恢复原项目状态，并发限制不会覆盖原状态；completed 置为不可恢复 `deleted`；
18. 普通软删除可恢复但不能替代隐私删除；completed project scope 请求不能通过 `/restore` 恢复；
19. `user:enable` 清空 `disabled_at` 但不恢复旧会话；
20. 删除请求与 AI 调用/写回并发时，命中 scope 的排队任务取消、在途结果丢弃，任何 AI 派生表和 UI 均无晚到结果；
21. completed 清理后外键完整，project tombstone 与 session/segment scope 摘要都不能反查正文、marker note、对象键或派生内容；
22. segment_range 请求创建后修改或软删 source marker 不会改变冻结范围、缩小 AI 阻断或减少最终清理；completed 后端点快照清空而摘要仍可验证；
23. 审计日志不记录正文或完整控制信封。
24. 角色修正重算不会覆盖人工确认记忆，也不会解除 `sensitive|restricted|do_not_ask|deletion_request`；
25. speaker calibration/remap 的权限、审计和错误响应不泄露其他项目正文、provider payload 或供应商原文。
26. `SPEC-DEV-006` 冻结跨 session watermark、job/segment/output provenance、stale 查询过滤和重算失败保持不可用；项目负责人 PASS 前不得启动 DEV-006，之后按 §7.5 运行实现验收。
27. 已展示问题的普通修正快照例外不适用于硬安全边界；`restricted`、`do_not_ask`、活动 deletion scope、授权/访问失效后正文立即撤下且不被查询/replay 恢复。
28. AI 过程记录和实际问题目录遵循最小权限、保留与删除 scope，技术日志只保存 ID/版本/状态/延迟。
29. 三类 retention root 到期时，普通 GET、cursor 续页、页面恢复和未来 WS replay 在物理删除前已经隐藏；实际清理失败与重试期间也不回流。
30. `ai_job` owned children 可 CASCADE/显式幂等清理，snapshot/current/published 等跨 root 指针先 detach 或 `SET NULL`，不存在 `RESTRICT` 阻塞；dependency expected count/manifest 防止缺行后重新 eligible。
31. retention cleanup 重放同 `(root_kind, root_id, expires_at, policy_version)` 不重复审计/状态动作；同 root 但绑定不同期限或策略的请求拒绝冲突，worker 崩溃后可从 hidden/purging/cleanup_failed 续跑。

## 9. 性能测试

| 项目 | 目标 |
|---|---:|
| 中间态上屏 | ≤ 2 秒 |
| 确定态落库 | ≤ 1 秒 |
| AI 建议 | 目标 ≤ 8 秒 |
| 普通交互反馈 | ≤ 200 毫秒 |
| 同时会话 | 5 |
| 60 分钟浏览器内存 | 不持续线性增长 |

测试报告需包含设备、浏览器、网络和供应商环境。

## 10. 故障注入

至少模拟：

- ASR 连接中断；
- LLM 超时；
- LLM 返回非法 JSON；
- 对象存储失败；
- 数据库短时不可用；
- Redis 不可用；
- WebSocket 重连；
- 重复结束请求；
- 重复分片；
- 导出任务失败；
- 删除任务中断。

验证系统是否按专项文档降级，而不是只验证错误消息。

### 10.1 会话安全结束验收矩阵

以下矩阵是 DEV-005C/005D 的直接测试依据；所有 snapshot 都从真实服务端读取，禁止固定延时或前端夹具推算：

| 场景 | 请求/前置 | 预期状态与响应 | 必须验证的不变量 |
|---|---|---|---|
| 正常 stop | `recording`，完整 commitments | 202；`stopping -> processing -> completed` | 新 PCM 被拒绝；manifest 复核；时长来自服务端；AI 不参与门禁 |
| 相同 stop 重放 | 相同 request ID/actor/session/payload | 返回首次响应 snapshot | 单一 finalization、单一审计/状态动作 |
| 同 key 不同 payload | 相同 request ID，count/checksum 不同 | 409 `IDEMPOTENCY_PAYLOAD_MISMATCH` | 不改变首次冻结事实 |
| 不同 request 并发 | 两个 key 命中同一 session | 一个冻结；相同快照后来者返回当前事实，不同快照 409 | 不出现双 finalization、双 audio object 或 ended_at 漂移 |
| stop 响应丢失 | 服务端已提交，客户端未收到 | 原 key 重试返回首次 snapshot | 不重新创建对象或扩大 count |
| stop 前掉线 | 尚无 snapshot | `interrupted` | 已上传分片保留；门禁有效可 resume，或用完整 commitments finalize |
| stop 后掉线 | 已有 snapshot | 保持 `stopping|processing` | recover 可重驱，不倒退 recording |
| assignment 撤销 | stop 前/后分别撤销 | stop 前禁止新动作；stop 后原 actor 重认证仅能受限补传 | 无普通读取、新 PCM、新 AI；冻结证据不丢失 |
| 授权在首次 snapshot 前撤回 | 尚无 finalization，assignment 仍有效，提交首次 stop 或 `finalize_interrupted` | 403 `FORBIDDEN`；session 为/保持 `interrupted` | 不创建 finalization、commitments 或补传例外；只保留服务端此前可靠收到的分片，证据边界不扩大 |
| 授权撤回 | stop 后撤回 | 项目受限，结束可继续收束已有证据 | 不新增采集/AI；撤回不覆盖或物理删除证据 |
| 登录 session 失效 | stop 后 Cookie 失效 | 401；重新认证 active 原 actor 后可受限补传 | 不存在匿名或长期 bearer 补传 |
| commitment 外分片 | stop 后新 sequence 或元数据变化 | 409 `AUDIO_COMMITMENT_CONFLICT` | 已有分片不覆盖，count 不扩大 |
| 缺片 | commitment 中至少一片未到 | 保持 `stopping/awaiting_upload` | 不进入 processing/completed，可查询缺失数量但不泄露对象键 |
| checksum 冲突 | 同 sequence 二进制/元数据冲突 | 409；最终可转人工失败 | 不覆盖原始对象 |
| manifest 不完整 | complete 校验缺片/存储不一致 | 409 `AUDIO_MANIFEST_INCOMPLETE`，保持可恢复 | 未完成 manifest 永不 completed |
| ASR 整场完整 | manifest complete，session/capture 无已知 gap，全部 attempt coverage 连续，最后 receipt 完整且 final 全落库 | `drained` 后 completed | 单个 attempt success 不足以证明整场完整；interim 不落库 |
| ASR 故障/超时/gap | manifest complete；accepted PCM 无终态、capture coverage 中断或 evidence 丢失 | sticky `degraded` 后 completed；整场从未建立可用 ASR 才为 `not_started` | 错误分类不泄密；后续 attempt success 不清除；录音/manifest 仍成功；不伪造 final |
| AI 故障 | 任意结束阶段 | 不阻塞 audio/ASR 状态推进 | AI 失败独立可见，不改变录音事实 |
| runner 进程故障 | `stopping|processing` 中重启 | 持久事实保留；recover/启动扫描重驱 | 不依赖内存任务或 WS replay |
| recover 重放 | 相同 recover request ID | 返回首次 snapshot | 不产生第二 runner 业务动作或状态倒退 |
| completed 重放 | stop/recover 命中 completed | 200 终态 snapshot | ended_at、duration、manifest 与链路终态稳定 |
| failed 查询 | 已确认不可自动收束 | 200 终态 snapshot | 保留已保存证据；只返回不泄密 failure code；不自动回 completed |

契约/API 自动测试还必须覆盖：`created|device_check` 非法 stop/recover、`recording` 上 reconcile、`interrupted` 的 resume/finalize 分支、跨 actor/target request ID 复用、账号停用、非原 actor、第二个 interview audio object、commitment 数量/顺序/时间/MIME/大小校验、公共响应字段白名单和审计脱敏。

### 10.2 DEV-005R 采集生命周期验收矩阵

| 场景 | 必须验证 |
|---|---|
| 页面加载/设备检测 | 不自动请求麦克风；临时检测流释放；检测失败不创建 interview audio object |
| 原子 start | MIME/stream/request ID 稳定；单事务创建唯一 object + generation 0；同 key 异 payload 冲突 |
| start 前后故障 | 请求未到可重试同 session；响应未知先重放旧 key；MediaRecorder 未 active 不宣布访谈中 |
| confirm active | 本地 checkpoint 在先；重复确认稳定；门禁变化失败关闭 |
| 单一正式流 | 同一 MediaStream 同时供 MediaRecorder 和 AudioWorklet；无第二次 getUserMedia |
| archive/delivery | Blob 只写一次；ACK 删除 delivery 不删除 archive；刷新后同 job/ID 高水位恢复 |
| AudioWorklet | mono/16k/s16le/100ms；20 帧背压；实时失败不停止原始录音 |
| 中断报告 | track/recorder/archive/auth/page recovery 原因正确；普通网络/WS 不误报 capture interrupted |
| 显式 resume | 同 session/object/job；下一 generation/新 stream；archive timeline 连续、PCM sequence 重置并带 offset |
| 空录音 | 零服务端 chunk、该 session 所有 capture generations 均无 `first_pcm_accepted_at`、同一 local job 累计 archive 为零才允许；至少覆盖 gen0 有 PCM、resume 后 gen1 无证据仍拒绝 abandon；结果 NO_AUDIO_CAPTURED，不能 completed |
| 竞态 | stop/report/revoke/resume/upload 按固定锁序，无双 object/generation/finalization 或状态倒退 |
| 正常结束 | 最终 dataavailable 已归档才 stop；正数 commitments；同一对象完成 manifest |
| 页面事实 | 采集、本浏览器、服务端录音、转录、session 分开；断网保留最后核验时间且不推算安全 |
| 响应式结构 | 1440×900、1024×768、768×1024、390×844、320×568 逐一截图/断言；无页面或状态 chip 横向滚动；正常态转录为最大连续区和唯一主要纵向滚动区 |
| 高密度转录 | 所有设备保持左侧说话人/时间、右侧正文；手机左栏 52–64px；确定态不重复标签；浏览器字体缩放可用 |
| 分状态重心 | recording/interrupted/stopping/processing/completed/failed/NO_AUDIO_CAPTURED 均验证；手机 interrupted 动作首屏可见；只有结束确认是 modal；stopping 最小化灰置、processing 启用 |
| Android Chrome 真机 | 至少一台目标设备完成 5–10 分钟准备→录制→转录→安全结束，并覆盖旋转、切后台/锁屏、权限/麦克风中断、刷新恢复；不能继续可靠采集时明确 interrupted |
| 无障碍 | 对话框焦点/Escape、键盘、触控目标不小于 44×44px、安全区、正文对比度不低于 4.5:1、aria-live/alert、reduced motion |

DEV-005R2 的首个真机基线为 OnePlus GM1900 / Android 12 / Chrome 150：正式路由连续采集约 6 分 20 秒，archive `0..371` 共 372 片且无序号缺口；旋转、切后台和锁屏继续同一 session/object/stream/generation。刷新后同一 archive/job 保留并以 `page_recovery_detected` 进入 `interrupted`；第二条正式会话撤销麦克风权限后保留 archive `0..70` 共 71 片并以 `microphone_ended` 进入 `interrupted`。该证据满足 R2 的 controller 生命周期事实门禁；显式 resume 到下一 generation、同一 object/job 累计时间轴、安全结束与 manifest 仍由 R3/R4 验收，不能据此提前宣布父 DEV-005 完成。

### 10.3 DEV-008A1/A2 倾听员工作区与新建入口矩阵

| 维度 | 必须证明 |
|---|---|
| 统一 shell | 登录后 `/` 同时承载“新建访谈”、已有项目/访谈、继续、回顾和退出；准备/工作台/回顾可回同一 home；不存在第二套导航或首页 |
| 列表权限 | `GET /projects` 只对有效 assignment 的 ordinary 项目返回普通行，对有效 assignment 的 restricted 项目返回下行固定中性投影；session read model 不服务 restricted。未认证、跨项目 cursor、撤销 assignment、deleted/软删除均失败关闭且不泄露存在性或正文 |
| restricted 项目投影 | 有效 assignment + `restricted` 只返回一条 `ProjectListRestrictedProjection`，精确字段为 `project_id/projection/status/display_label/status_label`，无姓名/出生年龄/地域/created_by/service/consent/session/统计/正文/主动作；deleted、软删除、assignment 失效完全无行 |
| session cursor | cursor 签名并绑定 `project_id + created_at + id`、方向、page size/过滤版本；跨项目、篡改、过期、参数不一致返回 `INVALID_SESSION_CURSOR` 或当前权限失败，不降级首页、不返回空列表掩盖 |
| 列表状态 | 逐项覆盖 `03` §17.2/`05` §3.1 的完整矩阵：created/device_check=继续准备；recording/reconnecting=返回访谈；interrupted=处理访谈中断；stopping=查看保存进度；processing/completed=查看回顾；failed 按 NO_AUDIO_CAPTURED/complete manifest/其他分流。每项只有一个主动作，未知组合失败关闭为查看保存事实 |
| 深链与 finalization 隔离 | restricted/soft-deleted/deleted 或 assignment 失效时，project/service-term/consent/session 普通 GET、prepare/workbench/review 深链均不返回正文；`interview_session.created_by`/`session_finalization.created_by` 不绕过 assignment。仅限制前已冻结 stop 的原 actor 可读专属 `EvidenceFinalizationResponse`，且不能取得普通 snapshot、项目/转录事实或页面动作 |
| 新建完整性 | 登录→POST project→service term→正式有效 consent→session→device check→start 全链路；只创建 draft 时明确显示未就绪，不能出现可开始假象 |
| 授权门禁 | 普通 UI 只走正式 MVP 口头授权证据；未说明、授权缺失/撤回/版本不匹配、无 assignment 时 start 被服务端拒绝；local/test fixture 不出现在普通 UI |
| 创建幂等 | project/service term/consent/session 每步首次网络请求前持久化稳定 request ID；响应丢失、超时、刷新、重开、双击复用同 ID。服务端以 actor/action/target-or-project-create-identity/canonical payload hash 权威重放首次响应；同 ID 异绑定 409，四步均无重复业务记录、历史或审计 |
| 重试与恢复 | GET 只用于展示/核对，不能凭相似项目、最新记录或计数猜测未知 POST 成功；workflow 收到 create 首次/replay 结果并持久化资源 ID/版本后才推进。深链仍可恢复但不能绕过 home 权限投影 |
| 共享视觉 | 复用现有 OKLCH tokens、排版、context-label、primary/secondary/danger、focus-visible；HomeShell/ListRow/StatusBadge 等价语义只有一套，无第二套按钮/卡片/状态颜色 |
| 响应式/无障碍 | 1440×900、390×844、320×568 无横向或页面级意外溢出；触控 ≥44×44px；键盘顺序、focus-visible、aria-live/alert、reduced motion 有效；新建与回顾使用同一导航/状态语言 |

### 10.4 DEV-008A3 回顾与本机副本删除矩阵

| 场景 | 前置/故障 | 必须证明 |
|---|---|---|
| 完整播放 | 本机 `0..N-1`、fresh complete manifest | 逐片 identity/size/checksum/MIME/time 对应后才可播放；离页释放 Object URL；不请求不存在的服务端下载 API |
| finalization 字节来源与 lifecycle | finalization absent；`awaiting_upload|verifying|complete|unrecoverable`；session `stopping|processing|completed|failed` | ordinary canonical GET 的字段只来自同一 finalization 关联的 `AudioObject.totalSizeBytes`；A3 runtime 后 finalization 存在即显式带 `total_size_bytes`。未证明状态为 null；正常 processing/completed 与 failed+complete-manifest 为非负 safe integer且非 null；不得从 commitment/local/client 值反推 |
| additive/legacy null | 旧 producer 缺键、显式 null、complete+null、unsafe BigInt | 缺键与 null 同义为未证明且绝不当 0；普通只读转录可继续，播放/删除为 `blocked_server_unverified`；unsafe 不得静默舍入；A3 的 exact-key response 测试防止 optional 契约被永久漏发 |
| response 字段白名单 | ordinary session、A1 session list、evidence-finalization | `InterviewSessionResponse.finalization` 显式含 `total_size_bytes`；`ProjectSessionListItem.finalization` 仍精确只有 recording/upload/transcript/failure/checksum；`EvidenceFinalizationResponse` 既有九字段不增加。A3 只读 ordinary canonical GET，不以 list/evidence seam 替代 |
| canonical GET 权限 | 未认证、跨 actor、assignment 撤销、restricted、deleted、软删除、created_by 命中、本机 archive 存在 | 只有当前 assignment + ordinary visibility 返回字段；其余失败关闭且不泄露 session/finalization/bytes。`created_by`、本机事实和 evidence 例外均不能绕权 |
| 本机缺片/Blob 不可读 | archive 存在但不完整 | 投影 `available_incomplete`，不播放部分录音，不影响服务端转录查看 |
| projection 优先级 | active/dirty、pending、server unavailable 同时组合 | 严格为 active/dirty > pending > server unverified > receipt/empty > verified completeness；每组输入只产生一个 state，A1/A3 不各自推断 |
| projection Schema 正例 | complete、incomplete、deleted、missing、三类 blocked | complete 为 bytes/chunks≥1+pending0+playback true；deleted/missing 全零+false；blocked/incomplete playback false；pending blocked 的 pending≥1 |
| projection Schema 反例 | complete+playback false/pending>0；deleted+payload/playback；pending blocked+pending0 | `local-audio-archive-v1` 必须拒绝，fixture 的 `expected_valid=false` 全部得到 invalid |
| 容量 | estimate 可用/不可用/NaN | archive bytes 精确；usage/quota/available 只标 origin 近似或 null；不宣称本 session 可回收字节或设备磁盘 |
| active tab | capture tab 持有 `elder-interview:capture:{session}` | delete `ifAvailable` 获取失败，零写入；关闭/停止后重试才能进入 preflight |
| Web Locks 不可用 | `navigator.locks` 缺失或异常 | 失败关闭，不以 localStorage/BroadcastChannel 假锁降级 |
| server preflight stale/离线 | session/manifest 读取失败、cache 命中、identity/count/bytes/checksum 不符 | 必须同时证明 session/finalization/manifest identity，`expected=manifest=local` chunk count，`finalization=manifest=manifest chunks sum=local` total bytes，以及逐片元数据；任一 missing/null/stale/mismatch 为 `blocked_server_unverified`，零部分清理、零回执、零服务端写入 |
| session/capture 门禁 | capture 非 stopped、session 非 processing/completed、job active/interrupted、checkpoint active/dirty | 删除拒绝且所有 stores 不变 |
| pending delivery | 任意 pending > 0 | 删除拒绝；不能丢失唯一待传字节 |
| failed session | NO_AUDIO_CAPTURED / complete manifest / manifest 不完整 | NO_AUDIO_CAPTURED 仅查看保存事实且无播放/删除；complete manifest 可只读回顾并播放完整本机 archive，但删除因 session 非 processing/completed 灰置；其他 failed 只看保存事实，不播放、不删除 |
| 原子成功 | 全部门禁通过 | 一个 transaction 清除 archive/delivery/session-state/formal job/all reports/checkpoint/legacy chunks，并写最小回执；其他 session、consent job、canary 不变 |
| 任一点失败 | cursor/delete/put/quota/abort 注入 | transaction abort；刷新后全部旧 payload/恢复事实仍在且无回执，不出现混合状态 |
| 幂等重放 | 首次 commit 后重复 | `deleted|already_deleted` 均必须返回回执首次 commit 的非空 `deleted_at` 且稳定；无服务端 deletion/audit 写入；Schema 拒绝成功无时间 |
| 删除未提交 | active/pending/server blocked、lock unavailable、transaction abort | `deleted_at=null`；Schema 拒绝 blocked/abort 携带时间，不得出现成功暗示 |
| 跨刷新 | commit 前/后刷新 | 前者旧集合一致；后者 payload 全空+回执，投影 `deleted_on_device`；转录/服务器保留提示持续可见 |
| 浏览器清站/eviction | payload 与回执都消失 | `missing_unknown`，文案为可能已删除或被浏览器清理；不得声称用户删除或服务器删除 |
| 隐私边界 | 点击/确认/成功 | danger 确认与成功反馈明确服务器录音、转录、记忆仍保留；不创建/推进 deletion_request，不关闭 CON-023 |

测试必须覆盖 v2/v3 legacy IndexedDB upgrade 后 `chunks` 仍有 Blob、多个 generation interruption report、同 store 其他 consent/job、两个 tab 并发删除、事务 commit 回调前页面卸载，以及真实 Chromium 刷新投影。机器响应结构按 `local-audio-archive-v1` Schema 校验。

## 11. 端到端验收场景

### 场景 A：首次访谈

DEV-005 的历史证据使用已分配、已预创建的虚构项目；DEV-008A 当前候选在不改写该历史的前提下增加统一工作区与正式新建入口：

1. 登录进入统一倾听员工作区，能够新建访谈、查看已有项目/访谈、继续未完成访谈或进入已结束访谈回顾；新建路径完成最低项目信息、服务说明和正式有效授权后才进入准备页；
2. 查看长者称呼、预计时长、正式授权状态和麦克风状态；
3. 未授权、未分配或状态不允许时不能开始；
4. 用户主动开始后，同一真实麦克风流同时建立本浏览器原始 archive、上传交付和实时 PCM；服务端只创建一个 interview audio object；
5. 进入转录优先工作台，区分 interim/final 和说话人；
6. 页面在正式录音已经开始后完成同一 speaker stream 的两句校准与用户确认；失败或跳过时录音继续、显示待确认并可重试；
7. 向上回看时不被强制拉回，显示新内容数并能回到最新；
8. 同时只显示一个当前问题及一句原因，或“建议继续倾听”；更合适且仍具资格的问题出现时允许自动替换，但不得高频抖动；
9. “下一个问题”每次只发起一个幂等请求，且不返回当前或高度相似问题；
10. 每次真正展示的建议进入稳定排序的会话历史；“上一个问题 / 更早的问题 / 更新的问题 / 回到当前问题”只浏览历史，不触发 AI、不改变当前问题、不恢复排除；
11. “曾展示”不得冒充“实际问过”；只有可信倾听员 final 参与 actual-question 去重，unknown 和校准控制句不参与，证据不足时建议继续倾听；
12. 硬边界撤下后，当前与历史读取均不得重新返回正文；
13. AI/ASR/普通网络故障不停止本浏览器原始 archive；
14. 刷新或采集故障后不自动请求麦克风，能按服务端 capture 与同一 local job 选择继续或安全结束；新 speaker stream 必须重新确认；
15. 在目标 Android Chrome 真机完成 5–10 分钟虚构内容访谈；旋转不得刷新、重新申请麦克风或创建新 capture；切后台/锁屏和设备中断必须按已冻结的平台证据继续可靠采集或显式进入 interrupted；
16. 正常态五个目标视口无横向滚动，顶部安全摘要和结束入口可见，转录保持最大连续区；中断/结束态按任务重分配注意力而非强制维持 80%；
17. 结束后先完成最后本地归档，再显示 `stopping/processing`，仅在真实完成后显示成功；
18. 在当前页查看保存明细或完成离开，返回同一工作区；已结束访谈可进入最小回顾，不出现题库、AI 问题历史、完整记忆/工作记录、编辑或导出入口；
19. 回顾页按 §10.4 播放本机完整 archive、显示原始/修订转录和本机容量事实，并在安全门禁全部满足时原子删除本机副本；服务器保留提示始终明确。

复杂标记、完整回顾编辑、题库/AI 问题历史和正式服务器隐私删除仍在后续切片；倾听员导出已取消。DEV-007 当前状态不作为 A1/A2/A3 的前置。

第 4、10-15 项必须由 `SPEC-DEV-005R` 与 DEV-005R1/2/3/4 共同提供真实服务端、IndexedDB、桌面 Chromium 和目标 Android Chrome 证据。页面 fixture、query harness、固定延时、桌面缩放模拟手机采集或仅切换本地状态不能作为通过证据。

第 10 项还必须分别显示录音采集、原始上传/manifest 和转录 `drained|degraded|not_started`；原始录音完整但 ASR 降级可以显示“录音已安全保存，转录待后续处理”，不得显示为全链路成功或录音失败。刷新、响应丢失或 WebSocket replay 过期后必须重新读取 session snapshot，不从 `localStorage`、页面计时或最后一条 final 推算。

### 场景 B：第二次访谈

1. 在同一 Home project 卡使用服务端权威“开始下一次访谈”，不得创建新 project；
2. 已有 session 行继续只负责“查看回顾/返回当前会话”，不新增摘要、待确认或记忆管理页面；
3. 新 session 重新检查当前适用授权、显示版本化轻提醒、显式 start、重新检查设备和麦克风，建立新 speaker stream 并重新校准；普通 covered 路径不得创建新 consent；
4. 校准 gate 终结后若 basis post-analysis 仍未 terminal，只显示内容无关 waiting，录音继续且不创建 opening attempt；两个 lane 均 terminal 后以稳定 system trigger 恰好发起一次基于既有资料的开场建议；failed/skipped/provider-unavailable gate 不携带当前 stream 可信角色；
5. Context 只读取同 project eligible current memory、published reliable actual asked、边界与可信 membership；
6. displayed 不冒充 actual asked，已拒绝/失效/跨项目内容不得进入；
7. AI/ASR 不可用时原始录音与安全结束继续，失败如实显示；
8. 结束后返回同一统一工作区；倾听员端不导出。

### 场景 C：ASR 故障

1. 正常开始；
2. ASR 中断；
3. 原始录音继续；
4. 页面显示降级；
5. 当前内部 MVP 明确显示转录降级；完整 MVP/真实试点阶段再执行后置补转录；
6. 服务端完整录音仍为权威副本；倾听员端不导出。

### 场景 D：授权撤回

1. 已有项目和访谈；
2. 撤回授权；
3. 项目变受限；
4. AI 调用停止；
5. 普通导出阻止；
6. 创建删除申请；
7. 完成清理和审计。

## 12. MVP 功能验收清单

### 12.1 首次访谈最小纵向闭环

1. DEV-005 历史链路可使用预创建虚构项目进入准备页；当前网页还须从统一倾听员工作区完成正式新建入口，不长期依赖深链；
2. 新建入口完成 project、service term、正式有效 consent、session、device check 与 start；创建 draft 不得冒充可开始；开始门禁、真实单流采集、本浏览器 archive、服务端上传/manifest、转录主界面、单问题建议和安全结束符合场景 A；
3. 自动替换保持稳定且每次展示可回看；“下一个问题”不返回当前或高度相似问题；历史浏览可回到当前且无业务副作用；
4. 真实 Chromium 使用虚构内容连续 5–10 分钟，刷新/网络/响应丢失/中断恢复可重复执行；
5. 页面不把内部 harness 或处理中状态宣称为正式完成能力。
6. 已结束访谈最小回顾与本机副本删除符合 §10.4；local deletion 不得冒充服务器隐私删除。

### 12.2 完整 MVP 后续验收

1. 仅填写称呼即可建项目；
2. 未确认费用和授权不能开始；
3. 一小时连续录音成功；
4. 实时转录显示；
5. 确定态可靠保存；
6. ASR 失败时录音继续；
7. AI 失败时录音和转录正常；
8. 可修正说话人；
9. 原始角色和文本保留；
10. provider 自动角色与用户确认可信度分离；
11. 校准失败/跳过不影响录音，unknown 不污染角色相关 AI；
12. AI 显示推荐原因；
13. AI 关联证据；
14. 单次只显示一个有原因的建议或“继续倾听”，并可请求替换；
15. 可标记重要原话；
16. 可标记禁止使用；
17. 已拒绝话题不再自动推荐；
18. 两次访谈共享项目记忆；
19. 倾听员端不导出；未来其他角色需要时另行验收；
20. 关键操作可审计；
21. 权限隔离有效；
22. 删除流程可验证。

## 13. 发布门禁

进入真实试点前必须同时满足：

- 类型检查通过；
- 静态检查通过；
- 单元测试通过；
- 集成测试通过；
- E2E 主链路通过；
- 60 分钟录音测试通过；
- 故障降级通过；
- 权限和导出安全通过；
- AI 指标达到门槛；
- 无 P0、P1 未解决缺陷；
- 数据迁移已验证；
- 回滚方案明确；
- 文档与实现一致；
- 项目负责人通过 GitHub 或独立验收 Agent 签署通过，且审查证据已写回项目记录。

## 14. 缺陷等级

### P0

- 原始录音丢失；
- 未授权仍可开始；
- 越权访问访谈资料；
- 受限内容进入普通导出；
- 密钥泄露；
- 删除误删其他项目。

### P1

- ASR 故障导致会话无法安全结束；
- 原始转录被覆盖；
- 已拒绝话题仍持续推荐；
- 两次访谈记忆错用；
- 导出严重缺失；
- 状态机无法恢复。

### P2

- 页面显示错误；
- “下一个问题”请求丢失、重复执行或返回高度相似问题；自动替换抖动、展示历史丢失或历史浏览误改当前问题；
- 部分非关键统计错误；
- 可绕过的交互问题。

P0、P1 必须在试点前清零。

## 15. DEV-ASR-PROVIDER-001 真实腾讯验收矩阵

测试资产为约 8 分钟完全虚构标准普通话双人剧本：固定校准句、虚构姓名/年份/地点/关系/事件、五个问题、短应答、快速轮换、轻微重叠、较轻音量、正常 stop/drain/reopen。只允许明确同意的非长者测试者、合成音频或完全虚构音频。

`SPEC-ASR-WIRE-PARAM-001` 的 non-Draft PR exact head 获项目负责人手动 PASS 后，DEV 可在完整三次 replay 之前执行恰好一次受控诊断连接：继续使用同一份虚构 TTS PCM、同一账号/endpoint/engine/其余 query、单连接且 `reconnect=0`，唯一有意变量是正式 profile 新增的 `speaker_diarization=1` 以及与 `enable_speaker_context=0` 一并进入 canonical query、`speaker_context_id` 省略的规则。该诊断只判断请求能否被供应商受理并产生可诊断响应，不构成 close 1005 因果证明，也不构成双人 label、三次 replay、Android、账单或 DEV provider PASS。若仍失败，保留不含敏感正文的 provider request ID、时间和安全错误分类，停止继续参数试错并转腾讯支持。

同一受控 PCM 对腾讯真实 provider replay 3 次；另在桌面 Chromium 与目标 Android 正式采集链路运行，并执行主动断线 fault lane。必须逐项保留 provider request ID、attempt/voice/speaker stream、时间、指标、账单核对和无敏感正文的证据。

| 门禁 | PASS 候选要求 | FAIL |
|---|---|---|
| wire/signature | `speaker_diarization=1` 与 `enable_speaker_context=0` 同时存在于实际 URL 和按 key 字典序构造的 canonical query；`speaker_context_id` 在二者中均不存在 | 只签不发、只发不签、传 `speaker_context_id=`、排序或签名覆盖偏离正式 profile |
| 原始证据 | archive、唯一 audio object、manifest、stop/reopen 全部完整 | ASR 影响录音、manifest 或安全结束 |
| interim/final | interim 只展示；final-only 落库，重复/乱序/replay 幂等 | interim 落库、final 重复/越 fence |
| 双人 label | 三次 replay 每次均由两句校准句得到两个可区分 label，用户确认后才 trusted | 任一次合并、缺 label、不能校准；不得增加第二 diarizer |
| unknown | `-1`、缺失、未确认、重连新流均 unknown，AI 不猜 | 自动继承/猜测 elder 或 interviewer |
| 控制句 | 保留原始证据，排除普通 AI 消费 | 进入 memory/Director 普通输入 |
| attempt drain | 当前 voice `final=1` + accepted PCM 终态 + final ingestion receipt；只证明该 attempt 收束 | close、末句、void resolve 或超时被当 receipt；单个 receipt 被当整场完整 |
| 整场 completeness | `no_known_gap` 仅单向转为 `known_unbackfilled_gap`；stop 时无 gap、全部 attempt 连续且末 attempt receipt 完整才 `drained` | 新 voice/connect/ready/final/receipt/reconcile 清除旧 gap；无证据 clear |
| namespace | 新 voice 新 speaker stream、旧 sink fenced、重新校准 | 继承旧 stream、label、speaker context 或 late 写入 |
| Android | 目标 Android 使用正式 MediaStream/archive/PCM/controller 路径 | synthetic/fake/桌面替代 Android |
| sticky fault lane | voice A 有 accepted PCM 后断线且留下未回补 gap → voice B 新 namespace/new speaker stream → B `final=1` + ingestion complete + attempt drain success → session/capture 仍 `degraded/incomplete`，录音继续 | B success 将整场恢复为 `drained/complete`；fake 冒充或 ASR 故障阻塞录音/结束 |
| 无 gap 多-attempt lane | A 在首个 PCM 前失败，或 A receipt 完整且 B 从紧邻 sequence/timeline 边界连续接管；B receipt 完整后整场可 `drained` | 把所有 reconnect/new voice 一概永久 degraded |
| evidence-loss/no-clear lane | 已可能接收 PCM 后 runtime/coverage evidence 丢失，即使新 voice 成功仍 `degraded`；本 SPEC 无 clear 路径 | 丢证据仍 complete，或当前 reconcile/reset 清除 sticky；权威 backfill 只能留给 HARDEN-ASR-001 |
| 安全/成本 | 后端注入、redaction、授权前零上传、预算/配额与 billed duration/SKU 核对 | 密钥/签名/正文日志泄露或未授权发送 |

营销“支持话者分离”、fake、单元测试、单次运行或 CI 绿均不能形成真实 provider PASS。speaker 实际计费 SKU、诊断保留和 DPA 在实证前继续标 unknown；后两项阻塞真实长者试点但不阻塞虚构 DEV 验收。

## 16. DEV-008A4 定向验收矩阵

| 类别 | 必须证明 |
|---|---|
| ServiceTerm dormant | 新用户普通页面无服务说明、价格、预计时长字段；数据库无伪 ServiceTerm row，project 能 ready 且 session 能 start；历史 CRUD 不被删除 |
| 当前页麦克风 | session 提前创建；首次访谈未通过当前页 permission+input check 不能录 consent；任何 session 的服务端已有 device_check 或页面刷新均不能自动 passed；start 再次取得 formal capture MediaStream并满足 reminder gate |
| 专属校准 | formal start/stream 前不 begin；独立显著阶段完成或诚实降级后才显示普通工作台；普通 realtime/transcript/review 不含 speaker_calibration，AI 既有排除测试继续通过 |
| 自动收尾 | happy path 自动 flush/stop/complete/reconcile/verify；刷新、`endHandoff` 内存态丢失或 exact complete ACK 后本机落盘中断均自动恢复；无“继续安全保存/继续处理收尾/重新核对”；pending/失败/权限漂移/unknown 不被掩盖且仅明确失败出现单一“重新核对保存状态” |
| A2/A3 整合 | exact complete ACK → local job complete → fresh projection available_complete → 播放与本机删除可用；dirty/pending/identity/count/bytes/checksum mismatch 仍灰置且零删除 |
| ASR 降级 | `transcript_status=degraded|not_started` 且 raw manifest/archive 完整时仍可回顾播放与本机删除；页面不宣称 ASR 成功 |
| 回顾重投影 | 首次 processing/临时 blocked 后在可见/online/有限短间隔重新执行完整 fresh verifier，核对状态可见且终态停止；无无限循环或旧响应回退；同 origin 完整 archive 最终播放/删除可用，不同 scheme/host/port 显示当前 origin 诊断 |
| unknown create | 首次 POST 连接重置/status=0 后页面自动以同一 ID/payload 重放；reload、online、visibility 均可恢复且不重复项目；无固定客户端 deadline 主动中止；普通文案不出现 request ID/payload/表单锁定 |
| 身份显示 | 首页只使用 `AuthUser.display_name`；`listener-a@example.test` 明确是测试 fixture，不硬编码“黎明”或修改公共 fixture；本地受控账号用一次性、审计可追溯的数据初始化方式修正 |
| 响应式与无障碍 | 1440×900、390×844、320×568 无横向/页面级意外溢出；可见控件至少 44×44；focus-visible、aria-live、reduced-motion、键盘焦点归宿通过 |

全量门禁仍包括 format、lint、typecheck、unit、fresh PostgreSQL migration deploy/status、integration、auth、build、smoke、Chromium 与 auth Chromium。普通 Chromium 必须增加同 origin `new → mic → consent → capture → end → auto completed → review → play enabled → delete confirm → cancel no-op → confirm local-only delete` 的单链路，并覆盖 1440×900、390×844、320×568、键盘、reduced-motion 与 overflow。本轮不把 deterministic fake 当真实 ASR 结论，只用于验证 ASR 降级不阻塞原始录音与页面纵向链路。

## 17. Repeat Interview V1 验收矩阵

| 类别 | 必须证明 | 失败条件 |
|---|---|---|
| Home action | ordinary project 仅在权威 projection eligible 时显示“开始下一次访谈”；restricted/deleted/assignment invalid/字段缺失均失败关闭 | 前端按 status 或 session 数量猜测；受限投影泄露 action/basis |
| 动作职责 | project 卡创建下一次；session 行只继续/回顾；全局“新建访谈”仍创建新 project；desktop/mobile 共用同一 HomeShell | 任一入口职责混淆、复制 shell/routes/styles |
| sequence/idempotency | 旧 `POST /projects/:id/sessions` 仅能并发产生一个 seq1且同 ID replay；已有任一 session 后必须 `NEXT_SESSION_REQUIRED`。project P 的 seq1 completed→并发点击/刷新/未知响应只产生一个 P/seq2；同 ID replay 返回同响应；不同 ID 并发输家稳定冲突并指向服务端返回的当前 session | 旧 endpoint 绕过创建 seq2+；本地按 basis+1/前 100 行猜 session；创建 seq3、重复 audit、payload 漂移被接受 |
| 跨项目 | P 的 basis/session/memory/actual-question 不能用于 Q；path project 是唯一 scope | body 改写 project 或跨项目 membership |
| 新采集 | seq2 新 MediaStream/audio object/capture generation/audio stream/provider voice/speaker stream/calibration/local job；重新 mic/device/start gate | 复用 seq1 capture/runtime/label/mapping/cursor |
| post-analysis 成功 | seq1 completed 后稳定 trigger 恰好各执行一次 memory extract 与 actual reconcile；current resolution、published actual asked 证据可追溯 | GET/render 重触发、displayed 冒充 actual asked、无 production caller 却宣称成功 |
| post-analysis 失败 | 任一 lane provider/Schema/policy 失败时 completed/review/next session/raw recording 不倒退；`unjudged|failed|cancelled|unavailable` 如实成为 terminal，保留 retry evidence且不等待无限 retry | AI 失败阻塞创建/录音，隐藏失败为 succeeded，或 opening 永久等待失败 lane |
| analysis-first | basis 两 lane 先 terminal，之后 seq2 calibration terminal；后一个事件冻结包含所有 eligible succeeded output 的 Context并创建恰好一个 opening attempt | 未触发、重复触发或漏掉已成功 basis output |
| calibration-first | seq2 calibration 先 terminal，而 basis 任一 lane `not_started|pending|running`；投影 `waiting_basis_analysis` 且 request/attempt 均 null，录音继续；最后 lane terminal 后才创建一个 opening attempt | waiting 阶段已建 job/attempt、消费 gate或冻结缺资料 Context |
| 单 lane 降级 | memory succeeded + actual unjudged/failed/cancelled/unavailable，以及 actual succeeded + memory failed/cancelled/unavailable；两 lane terminal 后各只触发一次，Context纳入成功 lane eligible output并记录另一 lane outcome | 降级被伪装成功、无限等待/重试、丢掉成功 lane或阻塞录音 |
| Context membership | seq2 opening 只含 P 的 eligible current memory、published reliable actual asked、边界与可信 membership；snapshot 绑定 basis session/analysis trigger 与两 lane terminal outcomes；retention/deletion/revision 漂移取消写回 | stale、unpublished、unjudged、跨项目、校准正文进入 Context，或成功 output 因抢跑永久漏掉 |
| opening exact once | seq2 calibration confirmed/failed/skipped 与 no-attempt provider-unavailable 各形成稳定 gate identity；再绑定 basis analysis trigger。calibration/lane 事件、刷新、启动恢复、WS replay、provider 恢复、重复 snapshot 和并发协调均汇聚同一 request/trigger，最多一个 attempt | 同一组合多 opening；waiting 消费 gate；降级 gate 伪造当前 trusted role；前置均 terminal 后无 opening |
| 刷新/重放并发 | calibration terminal 与最后 lane terminal 并发、任一 ACK 丢失、页面刷新、服务重启和事件重复投递；持久状态重算后同 request replay，零或一个 waiting projection、恰好一个 committed attempt | GET/render 触发、两个 request/attempt、lost wakeup 或 response replay 改写 basis |
| terminal 后显式 retry | opening 已基于某 lane 的 failed/unavailable terminal 提交后，该 lane 显式 retry 成功；同一 consumer session 不重开 gate，新证据仅按 future-eligibility 供后续 session 使用 | 第二个 opening attempt、覆盖已冻结 Context 或把旧失败隐藏为首次成功 |
| provider unavailable | 真实 LLM/ASR unavailable 时仍可创建、录音、安全结束和回顾；opening/analysis 诚实 unavailable/failed/unjudged | fallback 假装真实 AI、录音被停止或 completed 回退 |
| 安全/retention/drift | assignment、consent、restricted/deleted、deletion、retention、speaker/memory/actual revision 在 freeze/call/writeback/display 各阶段失败关闭 | 旧 Home/幂等/cache 恢复权限或展示失效内容 |
| UI 范围 | 不新增 memory/summary/pending-confirmation 页面；“回顾上次对话”只指系统继承和既有 session review | 偷增记忆管理 UI 或第二 AI/history |
| A4 分界 | DEV-008B1/B2 开工前 PR #44 已在新的 exact head 获项目负责人 PASS 且 merge；本 SPEC 不改 A4 completion/review/unknown-response UI | 在 A4 未接收时并发修改 Home/routes/styles/finish UI |

必须覆盖 1440×900、390×844、320×568，确认同一 HomeShell、44px、focus-visible、aria-live、reduced-motion 与无页面级横向溢出。实现门禁还包括 format/lint/typecheck/unit、fresh PostgreSQL migration deploy/status、integration、auth、build、smoke、ordinary Chromium 与 auth Chromium。本 SPEC 的 docs/shared-contract CI 通过不代表 runtime、真实 provider 或上述矩阵通过。

## 18. Continuing Consent V1 验收矩阵

| 类别 | 必须证明 | 失败条件 |
|---|---|---|
| 首次正式授权 | 首次 session 在当前页 mic/input 通过后显示完整正式文本，录制 `recorded_verbal`，音频 complete/verified 后只追加一条同版本 valid consent；文本明确覆盖同 project 当前及未来计划内访谈与随时暂停/停止/撤回 | mic 前录授权；文本未覆盖未来；无完整/匹配音频仍 valid；覆盖原始记录 |
| 普通后续复用 | 同 project seq1 completed、current consent covered 时 seq2 不显示完整授权页、不创建 consent audio/record；仅显示轻提醒后 start | 每 session 重录授权、追加 consent，或跨 project 复用 |
| action 优先级 | assignment/deletion/restriction 隐藏优先，其后固定为 access/project unavailable → session in progress → no completed → consent unavailable → reauthorization → eligible；每个组合只产生一个 primary action | 同一事实组合因检查顺序、刷新或 endpoint 不同返回不同 primary action/reason |
| session + reauthorization | 任一非终态 session 与 reauthorization 同时存在时，GET 只能为 `session_in_progress + primary_action=null + session basis/next 全 null`，嵌套 continuation 可如实 reauthorization；next-session 只按 session conflict 失败 | 暴露/执行 `record_formal_consent`、创建下一 session，或用 consent conflict 覆盖正在进行的 session |
| no completed + reauthorization | 无 canonical completed 且 reauthorization 同时成立时，只能 `no_completed_session + primary_action=null`；完成/修正 session 集合后重新计算 | 提前暴露 reauthorization action，或携带伪造 basis/next |
| DTO 判别联合 | 类型契约接受 covered/reminder、missing/null basis、existing-record reauth/non-null basis、unavailable/all-null；机械拒绝任一 status/reason/basis/version/action 矛盾组合 | 宽接口允许 covered+record action、missing+旧 basis、unavailable+版本或 eligible+reauth continuation |
| repeat/consent 交叉 | eligible 只能搭配 covered；reauthorization action 只能搭配 reauthorization；consent unavailable 只能搭配 unavailable；project/access unavailable continuation=null；session/no-completed 可携带合法 continuation 但 action 永远 null | primary reason/action 与嵌套 continuation 冲突仍能通过 shared contract typecheck |
| 轻提醒不是 consent | snapshot 逐值返回版本/text/“开始访谈”/两个 boolean；客户端原样展示并显式点击；start、双击、replay、刷新都保持 consent row/audio count 不变 | 点击开始、device check 或 reminder acknowledgement 被记录成新 consent/再次同意 |
| next-session 动态门禁 | 请求锁内重查 actor、assignment、project restriction/deletion 与 consent compatibility；旧 eligible Home 在任一事实变化后零 session 副作用失败 | 旧 Home/cache/idempotency 快照恢复权限或创建 seq2 |
| start 动态门禁 | start 前重查同一安全事实、device-check 和 reminder version；漂移时零 interview audio/capture/ASR/AI/consent 副作用 | 先创建 audio/generation 再报错，或旧 reminder 仍可开始 |
| 撤回/过期 | revoked 或明确 expired 不回退旧 valid record，要求完整新授权；revoked 继续触发 restricted/停止新处理 | 回退旧授权、轻提醒修复 revoked/expired、继续新录音/AI |
| 文本版本与音频 | 同版本只满足版本维度且仍检查 scope metadata；跨版本只接受服务端显式有向 allowlist 且当前范围是原范围子集；新版本必须新录匹配全文，旧 audio 跨版本关联稳定冲突 | 客户端判断兼容、反向/缺失映射被接受、同 audio 对应多个版本 |
| 既有文本未知范围 | 示例 `mvp-v1` 或现存 valid record 在独立 SPEC-CONSENT-TEXT-POLICY-001 未交付已审查正文与 `future_planned_interviews_covered=true` machine metadata 前，真实路径失败关闭；同版本字符串不能单独形成 covered | 猜测旧正文覆盖未来、用 valid/exact-version 绕过 scope 审查，或把 Agent fixture 当真实政策 |
| 范围扩大 | purpose、access subjects、provider region、public/training use 任一扩大，或旧文本未覆盖未来访谈，均 reason 精确且要求完整重授权 | 配置/隐私政策/轻提醒静默扩大；模糊归类 covered |
| 时间间隔 | 同 project、范围未变、consent covered，跨普通时间间隔仍复用；start 每次重查并提醒 | 仅因间隔重授权，或因历史 covered 跳过当前重查 |
| 不同设备 | 新设备/刷新只清除本页 mic/device 事实并重新检查；不新建 consent；start 返回当前 reminder | 复用旧 device passed，或设备变化触发/伪造 consent |
| 不同倾听员 | 新 actor 只有 active 且 assignment 有效才可继续；更换倾听员本身不重授权，旧 actor/created_by 不授予访问 | 合法换人被强制重授权，或无 assignment 借原 consent 进入 |
| 跨 session 隔离 | consent basis 为 project 级；每 session 的 start/idempotency/audio/capture 独立，reminder 每次显示；seqN start 不改 consent 历史 | 复用上次 start/audio/runtime，或 session reminder 只显示一次后永久跳过 |
| 刷新与幂等 | 提醒前/后刷新重新取得权威 snapshot并再显式点击；同 start ID/payload replay 逐值一致，变更 reminder version 为 payload mismatch；至多一个 recording | 本地缓存旧文案，未知响应换 ID，重复 recording/audit/consent |
| policy unavailable | policy/compatibility/current-consent reader 不可用时 projection unavailable、无动作、start/next-session 失败关闭 | 默认 covered、客户端猜版本、以错误为新 consent |
| ServiceTerm/范围 | ServiceTerm 仍 dormant；无页面/API/DB/runtime/ASR/LLM/删除扩张由本 SPEC 宣称完成 | 重新引入普通 ServiceTerm 或用 docs/shared-contract CI 宣称 runtime 通过 |

本 SPEC 验证只覆盖 docs/shared-contract 一致性；`packages/contracts/src/continuing-consent.contract-test.ts` 是编译期契约测试，必须随 typecheck 机械拒绝上述矛盾组合。DEV-008B1/B2 在本 SPEC 项目负责人 exact-head PASS、merge 与治理收口前保持 BLOCKED；该条件现已满足，但只使 B1 进入 fail-closed implementation-ready，B2 仍等待 B1 runtime。此外，真实 `covered` happy path 与 B1 DONE/端到端交付必须等待独立 SPEC-CONSENT-TEXT-POLICY-001 正式接收。任何未来 local/test fixture 只能是显式虚构演示，且不得解除真实路径的 fail-closed。实现阶段必须把本矩阵与 §17 全量门禁一并执行。
