# 09｜测试与验收规范

## 1. 目标

本文件定义 MVP 的测试范围、验收指标和发布门禁。

开发人员完成代码不代表任务完成。任务必须通过与风险相称的对应验证后才能关闭。

验收责任按风险分级：

- 文档整理、格式修复、简单配置和局部低风险修改，在不改变产品范围、业务规则或公共契约时，可由总控 Agent 自检关闭，但必须记录实际检查和证据；
- 涉及核心架构、关键业务规则、权限、安全与隐私、状态机、核心数据模型、跨模块契约或大规模合并时，必须由独立审查或验收 Agent 给出结论；
- MVP 进入真实试点前始终必须经过独立验收，并满足第 13 节发布门禁；
- 需要独立审查时，实现者不得代替审查者宣布通过。

## 2. 测试层级

- 单元测试；
- 模块集成测试；
- API 契约测试；
- WebSocket 契约测试；
- 端到端测试；
- 长时间录音测试；
- 故障注入测试；
- 安全和权限测试；
- AI 离线评测；
- 真实访谈试点。

## 3. 测试数据原则

1. 自动测试使用虚构或脱敏数据。
2. 不把真实访谈数据提交到仓库。
3. 测试录音使用明确授权或人工制作音频。
4. 固定测试数据可重复加载和重置。
5. 测试中的供应商调用应支持 stub。
6. 真实供应商集成测试单独标记。
7. 测试身份至少包含：
   - 倾听员 A；
   - 倾听员 B；
   - 管理员；
   - 数据管理员；
   - 无权限用户。

## 4. 单元测试

必须覆盖：

- 项目状态机；
- 会话状态机；
- 授权门禁；
- 服务时长计算；
- 超时提醒；
- 音频分片幂等；
- WebSocket 事件去重；
- JSON Schema 校验；
- 上下文构建；
- 记忆冲突处理；
- 人工确认优先级；
- 重复问题判断；
- 权限控制；
- 导出过滤；
- 删除流程幂等。

## 5. 集成测试

### 5.1 主链路

```text
浏览器采集
→ 音频分片上传
→ 业务 WebSocket
→ ASR
→ 确定态转录落库
→ AI 记录员
→ 结构化记忆
→ AI 采访导演
→ 问题展示
→ 会话结束
→ 导出
```

### 5.2 接口契约

验证：

- REST 路径和状态码；
- DTO 校验；
- 公共错误格式；
- WebSocket 事件字段；
- Schema 版本；
- 幂等请求；
- 枚举一致性；
- 权限失败；
- 状态前置失败。

## 6. 音频和 ASR 测试

场景：

1. 连续录音 60 分钟；
2. 录音 180 分钟边界测试；
3. 麦克风权限拒绝；
4. 无输入设备；
5. ASR 中途断开；
6. 网络中断后恢复；
7. 分片上传失败和重试；
8. 同一分片重复上传；
9. 浏览器刷新；
10. 浏览器崩溃后恢复；
11. 结束时最后确定态；
12. 说话人校准；
13. 单段修正；
14. 批量说话人修正；
15. 补转录。

验收：

- ASR 失败不影响原始录音；
- 正常链路无分片缺失；
- 分片重复上传不重复保存；
- 中间态不进入正式 AI 链路；
- 会话结束能生成完整 manifest；
- 缺失范围能够检测并显示。

## 7. AI 评测

### 7.1 评测集

不少于 30 段脱敏或虚构中文访谈。

覆盖：

- 没什么可讲；
- 冷场；
- 重复；
- 时间模糊；
- 人物混乱；
- 转折；
- 冲突；
- 明确拒绝；
- 敏感话题；
- 口音和方言；
- ASR 错字；
- 事实冲突；
- 连续讲述不应打断；
- 已问过类似问题。

### 7.2 人工标注

每段标注：

- 可接受追问；
- 不可接受追问；
- 应继续倾听；
- 风险等级；
- 应提取人物和事件；
- 应保留原话；
- 应识别边界；
- 允许的时间线结论。

### 7.3 指标

| 指标 | 首轮目标 |
|---|---:|
| 有用或修改后可用问题 | ≥ 60% |
| 重复问题率 | < 10% |
| 高风险不当追问率 | < 5% |
| JSON 解析成功率 | ≥ 98% |
| 证据回链正确率 | ≥ 95% |
| 明确拒绝边界识别率 | ≥ 90% |
| AI 目标返回时间 | ≤ 8 秒 |

### 7.4 提示词发布门禁

提示词或模型变更必须：

1. 固定评测集重跑；
2. 与当前生产版本对比；
3. 不降低边界安全指标；
4. 记录模型、提示词和 Schema 版本；
5. 通过人工抽查；
6. 才能进入预发布。

## 8. 权限和安全测试

验证：

- 倾听员 A 无法查看 B 的项目；
- 管理员默认不能直接下载访谈资料；
- 无有效授权不能开始；
- 授权撤回后 AI 停止；
- 受限内容不进入普通摘要；
- 受限内容不进入普通导出；
- 下载地址过期；
- 对象存储非公开；
- 日志不含完整转录；
- API 密钥不出现在前端包；
- 删除操作幂等；
- 审计记录完整。

## 9. 性能测试

| 项目 | 目标 |
|---|---:|
| 中间态上屏 | ≤ 2 秒 |
| 确定态落库 | ≤ 1 秒 |
| AI 建议 | 目标 ≤ 8 秒 |
| 普通交互反馈 | ≤ 200 毫秒 |
| 同时会话 | 5 |
| 60 分钟浏览器内存 | 不持续线性增长 |

测试报告需包含设备、浏览器、网络和供应商环境。

## 10. 故障注入

至少模拟：

- ASR 连接中断；
- LLM 超时；
- LLM 返回非法 JSON；
- 对象存储失败；
- 数据库短时不可用；
- Redis 不可用；
- WebSocket 重连；
- 重复结束请求；
- 重复分片；
- 导出任务失败；
- 删除任务中断。

验证系统是否按专项文档降级，而不是只验证错误消息。

## 11. 端到端验收场景

### 场景 A：首次访谈

1. 创建项目；
2. 填写服务信息；
3. 完成口头授权；
4. 设备检查；
5. 说话人校准；
6. 访谈 30 分钟；
7. 查看 AI 问题；
8. 标记重要原话；
9. 标记不再追问；
10. 结束；
11. 查看工作记录；
12. 导出。

### 场景 B：第二次访谈

1. 进入同一项目；
2. 查看上次摘要和待确认；
3. 重新校准说话人；
4. 继续访谈；
5. AI 能读取项目记忆；
6. 不重复推荐已拒绝话题；
7. 结束并导出全部资料。

### 场景 C：ASR 故障

1. 正常开始；
2. ASR 中断；
3. 原始录音继续；
4. 页面显示降级；
5. 结束后补转录；
6. 数据可导出。

### 场景 D：授权撤回

1. 已有项目和访谈；
2. 撤回授权；
3. 项目变受限；
4. AI 调用停止；
5. 普通导出阻止；
6. 创建删除申请；
7. 完成清理和审计。

## 12. MVP 功能验收清单

1. 仅填写称呼即可建项目；
2. 未确认费用和授权不能开始；
3. 一小时连续录音成功；
4. 实时转录显示；
5. 确定态可靠保存；
6. ASR 失败时录音继续；
7. AI 失败时录音和转录正常；
8. 可修正说话人；
9. 原始角色和文本保留；
10. AI 显示推荐原因；
11. AI 关联证据；
12. 可采用、忽略、稍后、改写；
13. 可标记重要原话；
14. 可标记禁止使用；
15. 已拒绝话题不再自动推荐；
16. 两次访谈共享项目记忆；
17. 可完整导出；
18. 关键操作可审计；
19. 权限隔离有效；
20. 删除流程可验证。

## 13. 发布门禁

进入真实试点前必须同时满足：

- 类型检查通过；
- 静态检查通过；
- 单元测试通过；
- 集成测试通过；
- E2E 主链路通过；
- 60 分钟录音测试通过；
- 故障降级通过；
- 权限和导出安全通过；
- AI 指标达到门槛；
- 无 P0、P1 未解决缺陷；
- 数据迁移已验证；
- 回滚方案明确；
- 文档与实现一致；
- 独立验收 Agent 签署通过。

## 14. 缺陷等级

### P0

- 原始录音丢失；
- 未授权仍可开始；
- 越权访问访谈资料；
- 受限内容进入普通导出；
- 密钥泄露；
- 删除误删其他项目。

### P1

- ASR 故障导致会话无法安全结束；
- 原始转录被覆盖；
- 已拒绝话题仍持续推荐；
- 两次访谈记忆错用；
- 导出严重缺失；
- 状态机无法恢复。

### P2

- 页面显示错误；
- 建议反馈未保存；
- 部分非关键统计错误；
- 可绕过的交互问题。

P0、P1 必须在试点前清零。
