# 01｜产品需求文档

## 1. 产品定位

本产品是一套面向长者人生故事访谈的 AI 辅助倾听工具。

产品不由 AI 独立采访长者，也不在 MVP 阶段自动生成书籍。

在访谈中：

- 倾听员负责建立信任；
- 倾听员负责理解现场情绪；
- 倾听员决定是否追问；
- 系统负责录音和转录；
- AI 负责保存上下文和发现故事线索；
- AI 结合访谈阶段、当前确定态对话、后台记忆、实际已问与近期展示历史，参考经审核的基础/深入题库，生成当前最合适的一个下一问；题库是访谈方法与内容灵感，不是问题正文白名单。

最终产出是一套完整、可追溯、方便后期人工整理的访谈资料。

## 2. 核心用户

### 2.1 倾听员

系统直接使用者。

主要任务：

- 创建长者档案；
- 说明服务时长、收费和隐私规则；
- 发起访谈；
- 查看实时转录；
- 查看 AI 追问建议；
- 自行决定是否把建议作为当场追问；
- 标记重要或敏感内容；
- 结束访谈；
- 导出资料。

倾听员不要求具备专业记者或传记作家的完整能力。

### 2.2 长者

故事讲述者，不直接操作系统。

长者可以：

- 拒绝回答；
- 停止当前话题；
- 暂停或停止录音；
- 要求某段内容不用于后期整理；
- 提出删除请求；
- 撤回后续处理授权。

### 2.3 管理员

MVP 中负责：

- 账号启停；
- ASR、LLM 和对象存储配置；
- 数据保存策略；
- 系统状态和错误日志；
- 删除申请处理。

管理员不参与具体访谈判断。

## 3. 用户问题

### 3.1 倾听员的问题

- 不知道怎样让长者开始讲述；
- 无法同时倾听、记录和思考；
- 容易错过转折、冲突和重要细节；
- 难以记住多次访谈中的人物和事件；
- 容易重复已经问过的问题；
- 不知道何时应该追问，何时应该继续倾听；
- 难以及时发现时间线矛盾。

### 3.2 长者的问题

- 认为自己没有值得讲的故事；
- 回忆顺序混乱；
- 经常重复同一件事；
- 难以主动回忆人物、场景和原因；
- 不希望被机械地审问；
- 需要保留随时拒绝和停止的权利。

## 4. MVP 核心验证命题

> 在只掌握少量基础信息的情况下，AI 是否能够随着访谈持续进行，帮助普通倾听员完成更加深入、连贯和少遗漏的人生故事访谈。

重点验证：

1. 录音和转录是否稳定；
2. AI 是否能记住人物、时间、地点和事件；
3. AI 是否能生成与当前故事紧密相关的追问；
4. AI 是否能减少重复提问；
5. 系统是否尊重长者明确表达的边界；
6. 多次访谈是否能延续上下文；
7. 已结束访谈是否能在网页中完成最小回顾，并让倾听员理解及管理当前浏览器的录音副本而不误解服务器留存。

## 5. 产品原则

### 5.1 人主导，AI 辅助

AI 不直接向长者发问。所有建议都由倾听员判断是否使用。

### 5.2 访谈前信息最小化

开始前不要求掌握完整家庭、教育、工作和人生经历。

只收集：

- 姓名或称呼；
- 可选的出生年份、籍贯和当前居住地；
- 服务时长与收费说明；
- 录音、转录和 AI 分析授权。

### 5.3 原始资料优先

AI 不能覆盖：

- 原始录音；
- 原始转录；
- 原始说话人识别；
- 原始授权记录。

### 5.4 AI 结论可追溯

人物、事件、冲突、原话和问题建议必须关联原始转录片段。

### 5.5 不强行显得智能

没有合适问题时，AI 可以返回“继续倾听”。

### 5.6 当前不自动写书

传记整理先由人完成。后续再根据真实工作流逐步自动化。

### 5.7 说话人角色必须经过人工确认

- 原子 start 先建立正式录音与正式 ASR 流，随后在同一正式流内立即完成简短说话人校准；校准不是开始录音的硬门禁；
- 供应商自动给出的角色只是不可信候选。只有倾听员确认的校准映射或人工角色修正，才能成为角色相关长期记忆、已问问题识别和问题建议上下文的可信依据；
- 校准失败、跳过或新流尚未重新确认时，录音与转录继续，片段按 `unknown` 的消费边界处理；不得为提高标签完整率停止或回滚原始录音；
- 固定校准句保留为原始录音和原始转录证据，但不进入故事记忆、真实已问问题、普通摘要或普通 AI 上下文；
- 人工修正不得覆盖原始角色。修正后只使受影响证据的 AI 派生结果失效并等待对应模块重算，不能自动解除人工确认的敏感、限制、禁止追问或删除边界。

### 5.8 第一版记忆只服务问题，不建设记忆管理界面

- 第一版结构化记忆的首要目标是改善“当前下一问”和第二次访谈开场，不追求完整人生知识图谱；
- 最小记忆只保留会改变问题或安全边界的人物与关系、地点、事件、时间/时间范围、重要选择与原因线索、未讲完故事，以及后台冲突/未知状态；
- 可信长者 final 产生的自动记忆无需人工确认即可作为当前后台记忆使用，但普通界面不展示候选、置信度、记忆清单或版本管理；服务端仍必须保存证据、版本和失效关系；
- 明显冲突通过自然澄清问题处理；长者明确更正后，新说法成为未来生成和跨会话继承的当前值，旧证据保留但不再作为当前事实；仍不确定时保留大致范围或 unknown，不强行选值；
- 已展示问题是一次不可变屏幕快照。说话人/文字/普通记忆修正、普通冲突或单独 `sensitive` 不自动撤下该快照，也不自动触发 AI 重算；但旧结果立即失去未来生成与继承资格；
- `restricted`、`do_not_ask`、活动 deletion scope、授权或访问权限失效是硬安全边界，必须立即隐藏当前问题正文，只显示中性的“继续倾听”或“AI 暂不可用”，且不自动生成替代问题；
- 倾听员拥有最高现场判断权，但不能替代软件自身的权限、禁问和删除义务。

### 5.9 先形成可试用旅程，再用真实倾听实践迭代

- 第一版先验证陌生倾听员能否帮助长者从低压力破冰、生平轮廓逐步进入具体故事，不要求项目负责人在缺少真实实践时预先描述理想全过程；
- 访谈阶段不是按固定题数或固定时间推进的脚本。系统根据表达意愿、回答具体度、当前人物/事件线索、话题延展性和安全事实在阶段之间可进可退；
- 第一版保留两类问题参考库：`basic` 服务破冰和生平轮廓，`deep` 服务人物、事件、选择、转折、情感与未讲完故事；它们帮助模型理解可用的访谈方向，不限定最终问题必须来自某道原题；
- AI 可以原样采用、广泛改写题库参考，也可以完全根据可信转录和 DEV-006 当前记忆生成题库未覆盖的问题。具体人物、事件、时间或因果前提仍必须有可信依据；上下文不足、长者正在连续讲述或安全事实不允许提问时返回“继续倾听”；
- 问题生成不得修改题库、原始/修订转录、memory current view、actual-question catalog、授权或边界等源事实；它只能追加自己的 generation attempt、候选、展示快照、历史、幂等与过程记录；
- 题库由负责人通过受控 CSV 模板维护和交换，系统导入后形成版本化运行时事实；负责人不直接编辑数据库；
- 纯工程 demo 可以使用极少量、明确标记的虚构 fixture。fixture 不属于产品内容，正式内部试用前必须导入项目负责人提供并完成来源/许可核验的题库版本。

## 6. MVP 范围

### 6.1 包含

- 长者基础档案；
- 服务时长和收费规则记录；
- 授权记录；
- 原始录音保存；
- 实时转录；
- 双人说话人区分和人工修正；
- 原始转录与修订文本分离；
- 项目级结构化记忆；
- 同时只展示一个当前最佳 AI 追问及一句推荐原因，无合适问题时建议继续倾听；
- 当系统判断出现了更合适且仍具未来资格的问题时，可以自动替换当前问题；每次真正展示过的问题都必须保留在本次会话的展示历史中；
- 倾听员可用“下一个问题”主动请求新问题，系统在当前会话排除当前问题和高度相似问题；“上一个问题”只浏览展示历史，不生成新问题、不改变当前问题，也不代表该问题已经实际问出；
- 重要原话、敏感、禁止使用、待确认标记；
- 多次访谈上下文继承；
- 后台自动记忆直接辅助当前问题和第二次访谈开场，不增加记忆确认 UI；
- 会后从可信倾听员 final 整理全部实际问出的问题，并与系统曾展示/换掉的问题建立可追溯匹配；
- 题库版本、可选参考、事实 grounding、访谈阶段与模型生成过程可追溯；
- 会话工作记录；
- 统一的倾听员网页工作区：登录后可新建访谈、查看已有项目与访谈、继续未完成访谈，并进入已结束访谈的最小回顾；
- 已结束访谈的录音播放、原始/修订转录查看、本机浏览器录音副本事实与本机副本删除；
- 审计和正式服务器隐私删除流程（由独立后续任务实现）。

第一版不新增记忆列表、冲突列表、会后确认页或第二次访谈前回顾页。会后整理只在后台工作；第二次访谈直接给出一个基于当前有效记忆和实际问过问题的开场问题。

### 6.2 不包含

- 支付、退款、发票；
- 完整 CRM；
- 老照片和档案管理；
- AI 直接语音采访；
- 自动传记写作；
- 文学化润色；
- 自动章节编排；
- 家属在线审稿；
- 电子书和印刷 PDF；
- 人生树展示；
- 完整人物关系图；
- 派单和平台撮合。
- 倾听员端项目资料导出；如未来其他角色确需导出，必须另建受控任务，不进入本 SPEC；
- PWA、Service Worker 离线应用、Capacitor/WebView 封装或 Android App。当前只交付响应式网页，并以 Android Chrome 等现代浏览器作为目标环境；未来 Android 软件只能在网页 MVP 经真实实践验证后作为独立产品阶段重新设计。

## 7. 核心用户旅程

```text
新建长者项目
  ↓
确认服务时长与费用说明
  ↓
完成录音、转录和 AI 分析授权
  ↓
设备检查
  ↓
原子开始正式录音与正式 ASR
  ↓
同一正式流内完成简短说话人校准
  ↓
实时录音、转录和 AI 追问辅助
  ↓
人工标记重要、敏感和待确认内容
  ↓
结束访谈
  ↓
生成会话工作记录
  ↓
返回倾听员工作区，继续未完成访谈或查看已结束访谈
```

详细业务规则见 `03-业务流程与交互规范.md`。

## 8. 页面范围

### 8.1 当前优先切片：首次访谈最小纵向闭环

已完成的 DEV-005 首轮内部验证曾从已分配、已预创建的单个虚构项目或会话深链进入；该历史边界继续保留，但不再是倾听员长期使用入口。当前网页 MVP 的统一路径为：

```text
登录
  ↓
倾听员工作区（新建访谈、已有项目/访谈、继续、回顾）
  ↓
新建访谈时：最低项目信息 → 服务说明 → 正式有效授权
  ↓
首次访谈准备页
  ↓
访谈工作台
  ↓
访谈结束页
```

准备页展示：

- 长者称呼；
- 本次预计时长；
- 简短访谈说明；
- 当前服务告知和正式授权状态；
- 麦克风与输入状态；
- 主操作“开始访谈”。

页面确认不能替代版本化授权文本、授权证据和服务端开始门禁。无有效授权、无项目分配或状态不允许时不得开始。

工作台以转录为主：

- 顶部窄状态栏显示长者称呼、已进行时长、录音/转录状态和结束入口；
- start 后先在同一工作台显示“正在录音 · 正在确认说话人”，以两句短校准内容和一次角色确认完成低负担校准；成功后显示“说话人已确认”，失败或跳过后持续显示“说话人待确认”并提供重试；
- 主区域约占可用空间的 80%，按时间、说话人和文本显示实时转录；
- 底部固定一个 AI 建议卡，只显示一个当前问题和一句推荐原因，或显示“建议继续倾听”；
- 当前问题提供“上一个问题”和“下一个问题”；浏览历史时提供“回到当前问题”，不要求倾听员登记采用、已问、忽略、稍后或改写；
- AI 仍读取真实转录中的倾听员问题，避免重复推荐。
- 自动记忆、冲突和会后问题整理全部在后台运行；访谈中只显示一个当前问题、继续倾听或 AI 暂不可用，不显示记忆清单和问题分类清单。

本轮正式工作台按业务状态而不是一套固定比例分配注意力：

- 正常录制桌面以顶部约 8%、转录约 79%、建议约 13% 为视觉基准；390×844 手机以约 9%/73%/18% 为基准，均允许约 ±3 个百分点；实现使用受控顶部/底部与中间自适应空间，不硬编码视口百分比；
- 320×568 兜底时顶部不高于 72px，活动建议不高于 120px，转录仍是最大连续区域且不低于可用高度的 60%；“继续倾听”或建议不可用时建议区收缩到约 64–72px；
- `interrupted`、安全保存中和终态不强行维持转录 80%；页面重心改为中断处置、保存事实或结果与下一步，只读转录保留为上下文；
- 顶部只常驻长者称呼、时长、一句录音安全摘要和结束入口；采集、本浏览器备份、管理服务录音/manifest、转录、session 五类事实在“保存状态”中分来源展开，关键异常自动提升但不抢键盘焦点。

手机是完整访谈主设备，不是只读或应急入口。首轮正式移动平台为 Android Chrome，必须覆盖准备、录制、实时转录/建议、中断恢复与安全结束；iPhone Safari 明确延期，本轮不得笼统宣称支持所有手机。准备页桌面/平板横屏约按 40% 说明、60% 准备状态与主动作分配横向视觉权重；平板竖屏和手机按任务顺序单列，主动作不长期悬浮遮挡错误说明。

所有设备采用高密度转录行：左侧窄栏显示说话人与时间，右侧显示正文；手机左栏约 52–64px，不改成聊天气泡或把元数据堆到正文上方。“说话人待确认”可视觉缩写为“待确认”，但无障碍名称保留全文。确定态不逐条重复标签，只有中间态显示“识别中”。

结束状态仍位于同一工作台 URL，展示长者称呼、实际时长、录音/转录处理状态、“查看保存明细”和“完成并离开”。在服务端真正收束前必须显示 `stopping`、`processing` 或失败状态，不得提前宣称已保存完成。状态面板的最小化入口在 `stopping` 灰置，在 `processing` 后启用；收起后保留持续状态条和重新展开入口，刷新后默认展开。只允许查看当前已加载的只读转录，不借此新增完整回顾页。

### 8.2 当前切片：统一倾听员网页工作区与最小回顾

同一个 authenticated home/app shell 承载以下入口和状态，不另建平行首页、回顾导航或视觉体系：

- 主动作“新建访谈”；
- 已有项目与访谈列表；
- 未完成访谈的“继续访谈”；
- 已结束访谈的“查看回顾”；
- 空态、加载态、错误态和受限态；
- 退出登录。

登录后不得长期依赖预创建项目深链。普通列表与详情只显示当前用户经有效 assignment 可访问且未受限、未删除的资源。仍有有效 assignment 的 `restricted` 项目只在首页显示一条无正文、无会话事实、无主动作的固定中性受限占位；普通详情与 prepare/workbench/review 深链继续失败关闭。`deleted`、软删除、assignment 失效和无权限完全不可见，不以占位或空字段泄露存在性。

#### 项目与访谈列表

展示：

- 长者称呼；
- 项目状态；
- 授权状态；
- 最近访谈时间；
- 访谈次数；
- 累计时长；
- 最近操作人；
- 每次访谈的状态与唯一主动作按完整生命周期投影：准备、返回访谈、中断处理、保存进度、只读回顾或保存事实；`processing` 不冒充继续访谈，`failed` 按服务端保存事实分流，不允许同一行出现互相冲突的主动作。

#### 新建访谈纵向入口

“新建访谈”不是只创建一个 `draft` project。完整最小纵向入口包含：

1. 基础信息；
2. 完成服务说明；
3. 取得正式有效授权；
4. 进入准备页完成设备检查；
5. 由现有服务端 start 门禁原子开始访谈。

该路径复用正式项目、service term、consent、session 和 start 契约，不提供绕过授权的生产捷径。`electronic|written` 等虚构 fixture 只能用于 local/test，不进入普通倾听员 UI；普通正式 UI 的 MVP 授权继续使用已冻结的口头授权证据。

#### 已结束访谈的最小回顾

包含：

- 从本 origin 的 IndexedDB archive 播放本机录音副本；
- 只读查看原始转录与已有修订转录；
- 显示本机副本的存在、完整性、字节数和 origin 级近似容量事实；
- “删除此设备上的录音副本”，并在确认前后明确服务器录音、转录和记忆仍保留。

本机 archive 不是文件管理器里的普通文件，不是跨设备档案，也不是长期权威副本；服务端完整 audio object/manifest 才是长期权威事实。本机副本删除不是正式隐私删除，不能更改服务端 session、audio、transcript、memory、marker、audit 或 deletion request。浏览器 archive 缺失时，本切片没有服务端音频下载/流式播放能力，不得伪造可播放状态。

现有题库、AI 问题历史、完整记忆/工作记录、文本编辑、复杂标记、批量说话人修正和任何导出都不是本切片前置，也不进入当前范围。正式服务器隐私删除与统一 deletion scope guard 由 `DEV-008D` 独立实现。

#### 一致的响应式网页体验

工作区、新建入口和回顾复用 `apps/web/src/styles.css` 已有 OKLCH tokens、排版、`context-label`、`button--primary|secondary|danger`、`focus-visible`、44px 触控和 reduced-motion 规则。实现可以选择组件名称，但全站只能有一套 HomeShell/ListRow/StatusBadge 等价语义、一套 empty/loading/error 状态和同一词汇表；不得创建第二套 token、按钮、卡片、导航或状态颜色体系。1440×900、390×844、320×568 均须验证无横向或页面级意外溢出、键盘焦点、live region、44×44 触控和 reduced motion。

## 9. 核心产品规则

1. 未完成费用说明和有效授权，不得开始正式访谈。
2. AI 不直接向长者播放或朗读问题。
3. 只有确定态转录才能进入长期记忆和 AI 分析。
4. 长者明确拒绝后，相关话题不得再次自动推荐。
5. 人工修正不得覆盖原始转录。
6. 禁止使用内容不得进入普通 AI 上下文和普通导出文件。
7. AI 可以建议问题，也可以建议继续倾听。
8. 超出套餐时长前必须由倾听员再次确认客户同意。
9. 同一长者的多次访谈属于同一个项目。
10. 项目级长期记忆由业务数据库维护，不依赖模型自身会话记忆。
11. 说话人校准只能在原子 start 后的正式 ASR 流中完成；准备页临时麦克风流不能建立正式说话人身份。
12. 新的供应商说话人流必须重新确认，短 `speaker_1` 等 provider label 不得跨流静默继承。
13. 角色相关消费使用“人工修正优先，否则原始角色”的展示值，但还必须同时具备用户确认的可信依据；未确认的 `elder|interviewer` 候选按 `unknown` 失败关闭。
14. 单段说话人修正是默认动作；批量修正必须限定同一正式说话人流、同一 provider label 和用户明确范围，执行前显示影响数量，并保持全成全败。
15. 自动记忆只能来自可信长者 final，并必须回链证据；可信倾听员 final 只用于识别实际问过的问题，unknown 和校准控制片段不得参与角色相关消费。
16. 冲突不自动覆盖；明确更正只改变未来当前值，原始证据继续保留。
17. 已展示问题在普通事实修正后可以继续保留，但不得再用于未来生成或跨会话；硬安全边界命中时必须立即隐藏正文。
18. 只有会后在可信倾听员 final 中实际观察到的问题才进入下一次访谈防重复集合；“明确换掉”和“未观察到问出”不得冒充已经问过。
19. AI 暂不可用时不自动提供基础题、不在后台无限重试；倾听员点击“下一个问题”每次只发起一次新的 AI 请求，录音和转录继续。
20. 正常问题生成将当前已激活、已启用且许可合格的题库条目作为可选参考；`basic` 与 `deep` 不是问题正文白名单，也不是 AI 失败时的静态兜底。
21. 访谈阶段至少区分 `rapport`（破冰）、`life_outline`（生平轮廓）和 `story_depth`（故事深入）；阶段转换必须可解释、可回退，题数或经过时间不能单独触发推进。
22. 模型每次只输出一个主要问题，可以大幅改写参考题或生成题库外问题；问题中的具体人物、事件、时间、关系或因果前提必须回链可信转录片段或 current memory resolution，不能把不确定推断写成事实。
23. 候选问题可以保存零到多个题库参考引用，用于复盘模型看过或声明借鉴了什么；题库引用不是访谈事实证据，也不是候选合法性的必要条件。
24. 公开题库只有在来源和许可已核验后才能引用、改写或导入；未知许可内容不得进入激活版本。
25. 题库条目的 `purpose` 仍是内容治理事实；最终候选自行声明一个受控 purpose 并由输出 Schema 校验，但不要求等于任一参考题的 purpose。
26. 第一版条件规则固定为：适用条件全部满足才可选，任一排除条件命中即不可选，且排除优先；空条件、非法条件和冲突条件必须按正式导入契约处理，不交给模型解释。
27. 旅程阶段按 `journey_policy_v1` 的受控信号和固定优先级确定；安全与不愿展开等保守信号高于具体事件和可深入信号，同一冻结输入必须产生同一阶段与原因码。固定题数和固定时间不得成为推进信号。

## 10. 成功指标

### 10.1 产品指标

- AI 问题展示率；
- “下一个问题”触发率、自动替换率与历史回看率；
- 人工观察或访谈后评价的建议有用率；
- 重复问题率；
- 高风险问题比例；
- 倾听员手动请求问题次数；
- 说话人修正率；
- 转录修正率；
- 单次访谈平均时长。

### 10.2 首轮内部记忆试用

DISC-006 和首个 DEV-006 内部切片采用虚构/脱敏数据的定性试用与完整过程记录，不把问题有用率、重复率或解析率作为开工和通过门槛。必须能够回放“原始片段 → 当时记忆 → 问题 → 实际问法 → 回答/更正 → 跨会话继承 → 版本调整”，并记录人工复盘结论。

下列数值保留为观察项和未来真实模型/真实试点门禁，不因首轮不设阈值而删除：

| 指标 | 目标 |
|---|---:|
| 人工评价为有用的问题 | 不低于 60% |
| 重复问题率 | 低于 10% |
| 高风险不当追问率 | 低于 5% |
| AI 结构化输出解析成功率 | 不低于 98% |
| AI 建议目标返回时间 | 8 秒以内 |

这些是验证目标，不是永久服务承诺。

### 10.3 首轮问题旅程试用

- 纯工程链路可用极少量 `synthetic_fixture` 题目验证 CSV 校验、版本激活、阶段判定、确定性选择和来源回链，但只能运行在 local/test 或明确的 internal demo；
- 正式内部试用必须先导入项目负责人提供的真实题库版本，并证明全部非原创内容已有来源和许可依据；
- 首轮不以固定题数、阶段停留时间或题库规模作为通过门槛；以“阶段判定可复盘、模型实际看过与声明使用的题库参考可分辨、grounding ID 来自本次可信 Context、源事实只读、现有 current/history/actual-question 事实不被破坏”为工程硬门禁。事实前提与 grounding 是否在自然语言上贴切由 Prompt、固定评测集和倾听员实践评估，不在第一版伪装成确定性语义校验；
- 真实倾听实践产生的观察用于后续调整阶段策略、题库内容和选择规则，不反向改写已经展示、实际问过或原始转录事实。

## 11. 后续规划

### 当前 MVP 内部切片顺序

1. 首次访谈最小纵向闭环：准备页、转录优先工作台、单个当前最佳追问、安全结束页；
2. 统一倾听员工作区与最小回顾：共享首页/列表/路由、新建访谈完整入口、已结束访谈回顾和本机副本管理；
3. 多次访谈、工作记录和正式服务器隐私删除；倾听员导出取消，其他角色未来需要时另立受控任务；
4. 真实麦克风、长时与真实试点所需的安全和可靠性加固。

第一项优先，但不取消后续三项。具体任务状态以任务板为准。

第一项内部按“准备页/路由外壳、转录工作台、会话安全结束服务端编排、结束页薄集成”分段推进。安全结束的 stop/recover、完成条件和失败事实必须先由 `SPEC-SESSION-END-001` 冻结，页面不得自行推算 `completed`。

### 阶段二：树状访谈内容总结

把既有谈话动态整理为可浏览的人生经历树，用于：

- 查看已经谈过的内容；
- 发现未展开分支；
- 选择下一次切入点；
- 减少重复；
- 为后期传记整理提供参考。

### 阶段三：人工传记整理工作流

由人配合通用大模型完成：

- 转录整理；
- 重复内容合并；
- 时间顺序调整；
- 重点故事选择；
- 原话保留；
- 事实缺口标记；
- 初稿和校对；
- 排版与印刷。

### 阶段四：半自动化整理

把稳定的人工步骤逐步产品化。

### 阶段五：完整成书服务

增加家属审稿、照片、封面、印刷文件和交付管理。

## 12. 真实流式 ASR 首版产品边界

- 首版只承诺标准普通话；方言和地域口音不作为验收目标。
- 唯一实施候选为腾讯云实时语音识别 V2，目标引擎 `16k_zh_en_speaker_2.0`；内部策略强制 `diarization_required=true`。当前腾讯官方会议话者分离指南与固定版本 Go SDK 已证明 V2 wire key，实际请求必须发送 `speaker_diarization=1` 并纳入签名 canonical query；`enable_speaker_context=0` 同样必发并参与签名，`speaker_context_id` 必须完全省略而不是发送空值。
- 单声道双人虚构访谈必须稳定产生两个可由固定校准句区分并由用户确认的临时 label；任一次受控三次 replay 不能满足即产品完整可用验收 FAIL，不并接第二 diarizer。
- provider label 不等于长者/倾听员。未确认、`-1`、缺失、重连或 namespace 重建后的片段均为 `unknown`，AI 不猜角色；录音和转录继续。
- 用户授权后实时 PCM 才可发送到腾讯中国大陆。训练、优化、测试用途授权固定关闭；真实长者试点仍受 retention、诊断日志和 DPA 门禁约束。
- 原始录音优先；ASR 故障不得损害 archive、manifest 或安全结束。gap/backfill 与离线补转录归 `HARDEN-ASR-001`，真实 LLM provider 依赖 `DEV-ASR-PROVIDER-001` 正式 PASS。

正式技术契约见 `docs/contracts/streaming-asr-provider-v2.md`。

## 13. DEV-008A4 首次访谈主链路修订

本节优先于本文件中旧 A2 的服务说明与后置准备页描述。普通倾听员正式顺序冻结为：最低项目信息 → 提前创建 draft session → 当前页面麦克风权限与真实输入确认 → 口头授权录音 → 正式有效 consent → 原子 start 建立正式录音与 ASR → 独立且显著的说话人校准 → 普通访谈工作台 → 自动 flush/complete/reconcile/verify → completed/review。

- 早期服务价格在线下或口头协商。普通倾听员网页暂时隐藏服务说明、固定价格与预计时长字段，也不创建零价、虚构时长或其他占位 ServiceTerm。现有 ServiceTerm 表、API 与历史记录保留为 dormant 能力，但不再构成普通项目 ready、session start 或 capture 恢复门禁。
- 麦克风检查必须先于口头授权录音。检查通过只证明当前页面生命周期内当前设备可取得输入；刷新、重新打开或更换页面必须重新确认，服务端已有 `device_check` 状态不得冒充当前页面已通过。
- 说话人校准在视觉上先于普通工作台，在技术上后于正式 start 与正式 provider stream。失败、跳过或 provider 不可用不停止原始录音，但必须诚实显示降级、保持角色不可信，且不得伪造校准成功。
- `speaker_calibration` 原始证据继续保留，但不出现在普通实时转录、普通回顾正文、故事记忆、已问问题、摘要或普通 AI 上下文。
- 正常结束不要求倾听员理解或点击“继续安全保存”“继续处理收尾”“重新核对”。系统自动推进稳定幂等收尾；只有明确失败、pending、权限漂移或未知响应时显示对应重试。只有 canonical session `completed` 才显示完成，并提供“查看回顾”“返回工作区”。
- 回顾中的本机完整录音能力只依赖完整原始录音和既有 fresh server manifest + local archive identity/count/bytes/checksum/dirty/pending 安全门禁，不依赖 ASR 成功。删除仍只删除当前浏览器/设备 IndexedDB；服务器录音、转录、记忆与审计继续保留。

## 14. 同一长者项目的下一次访谈

第一次访谈 canonical `completed` 后，当前有效倾听员可在同一 project 卡看到服务端权威的“开始下一次访谈”。该动作只创建此 project 下的新 sequence session；全局“新建访谈”仍表示创建一个新的、互不相干的 project。Home 的项目级动作与 session 行的“查看回顾”“返回当前会话”等动作职责分离，前端不得仅凭 project/session status 猜测是否可开始下一次。

新 session 必须重新检查当前账号、有效 assignment、project/consent/boundary/deletion 状态、当前页设备与麦克风，并创建新的 capture runtime、audio stream、speaker stream 和校准 attempt；不得复用上次 session 的 MediaStream、audio object、capture generation、speaker label/mapping、ASR runtime 或本机录音 job。每次录音、转录、收尾和回顾仍以各自 session 为事实边界。

第一次 session 完成后，系统以稳定 completion trigger 非阻塞启动两条会后分析：可信长者 conversation final 进入既有 Memory current-resolution 链路，可信倾听员 conversation final 进入既有 QuestionEvidence actual-question reconciliation。失败不回退 `completed`，不阻塞下一次 session 创建、录音或既有回顾；状态与重试证据只做内容无关投影。

第二次及以后 session 的创建、设备检查、校准、录音、ASR、安全结束和已有回顾完全不等待会后 AI。开场建议同时依赖两个持久条件：本次正式 calibration gate 已 terminal，以及创建该 session 时冻结的 basis session 的 `memory_extract` 与 `actual_question_reconcile` 均已进入权威 terminal。任一分析仍为 `not_started|pending|running` 时只记录/投影 `waiting_basis_analysis`，不得创建 generation attempt、冻结 opening Context 或消费 exact-once gate。

两个分析 lane terminal 后，系统才以稳定 trigger 最多一次创建 `second_session_opening` generation attempt。正常 calibration 路径是新 speaker stream `confirmed`；`failed|skipped` 或 provider unavailable 且无 attempt 的降级路径仍可使用先前 session 的可信资料，但不得伪造当前 stream trusted membership。analysis 的 `succeeded|unjudged|failed|cancelled|unavailable` 都是合法 terminal：成功输出在冻结时必须纳入 eligible membership；unjudged/failed/cancelled/unavailable 如实记录并省略不能证明的新增资料，不阻塞录音，也不等待无限重试。它只消费同 project eligible current memory、current published/eligible actual asked、硬边界和可信 membership；系统曾显示的问题不等于实际问过。结果继续经唯一 QuestionEvidence current/history 发布链路，不新建第二套 AI 或问题历史。LLM/provider unavailable 时显示“AI 暂不可用”，录音继续。

本轮“回顾上次对话”只表示上述后台继承以及进入已经存在的 session 回顾；不新增记忆列表、摘要确认、待确认事实或第二次访谈前回顾页面。界面称呼继续来自 `AuthUser.display_name`，正式契约、代码和公共 fixture 不写入个人本地身份。

## 15. 同项目持续授权与录音前轻提醒

本节补充并优先于 §13-14 中未区分“当前 consent 动态复核”和“每次重新授权”的表述。一位长者对应一个 project，project 下可有多次 session；普通后续访谈不是新 project，也不默认产生新 consent。

- 第一次访谈在当前页麦克风检查通过后，展示并完整录制 `recorded_verbal` 正式授权。真实使用文本必须明确覆盖同 project 的当前及未来计划内访谈、录音/转录/AI 辅助分析、适用访问主体与处理边界，并说明长者可随时暂停、停止或撤回；保存该文本版本、匹配音频、时间、操作者和状态。
- 同 project 的普通后续访谈在当前授权仍适用时不重录授权音频、不追加 consent record、不重复完整授权页。普通时间间隔、有效 assignment 范围内更换倾听员不自动触发重授权；更换设备只重做当前页 microphone/device check。
- 每次 formal start 前都显示服务端返回的版本化轻提醒：“本次仍会录音、转录并由 AI 辅助分析；长者可随时要求暂停、停止或撤回。”倾听员必须显式点击“开始访谈”。这是本次录音开始确认，不是新的 consent，不创建 consent record 或授权音频，也不能替代失效授权。
- next-session 与 start 分别在服务端动态重查 active actor、有效 assignment、project restriction/deletion、当前适用 consent 与政策版本；start 还必须核对页面展示的 reminder version。旧 Home 响应、缓存、幂等快照、上次操作者或上次设备都不能恢复权限。
- `revoked|expired`、当前文本版本不兼容、处理目的扩大、可访问主体扩大、供应商处理地区扩大、公开/训练用途扩大，或原授权文本未覆盖未来访谈时，必须回到完整正式授权流程。新版本必须录制与该版本一致的新授权音频并追加新记录；禁止跨版本复用旧音频。
- ServiceTerm 继续 dormant。持续授权不扩大原授权范围，不降低 deletion、retention、boundary、provider 或真实试点门禁。

真实持续复用的可交付前置为 SPEC-CONSENT-TEXT-POLICY-001 正式接收：有权主体提供真实授权正文，项目负责人/数据治理接收不可变版本/digest 与 machine scope。当前示例文本或测试 fixture 不代表法律文本已获批准；在该前置完成前真实 `covered` 路径必须失败关闭。

## 16. LLM 多模型选择、Prompt 编辑与隔离横评

- 正式访谈在一次 generation attempt 内只使用服务端绑定的一个 active provider/model/model-config；用户不能从请求体、页面或 Prompt 改写 provider、endpoint、region 或 secret，也不允许自动 fallback 到其他模型。
- 题库继续允许为空，Director 可以完全不使用题库。`journey_stage` 继续由后端确定性策略权威给出，模型只能服从 Context 中的阶段，不得创建第二套阶段状态。
- 用户可以编辑尚未发布的 Prompt draft，并把经固定虚构/脱敏评测冻结的 candidate 送审；formal bundle 一旦接收即不可原地修改，只能发布新版本。当前 formal v1 在 v2 正式切换前保持 active。
- 多模型横评必须对每个模型使用同一冻结 Context、Prompt candidate、正式 Context/Output Schema 和比较政策；结果只用于人工比较质量、风险、token 和 latency，不发布为 current/history，不改变 actual asked、memory、题库或源转录。
- 真实 provider/region/模型尚未选择，真实访谈内容默认不得离开本项目后端。境外处理、用途扩大或供应商地区扩大必须先完成数据治理决定与适用重授权，不得由 registry 条目或密钥存在自动放行。

## 17. 早期 staging 产品边界

- Quick Tunnel 只用于少量受控参与者的远程虚构排练；随机 URL 每次作废，不承诺稳定性，不用于真实长者、PII、真实录音/转录或固定试点入口。
- Named staging 使用固定 HTTPS hostname 与 Cloudflare Access，但 Access 只限制谁能抵达应用；倾听员仍需应用内登录，role、assignment、consent、restriction/deletion 仍由应用独立判断。
- Web、API、上传与 WebSocket 必须同源，浏览器只看到一个 HTTPS/WSS origin；不引入第二套跨域身份或 CORS 产品流程。
- Windows 主机重启、睡眠、断电、磁盘满、网络/Access/Tunnel/WS 中断时，界面必须诚实显示不可用/重连/降级，不能伪健康或以 Quick/公开直连绕过。原始录音保护优先级不变。
- `data_mode=synthetic_only` 是当前正式 deployment manifest 的唯一数据许可状态。只允许从源头为测试创作的虚构账号、虚构正文与合成/完全虚构音频；真实长者、真实访谈、PII、真实录音/转录、业务数据库或备份即使去标识/脱敏也不可用，provenance 不明同样拒绝。Named synthetic 可用不代表早期真实试用已放行；未来真实数据须新任务、数据治理决定、新版 machine contract 与项目负责人正式接收，门禁见 `08` §22 和 staging 契约 §11。
