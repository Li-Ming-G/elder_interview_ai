# 03｜业务流程与交互规范

## 1. 文档职责

本文件定义：

- 用户操作流程；
- 页面内容；
- 业务状态；
- 状态转换；
- 人机协作规则；
- 异常时用户看到什么。

具体数据结构见 `04-数据模型规范.md`，接口与实时事件见 `05-API与实时事件契约.md`。

## 2. 核心对象

- 长者项目：同一位长者的一组访谈资料；
- 服务条款记录：时长、次数、超时收费说明；
- 授权记录：录音、转录和 AI 分析同意；
- 访谈会话：一次具体访谈；
- 转录片段：带时间和说话人的文字；
- 结构化记忆：人物、事件、时间等；
- 问题建议：AI 给倾听员的候选追问；
- 内容标记：重要、敏感、禁止使用、待确认等；
- 导出任务：生成资料包的过程。

## 3. 主流程

```text
从已分配、已预创建的项目进入
  ↓
填写最低基础信息
  ↓
确认服务信息
  ↓
完成授权
  ↓
设备检查
  ↓
原子 start，正式录音与正式 ASR 开始
  ↓
同一正式流内立即说话人校准
  ↓
实时录音与转录
  ↓
AI 显示一个当前最佳追问或建议继续倾听
  ↓
倾听员继续对话，或点击“没用，换一个”
  ↓
人工标记重要或受限内容
  ↓
结束访谈
  ↓
生成工作记录
  ↓
下次继续或导出
```

## 4. 新建长者项目

### 4.1 最低信息

| 字段 | 必填 | 说明 |
|---|---:|---|
| 姓名或称呼 | 是 | 可以使用昵称或项目代号 |
| 出生年份 | 否 | 不清楚时留空 |
| 大致年龄 | 否 | 可代替出生年份 |
| 籍贯 | 否 | 可在访谈中补充 |
| 当前居住地 | 否 | 可在访谈中补充 |

家庭成员、职业、教育和人生事件不要求提前填写。

### 4.2 规则

1. 只填写称呼即可保存草稿。
2. 一个长者项目可以包含多次访谈。
3. 删除项目需二次确认。
4. 删除默认进入软删除和后续清理流程。
5. 无权限人员不得查看项目。

## 5. 服务信息

访谈前记录：

- 基础套餐包含时长；
- 预计访谈次数；
- 本次预计时长；
- 超时计价单位；
- 超时价格；
- 已向客户说明费用规则；
- 说明人员和时间。

### 5.1 超时规则

1. 系统显示当前会话时长和项目累计时长。
2. 接近套餐上限时提醒倾听员。
3. 超时前必须由倾听员与客户确认。
4. 系统只记录确认，不自动扣费。
5. 服务条款修改需保留历史记录。

## 6. 授权流程

### 6.1 告知内容

- 对话会被录音；
- 录音会被转成文字；
- 转录会用于 AI 分析；
- AI 会向倾听员提供问题建议；
- 数据会被保存；
- 长者可随时要求停止；
- 长者可要求某段内容不再使用；
- 内容不会未经确认直接公开。

### 6.2 MVP 口头授权

1. 页面显示固定授权文本；
2. 倾听员说明即将录制授权；
3. 点击“录制授权”；
4. 倾听员完整朗读文本；
5. 长者明确表达同意；
6. 保存授权音频、文本版本和时间；
7. 授权状态变为 `valid`；
8. 正式访谈按钮解锁。

### 6.3 规则

- 无有效授权不得开始正式访谈；
- 授权撤回后不得继续 AI 分析；
- 撤回后项目进入受限处理；
- 删除请求必须单独记录。

## 7. 设备检查

检查项目：

- 支持的浏览器；
- 麦克风权限；
- 当前输入设备；
- 输入音量；
- 网络连接；
- 业务 WebSocket；
- 分片上传能力；
- ASR 服务状态。

### 7.1 失败处理

- 检查失败不得进入正式访谈；
- 页面明确显示失败项和处理方法；
- 允许重新检测；
- 不因为检查失败创建正式录音。

首次访谈准备页加载时不得请求麦克风或自动创建录音对象。“检测麦克风”只打开临时流做输入检测；检测完成后创建或复用该项目唯一未开始 session、提交 device check，并立即释放临时流。“开始访谈”是第二个明确用户动作：它重新获取唯一正式流，完成 MediaRecorder/MIME、IndexedDB canary 与内部容量检查，先持久化本地作业和稳定 ID，再调用携带 `mime_type` 与 `audio_stream_id` 的原子 start。页面只有在 MediaRecorder 已 recording、本地 active checkpoint 已保存且服务端 `confirm_capture_active` 成功后才能显示“访谈进行中”。

设备检查的临时流只证明当前浏览器可以取得输入并检测到声音。它不连接正式 ASR，不产生 `speaker_stream_id`、校准 attempt 或说话人映射，也不得被后续实现当作正式角色证据。

## 8. 说话人校准

### 8.1 流程

1. 用户点击“开始访谈”，系统按正式 start 契约先建立原始录音和正式 ASR；
2. 页面明确显示“正在录音 · 正在确认说话人”；
3. 服务端为当前正式 `speaker_stream_id` 建立校准 attempt，并只在该 attempt 的权威控制范围内标记校准控制片段；
4. 倾听员说固定句，长者说固定句；
5. 系统展示当前流内已观察到的两个 provider speaker label，倾听员明确确认谁是长者、谁是倾听员；
6. 服务端在同一事务确认 attempt 并追加当前流的两条用户确认映射；
7. 页面显示“说话人已确认”并进入普通访谈。固定句仍保留原始证据，但不进入故事或普通 AI 派生。

### 8.2 规则

- 校准不是 session start、MediaRecorder 或原始录音的硬门禁；失败、跳过和重试不得停止、回滚或损害录音；
- 供应商自动判断不等于校准成功；只有倾听员确认的校准映射或人工角色修正才是可信角色依据；
- 校准失败或跳过后结束当前控制范围，页面持续显示“说话人待确认”，普通录音和转录继续，倾听员可以在访谈中重新开始一次新的校准 attempt；
- 未确认角色保存并展示为待确认，但角色相关长期记忆、确定的已问问题和自动建议上下文不得消费；信息不足时 AI 返回“继续倾听”；
- `speaker_stream_id` 是供应商 speaker label 的持久作用域。新的 provider/runtime speaker namespace 必须获得新的 `speaker_stream_id` 并重新确认；不得用 capture generation、`audio_stream_id` 或短时 WebSocket `event_stream_id` 模糊替代；
- provider label 即使在新流中仍叫 `speaker_1`，也不得继承旧流映射。旧片段保留其原始角色快照和既有人工修正；
- 首次工作台只承载低负担校准、状态和重试，不承载复杂批量历史编辑。

### 8.3 说话人修正

- 默认只修正当前单个 final 片段；人工修正写独立角色与操作者证据，不覆盖 `original_speaker_role`；
- 批量修正只作用于同一 `speaker_stream_id`、同一 provider label 和用户明确选择的片段范围；
- 执行前必须保存稳定预览并展示影响片段数量；默认排除已有单段人工修正；预览后目标发生冲突时整批拒绝，不允许半成功；
- 复杂批量修正入口属于完整访谈回顾切片。DEV-004C 可以实现和验证服务端预览/原子执行契约，但不得把复杂编辑器塞入正常访谈工作台；
- 修正后只标记受影响片段及其派生结果失效。真正的记忆、已问问题和建议重算分别由 DEV-006/007 消费该失效事实，不由 DEV-004C 伪造尚不存在的 AI 结果。

## 9. 访谈工作台

### 9.1 页面布局

当前优先切片采用“顶部状态 + 转录主体 + 底部建议”的纵向布局：

```text
┌──────────────────────────────────────────────────────────┐
│ 长者称呼｜已进行时长｜录音状态｜转录状态｜结束             │
├──────────────────────────────────────────────────────────┤
│                                                          │
│ 实时转录（主要空间，约 80%）                              │
│ 时间｜说话人｜中间态或确定态文本                           │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ 当前最佳下一问                                            │
│ 一句推荐原因                                  没用，换一个 │
└──────────────────────────────────────────────────────────┘
```

首轮工作台不放项目管理、完整回顾编辑、批量说话人修正、复杂标记或导出操作。这些能力按后续页面切片进入。

正式工作台由跨准备页、工作台和结束状态存活的 session 级 `InterviewCaptureController` 驱动。它拥有唯一正式麦克风流、MediaRecorder、本浏览器 archive、上传 delivery、实时 transport、session/audio object/generation/stream ID；页面组件只发命令和订阅快照。工作台必须分别显示采集、本浏览器备份、管理服务录音/manifest、实时/结束转录、session 五类事实，不得把 WebSocket 正常、ASR 正常或 URL 路径解释成原始录音安全。

start 后的低负担校准位于同一工作台：录音安全摘要始终可见，校准步骤不能遮蔽或弱化“正在录音”。状态至少区分“正在确认”“待确认”“已确认”“需要重新确认”和“校准暂不可用”。失败或跳过后的重试是次级动作，不得把说话人问题误报为录音失败。Android Chrome 小屏保持两句短提示、一个明确确认动作和不小于 44×44px 的触控目标。

正常录制的比例是视觉护栏而非固定 CSS 百分比：1440×900 以顶部/转录/建议约 8%/79%/13% 为基准，390×844 约 9%/73%/18%，允许约 ±3 个百分点；320×568 时顶部不高于 72px、活动建议不高于 120px、转录不低于可用高度 60%。“继续倾听”或建议不可用时建议区收缩到约 64–72px。正常态页面本身不纵向滚动，转录视口是唯一主要纵向滚动区。

五类事实按注意力分区：顶部仅常驻长者称呼、时长、采集与本浏览器 archive 合并成的一句安全摘要和结束入口；转录事实放在转录标题旁；“保存状态”展开区完整列出采集、本浏览器 archive、管理服务分片/manifest、转录、session 及最后核验时间。管理服务暂不可达而本地正常时显示“本浏览器仍在保存 · 管理服务暂不可核对”并继续录音；ASR 异常只在转录区提升并明确原始录音不受影响；采集中断或 archive 写入失败直接进入中断处置层。关键异常可以自动展开但不得主动抢走键盘焦点，折叠后顶部警示仍持续存在。

首轮移动端把 Android Chrome 作为完整访谈主设备，覆盖准备、正常录制、实时转录/建议、中断恢复和安全结束；iPhone Safari 延期。验收视口至少包括 1440×900、1024×768、768×1024、390×844、320×568。平板竖屏和手机顶部最多两行，不使用水平状态 chip carousel；触控目标不小于 44×44px并避开安全区。屏幕旋转只重排，不刷新、不重新申请麦克风、不创建新 capture。

### 9.2 转录显示

- 中间态弱化显示；
- 确定态正常显示；
- 长者、倾听员、未知角色有明确区分；
- 默认自动滚动；
- 用户向上回看时暂停自动跟随，并显示新内容数量；
- 提供“回到最新”，恢复自动跟随；
- 可以跳转对应录音位置；
- 人工修正后保留原文。

所有设备保持高密度左右结构：左侧窄元数据栏显示说话人与时间，右侧显示正文；手机元数据栏约 52–64px，“说话人待确认”视觉缩写为“待确认”但无障碍名称保留全文。桌面/平板正文约 16px，手机约 15px，行高 1.55–1.65，单条上下留白 8–10px；桌面正文行宽约 68–75 字符，手机使用剩余宽度。确定态默认不逐条重复“确定态”，只有中间态显示“识别中”并弱化。不得使用聊天气泡、卡片或大面积角色色块，浏览器字体缩放不得被禁用。

### 9.3 AI 建议显示

显示：

- 同时仅一个当前最佳问题；
- 一句推荐原因；
- “继续倾听”状态。

倾听员可以：

- 点击“没用，换一个”。

建议区状态为有建议、正在更换、更换完成、继续倾听或暂不可用。正在更换时保留原问题供参考并显示“正在换一个”，不得突然空白；问题最突出但不超过转录标题，原因更弱，所有建议操作使用次级样式，不使用高饱和大色块或循环动效。

项目负责人已确认“一层撤销上次更换”的产品语义：更换成功后才显示撤销，只能撤销最近一次，恢复上一条问题与原因并撤回该次相似问题排除；当前建议被后续谈话自动更新或会话状态改变后撤销失效。R3 只预留布局与状态容器，不实现业务；request ID、幂等、持久化、竞态和排除集合恢复必须由 `SPEC-AI-QUESTION-001` 冻结后交 DEV-007 实现。

首轮不要求登记采用、实际问出、忽略、稍后或改写。替换后不得返回当前问题或高度相似问题；AI 必须读取真实转录中的倾听员发问来减少重复。替换请求的幂等、节流和持久化细节由 `SPEC-AI-QUESTION-001` 在 DEV-007 开工前冻结。

## 10. 对话中的动态边界

当长者表达：

- 不想聊；
- 不方便说；
- 不要记录；
- 不要用于书中；
- 换话题；
- 暂停录音；

倾听员应立即尊重。

支持标记：

| 标记 | 业务含义 |
|---|---|
| 不再追问 | 行为边界；AI 不得再次主动推荐相关话题，但不自动删除或禁止使用既有内容 |
| 稍后确认 | 当前不问，可在未来由倾听员决定 |
| 不用于整理 | 原始证据保留，但正文和派生记忆从普通 AI、摘要和导出链路排除 |
| 删除请求 | 立即停止相关内容的新 AI 处理并进入删除申请流程；不表示已经物理删除 |
| 重要原话 | 后期人工整理重点关注 |
| 待核实 | 时间、人物或事实不明确 |
| 敏感 | 可以继续最小化处理，但普通摘要和普通导出默认遮蔽正文；不等同于禁止追问或删除 |

AI 可以提醒潜在拒绝，但不能自动删除或自行判断可以继续。

### 10.1 标记确认与解除

1. AI 或规则可以提示潜在敏感、拒绝或删除表达，但只能形成版本化 AI 输出中的候选；候选不是 `content_marker`，不会自动改变业务状态；其当前会话临时保守阻断由服务端根据最新未处置候选重建，候选确认、驳回、过期或会话结束时解除；
2. 倾听员确认 `sensitive`、`restricted` 或 `do_not_ask`；`do_not_ask` 必须填写短小、抽象、不含原话和详细个人信息的禁区描述；
3. 删除请求由倾听员登记，数据管理员负责身份核验、处理和完成；
4. `restricted`、`do_not_ask` 只能基于长者明确改变决定或纠正误标，由有权人员人工解除并记录原因；
5. `deletion_request` 不能由倾听员直接取消；AI 输出没有确认、解除或覆盖任何标记的业务效力；
6. 多个标记重叠时取限制并集，处理优先级为：`deletion_request > restricted > do_not_ask > sensitive > ordinary`。

无法加载跨会话边界、无法构造安全禁区描述或边界过滤失败时，系统暂停自动 AI 建议并显示降级状态，录音和转录继续。

存在非终态删除申请（`pending_verification`、`verified`、`processing`）时，普通回顾和普通导出均停止；仅数据管理员可以为处理该申请生成不含申请范围正文的删除处理证据包，包内只标记请求状态和最小审计信息。`rejected`、`withdrawn` 关闭后按其他限制重算；`completed` 后已删除内容永久不可恢复或重新导出。

非终态删除申请对 AI 的影响按 scope 执行：`project` 停止整个项目的新 AI 处理并把项目置为 `restricted`；`session` 只停止该会话内容及其派生记忆的新 AI 处理；`segment_range` 只停止标记范围及其派生记忆的新 AI 处理。无论 scope，项目级普通回顾和普通导出均拒绝；请求进入终态后按上一条规则处理。

`segment_range` 的范围以申请创建时冻结的起止片段为准；之后修改或软删除来源 marker 不改变 AI 阻断、核验或最终清理范围。

## 11. 冷场和开始讲述

当长者暂时无话可说：

1. 倾听员可以点击“帮我想下一问”；
2. AI 读取基础信息、已有记忆和问题历史；
3. 优先生成轻量、开放、低风险问题；
4. 不要求长者提前选择重点人生阶段；
5. 若资料不足，可以从出生地、童年环境、第一次离家等基础方向开始。

## 12. 中断、恢复与结束访谈

刷新、崩溃、麦克风 track ended、MediaRecorder 错误、本地 archive 写失败或确认的认证失效，在尚无 stop snapshot 时进入 `interrupted`。普通网络或实时 WebSocket 故障只降级对应链路，不自动终止原始采集。重进页面不得自动请求麦克风；系统先读取服务端 capture snapshot 与同一 local job，必要时报告 `page_recovery_detected`，再由用户选择继续同一访谈或安全结束已有音频。

继续访谈必须复用同一 session、interview audio object 与 local job，创建下一 capture generation 和新 audio stream；原始 archive sequence/timeline 延续高水位，新的实时 PCM sequence 从 0 开始并使用服务端确认的 timeline offset。没有服务端原始分片、没有已接受 PCM、没有 finalization 且本浏览器 archive 确认为零时，用户可以结束无音频会话；服务端以 `NO_AUDIO_CAPTURED` 终结，不得伪造 completed。

恢复后的 capture generation 与 `audio_stream_id` 变化不自动等同于新的或沿用的供应商说话人 namespace。只要 ASR adapter 建立了新的 provider speaker namespace，就必须生成新的持久 `speaker_stream_id`，页面重新进入“说话人待确认”；用户确认前的新片段按不可信角色处理。短时事件 replay 不得创建新 speaker stream，WebSocket `event_stream_id` 永远不能作为角色映射作用域。

结束按钮采用“两阶段结束”，确认对话框标题为“确定结束本次访谈？”，说明为“确认后将停止继续录音，并安全保存已经录下的内容。本次会话不能继续追加。”；动作是“继续访谈”和“确认结束”，默认焦点在继续访谈，Escape 取消且无副作用。客户端顺序固定为：

1. 立即停止向业务 WebSocket 发送新 PCM，并停止/释放主动 producer；
2. 调用 `MediaRecorder.stop()`，等待最终 `dataavailable`，不再创建新原始分片；
3. 冻结唯一访谈 audio object 的 `expected_chunk_count` 和 `0..N-1` 每片不可变 commitment；
4. 先持久化稳定 stop `request_id` 和上述快照，再调用服务端 stop；
5. 服务端接受 stop 后原子冻结采集截止事实并进入 `stopping`，此后拒绝所有新 PCM、第二个访谈 audio object、扩大分片总数或 commitment 外分片；
6. 已冻结 commitment 内的待传原始分片继续补传，完成后固化并复核 manifest；
7. manifest 完整后进入 `processing`，等待 ASR final drain 成为 `drained` 或明确降级为 `degraded`；
8. 原始录音完整且 ASR 已进入上述任一终态后，服务端计算实际时长并进入 `completed`。

`ended_at` 是服务端根据已冻结 commitment 的最后 `end_ms` 和 `started_at` 推导并校验的采集截止时间，不是 stop HTTP 到达、上传完成、ASR 关闭或页面到达结束页的时间。`duration_seconds` 由服务端按同一时间轴计算，客户端计时只用于录制中提示。

`completed` 不要求 ASR 成功产出完整文字，也不要求 AI、结构化记忆或工作记录成功。ASR 无法 drain 时，在原始录音完整的前提下以 `degraded` 和不泄密错误分类合法完成；AI 或后处理失败另行展示/重试，不能倒退或阻塞原始录音完成事实。

若仍等待已承诺分片或 manifest，页面显示 `stopping`；原始录音已经完整、正在收束转录时显示 `processing`。缺片、响应丢失或进程内 runner 中断先保持可恢复事实，不得立即伪造 `failed`。只有服务端确认无法通过受限 recover 自动收束、需要人工处置时才进入终态 `failed`。

确认后先在同一工作台显示“正在整理最后一段录音”；只有最终 `dataavailable` 已写入本浏览器 archive 后才能提交 stop 并显示 `stopping`。工作台随后变为只读并保留当前内存转录；隐藏录制、建议和结束动作，使用持久状态对话框解释事实。正常 stop 确认后不得恢复采集。

本轮不新增独立结束路由；同一 workbench URL 展示 `interrupted|stopping|processing|completed|failed`。`stopping` 显示“正在安全保存录音”，不提供应用内离开；只有本浏览器仍持有冻结待传分片时才注册 beforeunload 提示。`processing` 表示服务端 manifest 已完整，可安全离开。`completed` 必须分别解释转录 `drained|degraded|not_started`；`failed` 必须区分服务端录音 manifest 是否完整；`NO_AUDIO_CAPTURED` 明确没有可保存录音或转录。

`interrupted` 在同一 URL 原地切换为中断处置层：通俗显示原因、本浏览器保存事实、管理服务最后核验事实和转录可能不完整但不覆盖录音；按真实条件显示继续同一次访谈、安全结束已有音频、结束无音频，或无权限/不可恢复说明。初始中断不是 modal，手机动作必须在首屏可见；只有用户选择结束路径后才进入结束确认 modal。继续必须复用原 session/audio/local job，且只在用户选择后重新请求麦克风。

除结束确认外，中断和结束状态使用同页主导状态层，不用传统阻塞 modal 永久遮住转录。状态面板的最小化入口在 `stopping` 灰置，在 `processing` 后启用；点击后收起为持续状态条，允许查看当前已加载的只读转录并可重新展开；刷新后默认重新展开。`completed` 也可最小化，但不新增完整回顾入口。

“查看保存明细”只在当前页展开最小事实，不提供死链接或扩大为完整记录页；“完成并离开”返回已登录根路径 `/`；“重新准备一次访谈”返回该项目准备页但不自动创建 session。本轮不提供本地 archive 下载、播放、打开目录或删除 UI；真实试点前必须补齐本地备份管理/删除门禁。

前端只消费 `GET /sessions/:id`、capture action、stop 或 recover 返回的统一服务端 snapshot，并结合本浏览器 archive 自身事实分别展示；不得用固定延时、`localStorage`、URL、WebSocket 五分钟/512 事件 replay、最后一条 final 或本地分片数推算服务端成功。服务端暂不可达时保留最后核验事实及时间并显示“状态暂不可核对”，只提供只读“重新核对”，不得自动发起业务动作。

## 13. 会话工作记录

访谈结束后生成：

- 本次主题；
- 新人物；
- 新事件；
- 未讲完的故事；
- 待确认问题；
- 冲突信息；
- 重要原话；
- 禁止使用内容；
- 下次继续入口。

规则：

- 允许人工编辑；
- 不是传记正文；
- 所有 AI 提取内容可跳回原始片段；
- 受限内容不进入普通摘要。

## 14. 多次访谈

下一次进入项目时显示：

- 上次访谈时间；
- 上次主要内容；
- 尚未展开的话题；
- 待确认事项；
- 已标记的边界；
- 建议继续入口。

新会话必须重新完成设备检查和说话人校准。

同一会话在恢复或 provider/runtime 重建后出现新的 `speaker_stream_id` 时，也必须重新确认；不能因为仍是同一 session 而沿用旧短 provider label。

## 15. 访谈回顾

支持：

- 播放录音；
- 查看原始和修订转录；
- 修正说话人；
- 添加或修改标记；
- 查看问题历史；
- 编辑会话工作记录；
- 发起导出。

说话人修正默认从单段开始。复杂批量操作在本页先预览同一 speaker stream、同一 provider label 和明确范围内的稳定目标集合与数量，默认排除已有单段人工修正；确认后全成全败执行。

## 16. 导出流程

1. 倾听员选择导出项目；
2. 系统校验权限和项目状态；
3. 生成导出任务；
4. 检查音频、转录和标记完整性；
5. 普通内容与受限内容分离；
6. 生成 ZIP；
7. 返回短期下载地址；
8. 写入审计日志；
9. 到期后删除临时导出文件。

## 17. 状态机

### 17.1 项目状态

```text
draft → ready → active → completed
```

特殊状态：

- `restricted`
- `deleted`

| 状态 | 含义 |
|---|---|
| draft | 服务或授权未完成 |
| ready | 可以开始访谈 |
| active | 已存在访谈 |
| completed | 计划内访谈采集结束 |
| restricted | 授权撤回或限制处理 |
| deleted | project scope 删除申请已完成在线主存储与临时导出清理，备份已登记延迟清理；不可恢复 |

### 17.2 会话状态

```text
created
  ↓
device_check
  ↓
recording
  ↓
stopping
  ↓
processing
  ↓
completed
```

异常状态：

- `reconnecting`
- `interrupted`
- `failed`

结束相关事实：

| 状态 | 服务端事实 | 可恢复边界 |
|---|---|---|
| `recording` | 正在采集，可接收新 PCM 和创建原始分片 | stop 或短时断线进入后续状态 |
| `reconnecting` | 采集尚未截止，正在恢复主动 producer | 可继续采集或执行 stop；不是结束 recover |
| `stopping` | stop 快照和采集截止已持久化，拒绝新 PCM，只允许承诺范围内原始分片补传 | recover 可重驱上传/manifest 检查；缺片仍保持此状态 |
| `processing` | 原始录音 manifest 已完整并通过存储复核，正在等待 ASR `drained|degraded|not_started` | recover 可重驱进程内收束；进程重启不丢持久事实 |
| `completed` | manifest 完整，ASR 已终结为 `drained|degraded|not_started`，结束时间和时长已固化 | 终态；stop/recover 只返回稳定 snapshot |
| `interrupted` | 采集意外中断且尚无完整 stop 快照，或仍需要同一操作者恢复选择 | 非终态；门禁有效可恢复采集，否则只能冻结已有证据并安全结束 |
| `failed` | 服务端确认不能自动或通过受限 recover 收束，需要人工处置 | 终态；查询保留已保存证据和不泄密失败分类，不回退为 completed |

capture generation 另有 `preparing|active|interrupted|stopped|abandoned_empty` 状态。session 与 capture 的关系以 `04`/`05` 为准；session `recording` 不足以单独证明当前浏览器已开始采集，必须同时存在 current generation `active`。

### 17.3 AI 建议状态

首轮页面只表达“正在生成、当前建议、正在替换、继续倾听、暂不可用”。不再向倾听员暴露或要求操作 `adopted`、`asked`、`ignored`、`saved_for_later`。

持久化状态、替换动作和旧候选模型的处理方式由 `SPEC-AI-QUESTION-001` 冻结；在该任务完成前，DEV-007 不得按旧生命周期实现。

## 18. 关键异常交互

### 麦克风拒绝

阻止开始，显示权限设置方法。

### ASR 失败

继续录音，显示“实时转录暂不可用”，自动重连，结束后允许补转录。

### AI 超时

保留当前建议，不影响录音和转录，允许手动重试或使用基础问题。

### 网络中断

短时本地缓存录音分片，恢复后补传。无法恢复时明确标记缺失区间。

### 浏览器崩溃

保留已上传分片，会话标记 `interrupted`，重新进入后提供恢复入口。

### 对象存储故障

优先保护本地临时录音，提示安全结束，不允许开启新的正式访谈。
