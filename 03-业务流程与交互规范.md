# 03｜业务流程与交互规范

## 1. 文档职责

本文件定义：

- 用户操作流程；
- 页面内容；
- 业务状态；
- 状态转换；
- 人机协作规则；
- 异常时用户看到什么。

具体数据结构见 `04-数据模型规范.md`，接口与实时事件见 `05-API与实时事件契约.md`。

## 2. 核心对象

- 长者项目：同一位长者的一组访谈资料；
- 服务条款记录：时长、次数、超时收费说明；
- 授权记录：录音、转录和 AI 分析同意；
- 访谈会话：一次具体访谈；
- 转录片段：带时间和说话人的文字；
- 结构化记忆：人物、事件、时间等；
- 问题建议：AI 给倾听员的候选追问；
- 内容标记：重要、敏感、禁止使用、待确认等；
- 导出任务：生成资料包的过程。

## 3. 主流程

```text
新建项目
  ↓
填写最低基础信息
  ↓
确认服务信息
  ↓
完成授权
  ↓
设备检查
  ↓
说话人校准
  ↓
开始访谈
  ↓
实时录音与转录
  ↓
AI 生成追问建议
  ↓
倾听员采用、忽略或改写
  ↓
人工标记重要或受限内容
  ↓
结束访谈
  ↓
生成工作记录
  ↓
下次继续或导出
```

## 4. 新建长者项目

### 4.1 最低信息

| 字段 | 必填 | 说明 |
|---|---:|---|
| 姓名或称呼 | 是 | 可以使用昵称或项目代号 |
| 出生年份 | 否 | 不清楚时留空 |
| 大致年龄 | 否 | 可代替出生年份 |
| 籍贯 | 否 | 可在访谈中补充 |
| 当前居住地 | 否 | 可在访谈中补充 |

家庭成员、职业、教育和人生事件不要求提前填写。

### 4.2 规则

1. 只填写称呼即可保存草稿。
2. 一个长者项目可以包含多次访谈。
3. 删除项目需二次确认。
4. 删除默认进入软删除和后续清理流程。
5. 无权限人员不得查看项目。

## 5. 服务信息

访谈前记录：

- 基础套餐包含时长；
- 预计访谈次数；
- 本次预计时长；
- 超时计价单位；
- 超时价格；
- 已向客户说明费用规则；
- 说明人员和时间。

### 5.1 超时规则

1. 系统显示当前会话时长和项目累计时长。
2. 接近套餐上限时提醒倾听员。
3. 超时前必须由倾听员与客户确认。
4. 系统只记录确认，不自动扣费。
5. 服务条款修改需保留历史记录。

## 6. 授权流程

### 6.1 告知内容

- 对话会被录音；
- 录音会被转成文字；
- 转录会用于 AI 分析；
- AI 会向倾听员提供问题建议；
- 数据会被保存；
- 长者可随时要求停止；
- 长者可要求某段内容不再使用；
- 内容不会未经确认直接公开。

### 6.2 MVP 口头授权

1. 页面显示固定授权文本；
2. 倾听员说明即将录制授权；
3. 点击“录制授权”；
4. 倾听员完整朗读文本；
5. 长者明确表达同意；
6. 保存授权音频、文本版本和时间；
7. 授权状态变为 `valid`；
8. 正式访谈按钮解锁。

### 6.3 规则

- 无有效授权不得开始正式访谈；
- 授权撤回后不得继续 AI 分析；
- 撤回后项目进入受限处理；
- 删除请求必须单独记录。

## 7. 设备检查

检查项目：

- 支持的浏览器；
- 麦克风权限；
- 当前输入设备；
- 输入音量；
- 网络连接；
- 业务 WebSocket；
- 分片上传能力；
- ASR 服务状态。

### 7.1 失败处理

- 检查失败不得进入正式访谈；
- 页面明确显示失败项和处理方法；
- 允许重新检测；
- 不因为检查失败创建正式录音。

## 8. 说话人校准

### 8.1 流程

1. 倾听员说固定句；
2. 长者说固定句；
3. 系统建立供应商说话人 ID 与业务角色映射；
4. 映射结果在页面显示；
5. 倾听员可立即修正。

### 8.2 规则

- 每次新访谈重新校准；
- 不依赖跨天的供应商声纹 ID；
- 无法确定时使用 `unknown`；
- 后续允许单段或批量修正。

## 9. 访谈工作台

### 9.1 页面布局

推荐三栏：

```text
┌──────────────┬──────────────────────────┬──────────────────┐
│ 项目和链路状态 │ 实时转录                  │ AI 追问建议       │
│ 当前时长      │ 长者：……                 │ 首选问题          │
│ 累计时长      │ 倾听员：……               │ 推荐原因          │
│ 录音状态      │                          │ 证据片段          │
│ ASR 状态     │                          │ 采用/忽略/稍后    │
│ AI 状态      │                          │                  │
└──────────────┴──────────────────────────┴──────────────────┘
```

固定操作：

- 开始；
- 结束；
- 重要原话；
- 敏感；
- 禁止使用；
- 待确认；
- 帮我想下一问。

### 9.2 转录显示

- 中间态弱化显示；
- 确定态正常显示；
- 长者、倾听员、未知角色有明确区分；
- 默认自动滚动；
- 允许暂停自动滚动；
- 可以跳转对应录音位置；
- 人工修正后保留原文。

### 9.3 AI 建议显示

显示：

- 首选问题；
- 最多两个备选问题；
- 推荐原因；
- 问题目的；
- 风险等级；
- 证据片段；
- “继续倾听”状态。

倾听员可以：

- 采用；
- 标记实际问出；
- 忽略；
- 稍后再问；
- 手工改写；
- 评价重复、不合适或过于敏感。

新建议不得直接覆盖仍在查看的建议。

## 10. 对话中的动态边界

当长者表达：

- 不想聊；
- 不方便说；
- 不要记录；
- 不要用于书中；
- 换话题；
- 暂停录音；

倾听员应立即尊重。

支持标记：

| 标记 | 业务含义 |
|---|---|
| 不再追问 | AI 不得再次主动推荐相关话题 |
| 稍后确认 | 当前不问，可在未来由倾听员决定 |
| 不用于整理 | 原始证据保留，但从普通整理链路排除 |
| 删除请求 | 进入删除申请流程 |
| 重要原话 | 后期人工整理重点关注 |
| 待核实 | 时间、人物或事实不明确 |
| 敏感 | 后续展示和导出需谨慎处理 |

AI 可以提醒潜在拒绝，但不能自动删除或自行判断可以继续。

## 11. 冷场和开始讲述

当长者暂时无话可说：

1. 倾听员可以点击“帮我想下一问”；
2. AI 读取基础信息、已有记忆和问题历史；
3. 优先生成轻量、开放、低风险问题；
4. 不要求长者提前选择重点人生阶段；
5. 若资料不足，可以从出生地、童年环境、第一次离家等基础方向开始。

## 12. 结束访谈

结束按钮触发：

1. 停止接收新音频；
2. 等待最后确定态转录；
3. 关闭 ASR；
4. 完成音频分片上传；
5. 检查分片连续性；
6. 计算会话时长；
7. 更新累计时长；
8. 更新结构化记忆；
9. 生成会话工作记录；
10. 状态变为 `completed`。

若链路尚未完成，页面进入 `stopping` 或 `processing`，不得提前显示完整成功。

## 13. 会话工作记录

访谈结束后生成：

- 本次主题；
- 新人物；
- 新事件；
- 未讲完的故事；
- 待确认问题；
- 冲突信息；
- 重要原话；
- 禁止使用内容；
- 下次继续入口。

规则：

- 允许人工编辑；
- 不是传记正文；
- 所有 AI 提取内容可跳回原始片段；
- 受限内容不进入普通摘要。

## 14. 多次访谈

下一次进入项目时显示：

- 上次访谈时间；
- 上次主要内容；
- 尚未展开的话题；
- 待确认事项；
- 已标记的边界；
- 建议继续入口。

新会话必须重新完成设备检查和说话人校准。

## 15. 访谈回顾

支持：

- 播放录音；
- 查看原始和修订转录；
- 修正说话人；
- 添加或修改标记；
- 查看问题历史；
- 查看采用和忽略记录；
- 编辑会话工作记录；
- 发起导出。

## 16. 导出流程

1. 倾听员选择导出项目；
2. 系统校验权限和项目状态；
3. 生成导出任务；
4. 检查音频、转录和标记完整性；
5. 普通内容与受限内容分离；
6. 生成 ZIP；
7. 返回短期下载地址；
8. 写入审计日志；
9. 到期后删除临时导出文件。

## 17. 状态机

### 17.1 项目状态

```text
draft → ready → active → completed
```

特殊状态：

- `restricted`
- `deleted`

| 状态 | 含义 |
|---|---|
| draft | 服务或授权未完成 |
| ready | 可以开始访谈 |
| active | 已存在访谈 |
| completed | 计划内访谈采集结束 |
| restricted | 授权撤回或限制处理 |
| deleted | 等待物理清理 |

### 17.2 会话状态

```text
created
  ↓
device_check
  ↓
recording
  ↓
stopping
  ↓
processing
  ↓
completed
```

异常状态：

- `reconnecting`
- `interrupted`
- `failed`

### 17.3 AI 建议状态

- `generated`
- `displayed`
- `adopted`
- `asked`
- `ignored`
- `saved_for_later`
- `expired`

## 18. 关键异常交互

### 麦克风拒绝

阻止开始，显示权限设置方法。

### ASR 失败

继续录音，显示“实时转录暂不可用”，自动重连，结束后允许补转录。

### AI 超时

保留当前建议，不影响录音和转录，允许手动重试或使用基础问题。

### 网络中断

短时本地缓存录音分片，恢复后补传。无法恢复时明确标记缺失区间。

### 浏览器崩溃

保留已上传分片，会话标记 `interrupted`，重新进入后提供恢复入口。

### 对象存储故障

优先保护本地临时录音，提示安全结束，不允许开启新的正式访谈。
