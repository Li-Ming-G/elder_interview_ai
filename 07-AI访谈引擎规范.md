# 07｜AI 访谈引擎规范

## 1. 目标

AI 访谈引擎帮助倾听员：

- 记住已经谈过的内容；
- 发现人物、事件、时间和故事线索；
- 识别冲突、未确认信息和拒绝边界；
- 生成与当前谈话相关的追问；
- 判断当前是否应该继续倾听。

AI 不直接采访长者，不自动写传记。

## 2. 核心原则

1. 业务数据库是长期记忆来源。
2. 不依赖模型自身会话记忆。
3. 只处理确定态转录。
4. 所有结论必须关联证据片段。
5. AI 不得覆盖人工确认事实。
6. 禁止使用内容不得进入普通上下文。
7. 不确定信息必须标记，不得自动补全。
8. 可以返回“继续倾听”，不强制生成问题。
9. AI 失败不得影响录音和转录。
10. 提示词、模型和 Schema 必须版本化。
11. 角色相关消费只使用 `trusted_effective_role`；provider 自动候选、未确认角色和校准控制内容不得冒充长者事实或真实提问。

## 3. 两阶段 AI

### 3.1 阶段一：访谈记录员

职责：

- 提取人物；
- 提取关系；
- 提取地点；
- 提取时间和时间范围；
- 提取事件；
- 识别原因、选择、冲突和转折；
- 识别重要原话；
- 识别拒绝和边界；
- 识别未确认信息；
- 更新当前话题状态。

输出示例：

```json
{
  "memory_upserts": [],
  "conflicts": [],
  "important_quotes": [],
  "boundaries": [],
  "open_questions": [],
  "topic_state": {
    "current_topic": "",
    "topic_complete": false
  }
}
```

每个提取项必须包含：

```json
{
  "evidence_segment_ids": ["uuid"],
  "confidence": 0.86
}
```

### 3.2 阶段二：采访导演

输入：

- 固定访谈规则；
- 长者基础信息；
- 最近确定态对话；
- 相关结构化记忆；
- 已经问过的问题；
- 尚未确认的问题；
- 不再追问边界；
- 当前话题状态；
- 少量基础问题。

输出：

```json
{
  "primary": {
    "question": "",
    "reason": "",
    "purpose": "detail",
    "risk": "low",
    "confidence": 0.85,
    "evidence_segment_ids": []
  },
  "alternatives": [],
  "should_wait": false,
  "wait_reason": ""
}
```

允许：

```json
{
  "primary": null,
  "alternatives": [],
  "should_wait": true,
  "wait_reason": "长者仍在连续讲述完整事件，不适合打断。"
}
```

## 4. 结构化长期记忆

记忆类型：

- 人物；
- 人物关系；
- 地点；
- 时间；
- 时间范围；
- 事件；
- 工作经历；
- 家庭事件；
- 重要选择；
- 原因；
- 冲突；
- 转折；
- 重要原话；
- 未确认信息；
- 相互冲突的信息；
- 已问问题；
- 不再追问边界。

“已问问题”只能从真实 final 转录中的倾听员发问提取或回链，不根据 AI 建议的采用/已问按钮推断。

角色消费规则：

- `trusted_effective_role=elder`：可以作为长者事实、故事记忆和普通许可上下文的来源，不作为“已问问题”；
- `trusted_effective_role=interviewer`：可以用于识别真实提问和必要的近邻对话上下文，不得形成长者事实记忆；
- `trusted_effective_role=unknown`：保留证据但不得进入角色相关长期记忆、确定的已问问题或自动建议内容上下文；上下文不足时返回“继续倾听”；
- `content_kind=speaker_calibration`：不进入故事记忆、已问问题、普通摘要或普通 AI 上下文，无论其角色值为何；
- 角色显示值不能替代可信度。`effective_role=elder` 但 authority 未确认时，AI 仍必须按 unknown 处理。

状态：

- `candidate`
- `confirmed`
- `uncertain`
- `conflicting`
- `rejected`
- `restricted`

第一版产品不向倾听员展示这些内部状态、记忆清单、冲突列表或置信度。自动提取的可信 elder 记忆可以直接进入后台 current memory，用于当前下一问和第二次访谈开场；内部状态仍必须保留，不能因为 UI 简化而删除证据、冲突、版本、删除范围或失效关系。

### 4.1 更新规则

1. AI 新提取内容先保存为带证据和生成版本的自动记忆；只要来自 trusted elder final 且未命中边界，就可以成为后台 current memory，不要求普通 UI 人工确认。
2. 同一事实出现明显冲突时保留多份证据，不通过“最后一次出现”静默覆盖；当前问题优先生成自然澄清，而不是把任一版本写进问题前提。
3. 长者明确更正后，新说法成为未来生成和跨会话继承的当前值；旧证据和历史版本保留，但 future eligibility 失效。
4. 长者仍无法确定时，current view 使用大致范围或 unknown，不强行选择一个精确事实。
5. `restricted`、活动 deletion scope 或证据失效内容不得进入普通 current view；`do_not_ask` 只以最小控制信封影响问题生成；`sensitive` 按最小化和风险规则处理。
6. 人工确认事实和边界保持更高 authority，自动分析不能覆盖或解除，只能提出复核提示。
7. 第一版最小类型限于人物/关系、地点、事件、时间/范围、重要选择/原因线索和未讲完故事；只保存会改变当前下一问、第二次开场或安全边界的信息。

## 5. 上下文构建

每次调用不发送全部历史转录。

上下文分为三层：

### 5.1 服务端本地策略状态

不发送给模型，包含 marker ID、范围和状态、原始受限片段、完整操作备注、删除请求状态、权限、审计、哈希和内部关联。

### 5.2 最小控制信封

只用于约束模型，不作为故事事实。可以包含：

```json
{
  "policy_version": "boundary-v1",
  "avoid_topics": [
    {
      "boundary_id": "opaque-id",
      "label": "不要追问某段家庭矛盾",
      "action": "do_not_generate_questions"
    }
  ],
  "fallback": "continue_listening"
}
```

规则：

- `label` 必须由倾听员确认，是短小抽象主题，不包含原话、人名、地点、金额、疾病详情等；
- 不发送 `do_not_ask` 的原始标记正文、普通操作备注或内部权限信息；
- hash 只能用于完整性和去重，不能代替模型可理解的禁区标签；
- 无法构造安全标签时，不发送原文并暂停自动建议。

### 5.3 许可内容上下文

组成：

```text
固定规则
+
长者基础信息
+
当前相关记忆
+
最近 3 至 5 分钟确定态转录
+
最近问题历史
+
待确认事项
+
禁止追问边界
+
当前话题状态
+
相关基础问题
```

其中“禁止追问边界”只引用最小控制信封，不引用原始受限正文。`sensitive` 仅在授权有效且当前任务直接必要时最小化进入许可内容上下文，并使问题风险至少提升一级；内容上下文必须排除 `restricted`、待删除内容、`do_not_ask` 的原始标记范围及其受限派生记忆。

### 5.4 构建顺序

1. 获取最近确定态转录；
2. 排除校准控制片段，并按 `trusted_effective_role` 分离长者事实、倾听员提问和 unknown；
3. 识别当前人物和事件关键词；
4. 查询相关记忆；
5. 获取最近已问问题；
6. 获取未解决事项；
7. 在服务端加载并合并项目全部会话的有效边界；
8. 生成最小控制信封并过滤许可内容；
9. 按优先级截断；
10. 生成模型输入。

### 5.5 优先级

从高到低：

1. 禁止追问边界；
2. 最近原始对话；
3. 人工确认事实；
4. 当前人物和事件；
5. 未确认问题；
6. 最近已问问题；
7. AI 推断信息；
8. 通用问题库。

### 5.6 输出过滤

- 模型输出必须在服务端再次与有效边界匹配；
- AI job 在入队前、调用供应商前、结果持久化前和展示前都必须重新读取非终态 deletion scope；`project` 命中整个项目，`session` 命中该会话内容及派生记忆，`segment_range` 使用请求创建时冻结的 segment start/end 快照匹配包含该范围证据的任务，不读取可变 source marker；无法证明输出与命中范围隔离时丢弃整个 job 结果；
- 命中 scope 的排队任务标记 `cancelled`；供应商调用无法取消时允许其结束但必须丢弃输出，使用 `DELETION_REQUEST_ACTIVE` 作为内部错误码，不写 memory、question suggestion、boundary candidate 或 session note，不展示给用户，审计不得包含模型输出；
- 命中禁区、边界状态不可用、过滤器失败或是否违规无法确定时，不展示新问题，返回“继续倾听”；
- AI 不能创建最终有效 marker、解除边界、撤销删除请求或把候选状态写成已确认事实；
- 边界服务故障只暂停 AI，不能影响录音和转录。
- AI job 必须持久记录实际消费的逐 session `speaker_role_revision` 与 segment provenance；项目级记忆可能跨多个 session，禁止用单个 `ai_job.session_id` 水位冒充完整输入版本。角色修正 operation membership 命中其证据时，旧 job 派生结果立即变为不可用/待重算；在新 revision 重算完成前不得继续展示或消费，重算失败保持不可用。
- “立即不可用”适用于未来生成、后台 current view 和跨会话继承。已经展示在屏幕上的问题是不可变展示事实：普通说话人/文字/记忆修正、普通冲突或单独 `sensitive` 不自动撤下，也不自动调用 AI 重算；但下一次主动换题或正常生成必须使用最新 current memory 与水位。
- `restricted`、`do_not_ask`、活动 deletion scope、授权或访问权限失效不是上述快照例外：普通 UI 必须立即隐藏当前问题正文，查询/replay/刷新不得重新投影；只显示“继续倾听”或“AI 暂不可用”，且不自动生成替代问题。受限审计可以保留曾展示的 ID、版本和时间，不得把正文写入技术日志。
- 上述 consumer 数据模型、派生输出 stale 状态和查询过滤必须先由 `SPEC-DEV-006` 经项目负责人 PASS 冻结，DEV-006 才能开工。DEV-004C 只产生 revision 和受影响 membership；memory、已问问题、建议与 session note 的失效状态、重算、重试和错误展示分别由 DEV-006/007 实现。
- AI 自动重算不得覆盖人工确认记忆；不得解除或降低人工确认的 `sensitive|restricted|do_not_ask|deletion_request`。只允许创建人工复核提示。

### 5.7 AI 边界候选载体

- 候选由版本化 AI 输出产生并写入正式 `boundary_candidate`，不是 `content_marker`，不得被普通 marker 查询当作有效边界；
- 每项候选包含不透明 ID、候选 marker 类型、证据片段范围、最小抽象说明、状态和过期时间；原始受限正文不得复制到说明；
- 服务端根据最新未过期 `pending` 候选构造当前会话临时保守阻断；确认时以一个事务创建正式 marker 并更新候选状态，驳回、过期或会话结束后不得继续作为有效边界；
- 删除表达候选确认时还必须按 `05` 登记正式 `deletion_request` 工作流；候选本身不代表删除申请已经成立；
- 模型只能提出候选，候选状态变化和正式 marker 创建均由人工动作与服务端规则决定并审计。

## 6. 自动触发

只分析确定态文字。

触发条件：

1. 新增确定态文字达到阈值；
2. 距离上次分析达到时间阈值，且形成完整段落；
3. 倾听员点击“帮我想下一问”；
4. 长时间冷场，倾听员主动请求；
5. 出现明显人物、时间或事件转折。

初始配置：

| 参数 | 初始值 |
|---|---:|
| 最短自动调用间隔 | 20 秒 |
| 新增文字阈值 | 100 至 180 个中文字符 |
| 最近上下文 | 3 至 5 分钟 |
| 最近问题历史 | 20 个 |
| 首选问题 | 1 个 |
| 同时展示的备选问题 | 0 个 |

所有参数配置化。

## 7. 追问规则

AI 生成的问题应：

- 与最近对话直接相关；
- 尽量使用长者刚刚提到的人物或事件；
- 不重复已经问过的问题；
- 一次只问一个主要问题；
- 优先人物、场景、原因、选择、冲突和转折；
- 不把推测写进问题前提；
- 不重新推荐明确拒绝的话题；
- 显示推荐原因；
- 显示证据片段；
- 标明风险；
- 必要时建议继续倾听。
- 只把可信 `interviewer` final 当作真实已问问题，只把可信 `elder` final 当作长者故事证据；unknown 或校准控制内容不足以生成建议时返回继续倾听。

AI 不得：

- 自动播放问题；
- 自动打断长者；
- 自动删除内容；
- 自动绕过敏感边界；
- 把 AI 推断当作事实；
- 生成传记正文；
- 用其他项目内容影响当前项目。

## 8. 问题目的和风险

目的：

- `detail`
- `cause`
- `person`
- `scene`
- `emotion`
- `choice`
- `conflict`
- `turning_point`
- `clarify`
- `timeline`
- `transition`

风险：

- `low`
- `medium`
- `high`

高风险问题默认不自动突出，应明确提醒倾听员谨慎判断。

## 9. 问题生命周期

首次访谈最小纵向闭环同时只展示一个问题及一句原因，或返回“继续倾听”。倾听员无需登记采用、实际问出、忽略、稍后或改写；AI 以真实转录中的倾听员发问判断已经问过的内容。

唯一显式反馈为“没用，换一个”。替换后的问题不得与当前问题高度相似，也不得重复当前会话真实转录中已出现的问题。该反馈可用于当前会话排除和产品评测，但不自动用于训练外部模型。

访谈中不增加“采用/已问/忽略”操作。会后由版本化 Agent/LLM structured job 分析可信 interviewer final，提取倾听员实际问出的全部问题，包括从未由系统推荐的自发问题，再与系统曾展示问题和换题动作做语义匹配：

- `actual_asked`：可信倾听员 final 中找到相同或语义相近问题；
- `explicitly_replaced`：存在明确“换一个”动作；
- `not_observed`：没有转录匹配，也没有明确换题证据；
- `unjudged`：ASR degraded/not_started、角色不可信、证据不足或分析失败。

只有 `actual_asked` 进入下一次访谈防重复集合；其他状态不得冒充已问。会后分析失败不更新可靠目录，保留此前结果等待以后显式重新处理，不做无限自动重试。

替换请求的幂等键、节流、相似度规则、最小持久化状态及并发行为由 `SPEC-AI-QUESTION-001` 冻结。在此之前不得实现旧的 `adopted/asked/ignored/saved_for_later` 生命周期。

## 10. 基础问题库

基础问题库可作为后续人工访谈资源，但第一版 AI unavailable 时不自动回退展示。它可用于：

- 冷场；
- AI 服务不可用；
- 长者尚未进入具体事件；
- 第一轮低风险引导。

问题应本土化，按方向组织：

- 出生地和童年环境；
- 家庭生活；
- 求学与成长；
- 第一次离家；
- 第一份工作；
- 重要人物；
- 人生选择；
- 家庭变化；
- 城市和时代变化；
- 给晚辈的话。

基础问题库不是固定采访脚本，不能阻止对当前故事的实时追问。

## 11. 结构化输出

所有输出使用严格 JSON Schema。

规则：

1. 供应商支持时启用结构化输出。
2. 后端仍必须进行 Schema 校验。
3. 解析失败不在后台持续自动重试；是否允许一次受控格式修复由 `SPEC-DEV-006` 按调用类型冻结并完整记录。
4. 失败记录不泄密错误和供应商请求 ID。
5. 失败结果不得更新长期记忆。
6. 问题生成失败时显示“AI 暂不可用”，不自动回退基础问题库。
7. 模型自然语言不得直接写入正式数据字段。

## 12. 提示词版本

每次 AI 任务记录：

- 模型供应商；
- 模型名称；
- 提示词版本；
- Schema 版本；
- 上下文构建版本；
- 调用时间；
- 输入哈希；
- token 使用；
- 延迟；
- 输出状态。

提示词变更必须：

1. 新建版本；
2. 在评测集上对比；
3. 通过最低门槛；
4. 再进入测试或生产。

内部试用还必须建立可复盘的过程记录：模型、提示词、Schema/上下文版本；每次调用的 segment 和 memory 版本引用；触发时间、输出、证据、状态、错误和延迟；展示、换题、手动重试；实际问出问题和来源匹配；修正/边界时点及当时快照是否保留；第二次访谈继承内容；人工复盘备注与下一版调整项。技术日志只记录 ID/引用/版本/状态，不复制完整转录、完整模型输入或敏感正文。

## 13. 失败和降级

### 模型超时

- 不影响录音和转录；
- 页面显示 AI 暂不可用；
- 暂不提供问题，不返回基础题；
- 不持续后台自动重试；
- 每次“换一个”只发起一次新的幂等请求，失败后保持不可用。

### JSON 失败

- 按 SPEC 冻结的单次 attempt 处理，不持续后台修复；
- 失败不更新记忆或实际问题目录；
- 记录错误码和原始供应商请求 ID。

### 上下文超限

- 按优先级截断；
- 不截断拒绝边界和最近原始对话；
- 记录截断信息。

### 边界加载或过滤失败

- 暂停自动建议；
- 不发送受限原文作为补偿；
- 返回“继续倾听”并提示倾听员当前为安全降级；
- 新会话未成功加载跨会话边界前，不启动自动 AI 建议。

### 连续失败

- 暂停自动触发；
- 只保留手动请求；
- 告警管理员。

## 14. AI 评测集

建立不少于 30 段脱敏或虚构中文访谈片段，覆盖：

- 没什么可讲；
- 冷场；
- 重复；
- 时间模糊；
- 人物混乱；
- 转折；
- 冲突；
- 明确拒绝；
- 敏感话题；
- 口音和方言；
- ASR 错字；
- 事实冲突；
- 连续讲述不应打断；
- 已问过相似问题。

人工标注：

- 可接受追问；
- 不可接受追问；
- 应继续倾听；
- 风险等级；
- 应提取记忆；
- 应保留原话；
- 应识别边界。

## 15. 评测指标

- 有用问题率；
- 修改后可用率；
- 重复问题率；
- 高风险不当追问率；
- 边界识别召回率；
- 事实提取准确率；
- 证据回链准确率；
- JSON 解析成功率；
- 平均延迟；
- 单会话 token 和费用。

DISC-006 与首个内部记忆试用不设置问题质量百分比通过门槛，以虚构/脱敏场景、过程记录完整性和人工定性复盘为主。上述指标继续采集并作为观察项；真实模型、真实数据或真实试点前再由 `09` 恢复正式质量与安全门禁。

## 16. 后续规划

MVP 不使用独立向量数据库。

未来当单项目和跨项目数据量明显增加时，可以评估：

- PostgreSQL 内向量扩展；
- 语义检索；
- 树状人生经历；
- 人工传记整理工作流；
- 半自动初稿生成。

不得提前把这些规划写进当前主链路。
