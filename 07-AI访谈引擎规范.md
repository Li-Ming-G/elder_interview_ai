# 07｜AI 访谈引擎规范

## 1. 目标

AI 访谈引擎帮助倾听员：

- 记住已经谈过的内容；
- 发现人物、事件、时间和故事线索；
- 识别冲突、未确认信息和拒绝边界；
- 生成与当前谈话相关的追问；
- 判断当前是否应该继续倾听。

AI 不直接采访长者，不自动写传记。

## 2. 核心原则

1. 业务数据库是长期记忆来源。
2. 不依赖模型自身会话记忆。
3. 只处理确定态转录。
4. 所有结论必须关联证据片段。
5. AI 不得覆盖人工确认事实。
6. 禁止使用内容不得进入普通上下文。
7. 不确定信息必须标记，不得自动补全。
8. 可以返回“继续倾听”，不强制生成问题。
9. AI 失败不得影响录音和转录。
10. 提示词、模型和 Schema 必须版本化。

## 3. 两阶段 AI

### 3.1 阶段一：访谈记录员

职责：

- 提取人物；
- 提取关系；
- 提取地点；
- 提取时间和时间范围；
- 提取事件；
- 识别原因、选择、冲突和转折；
- 识别重要原话；
- 识别拒绝和边界；
- 识别未确认信息；
- 更新当前话题状态。

输出示例：

```json
{
  "memory_upserts": [],
  "conflicts": [],
  "important_quotes": [],
  "boundaries": [],
  "open_questions": [],
  "topic_state": {
    "current_topic": "",
    "topic_complete": false
  }
}
```

每个提取项必须包含：

```json
{
  "evidence_segment_ids": ["uuid"],
  "confidence": 0.86
}
```

### 3.2 阶段二：采访导演

输入：

- 固定访谈规则；
- 长者基础信息；
- 最近确定态对话；
- 相关结构化记忆；
- 已经问过的问题；
- 尚未确认的问题；
- 不再追问边界；
- 当前话题状态；
- 少量基础问题。

输出：

```json
{
  "primary": {
    "question": "",
    "reason": "",
    "purpose": "detail",
    "risk": "low",
    "confidence": 0.85,
    "evidence_segment_ids": []
  },
  "alternatives": [],
  "should_wait": false,
  "wait_reason": ""
}
```

允许：

```json
{
  "primary": null,
  "alternatives": [],
  "should_wait": true,
  "wait_reason": "长者仍在连续讲述完整事件，不适合打断。"
}
```

## 4. 结构化长期记忆

记忆类型：

- 人物；
- 人物关系；
- 地点；
- 时间；
- 时间范围；
- 事件；
- 工作经历；
- 家庭事件；
- 重要选择；
- 原因；
- 冲突；
- 转折；
- 重要原话；
- 未确认信息；
- 相互冲突的信息；
- 已问问题；
- 不再追问边界。

状态：

- `candidate`
- `confirmed`
- `uncertain`
- `conflicting`
- `rejected`
- `restricted`

### 4.1 更新规则

1. AI 新提取内容默认 `candidate` 或 `uncertain`。
2. 人工确认后变为 `confirmed`。
3. 同一事实出现不同值时建立冲突组。
4. 冲突不得通过“最后一次出现”自动覆盖。
5. 被拒绝的 AI 推断保留审计，但不再作为普通事实使用。
6. `restricted` 内容不得进入普通上下文。
7. 删除请求内容停止后续处理。

## 5. 上下文构建

每次调用不发送全部历史转录。

上下文组成：

```text
固定规则
+
长者基础信息
+
当前相关记忆
+
最近 3 至 5 分钟确定态转录
+
最近问题历史
+
待确认事项
+
禁止追问边界
+
当前话题状态
+
相关基础问题
```

### 5.1 构建顺序

1. 获取最近确定态转录；
2. 识别当前人物和事件关键词；
3. 查询相关记忆；
4. 获取最近已问问题；
5. 获取未解决事项；
6. 获取边界；
7. 按优先级截断；
8. 生成模型输入。

### 5.2 优先级

从高到低：

1. 禁止追问边界；
2. 最近原始对话；
3. 人工确认事实；
4. 当前人物和事件；
5. 未确认问题；
6. 最近已问问题；
7. AI 推断信息；
8. 通用问题库。

## 6. 自动触发

只分析确定态文字。

触发条件：

1. 新增确定态文字达到阈值；
2. 距离上次分析达到时间阈值，且形成完整段落；
3. 倾听员点击“帮我想下一问”；
4. 长时间冷场，倾听员主动请求；
5. 出现明显人物、时间或事件转折。

初始配置：

| 参数 | 初始值 |
|---|---:|
| 最短自动调用间隔 | 20 秒 |
| 新增文字阈值 | 100 至 180 个中文字符 |
| 最近上下文 | 3 至 5 分钟 |
| 最近问题历史 | 20 个 |
| 首选问题 | 1 个 |
| 备选问题 | 最多 2 个 |

所有参数配置化。

## 7. 追问规则

AI 生成的问题应：

- 与最近对话直接相关；
- 尽量使用长者刚刚提到的人物或事件；
- 不重复已经问过的问题；
- 一次只问一个主要问题；
- 优先人物、场景、原因、选择、冲突和转折；
- 不把推测写进问题前提；
- 不重新推荐明确拒绝的话题；
- 显示推荐原因；
- 显示证据片段；
- 标明风险；
- 必要时建议继续倾听。

AI 不得：

- 自动播放问题；
- 自动打断长者；
- 自动删除内容；
- 自动绕过敏感边界；
- 把 AI 推断当作事实；
- 生成传记正文；
- 用其他项目内容影响当前项目。

## 8. 问题目的和风险

目的：

- `detail`
- `cause`
- `person`
- `scene`
- `emotion`
- `choice`
- `conflict`
- `turning_point`
- `clarify`
- `timeline`
- `transition`

风险：

- `low`
- `medium`
- `high`

高风险问题默认不自动突出，应明确提醒倾听员谨慎判断。

## 9. 问题生命周期

```text
generated
  ↓
displayed
  ├─ adopted
  │    └─ asked
  ├─ ignored
  ├─ saved_for_later
  └─ expired
```

保存倾听员反馈：

- 采用；
- 实际问出；
- 忽略；
- 稍后；
- 改写；
- 重复；
- 不合适；
- 过于敏感。

这些反馈用于产品评测，不自动用于训练外部模型。

## 10. 基础问题库

基础问题库用于：

- 冷场；
- AI 服务不可用；
- 长者尚未进入具体事件；
- 第一轮低风险引导。

问题应本土化，按方向组织：

- 出生地和童年环境；
- 家庭生活；
- 求学与成长；
- 第一次离家；
- 第一份工作；
- 重要人物；
- 人生选择；
- 家庭变化；
- 城市和时代变化；
- 给晚辈的话。

基础问题库不是固定采访脚本，不能阻止对当前故事的实时追问。

## 11. 结构化输出

所有输出使用严格 JSON Schema。

规则：

1. 供应商支持时启用结构化输出。
2. 后端仍必须进行 Schema 校验。
3. 解析失败后只自动修复或重试一次。
4. 再次失败则记录错误。
5. 失败结果不得更新长期记忆。
6. 问题生成失败时回退到基础问题库。
7. 模型自然语言不得直接写入正式数据字段。

## 12. 提示词版本

每次 AI 任务记录：

- 模型供应商；
- 模型名称；
- 提示词版本；
- Schema 版本；
- 上下文构建版本；
- 调用时间；
- 输入哈希；
- token 使用；
- 延迟；
- 输出状态。

提示词变更必须：

1. 新建版本；
2. 在评测集上对比；
3. 通过最低门槛；
4. 再进入测试或生产。

## 13. 失败和降级

### 模型超时

- 不影响录音和转录；
- 保留旧建议；
- 页面显示 AI 暂不可用；
- 允许手动重试；
- 可返回基础问题。

### JSON 失败

- 自动修复或重试一次；
- 仍失败则不更新记忆；
- 记录错误码和原始供应商请求 ID。

### 上下文超限

- 按优先级截断；
- 不截断拒绝边界和最近原始对话；
- 记录截断信息。

### 连续失败

- 暂停自动触发；
- 只保留手动请求；
- 告警管理员。

## 14. AI 评测集

建立不少于 30 段脱敏或虚构中文访谈片段，覆盖：

- 没什么可讲；
- 冷场；
- 重复；
- 时间模糊；
- 人物混乱；
- 转折；
- 冲突；
- 明确拒绝；
- 敏感话题；
- 口音和方言；
- ASR 错字；
- 事实冲突；
- 连续讲述不应打断；
- 已问过相似问题。

人工标注：

- 可接受追问；
- 不可接受追问；
- 应继续倾听；
- 风险等级；
- 应提取记忆；
- 应保留原话；
- 应识别边界。

## 15. 评测指标

- 有用问题率；
- 修改后可用率；
- 重复问题率；
- 高风险不当追问率；
- 边界识别召回率；
- 事实提取准确率；
- 证据回链准确率；
- JSON 解析成功率；
- 平均延迟；
- 单会话 token 和费用。

首轮门槛见 `09-测试与验收规范.md`。

## 16. 后续规划

MVP 不使用独立向量数据库。

未来当单项目和跨项目数据量明显增加时，可以评估：

- PostgreSQL 内向量扩展；
- 语义检索；
- 树状人生经历；
- 人工传记整理工作流；
- 半自动初稿生成。

不得提前把这些规划写进当前主链路。
