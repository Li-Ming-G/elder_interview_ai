# 07｜AI 访谈引擎规范

## 1. 目标

AI 访谈引擎帮助倾听员：

- 记住已经谈过的内容；
- 发现人物、事件、时间和故事线索；
- 识别冲突、未确认信息和拒绝边界；
- 生成与当前谈话相关的追问；
- 判断当前是否应该继续倾听。

AI 不直接采访长者，不自动写传记。

## 2. 核心原则

1. 业务数据库是长期记忆来源。
2. 不依赖模型自身会话记忆。
3. 只处理确定态转录。
4. 所有结论必须关联证据片段。
5. AI 不得覆盖人工确认事实。
6. 禁止使用内容不得进入普通上下文。
7. 不确定信息必须标记，不得自动补全。
8. 可以返回“继续倾听”，不强制生成问题。
9. AI 失败不得影响录音和转录。
10. 提示词、模型和 Schema 必须版本化。
11. 角色相关消费只使用 `trusted_effective_role`；provider 自动候选、未确认角色和校准控制内容不得冒充长者事实或真实提问。
12. 第一版问题生成把当前激活、启用、许可合格且适用的版本化题库条目作为可选参考；模型可以采用、广泛改写或完全不用题库，自由生成仍必须受可信上下文与服务端硬边界约束。
13. 模型实际看过的题库 membership、模型声明使用的 attribution、事实 grounding 和运行时资格是四类不同关系：前两者分别回答“发送了什么”和“模型自述用了什么”；grounding 声明使用的可信转录/current memory ID；权限、安全、版本与 retention 决定能否发布。
14. 数据库读写、上下文裁剪、事务、幂等和安全裁决由确定性后端服务负责。模型不直连数据库、不决定查询范围，也不是第二个负责规划工具的智能体。

## 3. 两阶段 AI

### 3.1 阶段一：访谈记录员

职责：

- 提取人物；
- 提取关系；
- 提取地点；
- 提取时间和时间范围；
- 提取事件；
- 识别原因、选择、冲突和转折；
- 识别重要原话；
- 识别拒绝和边界；
- 识别未确认信息；
- 更新当前话题状态。

输出示例：

```json
{
  "memory_claims": [],
  "conflicts": [],
  "important_quotes": [],
  "boundaries": [],
  "open_questions": [],
  "topic_state": {
    "current_topic": "",
    "topic_complete": false
  }
}
```

每个提取项必须包含：

```json
{
  "evidence_segment_ids": ["uuid"],
  "value_kind": "exact|range|unknown"
}
```

### 3.2 阶段二：采访导演

模型实际输入只遵循 [`InterviewDirectorContextV1`](docs/contracts/interview-director-context.schema.json)，实际输出只遵循 [`InterviewDirectorOutputV1`](docs/contracts/interview-director-output.schema.json)。本规范不重复列举字段、枚举或 JSON 示例。

业务语义：后端把经过许可、安全和可信角色筛选的当前谈话事实、长期记忆、实际已问、近期展示、旅程阶段、最小边界以及可选题库参考投影成 Context；模型返回一个建议或继续倾听。题库引用可以为空；`grounding` 与声明题库引用都只允许指向本次 Context 中的 ID。模型输出不能修改任何输入源事实，只能作为后端基础硬校验后追加 question candidate/display history 的候选；是否真正提问始终由倾听员决定。

## 4. 结构化长期记忆

第一版记忆类型严格限于：人物、关系、地点、事件、时间、时间范围、重要选择、原因线索、未讲完故事。冲突、unknown/range、authority 和 current/history 是 resolution 语义，不是额外记忆类型；实际问题与边界分别由 `QuestionEvidenceModule` 和 boundary/deletion 权威模块持有，绝不塞入 memory。

角色消费规则：

- `trusted_effective_role=elder`：可以作为长者事实、故事记忆和普通许可上下文的来源，不作为“已问问题”；
- `trusted_effective_role=interviewer`：可以用于识别真实提问和必要的近邻对话上下文，不得形成长者事实记忆；
- `trusted_effective_role=unknown`：保留证据但不得进入角色相关长期记忆、确定的已问问题或自动建议内容上下文；上下文不足时返回“继续倾听”；
- `content_kind=speaker_calibration`：不进入故事记忆、已问问题、普通摘要或普通 AI 上下文，无论其角色值为何；
- 角色显示值不能替代可信度。`effective_role=elder` 但 authority 未确认时，AI 仍必须按 unknown 处理。

实现必须分离不可变 `memory_claim/evidence`、版本化 `memory_resolution/member` 和动态 future eligibility。第一版产品不向倾听员展示内部记忆、冲突列表或置信度；自动提取的可信 elder 记忆可以直接进入后台 current memory，用于当前下一问和第二次访谈开场，但证据、版本、冲突、删除范围和失效关系必须保留。

### 4.1 更新规则

1. AI 新提取内容先保存为带证据和生成版本的自动记忆；只要来自 trusted elder final 且未命中边界，就可以成为后台 current memory，不要求普通 UI 人工确认。
2. 同一事实出现明显冲突时保留多份证据，不通过“最后一次出现”静默覆盖；当前问题优先生成自然澄清，而不是把任一版本写进问题前提。
3. 长者明确更正后，新说法成为未来生成和跨会话继承的当前值；旧证据和历史版本保留，但 future eligibility 失效。
4. 长者仍无法确定时，current view 使用大致范围或 unknown，不强行选择一个精确事实。
5. `restricted`、活动 deletion scope 或证据失效内容不得进入普通 current view；`do_not_ask` 只以最小控制信封影响问题生成；`sensitive` 按最小化和风险规则处理。
6. 人工确认事实和边界保持更高 authority，自动分析不能覆盖或解除，只能提出复核提示。
7. 第一版最小类型限于人物/关系、地点、事件、时间/范围、重要选择/原因线索和未讲完故事；只保存会改变当前下一问、第二次开场或安全边界的信息。

## 5. 上下文构建

每次调用不发送全部历史转录。

上下文分为三层：

### 5.1 服务端本地策略状态

不发送给模型，包含 marker ID、范围和状态、原始受限片段、完整操作备注、删除请求状态、权限、审计、哈希和内部关联。

### 5.2 最小控制信封

只用于约束模型，不作为故事事实。可以包含：

```json
{
  "policy_version": "boundary-v1",
  "avoid_topics": [
    {
      "boundary_id": "opaque-id",
      "label": "不要追问某段家庭矛盾",
      "action": "do_not_generate_questions"
    }
  ],
  "fallback": "continue_listening"
}
```

规则：

- `label` 必须由倾听员确认，是短小抽象主题，不包含原话、人名、地点、金额、疾病详情等；
- 不发送 `do_not_ask` 的原始标记正文、普通操作备注或内部权限信息；
- hash 只能用于完整性和去重，不能代替模型可理解的禁区标签；
- 无法构造安全标签时，不发送原文并暂停自动建议。

### 5.3 许可内容上下文

组成：

```text
固定规则
+
长者基础信息
+
当前相关记忆
+
最近 3 至 5 分钟确定态转录
+
最近问题历史
+
当前访谈旅程阶段
+
当前激活题库的已过滤候选条目
+
待确认事项
+
禁止追问边界
+
当前话题状态
+
当前 published actual-question 防重复集合
```

其中“禁止追问边界”只引用最小控制信封，不引用原始受限正文。`sensitive` 仅在授权有效且当前任务直接必要时最小化进入许可内容上下文，并使问题风险至少提升一级；内容上下文必须排除 `restricted`、待删除内容、`do_not_ask` 的原始标记范围及其受限派生记忆。

### 5.4 构建顺序

1. 获取最近确定态转录；
2. 排除校准控制片段，并按 `trusted_effective_role` 分离长者事实、倾听员提问和 unknown；
3. 识别当前人物和事件关键词；
4. 查询相关记忆；
5. 获取最近已问问题；
6. 获取未解决事项；
7. 在服务端加载并合并项目全部会话的有效边界；
8. 生成最小控制信封并过滤许可内容；
9. 以当前 `journey_policy_version` 确定阶段，并从 active release 中筛选题库允许集合；
10. 按优先级截断；
11. 生成模型输入。

题库条目过多时按主题、阶段、适用条件和当前 evidence/memory 做确定性预筛，不把整个题库发送给模型。预筛结果只是参考集合，模型仍可生成题库外问题。

### 5.5 优先级

从高到低：

1. 禁止追问边界；
2. 最近原始对话；
3. 人工确认事实；
4. 当前人物和事件；
5. 未确认问题；
6. 最近已问问题；
7. AI 推断信息；
8. 低 authority 自动主张。

### 5.6 输出过滤

- 模型输出必须在服务端再次与有效边界匹配；
- AI job 在入队前、调用供应商前、结果持久化前和展示前都必须重新读取非终态 deletion scope；`project` 命中整个项目，`session` 命中该会话内容及派生记忆，`segment_range` 使用请求创建时冻结的 segment start/end 快照匹配包含该范围证据的任务，不读取可变 source marker；无法证明输出与命中范围隔离时丢弃整个 job 结果；
- 已落库 job 的任一 input membership 命中 deletion 时隐藏/清理整个 ai_job retention root；普通 correction/policy stale 才按逐业务输出最小单元失效。独立 display snapshot 另按其复制正文/证据是否命中 deletion 判定，不能因 candidate job 清理就误删未命中的历史展示事实；
- 命中 scope 的排队任务标记 `cancelled`；供应商调用无法取消时允许其结束但必须丢弃输出，使用 `DELETION_REQUEST_ACTIVE` 作为内部错误码，不写 memory、question suggestion、boundary candidate 或 session note，不展示给用户，审计不得包含模型输出；
- 命中禁区、边界状态不可用、过滤器失败或是否违规无法确定时，不展示新问题，返回“继续倾听”；
- AI 不能创建最终有效 marker、解除边界、撤销删除请求或把候选状态写成已确认事实；
- 边界服务故障只暂停 AI，不能影响录音和转录。
- AI job 必须持久记录评估范围内每个 session（包括 0 eligible segment）的 role revision/稳定 segment 高水位，以及实际消费 segment 的 text revision、role revision、role authority、content kind、effective text digest 和 membership。项目级记忆禁止使用单值 session revision 冒充完整水位。
- “立即不可用”适用于未来生成、后台 current view 和跨会话继承。已经展示在屏幕上的问题是不可变展示事实：普通说话人/文字/记忆修正、普通冲突或单独 `sensitive` 不自动撤下，也不自动调用 AI 重算；但下一次主动换题或正常生成必须使用最新 current memory 与水位。
- `restricted`、`do_not_ask`、活动 deletion scope、授权或访问权限失效不是上述快照例外：普通 UI 必须立即隐藏当前问题正文，查询/replay/刷新不得重新投影；只显示“继续倾听”或“AI 暂不可用”，且不自动生成替代问题。受限审计可以保留曾展示的 ID、版本和时间，不得把正文写入技术日志。
- `SPEC-DEV-006` 已冻结 consumer 数据模型、派生输出状态和权威查询过滤；项目负责人 PASS 前 DEV-006 仍保持 BLOCKED。DEV-004C 只产生 revision 与受影响 membership；AI 资格查询用 anti-join/version 谓词立即排除，不要求 C2 同步回写 AI 表。
- AI 自动重算不得覆盖人工确认记忆；不得解除或降低人工确认的 `sensitive|restricted|do_not_ask|deletion_request`。只允许创建人工复核提示。

### 5.7 AI 边界候选载体

- 候选由版本化 AI 输出产生并写入正式 `boundary_candidate`，不是 `content_marker`，不得被普通 marker 查询当作有效边界；
- 每项候选包含不透明 ID、候选 marker 类型、证据片段范围、最小抽象说明、状态和过期时间；原始受限正文不得复制到说明；
- 服务端根据最新未过期 `pending` 候选构造当前会话临时保守阻断；确认时以一个事务创建正式 marker 并更新候选状态，驳回、过期或会话结束后不得继续作为有效边界；
- 删除表达候选确认时还必须按 `05` 登记正式 `deletion_request` 工作流；候选本身不代表删除申请已经成立；
- 模型只能提出候选，候选状态变化和正式 marker 创建均由人工动作与服务端规则决定并审计。

### 5.8 输入冻结、调用与写回

每个 AI job 固定执行三段式协议：

1. 冻结：按 `request/trigger -> project -> session_id 升序` 加锁，重读 assignment、授权、项目状态、boundary、活动 deletion scope 和 policy revision；同一事务写 job、所有 session scope、实际 segment/memory membership 与输入 hash。没有合格片段的被评估 session 也必须有 scope 行；
2. 调用：提交事务后才调用供应商，不持数据库锁。实时问题生成先执行一次 primary；仅受控技术失败允许在同一 job/attempt 内执行一次 `same_input_retry`，并分别记录。两次使用完全相同的 Prompt、frozen Context、Output Schema、model config 和 input hash；第二次不接收第一次输出、错误或修复提示；
3. 写回：按相同锁序重锁并重新验证全部权限、policy、水位、membership、版本、digest、boundary 和 deletion scope。任何漂移都取消 job、丢弃响应且不写派生正文；校验成功时输出、依赖和 current/published 切换同一事务提交。

future eligibility 的最终权威是每次查询时的组合谓词，不是 `materialized_status`。普通 correction 提交后，旧输出即使尚未被异步标为 invalidated 也必须被 version/operation anti-join 排除；重算失败不能恢复旧输出。硬边界还要使 displayed snapshot 普通 visibility 立即 withdrawn。

写回 cardinality 不得由实现自行推断：每条 automatic memory claim、每条 automatic resolution、每个 question/boundary candidate、每个 generated note 版本和每个 context snapshot 各创建一条 derived output；一次 job 产出五条 claim 就创建五条资格记录。会后整理例外地以整个 `actual_question_analysis` 为一个 `actual_question_catalog` 输出，所有 actual question、evidence 和 outcome 都是该版本的 children；任一 segment/memory/question dependency 失效时整版 catalog future ineligible，不做部分保留。

每条 derived output 在同一写回事务冻结 segment、memory、question 三类 dependency 的 expected count 和 canonical manifest。读取时必须先证明实际行数与 manifest 完整，再逐项校验当前版本/目标；依赖 CASCADE 删除或跨 root FK 置空都会失败关闭，不能让“剩余依赖都有效”被误判为完整有效。实际问题整理消费的 displayed snapshot、replace/withdraw event 与既有 actual question 必须成为显式 question dependency。

### 5.9 跨会话上下文快照

第二次访谈开场和每次问题生成只消费：当前 eligible memory resolution、当前 published/eligible actual question、当前有效边界与最近 eligible transcript。构建器必须把实际 memory/actual-question membership、逐 session scope、policy revision、模型/提示词/Schema/context 版本固化为 context snapshot；这不是记忆回顾 UI。

明确更正只改变未来 current value；旧 claim/evidence 保留。conflict set 生成自然澄清，不选择一个值作为问题前提。`unknown|range` 只允许开放或范围化问法。任何继承来源无法证明 current/eligible 时失败关闭并返回继续倾听或不可用。

context snapshot 自身是一项受资格控制业务输出并与一条 derived output 一对一；它随创建它的 `ai_job` retention root。用于复盘不意味着永久保留，root hidden/expired/deletion 命中后普通 consumer 不得读取其正文或 memberships。

## 6. 自动触发

只分析确定态文字。

触发条件：

1. 新增确定态文字达到阈值；
2. 距离上次分析达到时间阈值，且形成完整段落；
3. 倾听员点击“帮我想下一问”；
4. 长时间冷场，倾听员主动请求；
5. 出现明显人物、时间或事件转折。

初始配置：

| 参数 | 初始值 |
|---|---:|
| 最短自动调用间隔 | 20 秒 |
| 新增文字阈值 | 100 至 180 个中文字符 |
| 最近上下文 | 3 至 5 分钟 |
| 最近问题历史 | 20 个 |
| 首选问题 | 1 个 |
| 同时展示的备选问题 | 0 个 |
| 当前问题自动替换最短稳定时间 | 15 秒 |
| 自动替换 debounce | 1500 毫秒 |
| 自动替换最小分差 | 0.12 |
| 高度相似阈值 | 0.88 |
| 手动下一问最短间隔 | 3 秒 |
| 手动下一问 60 秒窗口上限 | 6 次 |
| 手动下一问 attempt deadline | 8 秒 |

所有参数配置化。

## 7. 追问规则

AI 生成的问题应：

- 与最近对话直接相关；
- 尽量使用长者刚刚提到的人物或事件；
- 不重复已经问过的问题；
- 一次只问一个主要问题；
- 优先人物、场景、原因、选择、冲突和转折；
- 不把推测写进问题前提；
- 不重新推荐明确拒绝的话题；
- 显示推荐原因；
- 显示证据片段；
- 标明风险；
- 必要时建议继续倾听。
- 只把可信 `interviewer` final 当作真实已问问题，只把可信 `elder` final 当作长者故事证据；unknown 或校准控制内容不足以生成建议时返回继续倾听。
- 可以参考、改写或不用当前 eligible 题库条目；题库外问题同样必须一次一问、避免重复，并为具体事实前提提供 grounding。

AI 不得：

- 自动播放问题；
- 自动打断长者；
- 自动删除内容；
- 自动绕过敏感边界；
- 把 AI 推断当作事实；
- 生成传记正文；
- 用其他项目内容影响当前项目。
- 把没有可信依据的人物、事件、时间、关系或因果写成既定事实；
- 以题库来源/许可记录冒充长者事实证据；
- 用固定题数或固定时间决定访谈阶段。

## 8. 问题目的和风险

目的：

- `detail`
- `cause`
- `person`
- `scene`
- `emotion`
- `choice`
- `conflict`
- `turning_point`
- `clarify`
- `timeline`
- `transition`

风险：

- `low`
- `medium`
- `high`

高风险问题默认不自动突出，应明确提醒倾听员谨慎判断。

## 9. 问题生命周期

首次访谈最小纵向闭环同时只展示一个问题及一句原因，或返回“继续倾听”。倾听员无需登记采用、实际问出、忽略、稍后或改写；AI 以真实转录中的倾听员发问判断已经问过的内容。

系统判断出现更合适且仍具 future eligibility 的问题时，可以自动替换当前问题。`question-select-v1` 是服务端对本次 frozen Context、candidate grounding、stage/purpose 和 risk 的确定性 comparator，不是 `InterviewDirectorOutputV1` 的平行字段，也不引入第二个模型。分数固定为：`0.55 × grounding_freshness + 0.20 × latest_answer_coverage + 0.15 × stage_purpose_fit + 0.10 × risk_fit`。

- “最近实质长者回答”只取最近一次可信 interviewer final 之后、按时间相邻的最多 3 条非空可信 elder conversation final；更早整场文本和 memory 不产生 `response.*|engagement.*` 信号；
- `grounding_freshness`：grounding 命中该回答最新 segment 为 `1`，仅命中同回答较早 segment 为 `0.65`，仅命中更早 segment 为 `0.25`，仅命中 current memory 为 `0.20`，没有 grounding 为 `0`；
- `latest_answer_coverage`：grounding 覆盖最近回答 segment 的数量除以该回答 segment 总数，最大为 `1`；没有最近回答时，存在 memory grounding 为 `0.5`，否则为 `0`；
- `stage_purpose_fit`：`rapport` 匹配 `person|scene|timeline|transition`，`life_outline` 匹配 `person|scene|timeline|clarify|transition`，`story_depth` 匹配 `detail|cause|emotion|choice|conflict|turning_point|clarify`；命中为 `1`，否则为 `0.5`；
- `risk_fit`：`low|medium|high` 分别为 `1|0.5|0`。question-sim-v1 在评分前独立过滤高度相似问题，因此不再伪造一个由模型提供的 novelty 分数。比较同阶段 current 时，必须用本次最近回答重新计算 current candidate 的 grounding freshness/coverage；不能直接拿 current 当时保存的历史 score 与新 score 比较。

session 尚无 current presentation 时，initial automatic 发布最高排序的 eligible suggestion；没有合格问题则发布“继续倾听”。`journey_policy_v1` 判定 `shouldContinueListening=true` 时不得调用 Director，直接提交 `continue_listening` attempt outcome；已有 visible suggestion 时保持 current/history，不用中性状态覆盖。已有 visible suggestion 时，automatic 只能发布满足以下条件的新 suggestion，不能用“继续倾听”或“暂不可用”自动覆盖：按本次水位重算后的 candidate score 比 current 高至少 0.12；current 已稳定 15 秒；candidate 等待 1500 ms debounce；commit 前 basis presentation revision、manual intent fence、eligibility、policy、相似度与分差全部仍成立。同分以 evidence watermark 新者、candidate 创建时间早者、UUID 字典序小者依次决定；未达到分差不得因 tie-break 自动替换。相同证据 trigger 幂等重放，自动 attempt 单飞；旧 basis 的晚到结果取消，禁止 last-writer-wins。automatic provider 调用至少间隔 20 秒，早到 trigger 只做 trailing-edge 重排，不得先调用后丢弃。

attempt 与 snapshot 还必须记录 `journey_stage/journey_policy_version` 和题库来源版本。同一阶段 basis 下才直接比较既有 selection score。阶段发生变化且 current 原题的 bank 或适用条件不再兼容时，旧 candidate 立即 future ineligible；新阶段候选仍经过安全、相似度和 debounce 检查，但旧阶段 score 不参与最小分差阻断。阶段变化本身不创建 snapshot、不标记 actual asked，也不构成 hard withdrawal；已展示事实继续遵守既有 current/history/visibility 规则。

倾听员可点击“下一个问题”主动发起一次新请求。manual next 绕过上述 dwell、debounce 和最小分差，但不绕过安全、资格、相似度和节流；请求接受即建立 intent fence，优先于更早自动 attempt。新问题不得与 current、最近 20 个 displayed snapshot 或 current published actual-question catalog 高度相似。该动作可用于当前会话排除和产品评测，但不自动用于训练外部模型。

`question-sim-v1` 的规范化固定为 Unicode NFKC、ASCII 小写、全角/半角统一、去首尾与合并连续空白、移除 Unicode 标点，再对 UTF-8 字节计算 SHA-256。exact digest 相同直接判重复；否则调用版本化、供应商中立 matcher 得到 `[0,1]` score，最大值 `>=0.88` 视为高度相似。版本与阈值写入 attempt/candidate/snapshot；实现必须通过固定中文同义改写、否定差异、人物/时间槽差异和短问句 fixture，不能以字符串相等冒充语义相似。

上下文与排除顺序固定为：①权限/授权/project/正式边界/deletion/retention/trusted role/content kind/future eligibility；②current question；③本会话最近 20 个 displayed snapshot；④current published、eligible actual-question catalog；⑤current eligible memory 与人工 boundary；⑥同 attempt 候选去重和风险过滤。硬边界任何一步失败即停止，不得为计算相似度重新暴露撤下正文。memory 只提供 grounding：conflict/unknown 生成澄清或开放问题，不得被写进前提；人工 boundary 始终高于新颖度。

每个经服务端 publication 事务真正发布的问题及原因都以单调 `display_sequence` 进入本次会话不可变展示历史；不使用客户端像素 ACK，也不只依赖 `displayed_at` 排序。“上一个问题 / 更早的问题 / 更新的问题 / 回到当前问题”只浏览该历史，不触发模型、不改变 canonical current suggestion、不恢复旧排除，也不证明问题已经实际问出。浏览位置仅为客户端状态；自动更新只提示 current revision 变化，不移动历史锚点或焦点。跨会话防重复仍只消费会后从可信倾听员 final 得到的 actual-question catalog。

访谈中不增加“采用/已问/忽略”操作。`QuestionEvidenceModule` 会后通过版本化 structured job 分析可信 interviewer conversation final，提取倾听员实际问出的全部问题，包括从未由系统推荐的自发问题；actual question 本体保存证据和 `interviewer_spontaneous|matched_system_suggestion` 来源。随后才把系统 displayed snapshot 分类为 suggestion outcome：

- `actual_asked`：可信倾听员 final 中找到相同或语义相近问题；
- `explicitly_replaced`：存在已提交的 `manual_next_committed` 事件，证明手动请求真正使旧 snapshot 离开 canonical current；仅有 `manual_next_requested`、失败、超时、响应未知或节流拒绝均不足；
- `not_observed`：没有转录匹配，也没有明确换题证据；
- `unjudged`：ASR degraded/not_started、角色不可信、证据不足或分析失败。

只有 current published、eligible actual question 进入下一次访谈防重复集合；outcome 只是展示结果判断，其他状态不得冒充已问。`not_observed` 只允许完整、可信、judgeable 分析得出；ASR/角色/证据不足统一 unjudged。会后 unjudged 或失败不覆盖以前可靠目录，首次没有可靠目录时也不把空结果解释成“没有问过”；显式重试创建新 job，不做无限自动重试。

DEV-006 建立 actual-question analysis/catalog/evidence 和可靠版本发布；DEV-007 只经该模块写 generation/display/replace 事实并读取防重复集合。两者不得各建一套 question history。

自动替换、手动 next 和 history 都只能经 `QuestionEvidenceModule` 的 `04` §4.36-4.39 与 `05` §3.9 seam。attempt succeeded 与 publication committed 是不同事实；continue-listening/unavailable 不创建伪问题 snapshot。旧 `adopted/asked/ignored/saved_for_later/rewrite` 生命周期和 `attempt_kind=replace` 永久不进入首轮实现。

## 10. 问题题库参考、访谈旅程与自由生成

题库是第一版 AI 的可选访谈方法与内容参考，不是问题正文白名单，也不是 AI 不可用时冒充 AI 结果的静态兜底。模型可以采用、广泛改写或完全不用题库；现有“AI unavailable 时不自动回退基础题”的失败规则继续有效。

### 10.1 两类内容源

- `basic`：低压力破冰、生平轮廓和话题转场，适用于 `rapport|life_outline`，也可在 `story_depth` 退回或切换话题时使用；
- `deep`：人物、事件、选择、转折、情感和未讲完故事，只在存在匹配的可信上下文且长者有继续展开意愿时使用。

题库分类不等于阶段硬开关，也不替代安全边界。系统可以在 `life_outline` 使用低风险且条件充分的 `deep`，也可以在 `story_depth` 使用 `basic` 做转场；每次选择必须能解释。

### 10.2 内容交换与编辑格式

项目负责人以 UTF-8、首行表头的 CSV 文件维护和交换题库，不直接编辑数据库。第一版正式表头固定为：

```text
question_id,bank,topic,question_text,purpose,applicable_when,inapplicable_when,sensitivity,source_type,source_reference,license_status,license_reference,bank_version,enabled
```

| 字段 | 规则 |
|---|---|
| `question_id` | 跨版本稳定的题目 ID；同一版本内唯一，被引用时用于可选 attribution |
| `bank` | `basic|deep` |
| `topic` | 短主题标签，不包含个人资料 |
| `question_text` | 原题正文；单一主要问题 |
| `purpose` | 必填；复用 §8 的受控值，帮助模型理解该参考题的访谈作用；不限制最终候选必须保持同一 purpose |
| `applicable_when` | 以 `;` 分隔的 v1 受控条件码；空值表示无额外条件 |
| `inapplicable_when` | 以 `;` 分隔的 v1 受控排除码；空值表示无额外条件 |
| `sensitivity` | `low|medium|high` |
| `source_type` | `project_original|licensed_external|public_domain|synthetic_fixture` |
| `source_reference` | 可审计的原创登记、作品/页面或内部 fixture 标识 |
| `license_status` | `project_original|verified|unverified|fixture_only` |
| `license_reference` | 许可文本、核验记录或内部 fixture 限制说明；未知许可不得留空冒充可用 |
| `bank_version` | 本次原子 release 的不可变版本字符串；推荐 `YYYY.MM.N` |
| `enabled` | 该 release 内的编辑意图 `true|false`，不等同于运行时已激活 |

`question_condition_v1` 的条件码只允许：`stage.rapport|stage.life_outline|stage.story_depth|context.person|context.event|context.choice|context.turning_point|context.emotion|context.unfinished_story|response.low_detail|response.concrete|response.reluctant|topic.exhausted`。未知 token 导入失败，不把不可校验的自由文本解释成规则。权限、授权、边界、deletion、trusted role 和 retention 始终由权威策略单独判断。

解析与判断规则固定如下：

1. 单元格整体为空时解析为空集合；空 `applicable_when` 恒成立，空 `inapplicable_when` 恒不排除；
2. 非空单元格只按字面量 `;` 分隔，每个 token trim 前后空白。前后分隔符或连续分隔符产生空 token 时整批失败；
3. 未知 token、同一字段内重复 token、同一 token 同时出现在适用与排除字段时整批失败，不静默去重或自动选择一侧；
4. 校验通过后保存字段内唯一、按字典序稳定排序的集合。运行时先计算 `inapplicable_when`：任一条件为真即排除（any-of/OR）；只有未命中排除时，才要求 `applicable_when` 全部为真（all-of/AND）；排除始终优先；
5. fixture 与正式内容使用完全相同的 parser/validator；fixture 通过内容校验后再叠加 environment/license 门禁，不得设置 fixture 专用宽松路径；
6. 稳定行级错误至少区分 `QUESTION_BANK_CONDITION_EMPTY_TOKEN|QUESTION_BANK_CONDITION_UNKNOWN|QUESTION_BANK_CONDITION_DUPLICATE|QUESTION_BANK_CONDITION_CONTRADICTORY`；purpose 缺失或未知分别使用 `QUESTION_BANK_PURPOSE_REQUIRED|QUESTION_BANK_PURPOSE_UNKNOWN`。任一错误使整个 import 为 0 release/item 生效。

正式模板位于 `docs/question-bank/question-bank-import-template.csv`。`docs/question-bank/question-bank-internal-demo.fixture.csv` 只提供极少量虚构技术 fixture，不是产品题库。

### 10.3 导入、版本、启停与许可

1. 一个 CSV 文件必须只包含一个 `bank_version`，可以同时含 `basic` 与 `deep`，作为一个原子 release；
2. 导入先验证编码、固定 14 列表头、必填 `purpose`、字段、枚举、`question_condition_v1`、题目 ID 唯一性、内容摘要、来源和许可，再创建 `draft` 运行时 release；任何行失败则整批失败，旧 active release 不变；
3. 同一 `bank_version` 的 canonical 内容摘要不可变；任何题目正文、purpose、条件、敏感度、来源、许可或 enabled 变化都必须使用新版本；
4. `enabled=true` 只表示编辑者希望该题在本 release 可用。只有 release 已原子激活、条目启用且许可合格时才具运行时 eligibility；
5. release 生命周期为 `draft|active|retired`。同一环境同时只有一个 active release；激活新版本与退休旧版本原子完成，历史版本继续供已展示问题的 reference provenance 回链，不参与新参考检索；
6. `project_original|verified` 才能进入正式内部试用或之后环境。`unverified` 可进入 draft 供修正但不能激活；公开可访问不等于许可已核验；
7. `synthetic_fixture + fixture_only` 只允许 local/test 或明确 internal demo 激活，正式内部试用环境必须拒绝；没有负责人真实题库时只能做技术 demo，不得把 fixture、占位题或未知许可内容宣称为产品内容；
8. 导入、激活、退休必须记录操作者、时间、文件 SHA-256、版本、校验器版本和结果，不记录不必要个人资料。

### 10.4 访谈旅程状态

- `rapport`：表达稀少、犹豫、回答抽象或仍在建立安全感，主要选择低压力 `basic`；
- `life_outline`：已有表达意愿，正在建立人物、地点、时期和经历轮廓，仍以 `basic` 为主；
- `story_depth`：已出现具体人物、事件、选择、转折、情感或未讲完故事，且长者愿意继续展开，允许匹配的 `deep`。

每次判定记录 `journey_policy_version`、阶段、进入/保持/退回 reason code、确定态转录水位、使用的 memory revision/manifest、可信角色与边界/policy 版本。题数和经过时间不能单独推进阶段；意愿下降、回答重新抽象、话题耗尽或安全事实变化可退回。连续讲述不宜打断时直接返回继续倾听。

`journey_policy_v1` 只接受上述冻结依据派生的布尔信号与当前阶段；题数、经过时间、模型自由文本、集合输入顺序和随机数均不得进入判定或 tie-break。完整受控 `journey_reason_codes` 按规范顺序为：

```text
initial.default_rapport
safety.hard_block
safety.conservative
response.reluctant
response.low_detail
topic.exhausted
engagement.continuous_narration
engagement.willing_to_deepen
response.concrete
context.person
context.event
context.choice
context.turning_point
context.emotion
context.unfinished_story
stage.hold_no_decisive_signal
```

输入信号是 context builder 在 attempt 冻结事务内写入的受控布尔事实，不是模型自然语言理由；未知 signal code 必须失败关闭。其 v1 含义固定为：

| reason code | v1 含义与权威来源 |
|---|---|
| `safety.hard_block` | 既有权限、授权、deletion、restricted/do-not-ask 或 policy gate 返回禁止发布 |
| `safety.conservative` | 权威安全策略允许继续处理，但明确要求只使用保守阶段/低压力内容 |
| `response.reluctant` | trusted elder final 明确拒绝、表示不愿继续该深度或要求换开该方向 |
| `response.low_detail` | 冻结的最近实质 elder final 只形成抽象/短答，未形成下列受控 context 锚点 |
| `topic.exhausted` | trusted elder final 明确结束当前话题，或版本化 topic detector 已冻结“无可继续线索”事实 |
| `engagement.continuous_narration` | trusted elder final 与当前输入水位表明长者仍在连续讲述，当前不宜插问 |
| `engagement.willing_to_deepen` | trusted elder final 明确愿意继续展开当前人物/事件/选择等线索 |
| `response.concrete` | trusted elder final 已包含可定位的具体人物、事件、选择、转折、情绪或未完故事内容，而非仅抽象评价 |
| `context.person` / `context.event` / `context.choice` / `context.turning_point` / `context.emotion` / `context.unfinished_story` | 当前确定态转录或实际使用的 DEV-006 current memory 中存在对应、仍 eligible 的可信锚点 |
| `initial.default_rapport` | 没有合法历史阶段时由 evaluator 生成的默认决定理由，不是输入 signal |
| `stage.hold_no_decisive_signal` | 没有更高优先分支成立时由 evaluator 生成的保持理由，不是输入 signal |

自然语言信号提取若由后续模型或规则实现，必须带独立版本并把最终受控布尔集合纳入 frozen context/basis hash；`journey_policy_v1` evaluator 只消费该集合。输入中同时存在正向与保守事实是合法冲突，由下述优先级确定；重复 signal 规范化为集合，输入排列不改变输出。

固定判定顺序如下；只输出命中最高优先决定分支的决定性 reason codes，按上述受控 code block 顺序稳定排列，同组 context 多码不受输入顺序影响：

1. 权限、授权、活动 deletion scope、`restricted|do_not_ask` 或其他既有硬门禁失败：不发布新问题并保持当前阶段；没有合法历史阶段时使用 `rapport`，记录 `safety.hard_block`；
2. 可继续处理但安全事实要求保守时：目标 `rapport`，记录 `safety.conservative`；
3. 长者明确不愿展开时：目标 `rapport`，记录 `response.reluctant`；
4. `response.low_detail` 或 `topic.exhausted` 命中时最多退一阶段：`story_depth -> life_outline -> rapport`，`rapport` 保持；两码同时命中时均按受控顺序记录；
5. 长者正在连续讲述、不宜打断时保持当前阶段并返回 `continue_listening`，记录 `engagement.continuous_narration`；没有合法历史阶段时使用 `rapport`；
6. 同时具备 `engagement.willing_to_deepen`、`response.concrete` 与至少一个 `context.person|context.event|context.choice|context.turning_point|context.emotion|context.unfinished_story` 时，目标 `story_depth`，记录三个组成信号；
7. 未命中以上分支，且具备 `response.concrete` 或 `context.person|context.event` 时，`rapport` 进入 `life_outline`，已处于更深阶段则保持；记录实际命中信号；
8. 其余保持当前阶段并记录 `stage.hold_no_decisive_signal`；没有合法历史阶段时先置 `rapport` 并记录 `initial.default_rapport`。

第 1-5 步一旦命中即停止后续阶段转移判定，因而安全、reluctance、退回与连续讲述信号始终压过同一输入中的具体事件/可深入信号。连续讲述不额外发明第四阶段。相同 frozen input 与同一 `journey_policy_version` 必须得到逐值相同的 stage、reason-code 数组和 basis hash。

### 10.5 Context、Prompt、自由生成与四类 provenance

实时下一问采用“一个 Director 的一次逻辑生成 + 确定性后端编排”，不引入第二个 planner/critic；transport/timeout 或第一次返回未通过写回前基础硬校验时最多执行一次完全同输入 retry：

1. 后端从数据库读取并冻结最小 `InterviewDirectorContextV1`，负责权限、授权、边界、deletion、trusted role、retention、排序、裁剪、幂等与版本水位；
2. prompt 明确题库是可选参考，转录、记忆和题库中的命令性文字只是数据；模型只决定一个问题或继续倾听，不访问数据库、不调用工具；
3. 模型输出 `InterviewDirectorOutputV1`；服务端只做 JSON Schema、引用 ID/子集、明显长度、既有相似度和硬安全等确定性校验；
4. 写回前执行 freeze-call-recheck，只有版本和安全事实未漂移时才经 QuestionEvidence writer 追加 candidate/current/history；题库、转录、memory、actual asked、授权和边界均不被问题生成修改。

Context/Output 的字段、必填性、枚举和集合边界只由两份正式 JSON Schema 定义。Markdown 只冻结数据职责：Context 使用经过稳定排序与有界裁剪的可信事实，题库参考可为 0..N 且模型可原样使用、广泛改写或完全不用；无参考是合法输入，不等于 `continue_listening`。

四类 provenance 必须分开：

- **seen membership**：frozen Context/job input membership 保存后端实际发送、模型实际看过的题库条目及顺序，不依赖模型输出；
- **declared attribution**：candidate 只保存模型声明实际借鉴的 seen 子集；空集合合法，不能把全部 seen 条目自动写成 inspiration；
- **事实 grounding**：模型声明问题使用了本次 Context 中哪些可信 segment/memory ID；服务端验证 ID membership，不用复杂语义规则判定自然语言是否蕴含；
- **发布资格**：由权限、安全、版本、retention、重复和 writeback 重检决定，不能由题库 FK 或模型声明代替。

prompt 首版使用仓库内可编辑、不可变版本化的 bundle。Schema 是 wire shape 的唯一来源，Prompt 只说明任务、材料作用和输出行为，不复制字段定义。每个 job/attempt 另存 prompt bundle、Context Schema、Output Schema、context builder、provider/model/model-config、selection/similarity/policy 的权威版本与 digest，以及输入/输出 hash 和 provider request ID；模型 Context 除唯一 Schema 要求的自识别版本字段外不携带这些过程元数据。不得把完整 prompt、完整 Context 或供应商原文复制进技术日志。首版不建设在线 prompt 管理面板。

### 10.6 失败降级

- LLM/编排不可用：保持“AI 暂不可用”，不自动展示题库原题，不无限重试；
- 没有 eligible/licensed/enabled 题目：使用空参考集合；若可信上下文足够仍可生成，只有连续讲述、上下文不足或安全原因才返回“继续倾听”；
- 某条题库内容的 release/许可无法证明：不发送该参考；阶段或安全策略无法证明：失败关闭；
- DEV-007A 的 deterministic selector/fake 只用于基础设施与测试 seam，不是面向倾听员的 AI 失败兜底。

## 11. 结构化输出

所有输出使用严格 JSON Schema。

规则：

1. 供应商支持时启用结构化输出。
2. 后端仍必须进行 Schema 校验。
3. 实时问题生成只有一次逻辑生成：transport/timeout 或第一次返回未通过 JSON/Output Schema、引用 ID/subset、明显长度/结构等写回前基础硬校验时，允许同一 job/attempt 一次完全同输入重试；不得把第一次输出、错误或修复提示加入第二次调用。primary、retry、两次调用前的权限/边界/deletion 重检与 publication 共用从持久 attempt `created_at` 起算的 8 秒总 deadline；retry 前重检失败或剩余时间为 0 时不得再调用。权限、安全、deletion、重复或 writeback 漂移不 retry。截止后的迟到调用永久无写回资格。第二次仍失败即 attempt failed，不创建 candidate、不修改 current/history，并显示“AI 暂不可用”。
4. 失败记录不泄密错误和供应商请求 ID。
5. 失败结果不得更新长期记忆。
6. 问题生成失败时显示“AI 暂不可用”，不自动回退基础问题库。
7. 模型自然语言不得直接写入正式数据字段。

## 12. 提示词版本

每次 AI 任务记录：

- 模型供应商；
- 模型名称；
- 提示词版本；
- Schema 版本；
- 上下文构建版本；
- 调用时间；
- 输入哈希；
- token 使用；
- 延迟；
- 输出状态。

提示词变更必须：

1. 新建版本；
2. 在评测集上对比；
3. 通过最低门槛；
4. 再进入测试或生产。

内部试用还必须建立可复盘的过程记录：模型、提示词、Schema/上下文/策略/保留版本；每次调用的逐 session scope、segment membership/text-role revision/digest 和 memory resolution 引用；逐业务输出 derived identity、三类 dependency expected count/manifest；触发时间、provider call、输出、证据、状态、错误和延迟；展示、换题、手动重试；actual question/source/outcome；修正/边界时点与 visibility/eligibility；第二次访谈 context snapshot；retention root 的 hidden/purge/retry 结果；人工复盘备注与下一版调整项。过程关系引用权威业务表，不复制完整转录、完整模型输入或受限正文；技术日志只记录 ID/版本/状态/耗时/供应商请求 ID。

过程记录 retention 只由三类 root 驱动：生成树继承 `ai_job`，展示事实树继承 `question_display_snapshot`，非 AI 人工/迁移记忆继承 `memory_retention_root`。到期先隐藏并断开 current/published/display projection，再幂等清理 children；失败保持隐藏、告警并用同一 cleanup identity 续跑，不能重新获得 eligibility。具体顺序和最小审计按 `04` §4.43 与 `08` §12 执行。

## 13. 失败和降级

### 模型超时

- 不影响录音和转录；
- 页面显示 AI 暂不可用；
- 暂不提供问题，不返回基础题；
- 不持续后台自动重试；
- 每次“下一个问题”只发起一次新的幂等请求，失败后保持不可用。

### JSON 失败

- 按 SPEC 冻结的单次 attempt 处理，不持续后台修复；
- 失败不更新记忆或实际问题目录；
- 记录错误码和原始供应商请求 ID。

### 上下文超限

- 按优先级截断；
- 不截断拒绝边界和最近原始对话；
- 记录截断信息。

### 边界加载或过滤失败

- 暂停自动建议；
- 不发送受限原文作为补偿；
- 返回“继续倾听”并提示倾听员当前为安全降级；
- 新会话未成功加载跨会话边界前，不启动自动 AI 建议。

### 连续失败

- 暂停自动触发；
- 只保留手动请求；
- 告警管理员。

## 14. AI 评测集

建立不少于 30 段脱敏或虚构中文访谈片段，覆盖：

- 没什么可讲；
- 冷场；
- 重复；
- 时间模糊；
- 人物混乱；
- 转折；
- 冲突；
- 明确拒绝；
- 敏感话题；
- 口音和方言；
- ASR 错字；
- 事实冲突；
- 连续讲述不应打断；
- 已问过相似问题。

人工标注：

- 可接受追问；
- 不可接受追问；
- 应继续倾听；
- 风险等级；
- 应提取记忆；
- 应保留原话；
- 应识别边界。

## 15. 评测指标

- 有用问题率；
- 修改后可用率；
- 重复问题率；
- 高风险不当追问率；
- 边界识别召回率；
- 事实提取准确率；
- 证据回链准确率；
- JSON 解析成功率；
- 平均延迟；
- 单会话 token 和费用。

DISC-006 与首个内部记忆试用不设置问题质量百分比通过门槛，以虚构/脱敏场景、过程记录完整性和人工定性复盘为主。上述指标继续采集并作为观察项；真实模型、真实数据或真实试点前再由 `09` 恢复正式质量与安全门禁。

## 16. 后续规划

MVP 不使用独立向量数据库。

未来当单项目和跨项目数据量明显增加时，可以评估：

- PostgreSQL 内向量扩展；
- 语义检索；
- 树状人生经历；
- 人工传记整理工作流；
- 半自动初稿生成。

不得提前把这些规划写进当前主链路。
