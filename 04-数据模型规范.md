# 04｜数据模型规范

## 1. 文档职责

本文件是数据库实体、字段、关系、状态枚举和索引的正式契约。

任何表、字段、约束、状态枚举或索引变更，都必须先更新本文件，再修改迁移、代码和测试。REST API 与 WebSocket 契约见 `05-API与实时事件契约.md`。

## 2. 通用约定

- 主键：UUID；
- 时间：ISO 8601，数据库使用带时区时间；
- 金额：最小货币单位或精确数值；
- 软删除：`deleted_at`；
- JSON 字段：PostgreSQL `jsonb`；
- 公共请求追踪：`request_id`；
- WebSocket 事件版本：`schema_version`；
- 所有外部输入必须校验；
- 原始数据与修订数据分开保存。

## 3. 实体关系

```text
elder_project
 ├─ project_assignment
 ├─ service_term
 ├─ consent_record
 ├─ audio_object
 │   └─ audio_chunk
 ├─ interview_session
 │   ├─ speaker_stream
 │   │   ├─ speaker_calibration_attempt
 │   │   │   └─ speaker_calibration_attempt_segment
 │   │   └─ speaker_mapping
 │   ├─ transcript_segment
 │   │   └─ transcript_text_correction
 │   ├─ speaker_remap_preview
 │   │   └─ speaker_remap_preview_segment
 │   ├─ speaker_correction_operation
 │   │   └─ speaker_correction_operation_segment
 │   ├─ ai_job_session_scope
 │   ├─ ai_job_input_segment
 │   ├─ actual_question_analysis
 │   │   ├─ actual_question
 │   │   │   └─ actual_question_evidence
 │   │   └─ suggestion_outcome
 │   ├─ question_generation_attempt
 │   │   └─ question_candidate
 │   ├─ question_display_state
 │   │   └─ question_display_snapshot
 │   ├─ question_evidence_event
 │   ├─ content_marker
 │   ├─ ai_job
 │   │   ├─ ai_provider_call
 │   │   ├─ ai_job_session_scope
 │   │   ├─ ai_job_input_segment
 │   │   └─ ai_job_input_memory
 │   ├─ session_note
 │   └─ session_finalization
 │       └─ session_finalization_chunk
 ├─ memory_claim
 │   └─ memory_claim_evidence
 ├─ memory_resolution
 │   └─ memory_resolution_member
 ├─ ai_derived_output
 │   ├─ ai_output_segment_dependency
 │   └─ ai_output_memory_dependency
 ├─ interview_context_snapshot
 │   ├─ context_snapshot_memory
 │   └─ context_snapshot_actual_question
 ├─ deletion_request
 │   └─ deletion_request_transition
 ├─ boundary_candidate
 └─ export_job

user
 ├─ project_assignment
 ├─ auth_session
 └─ audit_log

question_bank_release
 └─ question_bank_item
```

## 4. 数据表

### 4.1 user

```text
id
email
display_name
password_hash
role
status
created_at
updated_at
disabled_at
```

`email` 必须先去除首尾空白并转换为小写 ASCII，再写入和查找；MVP 拒绝非 ASCII 邮箱，数据库对规范化后的 `email` 建立唯一约束。`password_hash` 仅保存 Argon2id 结果，不得保存明文、可逆密文或密码提示。production 不得存在默认密码账号。

角色：

- `interviewer`
- `admin`
- `data_admin`

状态：

- `active`
- `disabled`

production 用户由受控运维 CLI 创建；同一 CLI 提供交互式密码重置、账号停用和启用。CLI 的 `--operator-ref` 值映射到 `audit_log.actor_reference` 并写 `system_operator` 审计；`user:enable` 设置 `status=active` 并清空 `disabled_at`，但不得恢复历史会话；密码不得通过命令参数或环境变量传入；MVP 不提供公开注册、自助找回或在线角色变更。

### 4.2 elder_project

```text
id
display_name
birth_year
approximate_age
native_place
current_city
status
status_before_restriction
ai_policy_revision
ai_retention_policy_version
created_by
created_at
updated_at
deleted_at
```

项目状态：

- `draft`
- `ready`
- `active`
- `completed`
- `restricted`
- `deleted`

限制状态规则：

- 项目首次从非 `restricted` 进入 `restricted` 时，把原 `status` 写入 `status_before_restriction`；已有额外限制时不得覆盖该值；
- 关闭某一限制原因时必须重新检查有效授权撤回、非终态 project scope 删除申请和其他正式限制原因；仍有任一原因时保持 `restricted`；
- 删除申请 `rejected` 或 `withdrawn` 且不存在其他限制原因时，恢复 `status_before_restriction` 并清空该字段；project scope `completed` 表示在线清理已完成并把项目置为不可恢复的 `deleted`，不恢复原状态；
- `status_before_restriction` 不得由普通项目更新接口直接写入。
- `ai_policy_revision` 为从 0 开始的项目级安全策略版本；授权创建/撤回、assignment 变更、项目 restriction、正式 marker/boundary 变化和 deletion scope 状态变化必须在各自权威事务中递增。它用于 AI 输入和展示写回复核，但不能代替实时权限、授权、边界或 deletion scope 查询；
- `ai_retention_policy_version` 记录项目适用的 AI 派生/过程记录保留策略版本。具体期限由配置和策略表解析，不在本任务拍定固定天数。

project scope 删除完成后保留的 `elder_project` 仅是不可恢复 tombstone：保留 `id`、`status=deleted` 和最小审计时间，清空 `display_name`、出生信息、地点、`created_by`、`status_before_restriction` 等直接身份或业务字段，并删除 assignment 和全部业务子实体。tombstone 不得进入普通查询、AI、回顾或导出。

### 4.3 project_assignment

```text
id
project_id
user_id
assignment_role
created_at
revoked_at
```

用于控制倾听员只能查看被分配项目。

MVP 的 `assignment_role` 仅允许 `interviewer`。由 `interviewer` 创建项目时，必须在同一数据库事务中创建该项目及一条指向创建者的有效 `interviewer` assignment；`created_by` 只用于审计来源，不能替代 assignment 进行授权。assignment 撤销只写 `revoked_at`，不得覆盖或删除原记录；只有 `revoked_at=null` 的 assignment 参与访问判定。

#### 4.3A 项目首页读取投影（非持久表）

`ProjectListProjection` 是服务端从 `elder_project + project_assignment` 计算的共享 read model，不新增数据库表，也不得把受限占位写回项目正文。正式 TypeScript DTO 位于 `packages/contracts/src/index.ts`：

- 普通分支 `projection=ordinary` 只允许项目 `draft|ready|active|completed`、`deleted_at=null` 且 actor 有有效 assignment；
- 普通分支可按 §8.1 additive 返回项目级 `repeat_interview` 权威动作；rollout 缺键失败关闭，restricted 分支不得包含该字段；
- 受限分支固定为 `project_id/projection=restricted/status=restricted/display_label=受限项目/status_label=当前不可访问`；除 opaque `project_id` 外不得携带任何项目正文、创建来源、会话或可行动语义；
- `status=deleted`、`deleted_at!=null` 或无有效 assignment 不产生任何 list row；
- `created_by`、历史 assignment 和受限前状态都不能替代当前有效 assignment，也不能进入受限分支。

受限分支只服务 authenticated home list。项目详情、service term、consent、session 详情和普通 prepare/workbench/review 读取不消费该 DTO。

### 4.4 service_term

```text
id
project_id
included_minutes
estimated_session_count
expected_current_minutes
overtime_unit_minutes
overtime_price_minor
currency
explained_at
explained_by
effective_from
superseded_at
created_at
```

修改服务条款时新增记录，不覆盖旧记录。

### 4.5 consent_record

```text
id
project_id
consent_type
consent_text_version
consent_method
consented_at
consent_audio_object_id
status
revoked_at
created_by
created_at
```

MVP 的 `consent_type` 固定为：

- `recording_transcription_ai`：一次捆绑授权同时覆盖录音、转录和 AI 分析。

状态：

- `pending`
- `valid`
- `revoked`
- `expired`

方式：

- `recorded_verbal`
- `electronic`
- `written`

授权记录只追加，不覆盖。`recorded_verbal` 且进入 `valid` 时必须关联已保存的 `consent_audio_object_id`；该字段引用 `audio_object.id`，服务端必须在同一项目锁内确认对象属于当前项目、`purpose=consent`、`status=complete`，且 manifest、分片 checksum/size 与私有存储对象一致。一个授权音频对象只能证明其实际朗读的一个 `consent_text_version`，不得被另一版本的 consent record 再引用；相同或不同 request ID 的跨版本复用均稳定拒绝。`electronic`、`written` 的该字段为空。项目的当前授权候选为按服务端 `created_at,id` 稳定排序后的最新一条 `consent_type=recording_transcription_ai` 记录；客户端提交的 `consented_at` 不决定 current。若该候选 revoked/expired，不得回退到更旧 valid 记录。是否可用于新 session 还必须通过 §10 的 scope/version compatibility 判定。新增授权不能修改旧记录；撤回把目标有效记录置为 `revoked` 并写 `revoked_at`。探索期内部虚构数据可以使用 `electronic` 或 `written` 方法，不代表真实试点授权形式已经验收。

普通倾听员流程按 §7/§10 只要求上述当前适用捆绑授权即可从 `draft` 进入 `ready`，ServiceTerm 保持 dormant；已有 ServiceTerm 历史语义保留但不参与普通 ready/start gate。授权撤回后项目按 `03`、`08` 进入 `restricted`，不得启动新访谈。

### 4.6 interview_session

```text
id
project_id
sequence_no
status
started_at
ended_at
duration_seconds
asr_provider
llm_provider
prompt_version
capture_failure_code
speaker_role_revision
created_by
created_at
updated_at
```

状态：

- `created`
- `device_check`
- `recording`
- `reconnecting`
- `stopping`
- `processing`
- `completed`
- `interrupted`
- `failed`

结束字段语义：

- `ended_at` 是服务端以 `started_at + 最后一片 commitment.end_ms` 推导、校验并在首次接受 stop 快照时固化的采集截止时间；不得使用 HTTP 到达、上传、ASR 或 AI 完成时间；
- `duration_seconds` 由服务端以 `ceil(last_commitment.end_ms/1000)` 计算并与 `ended_at` 同时固化，客户端不得直接提交；
- 每个 session 同时最多一个 `purpose=interview` 的 `audio_object`，并由数据库唯一约束保证；授权音频不受此约束；
- `completed`、`failed` 为终态；`interrupted` 为非终态。缺片、进程内 runner 丢失或 ASR 暂时不可用不得仅凭一次失败直接写 `failed`。
- `capture_failure_code` 当前只允许 `null|NO_AUDIO_CAPTURED`，用于无 finalization 的空采集终结；不得把 commitment、manifest、ASR 或内部收束失败写入该字段，它们继续归 `session_finalization.failure_code`。
- `speaker_role_revision` 为从 0 开始的非负整数。每次用户确认映射、单段角色修正或批量角色修正成功时，在同一事务递增一次；它是未来 AI 派生结果判断角色证据是否过期的 session 级版本，不代替受影响片段清单。

### 4.6A speaker_stream

供应商 speaker label 的持久命名空间。一个 capture generation 可以因 ASR provider/runtime 重建拥有多个 speaker stream；短时 WebSocket event stream 与本表无关。

```text
id
session_id
capture_generation_id
status
opened_at
closed_at
created_at
```

状态：`active|closed`。

规则：

- `id` 是服务端生成的 `speaker_stream_id`，在 session 内和全局均稳定唯一；
- 新 provider/runtime speaker namespace 必须创建新记录，即使仍在同一 capture generation，或 provider 继续使用 `speaker_1` 等相同短 label；
- 只有能够证明 provider speaker namespace 未变化的短时 transport 重连才可继续使用原记录；
- `audio_stream_id` 只标识 capture PCM generation，`event_stream_id` 只标识短时下行事件恢复，二者均不得被实现静默当作本表主键；
- session 删除时随会话删除；普通关闭只写 `closed_at`，不删除历史。

### 4.7 speaker_mapping

```text
id
session_id
speaker_stream_id
speaker_provider_id
speaker_role
source
created_by
created_at
superseded_at
```

角色：

- `elder`
- `interviewer`
- `unknown`

来源：

- `calibration`
- `manual`
- `provider`
- `batch_remap`

规则：

- `id`、`session_id` 为 UUID；`speaker_provider_id` 最大 200 字符；`created_by` 为可空用户 UUID；
- 供应商 speaker ID 只在一个 `speaker_stream_id` 内有意义，不跨 speaker stream 或会话复用；`speaker_stream_id` 必须属于同一 session；
- 映射采用追加历史，不覆盖旧记录；同一 `(speaker_stream_id, speaker_provider_id)` 同时最多一条 `superseded_at=null` 的当前映射；
- 新映射生效时，在同一事务将旧当前映射写入 `superseded_at` 并新增记录；
- `created_by` 对自动 provider 映射可为空，对 calibration/manual/batch_remap 必须记录操作者；
- transcript 写入时快照当时解析出的原始业务角色；后续映射变化不能改写该原始角色，只能写修正角色。
- session 删除时映射随会话删除；操作者删除时 `created_by` 置空以保留证据历史。

来源可信度：

- `calibration|manual|batch_remap` 必须来自有权用户的明确确认，属于 `user_confirmed`；
- `provider` 只表示供应商候选，属于 `unconfirmed`，不能单独成为长期记忆、已问问题或建议上下文的可信角色依据；
- role value 与 authority 必须分开判断，不得因为 provider 候选值为 `elder` 就把它当作用户已确认。

### 4.7A speaker_calibration_attempt

```text
id
session_id
speaker_stream_id
capture_generation_id
audio_stream_id
attempt_no
status
start_sequence_no
end_sequence_no_exclusive
start_timeline_ms
end_timeline_ms
started_request_id
resolved_request_id
started_by
resolved_by
started_at
resolved_at
created_at
updated_at
```

状态：`collecting|confirmed|failed|skipped`。

规则：

- `(speaker_stream_id, attempt_no)` 唯一，同一 speaker stream 同时最多一个 `collecting` attempt；attempt 的 capture generation、audio stream 与 speaker stream 必须属于同一 session 当前正式流；
- begin 时写 `started_request_id`，resolve 时写 `resolved_request_id`；二者均走全局幂等绑定；
- begin/resolve 都必须向该 session 当前 producer 的服务端 PCM 串行泵插入有序控制 marker。marker 在其前方所有已排队 PCM 处理完、后方 PCM 尚未处理时执行；marker 回调必须在允许后方 PCM 继续前，以一个数据库事务提交边界和对应 attempt 状态/映射。事务失败则本次 marker 不形成已提交边界，后方 PCM 才可继续；客户端时间、按钮时间和 final 到达时间均不得充当边界；
- `start_sequence_no` 与 `start_timeline_ms` 在 attempt 创建前冻结且不可修改；`end_sequence_no_exclusive` 与 `end_timeline_ms` 在 resolve 前冻结，collecting 时为空，进入任何终态前必须非空。正式边界是同一 audio stream 上的半开区间 `[start_sequence_no, end_sequence_no_exclusive)`，以及由服务端 generation offset 推导的等价 `[start_timeline_ms, end_timeline_ms)`；
- final 的内容类型只按其服务端标准化 session 时间范围与上述不可变区间是否重叠判断，与 final 到达时 attempt 是 collecting 还是终态无关。任何非零重叠都保守标记为 `speaker_calibration`；完全落在区间外才是 `conversation`；
- `confirmed` 必须在同一事务验证并写入该流内两个不同且已在本 attempt 观察到的 provider label，分别映射为 `elder` 与 `interviewer`，同时递增 session `speaker_role_revision`；
- `failed|skipped` 不创建可信角色映射，结束控制片段范围后允许普通访谈继续；重试创建下一 attempt；
- 同一 speaker stream 已存在 confirmed attempt 时不得再创建 collecting attempt；本轮后续角色变化由 C2 人工修正表达，不用失败重校准覆盖已确认事实；
- attempt 历史只追加，终态不得改回 collecting。

### 4.7B speaker_calibration_attempt_segment

```text
id
speaker_calibration_attempt_id
transcript_segment_id
created_at
```

规则：

- `(speaker_calibration_attempt_id, transcript_segment_id)` 唯一；片段必须属于 attempt 的 session 与 speaker stream；
- final ingestion 使用服务端标准化后的 session `start_ms/end_ms` 与该 speaker stream 的不可变 attempt 区间做重叠查询；无论 final 在 begin 前延迟到达、还是在 resolve 后延迟到达，都据此写 membership 与不可变 `content_kind=speaker_calibration`，不得查看“此刻是否 collecting”来分类；
- 一个跨越边界的 final 因存在非零重叠而整体按 calibration 排除，宁可少消费相邻故事内容，也不得让控制句进入故事派生；需要更细粒度时只能由未来 provider/split 契约另行升级；
- membership 只证明控制内容范围，不改变原始文本、角色或时间证据；attempt resolve 后仍永久保留以供审计和消费过滤。

### 4.8 audio_chunk

```text
id
audio_object_id
sequence_no
object_key
start_ms
end_ms
size_bytes
checksum
mime_type
upload_status
retry_count
created_at
uploaded_at
```

上传状态：

- `pending`
- `uploading`
- `uploaded`
- `failed`
- `missing`

唯一约束：

```text
(audio_object_id, sequence_no)
```

分片记录和已保存对象不可覆盖。同一 `(audio_object_id, sequence_no)` 使用相同不可变元数据与相同 checksum 重试时返回原业务结果；任一不可变字段或二进制内容不同则稳定冲突。`object_key` 仅供服务端存储适配器使用，不返回普通 API。

### 4.9 transcript_segment

```text
id
session_id
ingest_key
provider_segment_id
speaker_provider_id
speaker_stream_id
original_speaker_role
original_speaker_role_authority
corrected_speaker_role
speaker_role_revision
text_revision
content_kind
start_ms
end_ms
original_text
corrected_text
source
provider_payload
corrected_by
corrected_at
created_at
```

`source`：

- `realtime`
- `backfill`
- `fixture`

说明：

- `id`、`session_id` 为 UUID；`ingest_key` 最大 240 字符；provider segment/speaker ID 各最大 200 字符；原文和修正文使用数据库 text；
- `transcript_segment` 只保存 `final` 确定态；`interim` 只通过实时事件短暂展示，不写入正式表；
- `ingest_key` 是后端适配器生成的供应商中立稳定身份，必须把供应商连接/流命名空间与 segment 身份纳入计算；同一 `(session_id, ingest_key)` 唯一；
- 相同 ingest key、相同不可变字段重放返回原片段；原始文本、时间范围、provider segment、provider speaker 或 source 任一不同时稳定冲突，不覆盖旧记录；
- `original_text` 永不被人工或 AI 覆盖；
- `original_speaker_role` 是首次 final 落库时按当前映射解析的 `elder|interviewer|unknown` 快照，永不覆盖；
- `original_speaker_role_authority` 为 `unconfirmed|user_confirmed`，快照首次 final 落库时映射的可信度；没有映射或仅有 provider 候选时为 `unconfirmed`；
- `corrected_text` 与 `corrected_speaker_role` 分别保存人工文字和角色修正；展示值为 `corrected_text ?? original_text` 与 `corrected_speaker_role ?? original_speaker_role`；
- `speaker_stream_id` 在 `speaker_provider_id` 非空时必填并属于同一 session；provider label 的相同字符串不能跨流匹配；
- `content_kind` 为 `conversation|speaker_calibration`，由服务端按校准 attempt 的权威范围写入，写入后不可由普通修正接口改变；
- `speaker_role_revision` 记录该片段最后一次角色修正成功后的 session revision；单段或批量修正使用乐观版本门禁，失败不得部分写入；
- `text_revision` 为从 0 开始的非负整数；任何未来正式文字修正必须在同一事务新增不可变文字修正记录并将目标片段 `text_revision + 1`，不得只改 `corrected_text`。现有旧片段前向迁移为 0，不据此声称迁移前不存在修正；
- `start_ms`、`end_ms` 使用非负整数毫秒且 `end_ms > start_ms`；
- `provider_payload` 是受限业务数据，不进入普通响应或日志；只保存排障所需最小 JSON，序列化后最大 64 KiB，并随片段执行相同访问、保留和删除规则；
- `fixture` 只允许 local/test 内部组合根使用，不得形成公开 HTTP/WebSocket 写入口；
- 展示文本优先使用 `corrected_text`，为空时使用 `original_text`；
- 只有本表中已持久化的 final 片段可进入长期记忆和 AI 分析。
- `effective_role = corrected_speaker_role ?? original_speaker_role`；`trusted_effective_role` 只有在 corrected role 存在，或 original authority 为 `user_confirmed` 时才等于 effective role，否则按 `unknown` 消费；
- AI 输入 membership 必须同时快照 `text_revision`、片段当时的 `speaker_role_revision`、trusted effective role、authority、content kind 与 effective text digest；仅保存 session `speaker_role_revision` 不能证明文字证据未变化；
- `content_kind=speaker_calibration` 的片段保留原始证据和普通有权回看，但不得进入故事记忆、真实已问问题、普通摘要或普通 AI 上下文；
- session 删除时片段随会话删除；修正人删除时 `corrected_by` 置空但修正内容和时间保留。

### 4.9A speaker_remap_preview

```text
id
session_id
speaker_stream_id
speaker_provider_id
target_role
segment_start_id
segment_end_id
exclude_individual_corrections
candidate_segment_count
excluded_segment_count
segment_count
preview_hash
status
expires_at
request_id
created_by
created_at
updated_at
```

状态：`active|executed|expired|invalidated`。

规则：

- preview 是执行前已持久化的稳定目标集合，不得仅在响应中返回临时查询结果；`request_id` 全局唯一且幂等绑定同一规范化选择；
- 本轮 `exclude_individual_corrections=true`；起止 ID 必须分别是同一 session、同一 speaker stream、同一 provider label 且尚无单段人工修正的 final。按 `(start_ms asc, id asc)` 总序形成包含两端的闭区间，再从区间内部排除其他已有单段人工修正；
- `preview_hash` 覆盖规范化选择、成员 ID、每段当前角色版本与排除结果；过期或任何成员漂移后不得执行；
- preview 不保存转录正文或 provider payload，普通列表接口也不得暴露给无权用户。

### 4.9B speaker_remap_preview_segment

```text
id
speaker_remap_preview_id
transcript_segment_id
speaker_revision_at_preview
excluded_individual_correction
created_at
```

规则：

- `(speaker_remap_preview_id, transcript_segment_id)` 唯一；
- preview 与全部 membership 在同一事务写入；执行时必须重新验证完整集合，不得重新查询后静默扩大或缩小目标。
- membership 覆盖闭区间内全部候选；`excluded_individual_correction` 明确记录本轮默认排除结果。preview 同时持久化候选、排除和最终计数，使执行可在不读取正文或 provider payload 的前提下验证完整集合。

### 4.9C speaker_correction_operation

```text
id
session_id
speaker_stream_id
operation_type
target_role
preview_id
preview_hash
request_id
revision_before
revision_after
created_by
created_at
```

`operation_type`：`single|batch`。

规则：

- 单段修正也创建操作记录；批量执行必须引用尚未过期的持久 preview；
- `preview_hash` 覆盖同一 speaker stream、provider label、用户选择范围、排除规则、目标 segment ID 及各 segment 当前角色版本；
- 执行前重新校验 preview 中全部成员与版本。任何成员漂移、越权、删除或已出现单段人工修正时，整批稳定冲突；不得只更新剩余成员；
- 成功时在同一事务写修正、membership、审计和 session revision `+1`；`revision_after = revision_before + 1`；
- 操作记录不包含转录正文或 provider payload。

### 4.9D speaker_correction_operation_segment

```text
id
speaker_correction_operation_id
transcript_segment_id
speaker_revision_before
speaker_revision_after
created_at
```

规则：

- `(speaker_correction_operation_id, transcript_segment_id)` 唯一；
- membership 是未来 DEV-006/007 使受影响派生结果失效的事实来源；DEV-004C 只生产该事实，不伪造尚不存在的 memory/suggestion 重算结果；
- 人工确认的敏感、限制、禁止追问、删除请求和人工确认记忆不因 membership 出现而自动解除或覆盖。

### 4.9E transcript_text_correction

未来正式文字修正的不可变 producer 记录；本 SPEC 只冻结 consumer 所需版本 seam，不把文字修正 UI/API 纳入 DEV-006。

```text
id
transcript_segment_id
request_id
revision_before
revision_after
corrected_text
previous_effective_text_digest
new_effective_text_digest
corrected_by
created_at
```

- `(transcript_segment_id, revision_after)` 与 `request_id` 唯一；`revision_after = revision_before + 1`；
- 写操作按 `request_id -> session -> transcript_segment` 加锁并重检 assignment、授权、project/boundary/deletion scope、当前 text revision；成功时同一事务追加 operation、更新 `transcript_segment.corrected_text/text_revision/corrected_by/corrected_at` 和审计；
- `corrected_text` 保存当次人工修正历史，`original_text` 永不改写；摘要使用相同 canonical normalization/SHA-256，不能代替有权读取正文；
- AI eligibility 通过 segment current revision/digest 与 job membership 立即排除旧输出，不要求本 producer 回写 AI 表；
- deletion 清理本记录与 segment 同 scope 删除或去标识化，外键不得阻塞清理。

### 4.10 memory_item

> 正式替代：DEV-006 不创建旧的单表 `memory_item`。第一版使用 §4.28-4.31 的 `memory_claim + evidence + resolution + member`，把不可变事实主张、项目当前解释和未来资格分开。

P0 记忆语义核心固定为：

- `episode`：一段可继续展开、具有上下文或时间进程的经历；
- `fact`：对下一问有用的结构化事实；
- `boundary`：独立的禁止/限制范围，不进入 claim/resolution value 表。

下列既有 `MemoryType` 值只允许作为可选 metadata tag，不是 Episode/Fact 的必填语义分类，也不得参与 P1 semantic identity、dedupe、revision 或 target CAS：

- `person`
- `relationship`
- `place`
- `event`
- `time`
- `time_range`
- `important_choice`
- `reason_clue`
- `unfinished_story`

正式规则：

- trusted elder conversation final 生成的自动 claim 可以直接进入后台 current resolution，不需要普通 UI 人工确认；unknown、校准控制片段、无权限、`restricted` 和活动 deletion scope 命中的片段不得成为输入；
- `memory_claim` 只追加，不覆盖。`memory_resolution` 是当前解释的唯一投影，必须能表达单一 current、范围、unknown、conflict set 和 review-required；不得用一个 `status` 同时承载历史、当前值和 future eligibility；
- 明确更正新增 claim/resolution，旧 claim、证据和生成 job 保留，但旧输出立即失去 future eligibility；普通查询只返回新 resolution；
- 明显冲突保留多个 claim 并形成一个 conflict resolution，不按最后写入覆盖；问题消费者只能据此生成自然澄清，不能选择一个冲突值写入问题前提；
- 自动输出不得覆盖 `authority=human_confirmed` 的 current resolution。冲突自动结果只能成为 `review_required` 提案，既有人工值继续 current；
- 当前 view 还必须通过 §4.32 的权威 eligibility 谓词：依赖片段版本、correction membership、输入 memory resolution、项目 policy revision、授权、访问和 deletion scope 任一不再成立时立即排除；物化状态滞后不得让旧值重新进入查询。

### 4.11 question_suggestion

> 当前状态：下列单表是历史候选，不得直接进入 migration。正式共享证据基座改由 §4.36-4.41 的 generation attempt、displayed snapshot、display state、actual question 和 suggestion outcome 表达；`SPEC-AI-QUESTION-001` REVIEW 候选只在该基座上补齐自动替换、手动“下一个问题”、展示历史导航、节流与相似度，没有创建第二套 question history。

```text
id
session_id
trigger_type
question
reason
purpose
risk
confidence
evidence_segment_ids
model_name
prompt_version
status
created_at
displayed_at
```

触发方式：

- `auto_text_threshold`
- `auto_time_threshold`
- `manual`
- `cold_start`
- `topic_shift`

目的：

- `detail`
- `cause`
- `person`
- `scene`
- `emotion`
- `choice`
- `conflict`
- `turning_point`
- `clarify`
- `timeline`
- `transition`

风险：

- `low`
- `medium`
- `high`

状态：

- `generated`
- `displayed`
- `expired`

旧单表不再增加替换状态。正式自动替换、手动“下一个问题”、稳定展示顺序与幂等语义只由 §4.36-4.39 的共享基座表达。

正式要求至少分离三个概念：

- `displayed_snapshot`：已经向倾听员展示的问题、原因、证据、当时记忆/边界/角色版本和展示时间的不可变事实；
- `future_eligibility`：该结果是否仍可用于未来生成、普通查询、上下文和跨会话继承。
- `display_visibility`：当前普通 UI 是否仍可投影快照正文；它独立于 future eligibility。

说话人/文字/普通记忆修正后，已展示快照可以作为历史展示事实保留，但 future eligibility 必须立即失效；`restricted`、`do_not_ask`、活动 deletion scope、授权或访问权限失效时，当前视图和历史浏览都必须立即隐藏快照正文。本 SPEC 已冻结共享字段/状态/事件基座；`SPEC-AI-QUESTION-001` REVIEW 候选只补自动替换、手动“下一个问题”、展示历史导航、节流和相似度，DEV-006/007 不得各自发明一套。

普通修正允许 `display_visibility=visible` 且 `future_eligibility=false`。硬安全边界命中时 visibility 立即变为 `withdrawn`，普通查询、刷新和 replay 只返回中性状态；不可变 snapshot 仍只在受限审计中保留最小展示事实。

实际问题目录由 `QuestionEvidenceModule` 单一拥有：从 trusted interviewer conversation final 提取全部真实问题，包含倾听员自发问题；suggestion outcome 才使用 `actual_asked|explicitly_replaced|not_observed|unjudged`，actual question 本体不使用这些值。跨会话防重复只读当前可靠 actual-question 目录。DEV-006 负责该目录和分析版本，DEV-007 只经正式 service seam 提供展示/换题事实，不得维护第二套“已问问题”。

### 4.12 suggestion_action

> 当前状态：废弃的历史候选模型，不进入任何 migration、API 或 DEV-007 实现。下列结构只保留原规划追溯；若未来恢复任一显式反馈动作，必须另开产品讨论与契约，不得复用本表形成第二套 question history。

```text
id
suggestion_id
action
rewritten_question
note
operator_id
created_at
```

动作：

- `adopt`
- `asked`
- `ignore`
- `save_for_later`
- `rewrite`
- `mark_duplicate`
- `mark_inappropriate`
- `mark_sensitive`

### 4.13 content_marker

```text
id
session_id
segment_start_id
segment_end_id
marker_type
note
created_by
created_at
updated_at
deleted_at
```

类型：

- `important_quote`
- `sensitive`
- `restricted`
- `do_not_ask`
- `verify_later`
- `deletion_request`

规则：

- `sensitive` 表示谨慎处理，不自动等同 `restricted` 或 `do_not_ask`；
- `restricted` 表示正文及派生内容不得进入普通 AI、摘要和导出；
- `do_not_ask` 表示追问行为边界；其 `note` 必填，保存人工确认的最小抽象禁区描述，不得复制原始受限正文；
- `deletion_request` marker 只表示触发请求的内容范围，不承载删除工作流状态；
- 多种 marker 重叠时取限制并集。

### 4.14 ai_job

```text
id
project_id
trigger_session_id
job_type
trigger_reason
request_id
request_identity_hash
trigger_dedupe_key
retry_of_job_id
input_hash
status
error_code
model_provider
model_name
prompt_version
schema_version
context_builder_version
policy_revision
retention_policy_version
expires_at
retention_state
retention_hidden_at
retention_cleanup_request_id
retention_cleanup_attempt_count
retention_cleanup_started_at
retention_cleanup_error_code
created_at
started_at
completed_at
```

任务类型：

- `memory_extract`
- `question_generate`
- `actual_question_reconcile`
- `session_note`
- `context_snapshot`
- `boundary_detect`

状态：

- `pending`
- `running`
- `succeeded`
- `failed`
- `cancelled`

正式规则：

- `session_id` 单值被 `project_id + trigger_session_id + ai_job_session_scope` 取代。`trigger_session_id` 只表示触发来源，可空，不能证明完整输入水位；
- `request_id` 全局唯一并绑定 action、actor/system trigger、target 和规范化 payload；该绑定以 `request_identity_hash` 持久证明，不能从当下输入重新推断。响应未知必须复用并权威重放首次 job/结果；同 ID 不同 hash 拒绝冲突；权威失败后显式重试使用新 ID 并写 `retry_of_job_id`；
- 自动 trigger 的 `trigger_dedupe_key` 由 job type、trigger reason、input snapshot 和版本规范化生成并唯一，使同一权威事件只创建一次；人工动作不得借此替代 `request_id`；
- job 创建事务必须写入 §4.33-4.35 的全部 session scope、segment membership 和 memory membership 后才可进入 `running`；即使某个评估范围 session 当次 0 个 eligible segment，也必须写 scope 行；
- 外部模型调用期间不持数据库锁。结果写回前重新校验 assignment、授权、项目状态、policy revision、活动 deletion scope、全部 session watermark、segment membership/version/digest 和 memory membership；任一漂移时把 job 置为 `cancelled`，错误码为不泄密分类，丢弃供应商结果；
- `succeeded` 只表示结构化输出已校验并与业务输出同一事务提交，不表示输出仍具 future eligibility；后续资格由 §4.32 决定；
- `ai_job` 是本 job 的过程记录、输入 membership、provider call、derived output 及其自动生成业务输出的 retention root。`retention_state` 为 `active|hidden|purging|cleanup_failed`；只有 root 保存策略版本、到期时间和清理状态，子记录不得另存一份可漂移的 `expires_at`；
- 每次 provider call 都写 `ai_provider_call`。对 `question_generation`，同一 job 只允许一次 `primary`，并在 transport/timeout 或返回内容未通过写回前基础硬校验时最多一次 `same_input_retry`；两次的 Prompt、frozen Context、Output Schema、model config、版本/digest 与 input hash 必须相同，第二次不含前次输出、错误或修复提示。权限、安全、deletion、重复或 writeback 漂移不触发重试；其他 job type 已有重试语义不由本专项改写；
- `token_usage`、`latency_ms` 和 `provider_request_id` 下沉到每次 `ai_provider_call`，避免多次调用被合并成不可审计单值；
- 过程记录依靠关系与 digest 还原，不在 job 复制完整转录、完整模型输入或敏感正文。

### 4.15 session_note

```text
id
session_id
generation_job_id
ai_derived_output_id
note_revision
replaces_session_note_id
status
summary_json
edited_content
generated_at
edited_by
edited_at
```

`status=current|superseded`；`(session_id,note_revision)` 唯一且每个 session 至多一条 current。模型生成的一个 session-note 版本必须绑定一个 `generation_job_id` 和一个唯一 `ai_derived_output_id`，并以新版本替换旧版本，不覆盖历史。第一版 DEV-006 不创建纯人工 session-note root；人工 `edited_content` 是该 generated version 的 overlay，普通资格失效只撤下 generated `summary_json` 的 future consumption 而不改写 overlay，但该 note 的 ai_job retention/deletion root 到期时整条记录（包括 overlay）共同隐藏/清理。未来若允许纯人工 note，必须先另案冻结其 retention root。

### 4.16 export_job

本表保留为历史正式设计，不表示当前 runtime 已实现，也不授权倾听员端导出。SPEC-DEV-008A 明确不创建或消费该表；未来其他角色若需要导出，必须另立受控任务后再决定是否实现。

```text
id
project_id
status
object_key
expires_at
requested_by
request_id
created_at
completed_at
error_code
```

状态：

- `pending`
- `running`
- `succeeded`
- `failed`
- `expired`

### 4.17 audit_log

```text
id
actor_type
actor_id
actor_reference
action
entity_type
entity_id
request_id
metadata
created_at
```

`actor_type` 为 `user`、`anonymous` 或 `system_operator`。普通已认证应用操作必须使用 `user` 和正式 `actor_id`；production 运维 CLI 使用 `system_operator`、空 `actor_id` 和显式非秘密 `actor_reference`，用于首次创建管理员等尚无应用用户可归属的操作。

`anonymous` 只允许用于尚无合法应用用户 actor 的认证安全事件，例如未知账号登录失败；此时 `actor_id`、`entity_id` 必须为空，`entity_type=authentication`，`actor_reference` 必须为 `auth_subject:v1:<64 lowercase hex>`。摘要以独立登录限流 pepper 对规范化身份做带版本、用途分隔的 HMAC-SHA-256，不保存邮箱或客户端 IP 原文；metadata 只允许稳定结果分类和不可逆客户端 IP 摘要，不得表达“账号不存在”等可枚举原因。已知用户的登录成功/失败继续使用 `user` actor。不得把 OS 凭据、密码、Cookie、CSRF、Access header 或令牌写入 `actor_reference` 或 metadata。

数据库必须以 `CHECK` 至少约束 `anonymous` 的 `actor_id/entity_id` 为空、`entity_type=authentication` 和 `actor_reference` 版本/长度/十六进制形状，防止其他模块把匿名 actor 扩成通用无主体审计；用途和 metadata 最小化继续由唯一身份域 writer 与测试保证。

### 4.18 deletion_request

```text
id
project_id
session_id
source_marker_id
scope_segment_start_id
scope_segment_end_id
scope_reference_hash
scope
status
request_id
requested_by
requested_at
verified_by
verified_at
processing_started_at
completed_by
completed_at
cleanup_manifest_hash
backup_cleanup_status
backup_cleanup_due_at
resolution_note
created_at
updated_at
```

状态：

- `pending_verification`
- `verified`
- `processing`
- `completed`
- `rejected`
- `withdrawn`

`scope`：

- `project`
- `session`
- `segment_range`

规则：

- `request_id` 唯一，用于保证删除申请幂等；
- `scope=segment_range` 创建时必须把 source marker 当时的 `segment_start_id`、`segment_end_id` 复制到不可变的 `scope_segment_start_id`、`scope_segment_end_id`；AI 阻断和清理只使用该冻结快照，不再依赖 marker 后续值；
- `scope_reference_hash` 在创建时使用独立删除审计 pepper，对 scope 类型、project/session UUID及 segment 快照端点生成 HMAC-SHA-256，不含正文，用于物理清理后的最小范围证明；只哈希 marker UUID 不足以证明范围；
- 非终态 `scope=session` 时 `session_id` 必填且关联同一项目的正式访谈会话；非终态 `scope=segment_range` 时 `source_marker_id` 必填；两者外键均使用 `ON DELETE SET NULL`，只允许在 `processing -> completed` 的清理事务或上级 project 物理清理中置空；
- 非终态是 `pending_verification`、`verified`、`processing`：`scope=project` 使项目保持 `restricted` 并停止整个项目新的 AI 处理；`scope=session` 只停止该会话内容及派生记忆的新 AI 处理；`scope=segment_range` 只停止 marker 范围及派生记忆的新 AI 处理；所有 scope 都阻止项目级普通回顾和普通导出；
- `withdrawn` 只能由数据管理员在核验请求人撤回后设置；
- `processing` 执行范围内在线对象存储、数据库业务数据和临时导出的实际清理；只有这些清理成功、SHA-256 `cleanup_manifest_hash` 已登记，且备份清理以 `backup_cleanup_status=scheduled` + `backup_cleanup_due_at` 或 `not_applicable` 登记后才能进入 `completed`；清单不得包含被删正文或对象密钥原值；
- `completed` 表示请求范围在线主存储与临时导出清理完成，不等同于删除审计记录，必须保留最小必要审计证据；project scope 同时把项目置为不可恢复的 `deleted`，session/segment scope 不改变项目状态；
- `content_marker.deletion_request` 可以为空；项目级删除请求不要求先存在内容 marker。
- 非终态 `scope=segment_range` 时必须关联同一项目、类型为 `deletion_request` 的 `source_marker_id`，且 `session_id` 为空；该归属与类型由服务层事务校验并由外键保证实体存在。`project`、`session` 范围不得用自由文本冒充正式关系；
- 完成 session/segment 清理时必须先保留 `scope_reference_hash`，再在同一事务清空 FK 和 segment 快照并删除范围内正文、对象键、派生记忆和 marker 业务字段；不得保留 `note`、segment 关联或可据以恢复正文的 tombstone。

状态流转：

- `pending_verification -> verified | rejected | withdrawn`；
- `verified -> processing | withdrawn`；
- `processing -> completed`；
- `completed`、`rejected`、`withdrawn` 为终态；
- 非法流转必须拒绝，不能通过直接改字段绕过 `deletion_request_transition` 和审计。

### 4.19 auth_session

```text
id
user_id
token_hash
csrf_token_hash
created_at
expires_at
last_seen_at
revoked_at
revoked_reason
```

规则：

- 浏览器只持有高熵不透明会话 ID；数据库只保存其安全哈希；
- 会话 ID 至少包含 256-bit 安全随机值；
- CSRF token 使用独立的至少 256-bit 安全随机值，数据库只保存 SHA-256 哈希并绑定当前会话；
- 账号停用、登出、权限安全事件或管理员撤销时必须使相关会话失效；
- 过期或撤销会话不得恢复使用；
- 登录必须创建全新的 session ID 与 CSRF token，绝不复用请求携带的旧 session；权限或密码安全事件撤销旧 session，不通过修改旧 token 恢复。认证 touch、CSRF 轮换与撤销必须以同一持久会话当前仍有效为条件；撤销或过期先提交时，后续 touch/轮换必须原子失败关闭；
- 会话原值、Cookie 和 CSRF token 不得进入日志或审计 metadata。

### 4.20 auth_login_throttle

```text
id
key_hash
key_kind
window_started_at
failure_count
blocked_until
updated_at
```

规则：

- `key_kind` 为 `identity_ip` 或 `ip`；`key_hash` 使用服务端 pepper 对规范化邮箱和/或可信客户端 IP 生成不可逆摘要；
- 在部署契约接收可信代理前，“可信客户端 IP”只指服务器直接对端；所有转发/Cloudflare IP header 均忽略。未来 resolver 必须只在直接对端属于已批准可信代理集合且 origin 无法绕过时消费部署契约指定的唯一 header，并对缺失、重复、语法错误、私有/保留地址策略不符或 hop 不一致失败关闭；
- 同一身份与 IP 在 15 分钟内失败 5 次，或同一 IP 在 15 分钟内失败 30 次，阻断 15 分钟；成功登录清理对应身份与 IP 组合记录；
- 限流判断和失败计数必须原子更新；接口无论是账号不存在、密码错误还是被限流，均不得泄露账号存在性；
- 过期记录按维护任务清理，不进入业务导出。

### 4.21 boundary_candidate

```text
id
project_id
session_id
ai_job_id
ai_derived_output_id
segment_start_id
segment_end_id
marker_type
abstract_note
status
expires_at
resolved_by
resolved_at
created_at
updated_at
```

状态：

- `pending`
- `confirmed`
- `rejected`
- `expired`

规则：

- 只允许 AI 提议 `sensitive`、`restricted`、`do_not_ask` 或 `deletion_request` 候选；候选不是 `content_marker`，不得被普通 marker 查询当作有效业务标记；
- `project_id`、`session_id`、证据片段和 `ai_job_id` 必须归属一致；`abstract_note` 不得复制受限原文；
- 每个 AI 生成的候选恰好绑定一条 `output_type=boundary_candidate` 的 `ai_derived_output`；候选的业务到期 `expires_at` 只控制临时阻断，不是 AI retention deadline；
- `pending` 候选在当前会话形成临时保守阻断；确认时以一个事务创建正式 marker、写审计并更新为 `confirmed`；
- 驳回、到期或会话结束时更新相应状态并解除临时阻断；跨会话只加载正式 marker，不加载候选。

### 4.22 deletion_request_transition

```text
id
deletion_request_id
request_id
from_status
to_status
actor_id
resolution_note
created_at
```

规则：

- 每次核验、拒绝、开始处理、完成或经核验撤回都创建一条记录，并与 `deletion_request` 状态更新置于同一数据库事务；
- `request_id` 全局唯一；重复请求返回首次业务结果，不产生第二次状态变化；
- `actor_id` 必须是执行动作的 `data_admin`；`resolution_note` 不得包含待删除正文或不必要个人信息；
- 终态不得再产生状态流转。

### 4.23 idempotency_record

```text
id
request_id
action
actor_id
target_type
target_id
create_identity
request_payload_hash
response_payload
created_at
```

规则：

- `request_id` 全局唯一；记录必须与首次请求的 `action`、`actor_id`、`target_type`、现有 `target_id` 或创建前 `create_identity` 二选一绑定，并同时保存规范化业务 payload 的 `request_payload_hash`；`request_id` 本身不进入 payload hash；
- 已有父资源内追加时使用该父资源作为 target：service term、consent record、session create 均绑定 `target_type=elder_project,target_id={project_id}`。新建 project 尚无 target ID，固定使用 `target_type=elder_project,target_id=null,create_identity=project:create:{actor_id}:{request_id}`；成功响应中的 project ID 只保存在首次响应快照，不得在重放时改写 create identity；
- `request_payload_hash` 为 UTF-8 RFC 8785 canonical JSON（排除 `request_id`）的 SHA-256 lowercase hex。session create 无其他业务字段时哈希 `{}`；consent record 的 hash 必须包含 consent type/version/method/time/audio object ID，不能复用 consent audio init/chunk/complete 的 request identity；
- 首次业务写入、审计和幂等记录必须在同一数据库事务提交；`response_payload` 保存首次成功业务响应的最小快照；
- 相同 `request_id` 且 actor/action/target-or-create-identity/payload hash 全部一致时返回首次响应快照，不重复状态变化或审计；任一绑定信息不一致时返回稳定冲突，不得返回其他资源结果；
- 需要幂等的状态变更仍须按业务资源串行化或使用带前置状态的原子更新；`request_id` 锁不能代替资源锁。

### 4.24 audio_object

```text
id
project_id
session_id
purpose
status
mime_type
chunk_count
total_size_bytes
manifest_checksum
created_by
created_at
completed_at
```

`purpose`：

- `consent`：项目级口头授权音频；`session_id` 必须为空，可在项目仍为 `draft` 且正式访谈尚未 start 时创建；
- `interview`：正式访谈录音；`session_id` 必须属于同一项目，创建和上传时 session 必须处于允许录音的状态。

`status`：

- `initiated`
- `uploading`
- `complete`
- `failed`

规则：

- 对象与分片只追加、不覆盖；本任务不提供普通删除或替换接口；
- `complete` 前必须在同一资源锁内确认 `0..chunk_count-1` 连续、全部分片为 `uploaded`、数据库 size/checksum 与私有存储对象一致，并对按 sequence 排序的最小分片清单计算 SHA-256 `manifest_checksum`；
- `chunk_count`、`total_size_bytes`、`manifest_checksum` 和 `completed_at` 只在首次成功 complete 时写入；complete 后不得再新增或修改分片；
- `consent_record` 只能引用同项目、`purpose=consent`、`status=complete` 的对象。对象不存在、跨项目、未完成、缺片或存储校验失败时，授权创建必须失败关闭；
- 内部原型使用本地私有存储适配器和虚构音频。真实对象存储、签名上传、加密和保留策略在真实试点前验收，不阻塞本地可靠保存验证。

### 4.25 session_finalization

一次会话至多一条结束收束聚合，作为 stop/recover、公共查询和未来持久任务/outbox 的唯一持久事实来源。

```text
id
session_id
audio_object_id
stop_request_id
expected_chunk_count
commitments_checksum
capture_ended_at
processing_started_at
completed_at
audio_status
transcript_status
transcript_error_code
asr_last_audio_sequence_accepted
asr_drain_completed_at
failure_code
created_at
updated_at
```

`audio_status`：

- `awaiting_upload`：等待冻结范围内分片补传；
- `verifying`：分片齐全，正在完成/复核 manifest；
- `complete`：audio object 已 complete 且私有存储、size、checksum 和 canonical manifest 已复核；
- `unrecoverable`：已确认不能通过受限 recover 自动收束，需要人工处置。

`transcript_status`：

- `pending`：尚未开始 final drain；
- `draining`：正在关闭实时 ASR 并持久化已接受 PCM 对应的最后 final；
- `drained`：session/capture 级 completeness 始终无已知 gap，全部 attempt 边界连续，最后 attempt 的明确 drain receipt 成立且所有已返回 final 均已落库；
- `degraded`：任一 attempt 留下已知未回补 gap，或 ASR 不可用、超时、runtime/coverage evidence 丢失而无法证明整场完整，但原始录音不受影响；该业务投影在本 SPEC 内不可由后续 attempt success 清除；
- `not_started`：会话未建立可用 ASR stream，属于可合法完成的明确降级终态。

规则：

- `session_id`、`audio_object_id`、`stop_request_id` 分别唯一；audio object 必须属于同一 session、`purpose=interview`，且不得在 stop 后更换；
- 公共 `SessionFinalizationSnapshot.total_size_bytes` 是从本记录关联的既有 `audio_object.total_size_bytes` 派生的只读投影，不是 `session_finalization` 新字段，也不得从 commitment、已上传数量、本机 archive 或客户端输入反推。本契约先以 additive optional+nullable 字段兼容尚未更新的 producer；DEV-008A3 runtime 落地后，普通 canonical `GET /sessions/:id` 只要存在 finalization 就必须显式返回该 key；
- 当 `audio_status=awaiting_upload|verifying|unrecoverable`，或关联对象尚不能证明 `status=complete`、`total_size_bytes` 非空且可精确表达为 JavaScript 非负安全整数时，公共值为 `null`。`audio_status=complete` 的正常事实必须返回同一 `AudioObject.totalSizeBytes`；正常 `processing|completed` 以及 `failed + complete manifest` 因此必须非空。legacy 缺键、终态 `null`、unsafe integer 或任一不一致都表示“服务端录音事实不可证明”：普通只读转录可继续，但 DEV-008A3 的播放和本机删除必须失败关闭，绝不能把它解释为 `0`；
- stop 接受时在 session 资源锁内创建本记录、全部 chunk commitment、幂等记录并把 session 置为 `stopping`；这些变化必须同事务提交；
- `expected_chunk_count` 为正整数和 `0..N-1` 的冻结高水位，必须与 commitment 数量相等；`commitments_checksum` 是按 sequence 排序后对最小 commitment 清单计算的 canonical SHA-256；
- `capture_ended_at` 与 `interview_session.ended_at` 相同；`processing_started_at` 只在 manifest 首次完整、session 进入 `processing` 时写入；`completed_at` 只在进入 `completed` 时写入；
- `completed` 的必要且充分完成门禁是 `audio_status=complete` 且 `transcript_status in (drained,degraded,not_started)`；AI、memory、suggestion、session note 不在门禁内；
- `transcript_error_code`、`failure_code` 只使用 `05` 冻结的不泄密枚举，不保存供应商原文、SQL、堆栈、正文、对象键或 token；
- stop 接受时如存在 active runtime，固化 `asr_last_audio_sequence_accepted`；没有可用 stream 时为空并最终进入 `not_started|degraded`。`asr_drain_completed_at` 只在 session/capture 聚合仍无已知 gap、所有 attempt coverage 连续且最后 attempt receipt 明确结束时写入；后续 attempt receipt 不得用来覆盖旧 gap，也不得用“数据库最后一条 final”替代整体终止确认；
- 进程内 runner 可以丢失；recover 必须只凭本聚合、commitment、audio manifest 和 final 持久层安全重驱，不依赖 WebSocket replay。

### 4.26 session_finalization_chunk

```text
id
session_finalization_id
sequence_no
start_ms
end_ms
size_bytes
checksum
mime_type
created_at
```

规则：

- `(session_finalization_id, sequence_no)` 唯一，sequence 必须连续覆盖 `0..expected_chunk_count-1`；
- 字段是客户端在 `MediaRecorder.stop()` 最终 `dataavailable` 后提交的不可变 commitment；服务端接受 stop 后不可新增、删除、扩大或修改；
- stop 前已上传分片必须与对应 commitment 全字段一致；stop 后只允许上传 commitment 中尚缺的 sequence，且二进制、时间、size、checksum、MIME 必须一致；
- commitment 只证明允许补传的字节边界，最终完整性仍以 audio complete 对私有存储的重新读取和 canonical manifest 为准；
- commitment 和公共响应不得包含 Blob、对象键、下载地址或正文。

### 4.27 session_capture_generation

一次 session 的每次明确采集尝试各有一条 generation；所有 generation 复用同一 interview audio object，记录意外中断与恢复边界，不代替原始分片或 finalization。

```text
id
session_id
audio_object_id
generation_no
audio_stream_id
timeline_offset_ms
status
interruption_reason
confirmed_active_at
first_pcm_accepted_at
interrupted_at
stopped_at
created_at
updated_at
```

状态：`preparing|active|interrupted|stopped|abandoned_empty`。

中断原因：`capture_start_failed|page_recovery_detected|microphone_ended|recorder_error|local_archive_failed|auth_lost|unknown`。

规则：

- `(session_id, generation_no)`、`audio_stream_id` 唯一；同一 session 同时最多一条 `preparing|active` current generation；
- generation 0 与唯一 interview audio object 必须在原子 start 事务创建；后续 resume 只能递增 generation，并继续绑定同一对象；
- `timeline_offset_ms` 是该 generation 在完整原始录音时间轴上的起点。generation 0 为 0，后续值不得小于已保存 archive/服务端 chunk 的 end high-water；不得用静音填补中断区间；
- `confirm_capture_active` 只把当前 `preparing -> active` 并写首次确认时间；它本身不是音频证据；
- realtime adapter 首次成功接受该 generation 的 PCM 后，只在 `first_pcm_accepted_at` 为空时写一次；不得每帧更新数据库。进程重启后该字段仍是“曾接受 PCM”的持久证据；空录音放弃必须确认该 session 的所有 capture generations 均不存在非空 `first_pcm_accepted_at`，不能只检查当前 generation；
- `report_interrupted` 只把当前 `preparing|active -> interrupted`，写固定原因和时间并使 session 进入/保持 `interrupted`；stop/finalization 已提交后不得回退；
- 正常 stop 把当前 generation 置为 `stopped`；零证据放弃把它置为 `abandoned_empty`，同时 audio object 置 `failed`、session 置终态 `failed` 并写顶层 `capture_failure_code=NO_AUDIO_CAPTURED`；此路径保持 `session_finalization=null`；
- 记录只保存控制元数据，不保存 local job ID、Blob、对象键、转录正文或浏览器存储路径。

### 4.28 memory_claim

项目记忆的一次不可变主张。每次 AI 提取、明确更正、人工确认或系统迁移都新增记录，不覆盖历史。

```text
id
project_id
memory_type
canonical_key
value_kind
value_json
authority
generation_job_id
ai_derived_output_id
memory_retention_root_id
correction_of_claim_id
created_by
created_at
```

legacy `semantic_kind IS NULL` 记录的 `memory_type` 使用 §4.10 的最小类型；P1 `semantic_kind IS NOT NULL` 记录仅可将该列作为 nullable optional metadata tag。`value_kind` 为 `exact|range|unknown`；`authority` 为 `automatic|human_confirmed|system_migration`。

- `semantic_kind=episode|fact` 与 `canonical_key` 共同构成 P1 稳定语义槽；`memory_type` 对 P1 为 nullable optional metadata tag，不包含原始正文，也不参与 P1 identity。legacy `semantic_kind IS NULL` 仍按旧 `memory_type + canonical_key` 解释；唯一性由 current resolution 而非 claim 表保证；
- `generation_job_id` 对 automatic 必填，对人工或迁移 claim 可空；`correction_of_claim_id` 必须属于同项目、同类型/槽；
- automatic claim 的 `ai_derived_output_id` 必填且唯一、`memory_retention_root_id` 为空；人工确认/系统迁移 claim 的 derived ID 必须为空且 `memory_retention_root_id` 必填。一个 job 产出五条独立 claim 时，必须保存五条 claim 和五条 derived output，禁止把它们合并为一条资格记录；
- `value_json` 只保存结构化最小值，不保存完整转录或模型原始输出；range 必须含明确边界/精度，unknown 不得伪造精确值；
- automatic claim 必须至少一条 evidence，并且全部来自 trusted elder conversation final；human-confirmed claim 仍必须关联人工来源或审计证据，AI 不得改写。

### 4.29 memory_claim_evidence

```text
id
memory_claim_id
ai_job_input_segment_id
transcript_segment_id
created_at
```

- `(memory_claim_id, transcript_segment_id)` 唯一；automatic claim 的 `ai_job_input_segment_id` 必填并必须属于其 generation job；
- 只保存引用，不复制 segment 正文；删除 scope 完成时删除命中 membership/claim 或把无法证明隔离的上层输出整体删除，不得用 `ON DELETE RESTRICT` 阻塞隐私清理；
- 原始证据存在期间，普通失效只改变 resolution/eligibility，不删除本表。

### 4.30 memory_resolution

同一 P1 `(project_id, semantic_kind, canonical_key)` 的当前解释投影；legacy `semantic_kind IS NULL` 继续使用旧 `(project_id, memory_type, canonical_key)`。

```text
id
project_id
memory_type
canonical_key
resolution_kind
authority
generation_job_id
ai_derived_output_id
memory_retention_root_id
resolution_revision
replaces_resolution_id
status
created_at
superseded_at
```

`resolution_kind`：`single|range|unknown|conflict_set|review_required`。`status`：`current|pending_review|superseded`。

- 同一语义槽同时至多一条 `status=current`，以 legacy/P1 互斥部分唯一索引保证；P1 的可选 `memory_type` tag 不参与该约束。可同时存在 future-ineligible 的 `pending_review` 提案；写入使用 project + semantic kind + canonical key 资源锁；
- `single|range|unknown` 恰好一条 member；`conflict_set` 至少两条互不相同的 eligible claim；`review_required` 必须为 `pending_review`、至少一条自动提案且不得替换 current human-confirmed 值；
- 自动生成的 resolution（包括自动冲突/澄清提案）恰好绑定一条 `output_type=memory_resolution` 的 derived output，且 `memory_retention_root_id` 为空；人工确认/迁移的 resolution 的 job/derived ID 为空、`memory_retention_root_id` 必填，并由 §4.43 的 root 管理生命周期；
- 明确更正事务新增 claim 和 resolution、把旧 resolution 置 superseded，并使旧 claim 对应 derived output future ineligible；旧 claim/evidence 不删除；
- current view 只投影 `status=current`，且 automatic resolution 通过 §4.32 权威 eligibility、human-confirmed/system-migration resolution 通过 authority + active `memory_retention_root` + 当前授权/boundary/deletion 谓词。pending review 永不进入普通消费；冲突返回 conflict 元数据与 claim ID，不把任一值投影为单一事实。

### 4.31 memory_resolution_member

```text
id
memory_resolution_id
memory_claim_id
member_order
created_at
```

`(memory_resolution_id, memory_claim_id)` 与 `(memory_resolution_id, member_order)` 唯一；member order 只用于稳定 canonical projection，不代表置信度排名。

### 4.32 ai_derived_output 与依赖

所有可进入 future generation、current view 或跨会话的 AI 业务输出逐项绑定资格记录。采用“每个独立业务输出一条 derived output”，不采用一个 output-set 覆盖多个业务输出。

```text
ai_derived_output
  id
  project_id
  ai_job_id
  output_type
  business_output_id
  expected_segment_dependency_count
  segment_dependency_manifest_hash
  expected_memory_dependency_count
  memory_dependency_manifest_hash
  expected_question_dependency_count
  question_dependency_manifest_hash
  materialized_status
  invalidation_reason
  invalidated_at
  created_at
  updated_at

ai_output_segment_dependency
  id
  ai_derived_output_id
  ai_job_input_segment_id
  transcript_segment_id
  text_revision
  speaker_role_revision
  effective_text_digest
  created_at

ai_output_memory_dependency
  id
  ai_derived_output_id
  memory_resolution_id
  resolution_revision
  created_at

ai_output_question_dependency
  id
  ai_derived_output_id
  dependency_kind
  question_display_snapshot_id
  question_evidence_event_id
  actual_question_id
  dependency_revision
  dependency_at
  dependency_digest
  input_order
  created_at
```

`output_type`：`memory_claim|memory_resolution|question_candidate|boundary_candidate|actual_question_catalog|session_note|context_snapshot`。`materialized_status`：`current|invalidated|waiting_recompute|recompute_failed|review_required|superseded`。`dependency_kind`：`display_snapshot|question_event|actual_question`，三种目标 FK 恰好一个非空。

关联和 cardinality 是正式约束：

1. `ai_job 1 -> 0..N ai_derived_output`；`(ai_job_id, output_type, business_output_id)` 唯一，允许一个 job 产生同类型多个独立输出；
2. automatic `memory_claim`、automatic `memory_resolution`、`question_candidate`、`boundary_candidate`、generated `session_note`、`interview_context_snapshot` 各自恰好引用一条 derived output，且各表的 `ai_derived_output_id` 唯一；
3. 一次成功、judgeable、可发布的 `actual_question_analysis` 恰好引用一条 `output_type=actual_question_catalog` 的 derived output。它代表这一 analysis 下全部 `actual_question/evidence/suggestion_outcome`；子 question 不再各建 derived output；pending/failed/unjudged analysis 不得绑定可发布资格记录；
4. `question_display_snapshot` 是已经展示的不可变历史事实，不建 derived output；其动态正文可见性独立于 candidate future eligibility，并由自己的 retention root 管理；
5. 写回事务在 deferred constraint check 时必须证明：derived row 只被一个与 `output_type/business_output_id` 对应的业务 root 引用，业务 root 与 derived row 的 project/job 一致。Prisma 单表唯一性不能替代该跨表检查，DEV-006 migration 必须用 PostgreSQL deferred constraint trigger 或等价、可自动测试的数据库约束实现；
6. 最小失效单元为单条 claim、单条自动 resolution、单个 candidate、单个 boundary candidate、单个 generated note 版本或单个 context snapshot；`actual_question_catalog` 的最小失效单元是整个 analysis 版本。任一依赖命中时撤下整版目录，不允许只留下“看起来仍有效”的部分问题。

| 业务输出 | derived cardinality | correction/policy 失效范围 | deletion/retention 清理范围 |
|---|---|---|---|
| automatic memory claim | 1 claim : 1 derived | 命中该 claim；同 job 其他未命中 claim 可保持 eligible | deletion 命中共享 job input 时隐藏/清理整个 ai_job root；普通 retention 到期同样清理整棵 job tree |
| automatic memory resolution | 1 resolution : 1 derived | 命中该 resolution；current projection 原子 detach/切换 | 随 ai_job root 清理 resolution/member/derived；先隐藏不属于本 root 的依赖者 |
| question/boundary candidate | 1 candidate : 1 derived | 只使该 candidate future ineligible；已展示 snapshot 按自己的 visibility 保留 | 随 ai_job root 清理 candidate/derived；snapshot FK detach。若 deletion 同时命中展示证据，再独立隐藏/清理 snapshot root |
| generated session note | 1 note version : 1 derived | 撤下该版本的 generated summary；不覆盖人工 edited overlay | 随 ai_job root 删除该 generated business row/derived，不得保留生成正文 |
| actual-question catalog | 1 analysis version : 1 derived | 任一 dependency 命中即整版 unpublished/ineligible | 随 ai_job root 删除 analysis 下全部 questions/evidence/outcomes 和 derived，不得部分保留 |
| interview context snapshot | 1 snapshot : 1 derived | 命中该 snapshot，future consumer 不得读取任何 membership | 随 ai_job root 删除 snapshot/memberships/derived，不得保留部分 manifest |

human-confirmed/system-migration memory 没有 derived row，不参与上表资格状态；它们由 current resolution authority 与 `memory_retention_root`、授权/boundary/deletion 查询共同控制。

三个 expected count 与 manifest hash 在业务输出写回事务冻结。manifest 对按 `input_order` 排序的稳定目标 ID、版本/时间和 digest 计算；没有相应依赖时 count 为 0 且使用规范化空 manifest。查询必须同时验证“实际行数等于 expected count、canonical manifest 相等、目标 FK 均存在”，然后才逐行判断当前资格。CASCADE 或 `SET NULL` 导致依赖缺失时谓词为 false，禁止用剩余行的全称判断产生 vacuous eligibility。

权威 future eligibility 不是单看 `materialized_status`，而是以下谓词全部成立：

1. job 成功且输出未 superseded；
2. 每个 segment dependency 仍存在、text/speaker revision 与 digest 一致、trusted role/content kind 仍符合该 output type；
3. 不存在命中 dependency 的 correction operation membership 或更高 evidence revision；
4. 每个 memory dependency 仍是相同 current resolution/revision；
5. 每个 question dependency 的目标、revision/time/digest 仍存在且满足其输出类型规则；actual-question reconcile 消费过的 displayed snapshot、replace/withdraw event 和已有 actual question 必须逐项入表，不能只保存一个聚合 hash；
6. 项目当前 assignment、授权、状态允许消费，policy revision 已安全重评；
7. retention root 仍为 `active` 且未到期；
8. 不命中 `restricted|do_not_ask` 的适用规则或活动 deletion scope；无法证明隔离时失败关闭。

物化状态用于可观察性和重试，不得绕过查询谓词。普通修正提交后即使异步状态尚未刷新，anti-join/version 谓词也必须立即排除旧输出；失败不得把旧输出恢复为 eligible。`invalidated` 本身不自动启动 AI；只有正常触发或显式重试创建新 job 时才可进入 `waiting_recompute`。

### 4.33 ai_job_session_scope

```text
id
ai_job_id
session_id
speaker_role_revision
max_segment_start_ms
max_segment_id
eligible_segment_count
scope_reason
created_at
```

- `(ai_job_id, session_id)` 唯一；覆盖本 job 评估范围内的每个 session，即使 `eligible_segment_count=0`；
- `speaker_role_revision` 是输入冻结时 session 当前值；`max_segment_start_ms/max_segment_id` 共同构成稳定 `(start_ms,id)` 高水位，只能同时为空且仅当 session 没有任何 final；
- scope 行证明“检查过哪些来源版本”，不能替代 §4.34 实际 membership；新 final 超过高水位只形成以后正常 trigger，不自动使已完成 job 伪装为读过它；
- 项目级 job 禁止只写 trigger session 一行。

### 4.34 ai_job_input_segment

```text
id
ai_job_id
session_id
transcript_segment_id
text_revision
speaker_role_revision
trusted_effective_role
role_authority
content_kind
effective_text_digest
input_order
created_at
```

- `(ai_job_id, transcript_segment_id)` 与 `(ai_job_id, input_order)` 唯一；
- `effective_text_digest` 对 effective text 仅把 CRLF/CR 统一为 LF 后，以 UTF-8 字节计算 SHA-256 小写 hex；不 trim、不改大小写、不做 Unicode 归一化。它只用于版本/完整性验证，不能代替正文作为模型输入；正文按请求时有权读取并只在调用内存中组装；
- role、authority、content kind 和 revision 是消费时快照。unknown、unconfirmed、calibration 或命中 policy 的片段不得进入相应角色消费；
- membership 证明“实际消费了什么”，不能替代 session scope；两者必须同一输入冻结事务提交。

### 4.35 ai_job_input_memory 与 ai_provider_call

```text
ai_job_input_memory
  id
  ai_job_id
  memory_resolution_id
  resolution_revision
  input_order
  created_at

ai_provider_call
  id
  ai_job_id
  call_no
  call_kind
  status
  requested_provider_id
  requested_provider_model_id
  observed_response_model_id
  observed_response_model_id_source
  ai_model_config_manifest_id
  model_config_schema_version
  model_config_version
  model_config_digest
  sdk_core_version
  sdk_provider_package
  sdk_provider_package_version
  connection_mode
  endpoint_origin
  data_region
  provider_request_id
  provider_request_id_source
  sdk_response_id
  sdk_response_id_source
  config_application_status
  sanitized_warnings_json
  input_hash
  output_hash
  token_usage_json
  latency_ms
  error_code
  started_at
  completed_at
```

```text
ai_model_config_manifest
  id
  schema_version
  canonicalization_version
  model_config_version
  model_config_digest
  canonical_manifest_json
  created_at
```

`ai_model_config_manifest.model_config_version` 与 `model_config_digest` 分别唯一；manifest 通过 `llm-model-config-v1`，canonical bytes 与 SHA-256 必须按 `llm-model-config-canonical-json-v1` 重算一致。temperature、max output、top-p/top-k、penalties、seed、stop、reasoning、response format、tools 与 sanitized provider options 全部显式保存；内容变化创建新 version，不原地覆盖。manifest 禁止 secret、credential、header、完整 Prompt/Context 或账户信息。`ai_provider_call.ai_model_config_manifest_id` 必填并与冗余 version/digest 逐值相等，保证历史不依赖当前部署配置反推。

`call_kind` 至少支持 `primary|same_input_retry|format_repair`；`question_generation` 只允许 `primary|same_input_retry`，后者的 `call_no=2` 且只在第一次发生 transport/timeout，或返回内容未通过 JSON/Output Schema、引用 ID/subset、明显长度/结构等写回前基础硬校验时存在。第二次的输入身份必须与第一次逐值相同；第一次超时后的迟到输出永久失去写回资格。`format_repair` 只为既有非 Director job 的已冻结语义保留，不得用于实时问题生成。`(ai_job_id, call_no)` 唯一。

每次真实调用必须逐值冻结 requested provider/model/config、observed response model、AI SDK core/provider package 精确版本、`direct_vendor`、endpoint origin 与 data region；不得只从当前部署配置反推历史。observed model source 为 `provider_origin|sdk_normalized|unknown|unavailable`，unavailable 时 ID 为空；provider request ID 只允许 `provider|unavailable`，不能由 SDK-generated 值补位；SDK response ID 独立记录 `provider_origin|sdk_generated|unknown|unavailable`，unavailable 时为空。

`config_application_status=as_requested|diverged|unknown`。unsupported/ignored/adjusted setting 必须是 `diverged` 并记录 setting JSON Pointer 与稳定 sanitized code；provider 只给模糊 warning 或 adapter 无法观察 warning 时为 `unknown`。`as_requested` 必须 warnings 为空。warning 不保存完整 provider 原文、header、请求/响应正文或 secret。provider 原始输出、完整输入和完整 response headers 不进入本表或技术日志；`output_hash` 不等于保存供应商正文。

### 4.35A question_bank_release

题库 CSV 是内容交换/编辑格式，不是运行时权威。受控导入后以 release 形成版本化运行时事实；项目负责人不直接编辑数据库。

```text
id
bank_version
content_digest
item_count
membership_sealed_at
status
source_file_digest
validator_version
environment_scope
imported_by
imported_at
activated_by
activated_at
retired_at
created_at
```

`status`：`draft|active|retired`；`environment_scope`：`product|internal_demo`。

- `bank_version` 全局唯一；同一版本的 canonical CSV 内容和 SHA-256 `content_digest/source_file_digest` 不可变，内容变化必须新建版本；
- canonical membership 按 `question_id` 排序，对 validator/bank version 与每个 item 的全部版本化字段做 UTF-8 byte-length framing 后计算 SHA-256 `content_digest`；数据库在 seal 时按同一算法重算，`item_count` 必须等于实际 membership；
- 一个 release 同时承载 `basic` 与 `deep`，避免两个题库版本组合漂移；同一环境同时至多一个 `active` release；
- 导入必须先全量校验，release 与全部 item 同一事务落为 `draft`；新 release 只在该未提交事务内以 `membership_sealed_at=NULL` 打开构建窗口，全部 item 写入后由数据库核对 count/digest 并一次 seal；deferred 提交约束禁止未 seal、count/digest 不符或许可/scope 不符的 release 提交，任一失败不得留下部分 release/item；
- seal 后的 `draft|active|retired` release 均禁止 item INSERT/UPDATE/DELETE，`item_count/membership_sealed_at/content_digest` 不可修改；激活和退休前继续复核 seal 与实际 membership；
- 激活新 release 与退休旧 active release 在同一事务完成；旧 release/item 保留以支持 candidate/display provenance，但不进入新选择；
- `environment_scope=internal_demo` 只允许全部 item 均为 `synthetic_fixture + fixture_only`，production 或正式内部试用配置必须拒绝激活；
- `activated_by` 对人工激活必填；自动化内部 fixture 激活仍必须有明确 system actor 和审计。题库管理 UI 与普通公网导入 API不属于第一版。

### 4.35B question_bank_item

```text
id
question_bank_release_id
question_id
bank
topic
question_text
purpose
applicable_condition_codes
inapplicable_condition_codes
sensitivity
source_type
source_reference
license_status
license_reference
enabled
created_at
```

`bank`：`basic|deep`；`purpose` 必填且复用 `07` §8 的受控枚举；`sensitivity`：`low|medium|high`；`source_type`：`project_original|licensed_external|public_domain|synthetic_fixture`；`license_status`：`project_original|verified|unverified|fixture_only`。

- `(question_bank_release_id, question_id)` 唯一；`question_id` 在不同 release 间稳定，用于识别同一内容槽，版本事实仍以 release 为准；
- 条件码保存由 `07` §10.2 校验为字段内唯一、跨字段不相交并稳定排序的集合；导入器拒绝未知、重复、空 token 和适用/排除交集，不得静默去重或保存并执行自由文本表达式/任意代码；
- `enabled` 只表达该 release 的编辑状态；运行时 eligibility 还要求 release active、许可合格、阶段/条件匹配和全部权威安全门禁通过；
- runtime reader 必须投影参考题的 `purpose`；候选另存本次模型输出的受控 purpose，不要求与参考题相同；
- `unverified` 只允许 draft；`fixture_only` 只允许 internal demo。`project_original|verified` 才能进入正式内部试用及之后环境；
- item 正文是受版本和许可治理的参考内容，不等于长者个人事实。最终问题的访谈事实依据仍通过 candidate derived dependencies 单独记录。

### 4.36 question_generation_attempt

由 `QuestionEvidenceModule` 保存问题生成尝试的共享身份。attempt 记录生成意图，不等于问题已经发布；只有 §4.39 的权威发布事务才能改变 canonical current 或产生展示历史。

```text
id
session_id
ai_job_id
request_id
attempt_kind
retry_of_attempt_id
basis_presentation_revision
basis_snapshot_id
manual_intent_sequence
selection_policy_version
similarity_policy_version
journey_stage
journey_policy_version
journey_reason_codes
journey_basis_hash
prompt_bundle_version
prompt_bundle_digest
context_schema_version
context_schema_digest
output_schema_version
output_schema_digest
context_builder_version
context_builder_digest
model_config_version
model_config_digest
status
result_kind
publication_outcome
failure_code
created_at
started_at
completed_at
```

`attempt_kind`：`automatic|manual_next|second_session_opening`。旧 `replace` 不再使用；替换是 publication outcome，不是另一类生成请求。`status`：`pending|running|succeeded|failed|cancelled`。`result_kind`：`suggestion|continue_listening|unavailable`。`publication_outcome`：`published|not_better|duplicate_filtered|stale_basis|superseded_by_manual|policy_blocked|not_applicable`。

- `request_id` 全局唯一。manual next 使用客户端在首次请求前持久化的 ID；automatic/second-session 使用服务端由权威 trigger 生成的稳定 ID 与 `trigger_dedupe_key`。响应未知重放原 ID，权威成功、失败或节流拒绝后的下一次用户动作才使用新 ID；
- `basis_presentation_revision` 与 `basis_snapshot_id` 冻结请求接受时的 canonical current。manual next 必须同时绑定 actor、session 和二者；不匹配时不得创建 attempt。automatic 写回必须以二者和 `manual_intent_sequence` 做 CAS；
- `manual_intent_sequence` 来自 §4.39 display state。每次接受新的 manual next 时先递增并写入 attempt，形成 intent fence；任何基于更早 fence 的 automatic 结果即使供应商调用已经返回，也只能 `cancelled/superseded_by_manual`，不得覆盖手动意图；
- `journey_stage` 为 `rapport|life_outline|story_depth`；`journey_policy_v1` 的输入、完整受控 reason code、优先级与转移顺序以 `07` §10.4 为准。同一冻结输入与 policy version 必须产生稳定阶段、按受控顺序输出的 reason codes 与 basis hash。题数/经过时间不得进入 stage 决策或 tie-break；
- stage basis 必须覆盖确定态转录水位、实际使用的 memory manifest、可信角色和 boundary/policy revision。它补充既有 job scope/membership，不复制转录或记忆正文；
- prompt/context/output/context-builder/model-config 各版本必须与 digest 同时保存，防止同名内容漂移。这些是 job/attempt 的权威过程元数据；模型 Context 只保留唯一 Context Schema 自己要求的自识别版本字段，不携带 digest、request/job 身份或模型配置。完整 prompt、完整 Context 或供应商原始输出不得复制进技术日志，实际输入范围由 ai_job scope/membership、题库 input membership 与不可逆摘要证明；
- 同一 session 同时最多一条 `pending|running` manual-next attempt，且最多一条 `pending|running` automatic attempt。manual next 到来时，尚未调用供应商的 automatic 直接取消；已在途的调用可结束但写回必须失败关闭；
- `succeeded` 仅表示结果通过 Schema/策略校验，不表示已发布。`published` 只有在 candidate/derived output 或中性结果、display state、snapshot（如有）与 evidence event 同一事务提交后写入；
- attempt 内首先执行一次 primary；transport/timeout 或返回内容未通过写回前基础硬校验时，最多一次完全同输入的 `same_input_retry`，不形成第二 attempt。第二次仍失败则 attempt failed，不创建 candidate、不修改 current/history；权限、安全、deletion、重复或 writeback 漂移不 retry。AI unavailable 不自动创建基础题或后台重试，后续用户点击“下一个问题”必须使用新 request。

### 4.36A question_generation_bank_input_membership

保存 frozen `InterviewDirectorContextV1.bank_references` 中后端实际发送、模型实际看过的题库参考；它属于 ai_job 输入事实，不依赖模型是否声明使用：

```text
ai_job_id
question_bank_item_id (nullable after authorized cleanup)
input_order
question_id
bank_version
content_digest
license_status
created_at
```

同一 job 的 `input_order` 和 `question_bank_item_id` 均唯一。membership 只允许来自冻结时 active/licensed/safe 的 Context 条目，并进入 input count/manifest 与 writeback 重检；它证明“发送过什么”，不证明模型实际借鉴、访谈事实正确或候选可发布。

### 4.37 question_candidate

```text
id
question_generation_attempt_id
ai_derived_output_id
generation_origin
question_text
reason_text
purpose
risk
normalized_question_digest
selection_score
selection_policy_version
similarity_policy_version
created_at
```

`generation_origin`：`model_generated`。第一版每个 succeeded suggestion attempt 恰好一条 candidate，且 candidate 与 `output_type=question_candidate` 的 derived output 一对一；continue/unavailable 没有 candidate 或 derived output。

- candidate 不要求单一题库来源。模型可以原样采用、广泛改写参考题，或完全根据可信访谈上下文生成题库外问题；
- candidate 的 `purpose` 是本次模型输出的受控业务分类，不要求等于任何参考 item 的 purpose；
- 模型为其使用的具体人物、事件、地点、时间、关系、选择或因果前提声明本次 Context 中的 segment/memory grounding；后端只验证 ID membership 并把声明写为 derived dependency，不用复杂自然语言规则证明语义蕴含。通用开放问题可以没有 grounding；题库引用不能代替访谈事实依赖；
- question generation 对题库、转录、memory、actual-question、边界和授权等源事实只读；candidate、derived output、attempt 与展示事实只允许追加或按正式状态机失效，不得反写来源；
- `normalized_question_digest` 使用 `07` §9 的版本化规范化规则；`selection_score` 为服务端 comparator 的内部 `[0,1]` 值，不是模型 confidence，也不得进入公共 DTO。正文属于 AI 派生敏感业务数据，普通读取必须经动态 policy projection；不得复制到技术日志。

### 4.37A question_candidate_bank_reference

候选与题库的 0..N 可选 attribution，只记录 `InterviewDirectorOutputV1.declared_bank_references` 中模型声明实际借鉴的条目：

```text
question_candidate_id
question_bank_item_id (nullable after authorized cleanup)
question_id
bank_version
bank
purpose
reference_usage
created_at
```

`reference_usage` 的允许值以 Output Schema 为唯一技术定义。每行必须指向同一 ai_job 的 §4.36A seen membership；未知、重复或未发送的 ID 由确定性后端拒绝。空集合合法；不得把 Context 中全部 seen 条目自动写成 inspiration。它只记录模型自述 attribution，不证明模型确实使用、访谈事实正确或候选可发布。一个引用被清理或退休不得删除已经展示的历史快照。

### 4.38 question_display_snapshot

一次成功展示的不可变事实。

```text
id
session_id
question_candidate_id (nullable after retention/deletion cleanup)
display_sequence
published_presentation_revision
question_text
reason_text
normalized_question_digest
selection_score
selection_policy_version
similarity_policy_version
evidence_manifest_hash
memory_manifest_hash
role_watermark_hash
boundary_policy_revision
purpose
journey_stage
journey_policy_version
retention_policy_version
expires_at
retention_state
retention_hidden_at
retention_cleanup_request_id
retention_cleanup_attempt_count
retention_cleanup_started_at
retention_cleanup_error_code
displayed_at
created_at
```

- snapshot 复制展示当时的 question/reason，并通过 dependency/membership 关联实际 evidence、memory resolution、逐 session role watermark 和边界版本；这些字段创建后不可修改；
- snapshot 复制候选 purpose 与阶段/policy 版本；可选题库参考通过独立 snapshot reference membership 保存稳定 ID/版本/usage，不要求存在，也不复制来源页面或许可全文；
- `display_sequence` 是在 session display-state 行锁内分配、从 1 开始严格递增且永不复用的会话内发布序号；`published_presentation_revision` 是该快照成为 canonical current 时的 display-state revision。`displayed_at` 只作时间证据，不承担排序唯一性；
- “真正展示”定义为服务端已经原子发布到 canonical current projection，不要求客户端像素曝光 ACK。客户端崩溃、离线或未绘制不能删除或分叉该权威历史；
- 普通说话人/文字/记忆修正、普通冲突或单独 sensitive 不修改/删除 snapshot，也不自动重算；candidate future eligibility 立即失效；
- snapshot 作为历史事实保留不代表普通查询永远可返回正文。当前 visibility 由 §4.39 动态控制；技术日志只记 snapshot ID、版本和时间。
- snapshot 是独立 retention root，因为 candidate 失效后展示事实仍可能按另一期限保留；它及其 owned display/event 子记录共同到期，子记录不得拥有独立 retention deadline。candidate root 先清理时 `question_candidate_id ON DELETE SET NULL`，snapshot 复制的正文仍只按自身 root、动态 visibility 和 deletion policy 决定是否保留/返回；

### 4.39 question_display_state 与 question_evidence_event

```text
question_display_state
  session_id
  current_snapshot_id
  presentation_revision
  next_display_sequence
  manual_intent_sequence
  presentation_kind
  visibility
  withdrawal_reason
  policy_revision_checked
  last_auto_published_at
  last_manual_attempt_accepted_at
  updated_at

question_evidence_event
  id
  session_id
  snapshot_id
  request_id
  event_type
  event_at
  actor_id
  retention_owner_kind
  retention_ai_job_id
  retention_display_snapshot_id
  metadata_json
```

`presentation_kind`：`suggestion|continue_listening|unavailable`；`visibility`：`visible|withdrawn|none`。`replaced` 只由旧 snapshot 的不可变 event/outcome 表达，不是唯一 current state 的 visibility。`presentation_revision` 从 0 开始，在每次 canonical projection 变化时递增；`next_display_sequence` 从 1 开始，只在发布新 snapshot 时分配后递增；`manual_intent_sequence` 从 0 开始，在接受 manual next 时递增，即使当前 projection 尚未变化也能阻止旧 automatic 写回。

`event_type` 冻结为：`displayed|automatic_replace_succeeded|manual_next_requested|manual_next_committed|manual_next_failed|presentation_continue_listening|presentation_unavailable|hard_withdrawn`。`manual_next_requested` 只证明请求被接受；只有 `manual_next_committed` 且其结果真正离开旧 snapshot 时，才可作为 §4.41 `explicitly_replaced` 的证据。请求失败、响应未知或仅被节流均不得冒充换题成功。

权威 publication 规则：

- 以 `request_id -> project -> session -> question_display_state` 固定锁序执行；先动态复核 assignment、授权、项目、边界、deletion、candidate eligibility 和 retention，再对 `basis_presentation_revision/manual_intent_sequence` 做 CAS；
- 发布 suggestion 时，在一个事务内完成 attempt/candidate/derived output（若尚未完成）、分配 display sequence、创建 snapshot、为旧 current 写 replace event、更新唯一 state、写新 publication event 和幂等响应；snapshot 本身保持不可变，任何一步失败均不得出现半个 snapshot 或 current 漂移；
- 发布 `continue_listening|unavailable` 时不创建伪 snapshot；更新 presentation kind/revision 和 event。历史 snapshot 保持不可变，current pointer 是否保留只作受限追溯，普通 projection 不返回其正文；
- hard withdrawal 在权威边界事务或下一次读取时把 visibility 失败关闭并写 `hard_withdrawn`；它不创建替代 attempt。边界解除也不能自动恢复旧 snapshot 为 visible/current；
- 历史顺序固定为 `(display_sequence desc, id desc)`。浏览位置只存在于客户端 history state、URL/sessionStorage 或请求 cursor，不写本表、不写 event、不需要 request ID，也不改变 current、eligibility、排除集合或 actual-question；
- 新 automatic publication 到达时，如果客户端正在浏览历史，只更新 current revision 和“有更新”提示；不得改变历史锚点、抢焦点或强制跳回 current。

- 普通修正可以得到 `visibility=visible` 且 candidate future ineligible；
- `restricted|do_not_ask|活动 deletion scope|授权/访问失效` 命中，或 boundary/policy revision 变新但尚未安全重评时，普通 projection 立即失败关闭：`presentation_kind=continue_listening|unavailable`、`visibility=withdrawn`，question/reason/evidence 正文不返回且不自动生成替代问题；
- 权威查询必须每次重检当前 assignment、授权、project/boundary/deletion 状态；`policy_revision_checked` 只是缓存证明，不能代替重检；
- event metadata 只保存必要 ID、分类和版本，不复制问题/转录正文。
- `retention_owner_kind` 为 `ai_job|display_snapshot`，两个 owner FK 恰好一个非空：生成过程事件随 job root；display/replace/withdraw 事件随当时 snapshot root。跨 root 引用必须先使依赖输出失效/隐藏，再 `SET NULL` 或显式清理，不能用 `RESTRICT` 阻塞到期或删除。

### 4.40 actual_question_analysis

一次会后实际问题整理的发布版本。

```text
id
project_id
session_id
ai_job_id
ai_derived_output_id
analysis_revision
status
judgeability
transcript_status
semantic_match_version
replaces_analysis_id
created_at
published_at
```

`status`：`pending|running|succeeded|failed|cancelled|superseded`；`judgeability`：`judgeable|unjudged`。

- `(session_id, analysis_revision)` 唯一；同 session 同时至多一条 published current analysis；
- 只有 transcript `drained`、输入均为 trusted interviewer conversation final、job 成功且写回复核通过时才可 `judgeable` 并原子切换 current published version；
- 只有上述可发布版本绑定唯一的 `actual_question_catalog` derived output；该 derived output 的 `business_output_id` 等于 analysis ID，并对整版 questions/evidence/outcomes 共同判定资格；
- ASR `degraded|not_started`、角色不可信、证据不足、job/Schema/过滤失败时为 unjudged。unjudged/failed 版本不得覆盖该 session 以前可靠 published 目录；首次无可靠目录时只暴露 analysis state，不伪造空的“没有问过”；
- 显式重试使用新 request/job，保留旧可靠目录直到新 judgeable 版本原子发布。

### 4.41 actual_question、evidence 与 suggestion_outcome

```text
actual_question
  id
  actual_question_analysis_id
  session_id
  question_text
  normalized_digest
  source_kind
  asked_at_ms
  created_at

actual_question_evidence
  id
  actual_question_id
  ai_job_input_segment_id
  transcript_segment_id
  evidence_order
  created_at

suggestion_outcome
  id
  actual_question_analysis_id
  question_display_snapshot_id
  outcome
  matched_actual_question_id
  semantic_match_version
  created_at
```

`source_kind`：`interviewer_spontaneous|matched_system_suggestion`。`outcome`：`actual_asked|explicitly_replaced|not_observed|unjudged`。

- actual question 可跨多个相邻 trusted interviewer final，evidence order 稳定；unknown、calibration 和 elder final 不得作为问句证据；
- `actual_asked` 必须关联 actual question；`explicitly_replaced` 必须有不可变 replace event；`not_observed` 只允许 judgeable 分析在完整可信输入中得出；unjudged 不作否定结论；
- 同一 snapshot 的 outcome 优先级为 `actual_asked > explicitly_replaced > not_observed`；analysis unjudged 时统一为 `unjudged`，独立 replace event 仍保留但不冒充已问；
- 第二次访谈防重复只消费 current published、eligible actual question；其他 outcome 不进入，且不降低未来资格，除非未来专项正式决定；
- `QuestionEvidenceModule` 是这些表及 §4.36-4.39 的唯一 repository/service owner。DEV-006 建立 actual-question 写模型；DEV-007 只能通过该模块写展示/换题事实和读取防重复目录。

### 4.42 interview_context_snapshot

记录第二次开场或一次 question generation 实际继承的项目上下文，不是用户可见记忆回顾。

```text
interview_context_snapshot
  id
  project_id
  consumer_session_id
  ai_job_id
  ai_derived_output_id
  policy_revision
  memory_count
  memory_manifest_hash
  actual_question_count
  actual_question_manifest_hash
  created_at

context_snapshot_memory
  context_snapshot_id
  memory_resolution_id
  resolution_revision
  input_order

context_snapshot_actual_question
  context_snapshot_id
  actual_question_id
  input_order
```

- snapshot 与 `output_type=context_snapshot` 的 derived output 一对一；membership、两个业务 expected count/manifest 和 derived dependency expected count/manifests 在同一事务冻结，只包含当时 current/eligible memory 与 current published/eligible actual questions；写回必须逐项重检，任一 analysis 在两事务间 supersede 时取消 job，不得发布旧 question membership；
- 明确换掉、not observed、unjudged、旧更正值、restricted 或 deletion 命中内容不得进入；
- snapshot 只用于过程复盘和生成 provenance，不授权普通 UI 展示记忆列表。

### 4.43 状态转换、删除与 legacy 默认值

- `ai_derived_output`: `current -> invalidated -> waiting_recompute -> current(new output)`；失败为 `recompute_failed`，旧 output 不回 current。`review_required` 只能由有权人工处置或新正式分析取代；
- displayed snapshot 永不被普通修正改写；display state 可 `visible -> replaced|withdrawn`，hard withdrawal 不得自动回 visible。边界人工解除后是否重新生成必须由新的用户/正常 trigger 决定，不能恢复旧正文；
- correction、boundary、授权、assignment 和 deletion producer 必须在自己的权威事务中递增相应 revision/写不可变 membership。AI 查询用 anti-join/version/policy 谓词立即过滤，不要求 C2 回写尚不存在的 AI 表；
- retention root 只允许三类：① `ai_job`，拥有该 job 的 scope/input membership、provider call、derived output、output dependency，以及 automatic memory claim/resolution、question attempt/candidate、boundary candidate、generated session note、actual-question analysis/catalog/evidence/outcome、context snapshot/membership；② `question_display_snapshot`，拥有不可变展示副本和 display/replace/withdraw event；③ `memory_retention_root`，拥有不隶属 AI job 的 human-confirmed/system-migration claim、resolution 和 member。`actual_question_analysis` 不是第四个 root，它随自己的 `ai_job` root；

```text
memory_retention_root
  id
  project_id
  source_kind
  source_operation_id
  retention_policy_version
  expires_at
  retention_state
  retention_hidden_at
  retention_cleanup_request_id
  retention_cleanup_attempt_count
  retention_cleanup_started_at
  retention_cleanup_error_code
  created_at

ai_retention_cleanup_audit
  root_scope_hmac
  root_kind
  retention_policy_version
  expired_at
  cleanup_request_id
  result_code
  completed_at
```

- `memory_retention_root` 是一次 human-confirmed 或 system-migration 记忆写操作的 root；`source_kind=human_confirmation|system_migration`，`source_operation_id` 绑定不可变业务操作且全局唯一。一次操作可拥有 1..N 非 AI claims 与 0..N resolutions，每条非 AI claim/resolution 恰好属于一个 root；自动生成记忆只属于 ai_job root，禁止双 owner；
- root 的 `expires_at` 在创建时取该类别策略期限、全部可识别来源证据期限、所引用其他 retention root 期限的最早值；具体天数由策略配置解析，本 SPEC 不拍固定值。子表不保存 retention policy/version/deadline；业务用途的 `boundary_candidate.expires_at` 不参与这一规则；
- 到期清理固定为：原子 `active -> hidden`，立即从 current/published/display projection 脱钩并使依赖它的其他 root/derived output hidden 或 ineligible；再置 `purging`，先删/去标识正文与跨 root membership，后删 owned membership/事件/业务输出/derived output/provider call/input scope，最后删除 root。owned FK 使用 `ON DELETE CASCADE` 或同序显式幂等清理；current/published/display 等跨 root 指针使用 `ON DELETE SET NULL` 或先显式 detach，禁止 `RESTRICT`；
- 清理幂等键绑定 `(root_kind, root_id, expires_at, retention_policy_version)`。中断重试从持久状态续跑；失败置 `cleanup_failed`、告警并保持普通查询隐藏和 future ineligible，绝不回到 `active`。完成后只保留上述最小 `ai_retention_cleanup_audit`；`root_scope_hmac` 用独立 pepper 生成，表中不得保留 root/business UUID、membership、正文、memory value、问题、对象键或 provider payload；
- 跨 root 依赖被清理前，先按 reverse dependency 查找并隐藏依赖输出；随后目标 FK 可 `SET NULL`。§4.32 的 expected count/manifest 校验保证缺行、空 FK 或剩余子集永远不能重新 eligible。普通 correction 不触发 retention cleanup；它只立即改变 future eligibility，原始证据继续按自己的 root 生命周期保留；
- active deletion scope 命中任一 `ai_job` input membership 时，因为 provider call、input scope 与多个输出共享同一 root，必须先隐藏并清理整个 ai_job root，不得只删一条 dependency 后保留无法证明隔离的过程树。同 job 的 correction/policy 失效仍按 §4.32 的业务输出最小单元执行。若同一 scope 也命中已展示 snapshot 的复制正文/证据，则另行隐藏并清理该 snapshot root；candidate job 被清理本身不自动删除未命中 deletion 的历史 snapshot；
- project 删除完成时删除全部 memory/question/job/context/process 业务记录。session/segment scope 删除命中 membership 时删除或去标识化相应过程关系，并删除无法证明与范围隔离的派生正文；只保留独立 pepper 生成的 scope 摘要和最小 audit，provenance FK 不得以 RESTRICT 阻塞清理；
- 当前 runtime 尚无 deletion producer/read model。以上是 DEV-008D 必须接入的正式目标；CON-023 在 producer、统一 reader、C2 回接与并发测试完成前保持 OPEN，coverage 明确为 `NOT IMPLEMENTED / NOT VERIFIED`；
- forward migration 对新表默认空。新增 association/expected count/manifest/retention owner 字段先 nullable expand；只对能从正式 job 与业务 FK 无歧义证明的记录回填。无法证明唯一业务关联或完整依赖的 derived/business row 置 `review_required` 且 future ineligible，expected count/manifest 不得用现存残行猜测；
- legacy root 不得用“迁移当天 + 默认天数”伪造历史期限。能解析创建时策略与来源期限的记录按最早值回填；不能证明的 root 回填当前策略版本、`retention_state=hidden`、`expires_at=migration_started_at` 并排入幂等清理，普通读取不得在 backfill 窗口放行。全新 root 必须在创建事务写非空 policy/version/expiry；child retention 字段若历史存在，迁移完成后删除且不作为权威；
- 若环境存在旧候选数据：无完整 provenance 的 memory/suggestion 迁为 `review_required|unjudged` 且 future ineligible；旧 suggestion 不推定已 display；旧 action 不推定 actual asked；旧转录 authority 不提升，`text_revision=0`；迁移前过程记录标记 `not_available_legacy`，不得伪造完整回放；
- 新表、枚举和字段只允许前向 migration。回滚依靠备份恢复或修复 migration；迁移必须验证空库和含旧 trusted/untrusted transcript、C2 membership 的升级，原始 text/role/authority/revision 不得改变。

### 4.44 浏览器本机录音副本与删除回执

本节描述当前 origin 下 `elder-interview-audio-buffer` IndexedDB 的客户端持久事实，不是 Prisma 或服务端业务数据库模型。现有 v4 stores 至少包括：

```text
chunks                       # legacy，可能仍保留迁移前 Blob
archive-chunks               # 当前不可变 archive，by-session
delivery-queue               # 待交付引用，by-session
session-state                # sequence/timeline/ACK 高水位
upload-jobs                  # 正式 interview job 与 interruption report
capture-checkpoints          # active/dirty/stopped 检查点
canary                       # 存储可写性探针，不属于 session 删除范围
```

DEV-008A3 必须用前向 IndexedDB upgrade 增加必要的 session 索引和最小 `local-deletion-receipts` store。一次本机删除只作用于目标 `session_id`，在同一个 `readwrite` transaction 中清理：

- `archive-chunks` 和 `delivery-queue` 的全部目标 session 记录；
- `session-state`；
- `job_id=interview-capture:{session_id}` 的正式 upload job；
- 所有 `record_type=capture-interruption-report-v1` 且 `session_id` 匹配的记录；
- `local_job_id=interview-capture:{session_id}` 及任何目标 session checkpoint；
- legacy `chunks` 中全部目标 session Blob；
- 原子写入不含音频、正文、对象键、checksum、manifest 或个人信息的最小回执：`session_id`、`deleted_at`、`contract_version=local-audio-archive-v1`、`result=deleted`。

回执只证明该 origin 曾成功提交本机事务，用于刷新后投影和幂等 replay；它不是服务端 deletion request、隐私删除审计或跨设备事实。重复请求若目标 payload 已为空且回执匹配，返回稳定 `already_deleted`，不得新建服务端记录或改变 `deleted_at`。若浏览器清站导致 archive 与回执一同消失，只能投影 `missing_unknown`，不能推断用户删除。

本机 projection 以同一 fresh snapshot 按唯一优先级计算：`active/dirty > pending delivery > server unverified > receipt/empty > verified local completeness`。Schema 的 `state_basis` 固定携带 `active_or_dirty`、`server_manifest_verified`、`deletion_receipt_present`、`local_archive_complete` 四个布尔事实，使优先级可机械验证。因此 active/dirty 与 pending 同时命中时只能为 `blocked_active_or_dirty`；无 active/dirty 且 pending≥1 才为 `blocked_pending_delivery`；本机有 payload 且无法 fresh 证明服务端 manifest 时才为 `blocked_server_unverified`。无 payload 时，有回执为 `deleted_on_device`、无回执为 `missing_unknown`；只有 server verified 后才在 `available_complete|available_incomplete` 中二选一。

交叉字段是正式不变量：`available_complete` 必须 bytes/chunks≥1、pending=0、playback=true；`deleted_on_device|missing_unknown` 必须 bytes/chunks/pending=0 且 playback=false；`available_incomplete|blocked_*` 均 playback=false；`blocked_pending_delivery` 必须 pending≥1。本机删除成功与 `already_deleted` 的 `deleted_at` 都取回执中首次 commit 时间且非空、重放稳定；任何 blocked/lock/transaction-aborted 结果的 `deleted_at=null`。`local-audio-archive-v1` Schema 和正反例 fixture 必须机械拒绝矛盾组合。

删除事务前置不在 IndexedDB 自己猜测。持有同一 session 的浏览器排他锁后，必须 fresh 读取普通 canonical session snapshot 与服务端 manifest，并在锁内重读本地 job/checkpoint/delivery；只有 `capture.status=stopped`、session `status=processing|completed`、服务端 manifest 完整且身份匹配、pending delivery 为 0、无 active capture 且 checkpoint 不 dirty 时才可进入 transaction。A3 还必须机械证明 `finalization.total_size_bytes = manifest.total_size_bytes = manifest chunks.size_bytes 求和 = 本机 archive bytes`，并与 expected/manifest/local chunk count、manifest checksum 和逐片元数据同时一致。字段缺失、`null`、unsafe、legacy 或任一 mismatch 都投影为 server unverified，禁止播放和删除。任何 preflight、请求、游标或 transaction 失败都不得写回回执或部分清理；事务 abort 后刷新看到原集合，事务 commit 后刷新看到全部 payload 缺失与回执存在。

## 5. 索引建议

- `elder_project(status, updated_at desc)`
- `project_assignment(user_id, revoked_at)`
- `interview_session(project_id, sequence_no)`
- `audio_object(project_id, purpose, created_at desc)`
- `audio_object(session_id, purpose)`
- `audio_chunk(audio_object_id, sequence_no)`
- `transcript_segment(session_id, start_ms)`
- `transcript_segment(session_id, created_at)`
- `transcript_segment(session_id, ingest_key)` 唯一
- `transcript_segment(speaker_stream_id, speaker_provider_id, start_ms)`
- `speaker_stream(session_id, capture_generation_id, opened_at)`
- `speaker_mapping(speaker_stream_id, speaker_provider_id, superseded_at)`；另以部分唯一索引保证同一 speaker stream/provider label 只有一条当前映射
- `speaker_calibration_attempt(speaker_stream_id, attempt_no)` 唯一；每流仅一条 collecting 的部分唯一约束
- `speaker_calibration_attempt_segment(speaker_calibration_attempt_id, transcript_segment_id)` 唯一
- `speaker_remap_preview(request_id)` 唯一
- `speaker_remap_preview(session_id, status, expires_at)`
- `speaker_remap_preview_segment(speaker_remap_preview_id, transcript_segment_id)` 唯一
- `speaker_correction_operation(session_id, revision_after)` 唯一
- `speaker_correction_operation(request_id)` 唯一
- `speaker_correction_operation_segment(speaker_correction_operation_id, transcript_segment_id)` 唯一
- `transcript_segment(session_id, text_revision, speaker_role_revision)`
- `transcript_text_correction(request_id)` 与 `(transcript_segment_id, revision_after)` 唯一
- legacy `memory_claim(project_id, memory_type, canonical_key, created_at desc)`；P1 增加 `(project_id, semantic_kind, canonical_key, created_at desc)`
- `memory_claim_evidence(memory_claim_id, transcript_segment_id)` 唯一；另建 `transcript_segment_id` 反向索引
- legacy `memory_resolution(project_id, memory_type, canonical_key, status)` 与 P1 `(project_id, semantic_kind, canonical_key, status)` 使用互斥部分索引；两者分别对 `status=current` 建部分唯一索引
- `memory_resolution_member(memory_resolution_id, memory_claim_id)` 唯一
- `ai_job(request_id)` 唯一；`trigger_dedupe_key` 非空时建立部分唯一索引
- `ai_job(project_id, job_type, status, created_at desc)`
- `ai_job_session_scope(ai_job_id, session_id)` 唯一；另建 `(session_id, created_at desc)`
- `ai_job_input_segment(ai_job_id, transcript_segment_id)` 与 `(ai_job_id, input_order)` 唯一；另建 `transcript_segment_id` 反向索引
- `ai_job_input_memory(ai_job_id, memory_resolution_id)` 与 `(ai_job_id, input_order)` 唯一
- `ai_provider_call(ai_job_id, call_no)` 唯一
- `ai_derived_output(ai_job_id, output_type, business_output_id)` 唯一；另建 `(project_id, output_type, materialized_status, created_at desc)`
- `ai_output_segment_dependency(ai_derived_output_id, transcript_segment_id)` 唯一；另建 `transcript_segment_id` 反向索引
- `ai_output_memory_dependency(ai_derived_output_id, memory_resolution_id)` 唯一；另建 `memory_resolution_id` 反向索引
- `ai_output_question_dependency(ai_derived_output_id, input_order)` 与相应非空目标唯一；三个目标分别建反向索引
- `question_generation_attempt(request_id)` 唯一；另建 `(session_id, created_at desc)`；分别以部分唯一索引限制每个 session 至多一条 active manual-next 和一条 active automatic attempt
- `question_bank_release(bank_version)` 唯一；对每个 environment scope 的 `status=active` 建部分唯一索引，保证运行时只读取一个完整激活版本
- `question_bank_item(question_bank_release_id, question_id)` 唯一；另建 `(question_bank_release_id, bank, enabled)` 供版本内资格过滤，禁止跨 release 拼装题库
- `question_candidate(question_generation_attempt_id)` 唯一
- `question_candidate(ai_derived_output_id)` 唯一
- `question_display_snapshot(session_id, display_sequence desc, id desc)` 唯一稳定顺序；`(session_id, display_sequence)` 与 `question_candidate_id` 唯一；另建 `(retention_state, expires_at)`
- `question_display_state(session_id)` 唯一；`current_snapshot_id` 非空时唯一
- `question_evidence_event(request_id)` 非空时唯一；另建 `(session_id, event_at, id)`
- `actual_question_analysis(session_id, analysis_revision)` 唯一；对同 session 已发布且未 superseded 的版本建部分唯一索引
- `actual_question_analysis(ai_derived_output_id)` 非空时唯一
- `actual_question(actual_question_analysis_id, asked_at_ms, id)`；防重复按 analysis 的 project/session 过滤后使用 `normalized_digest`，不为索引便利复制 project_id
- `actual_question_evidence(actual_question_id, evidence_order)` 与 `(actual_question_id, transcript_segment_id)` 唯一
- `suggestion_outcome(actual_question_analysis_id, question_display_snapshot_id)` 唯一
- `interview_context_snapshot(ai_job_id)` 唯一；另建 `(project_id, consumer_session_id, created_at desc)`
- `interview_context_snapshot(ai_derived_output_id)` 唯一
- `context_snapshot_memory(context_snapshot_id, input_order)` 与 `(context_snapshot_id, memory_resolution_id)` 唯一
- `context_snapshot_actual_question(context_snapshot_id, input_order)` 与 `(context_snapshot_id, actual_question_id)` 唯一
- `content_marker(session_id, marker_type)`
- `audit_log(entity_type, entity_id, created_at desc)`
- `deletion_request(project_id, status, requested_at desc)`
- `deletion_request(session_id, status, requested_at desc)`
- `deletion_request(request_id)` 唯一索引
- `auth_session(token_hash)` 唯一索引
- `auth_session(user_id, expires_at desc)`
- `auth_login_throttle(key_hash, key_kind)` 唯一索引
- `boundary_candidate(session_id, status, expires_at)`
- `boundary_candidate(ai_job_id, created_at)`
- `boundary_candidate(ai_derived_output_id)` 唯一
- `memory_claim(ai_derived_output_id)` 非空时唯一；`memory_resolution(ai_derived_output_id)` 非空时唯一；`session_note(ai_derived_output_id)` 非空时唯一
- `session_note(session_id, note_revision)` 唯一；对 `status=current` 建部分唯一索引
- `memory_retention_root(source_operation_id)` 唯一；另建 `(project_id, retention_state, expires_at)`；非 AI claim/resolution 的 retention owner FK 必须非空且唯一归属
- `ai_job(retention_state, expires_at)`
- `ai_retention_cleanup_audit(root_scope_hmac, retention_policy_version, expired_at)` 唯一；`cleanup_request_id` 唯一
- `deletion_request_transition(deletion_request_id, created_at)`
- `deletion_request_transition(request_id)` 唯一索引
- `idempotency_record(request_id)` 唯一索引
- `audio_object(session_id)` 对 `purpose=interview` 建部分唯一索引
- `session_finalization(session_id)` 唯一索引
- `session_finalization(audio_object_id)` 唯一索引
- `session_finalization(stop_request_id)` 唯一索引
- `session_finalization_chunk(session_finalization_id, sequence_no)` 唯一索引


## 6. 数据模型变更规则

1. 新增字段优先保持向后兼容。
2. 删除、改名或改变字段语义必须提供数据迁移方案。
3. 状态枚举变更必须同步业务流程、API 契约和测试。
4. 数据迁移必须提供回滚、补偿或可验证的恢复方案。
5. 任何模型变更都必须同步更新需求追踪矩阵。

## 7. DEV-008A4 数据语义修订

- `service_term` 表、现有记录、追加式历史、幂等与审计全部保留，不做删除、回填或伪造 migration。普通倾听员主链路不读取其价格/时长作为页面字段，也不以是否存在 current row 决定 project ready、session start 或 capture current gate。
- `draft` project 在 §10 当前适用的 `recording_transcription_ai` consent covered 后可进入 `ready`；session 允许在此前按既有约束提前创建。`ready` 不表示存在 ServiceTerm，也不表示后续 start 可以跳过动态 consent/reminder 重查。
- `interview_session.status=device_check` 是服务端曾接受设备检查的持久事实，不是“当前页面仍已确认麦克风”的持久凭据。当前页检查只存在于页面生命周期，不新增数据库字段；刷新后必须重新检查，首次访谈才可继续录制 consent，任何 session 都必须在 formal start 前重新满足当前页 mic/reminder 门禁。
- formal `AudioUploadJob` 收到与冻结 handoff 完全一致的 complete manifest ACK 后才可从 `uploading|completing` 变为 `complete`；ACK mismatch、未知响应或本机写回失败都不得标 complete。已有 `complete` 状态足够表达本轮修复，无需新增枚举或表。
- `TranscriptSegment.content_kind=speaker_calibration` 继续保留原始证据、attempt membership 与受控审计读取，但普通 transcript/review query 只返回 `conversation`。该过滤不删除或覆盖校准证据，也不改变既有 AI 排除规则。

## 8. Repeat Interview V1 数据与派生事实

本 SPEC 初始冻结时不要求新增 Prisma 字段、表、migration 或所有权。REV-052 定向修复要求在不新增 owner 的前提下为既有 `interview_context_snapshot` 增加 opening provenance 字段，并为既有 `question_generation_attempt` 增加 `interview_context_snapshot_id` 绑定；仍复用 `elder_project`、`interview_session(project_id,sequence_no)` 唯一约束、`idempotency_record`、`session_finalization`、`ai_job`、memory current-resolution、`actual_question_analysis`、`question_generation_attempt(attempt_kind=second_session_opening)`、`interview_context_snapshot` 及其 membership。legacy opening attempt 可保持 snapshot ID 为空且不得伪造 provenance；新 opening attempt 除正式 pre-context unavailable 降级外必须绑定 snapshot。

### 8.1 权威项目动作投影

`repeat_interview` 是普通 project read model 的 additive、非持久化投影：

```text
primary_action      start_next_session | record_formal_consent | null
reason              eligible | no_completed_session | session_in_progress |
                    project_unavailable | consent_reauthorization_required |
                    consent_unavailable | access_unavailable
basis_session_id    uuid | null
basis_sequence_no   positive int | null
next_sequence_no    positive int | null
workflow_version    repeat-interview-v1
consent_continuation ConsentContinuationProjection | null(project/access unavailable only)
```

`repeat_interview` 一旦存在，内部 `consent_continuation` 必须按下表完整出现；只有外层 `repeat_interview` 在 contract-first rollout 期间可缺失，缺失等同失败关闭。服务端按固定优先级取第一个命中项，不能让较低优先级 action 覆盖较高优先级阻断：

| 优先级 | 首个命中条件 | `reason` | `primary_action` | session basis/next | `consent_continuation` |
|---|---|---|---|---|---|
| 0 | assignment 无效、deleted/soft-deleted 或无法安全披露 project | 不返回 ordinary row | 不返回 | 不返回 | 不返回 |
| 1 | ordinary row 已可安全披露，但 action 所需 access reader 不可用 | `access_unavailable` | `null` | 全 `null` | `null` |
| 2 | project 非 active、active deletion/restriction reader 命中或 project 组合不可证明 | `project_unavailable` | `null` | 全 `null` | `null` |
| 3 | 存在任一非终态 session | `session_in_progress` | `null` | 全 `null` | 当前合法 continuation，可为 covered/reauthorization/unavailable |
| 4 | 不存在 canonical completed session | `no_completed_session` | `null` | 全 `null` | 当前合法 continuation，可为 covered/reauthorization/unavailable |
| 5 | consent/policy 必要事实不可用 | `consent_unavailable` | `null` | 全 `null` | 只能是 `unavailable/policy_unavailable` |
| 6 | 当前 consent 明确需要重新取得正式授权且入口可用 | `consent_reauthorization_required` | `record_formal_consent` | 全 `null` | 只能是 `reauthorization_required` |
| 7 | 全部条件满足 | `eligible` | `start_next_session` | 三项均非空 | 只能是 `covered` |

因此“非终态 session + consent reauthorization”组合只能投影 `session_in_progress + primary_action=null + session basis/next 全 null`，同时允许嵌套的 continuation 如实为 `reauthorization_required`；不得显示或接受 `record_formal_consent` project action。类似地，“无 completed + reauthorization”先得到 `no_completed_session/null`。只有没有 session/project/access 高优先级阻断时，重授权才能成为 primary action。完成新 consent 后必须重新查询全部权威事实，不能沿用旧 basis。revoked 已使 project restricted 时不返回 ordinary row/action。

shared TypeScript contract 使用 discriminated union 机械约束每种 reason/action/basis/continuation 组合。DEV-008B1 上线前必须让每个 ordinary row显式投影外层键；restricted projection 永不包含它。

eligible 的必要且充分条件是：当前 actor 为 active interviewer 且 assignment 有效；project 普通可读、未 restricted/deleted/soft-deleted 且 `status=active`；§10 `consent_continuation.status=covered`；至少一条 canonical completed session，basis 为最大 sequence 的 completed session；不存在 `created|device_check|recording|reconnecting|interrupted|stopping|processing` session，也不存在更大 sequence 的终态 session。AI job pending/failed、ASR `degraded|not_started` 和真实 LLM 不可用不影响此资格。已明确需要重授权时 action 为 `record_formal_consent` 而不是 `start_next_session`；任何依赖读取不可用或组合无法证明时返回 null action。

### 8.2 会后 job 与状态投影

completion trigger root 为 `post-session-analysis-v1:{session_id}:{completed_at}`；两个 lane 的 `ai_job.request_id/trigger_dedupe_key` 分别由 root + `memory_extract`、root + `actual_question_reconcile` 确定性生成。同 completion/lane 只能有一个首发 job；显式 retry 保存 `attempt_no`、新 request ID、`retry_of_job_id` 或等价关联，但不得改变 trigger root、session/project membership。

session list 的 additive `post_session_analysis` 只投影 `trigger_identity` 和两 lane 的 `status/job_id/request_id/attempt_no/retryable/error_code/updated_at`，不含转录、memory、问题正文、Prompt 或 provider payload。字段缺失不是成功；`unjudged` 仅用于 actual-question 输入完整性不足，`unavailable` 表示本 lane 的必要 provider/policy seam 当前不可用。terminal 集合固定为 `succeeded|unjudged|failed|cancelled|unavailable`；`not_started|pending|running` 都不 terminal。Home 读取不得创建/重试 job。

### 8.3 Opening Context

opening 的 basis session 是 next-session 创建时已冻结并可由同 project 直接前序 sequence + `next_session.create` 首次响应身份确定的 completed session；刷新或重放不得改绑。协调器状态由持久事实确定性派生，不新增等待表：calibration 非 terminal 为 `waiting_calibration`；calibration 已 terminal但 basis 任一 analysis lane 非 terminal 为 `waiting_basis_analysis`；二者均 terminal、尚无 attempt 为 `ready`。这些状态不创建 `ai_job`/`question_generation_attempt`，不冻结 Context，也不消费 gate。

二者均 terminal 后，opening 继续使用唯一 `question_generation_attempt` owner，`attempt_kind=second_session_opening`，稳定 request/trigger 为 `second-session-opening-v1:{consumer_session_id}:{basis_analysis_trigger_identity}:{calibration_gate_identity}`。terminal calibration attempt identity 必须绑定 consumer 当前 active `speaker_stream_id`、该 stream 当前 active/preparing `capture_generation_id` 对应的 `attempt_id + terminal_status`；旧 stream terminal 不得满足新 stream。provider unavailable 且明确无 attempt时绑定当前 `capture_generation_id + provider-unavailable policy revision`。request ID/trigger dedupe 处理同 gate replay；consumer-session consumed fence 在 session 事务锁内检查任意既有 `second_session_opening` attempt，处理 gate identity 变化后的 exact-once。calibration 事件、两个 lane terminal 事件、进程启动恢复或显式协调可并发重算，但同 consumer 最多一个事务提交 opening attempt。

opening `interview_context_snapshot` 必须持久绑定 `basis_session_id`、`basis_analysis_trigger_identity`、`calibration_gate_identity/confirmed`、memory lane terminal outcome+job ID、actual lane terminal outcome+job ID，以及实际 segment/memory/actual-question membership。Director 的 `question_generate` job 必须以该 snapshot ID 为 source，逐项验证 scope、revision、digest 和 membership 后冻结；不得在 snapshot `create()` 与 Director `prepare()` 分别读取两个时点的集合。snapshot 与 attempt 只保存标量身份绑定，不新增 retention root 或第二 owner。

对应 `interview_context_snapshot.consumer_session_id` 必须是新 session；snapshot 还必须记录 basis session、basis analysis trigger identity 与两 lane terminal outcome/job identity（复用现有 version/digest/process metadata，不复制正文）。`succeeded` lane 的当次 eligible current/published 输出不得因 calibration 抢跑而遗漏；`unjudged|failed|cancelled|unavailable` 是合法 terminal，Context 只省略无法证明的新增 membership并保留 outcome。memory/actual membership 只能来自同 project、在冻结和写回时均 current/eligible 的条目。只有 confirmed calibration gate 可加入当前 stream trusted membership；failed/skipped/unavailable gate 只能消费先前 session 的可信资料。actual-question 必须来自 current published、judgeable、eligible catalog；display snapshot 或 suggestion outcome 不能替代 actual asked。边界、policy、trusted role、retention 或 deletion 漂移按既有 freeze-call-recheck 取消写回。

`ProjectSessionListItem.second_session_opening` 是 additive、内容无关派生投影，状态为 `waiting_calibration|waiting_basis_analysis|ready|running|succeeded|failed|cancelled|unavailable`，并携带 basis session/analysis trigger、calibration gate、request/attempt identity、error code 与更新时间。waiting/ready 的 `request_id/attempt_id` 必须为 null；`updated_at` 取组成当前状态的最新持久事实时间，不得使用 GET 响应时间制造刷新漂移。字段缺失不能解释为已触发或成功。GET/list 只投影，不协调、不消费 gate。

## 9. Streaming ASR Provider V2 持久事实边界

本次契约不新增表、字段、枚举或 migration。既有 `speaker_stream` 仍是 provider namespace 的数据库权威作用域，`transcript_segment.ingest_key` 仍承担 final 幂等，`session_finalization` 仍只保存业务投影 `pending|draining|drained|degraded|not_started`。

adapter attempt、`voice_id`、provider request ID、accepted/sent 计数和结构化 drain receipt 是供应商 port 的运行时/指标证据，不得借此产生第二套 speaker 身份或 session 状态。当前 attempt 的 `final=1`、PCM 终态和 ingestion complete 只形成 attempt receipt；runtime 另行维护 session/capture 级单调 completeness 聚合。任一已知未回补 gap 或无法恢复的 coverage evidence 使聚合 sticky incomplete，后续 attempt success 不得恢复；stop 时投影为既有 `degraded`。只有整场聚合始终无已知 gap、attempt 边界连续且最后 receipt 完整才写 `drained`；从未建立可用 ASR 才写 `not_started`。当前不持久化 attempt/gap 明细且不提供 clear 路径；进程重建无法证明完整时保守 `degraded`。只有未来 `HARDEN-ASR-001` 冻结权威 backfill 覆盖并关闭全部 gap 后才可另行定义重算。若后续 DEV 证明这些证据必须持久化，须先另行更新 `04/05/06/09` 并做 migration 审查，不得在实现中静默加列。

## 10. Continuing Consent V1 派生事实

本 SPEC 不新增 Prisma 字段、表、migration 或持久 reminder acknowledgement。continuing consent 是当前 `consent_record`、服务端版本政策、project/assignment/restriction/deletion 与当前处理配置的动态派生事实。

### 10.1 版本与范围政策

服务端必须维护版本化、可审查的 consent policy：`required_consent_text_version`、该版本覆盖的同 project current/future planned interviews、processing purposes、authorized access subjects、provider processing regions、public/training flags，以及从旧版本到当前版本的有向 compatibility allowlist。精确同版本只默认满足“版本相同”维度，不能替代 scope metadata；不同版本只有在显式 allowlist 证明当前处理范围完全包含于原授权范围时兼容。缺 policy、反向映射、未知版本或任一维度无法证明都不兼容，客户端不得自算。

仓库当前没有经项目负责人/数据治理角色审查并标记 `future_planned_interviews_covered=true` 的真实授权正文或 machine policy；示例 `mvp-v1` 不能因名称、旧 valid 记录或 exact-version 相等被猜成覆盖后续访谈。独立 SPEC-CONSENT-TEXT-POLICY-001 必须接收真实正文、版本/digest 与上述 machine scope 后，真实 `covered` 分支才可交付；本 SPEC 不自行撰写、解释或批准法律文本。在该前置完成前，真实后续 session 的 scope 判定必须失败关闭并要求新正式授权。若未来使用 local/test fixture，必须显式虚构、仅在隔离测试配置可达，生产/真实路径继续失败关闭。

current candidate 只取 §4.5 的最新记录，不回退旧记录。`ConsentContinuationProjection` 是 discriminated union，不允许 status/reason/basis/version/action 自由组合：

| 分支 | 固定组合 |
|---|---|
| covered | `status=covered`、`reason=same_project_planned_interviews_covered`、basis record/version 和 required version 均非空、`required_action=show_recording_reminder` |
| consent missing | `status=reauthorization_required`、`reason=consent_missing`、两个 basis 均为 null、required version 非空、`required_action=record_formal_consent` |
| existing record reauthorization | `status=reauthorization_required`、reason 为 revoked/expired/version incompatible/scope expansion/future not covered 之一、两个 basis 与 required version 均非空、`required_action=record_formal_consent` |
| unavailable | `status=unavailable`、`reason=policy_unavailable`、两个 basis 与 required version 均为 null、`required_action=null` |

current candidate 的 `status=valid`、`revoked_at=null`、未明确过期、文本覆盖未来计划内同 project 访谈，且 compatibility 判定通过时，covered 分支为：

```text
status                         covered
reason                         same_project_planned_interviews_covered
basis_consent_record_id        current candidate id
basis_consent_text_version     current candidate version
required_consent_text_version  server policy current version
required_action                show_recording_reminder
workflow_version               continuing-consent-v1
```

`revoked|expired`、文本不兼容、目的/访问主体/provider region/public-or-training 扩大、未覆盖未来访谈或无记录时为 `reauthorization_required`，reason 使用 shared contract 的对应枚举。该 continuation 分支不等于 project primary action：只有 §8.1 更高优先级均未命中、project 仍 ordinary 可读且正式重授权入口可用时，repeat action 才能是 `record_formal_consent`；revoked/restricted 不向普通投影暴露该动作。policy reader/必要事实不可用只能组成完整 `unavailable/policy_unavailable` null 分支。普通时间间隔、有效 assignment 内操作者变化和设备变化不是 compatibility 维度。

### 10.2 Reminder 与 start

`RecordingStartReminderProjection` 是 session 的非持久化服务端权威投影，固定包含 `recording-reminder-v1`、正式逐字文案、“开始访谈”、`requires_explicit_action=true` 与 `creates_consent_record=false`。它不新增 consent/audio/ack 表；本次开始事实继续由既有 start idempotency/audit 表达。

start 在既有锁序内重算 consent continuation 并核对请求中的 reminder version 与当前服务端版本。二者均匹配才可创建 interview audio/capture generation。任何漂移必须零 audio/capture/ASR/AI/consent 副作用失败关闭。提醒投影、start request/replay、device check、操作者或设备变化都不得修改或追加 consent record。

## 11. LLM Provider Runtime V1 持久事实边界

本节先冻结后续真实 provider runtime 所需的正式表契约；本 SPEC 不修改 Prisma 或 migration。当前 Prisma 尚未实现正式 `ai_job.model_provider`，也缺少 §4.35 新增的逐调用 SDK/package/region/request-ID-source 字段，因此当前 runtime 不具备真实 provider provenance 完整性，后续 DEV 必须先做前向 migration 与 legacy fail-closed 兼容，不能仅改 adapter。真实调用 provenance 必须可逆联表得到，不能用 `local-test-*` 常量冒充真实供应商：

- `ai_job` 保存本 job 冻结的 requested provider/model；正式访谈同一 job 只有一个 active binding；
- `question_generation_attempt.model_config_version/model_config_digest` 与 prompt/context/output/context-builder 等 version/digest 保存不可变生成配置；
- `ai_provider_call` 逐次保存 `call_no`、`primary|same_input_retry`、provider request ID、input/output hash、sanitized token usage、latency、状态、错误分类和时间；
- `ai_model_config_manifest` 保存完整非敏感 canonical config 与 digest；AI SDK core/provider package 精确版本、endpoint/data region、requested/observed model、两类 ID source、config application status 与 sanitized warnings 进入 `ai_provider_call`，不复制 secret、完整 Context、完整 Prompt 或 provider 原文；
- Prisma 前向 migration 还必须实现 manifest FK、四类 provenance 字段和 warning/status；legacy 缺任一值为 incomplete/unjudged，不得从现有 `model_provider`、SDK response ID 或当前 registry 猜回；
- join 中任一关键事实缺失时，该调用 provenance 为 `incomplete/unjudged`，不得推断为真实 provider PASS，也不得回填 `local-test`。

独立多模型横评使用单独的离线 evaluation artifact/root，不创建 `question_generation_attempt`、`question_candidate`、`question_display_snapshot`、`question_display_state` 或 `question_evidence_event`，因此没有写入 current/history 的合法路径。若未来需要持久数据库评测表，必须先做新的数据模型契约与 retention/deletion 审查。

## 12. Staging 持久数据与恢复边界

本 SPEC 不新增表、字段或 migration。部署备份必须覆盖 PostgreSQL 与服务器音频/对象数据，并以登记恢复点关联 migration status、audio manifest、chunk count/bytes/checksum；浏览器 IndexedDB、本机日志和 Cloudflare 日志都不是服务器权威备份。

恢复旧备份后，在允许普通读取前必须重新应用 retention/deletion tombstone/ledger 并验证当前 restriction/deletion projection。DEV-008D/CON-023 尚未提供该闭环，因此本阶段恢复演练只能使用从源头为测试创作的 synthetic/fictional 数据；真实业务备份即使去标识/脱敏也禁止加载。正式 manifest 的 `data_mode=synthetic_only` 不可由恢复成功改变。

## 13. Memory Maintainer V1 持久事实边界

正式契约见 [`memory-maintainer-v1`](docs/contracts/memory-maintainer-v1.md)。`memory_claim/evidence + memory_resolution/member` 继续唯一持有记忆 value、claim、resolution 和 evidence；不得新建另一张 Working value 表。后续 forward-only migration 只允许给现有 claim/resolution 增加 legacy-nullable 的 `semantic_kind=episode|fact`、`layer=working|mid|long`、`source_session_id`、`thread_id`。

新增 `memory_thread/revision`、`memory_boundary/revision/evidence`、immutable `memory_working_snapshot` 及其 resolution/thread/boundary membership、`memory_maintenance_input_segment(new|overlap)` 和成功事务内的 `memory_working_consumption`。这些表只保存稳定身份、append-only revision、冻结 membership 和 consumption，不复制完整转录、Prompt、Context 或 provider 原文。

一次成功 P1 写回必须在同一事务创建 claim/resolution、thread/boundary revision、derived dependencies、Working snapshot/membership、new segment consumption，并以 `ai_job.status=running` CAS 提交 succeeded。任一步失败全部回滚。Legacy 新字段为空表示 layer/thread/semantic authority unavailable，禁止猜回 Working 或 revision 0。P3/P4 后续只读取最新动态 eligible 且完整 committed 的 snapshot；running/failed/半完成记录不可见。

## 14. Memory Maintainer V1.1 前向数据修订

PR #66 接收的 v1 保留为 `accepted-history/pre-runtime-superseded`；任何 runtime 只能在 [`memory-maintainer-v1.1`](docs/contracts/memory-maintainer-v1.1.md) exact-head 独立 PASS 并 merge 后加载 v1.1。本节不修改 Prisma/migration，只冻结后续 forward-only migration 的唯一目标。

1. `transcript_segment.text_revision` 是非负修订次数：`0`=从未正式修订，`N`=已正式修订 N 次。DB、Context/input membership、Decision Trace 与 writeback CAS 的 segment key set、unique count 和逐 key revision 必须完全相等；任一层缺失、额外、重复、`+1/-1` 或把 0 当缺失均失败关闭。
2. 新增 `MemorySemanticStatus(current|uncertain|disputed)`，只给 `memory_resolution.semantic_status` 新增 legacy-nullable 列。legacy `NULL`=semantic authority unavailable，禁止推断。既有 `MemoryResolutionStatus(current|pending_review|superseded)` 继续只表示 lifecycle。
3. 非 disputed 时 `MemoryValueKind exact|range|unknown` 分别映射 `MemoryResolutionKind single|range|unknown`。disputed 必须是 `conflict_set`，target 必须存在于同 project current Context 且 expected revision 精确相等，member 至少含两个不同、属于该 target 当前 membership 的 eligible claim IDs；automatic `NEW|BRANCH|RELATED` 不得创建 disputed。
4. `AiJobType` 前向增加 `working_memory_maintain` 和 `attempt_no>=1`。现有全局 trigger unique index 由 v1.1 §4 的两个互斥 partial unique indexes 替代：所有 non-maintainer 任意状态仍保持原唯一语义；Maintainer 只对 `pending|running|succeeded` 唯一，failed predecessor 可用相同稳定 trigger identity、新 request ID、`retry_of_job_id` 和连续 attempt 创建 retry。Maintainer 只允许 `memory-p1-v1.1:*` namespace，non-maintainer 禁止占用该 namespace。
5. `memory_working_consumption.transcript_segment_id` 必须 `NOT NULL UNIQUE` 并 `ON DELETE CASCADE` 到 transcript；snapshot/job-input 指针 nullable 且 `ON DELETE SET NULL`。pending 只看 transcript segment ID 是否已有 consumption。AI retention cleanup 不得删除 consumption 或使 segment 重新 pending；transcript 删除才 cascade 删除该子事实。
6. 成功写回在一个事务中建立 business rows、snapshot/membership、每个 new segment consumption 和 succeeded CAS。P1 runtime cutover 后，旧 post-session `MemoryService.extract/memory_extract` 禁止与 scanner 并行写相同 authority；post-session memory lane 只委托同一 P1 final flush 或投影其 terminal outcome。

## 15. Memory Maintainer V1.2 optional tag 与累计触发修订

v1/v1.1 的已接收 Schema、fixtures、migration 与数据历史不可改写。v1.2 只以前向 contract/migration/runtime 替代当前加载版本：

1. P1 `memory_claim.semantic_kind` / `memory_resolution.semantic_kind` 必须是 `episode|fact`；Boundary 继续由独立 `memory_boundary/revision/evidence` authority 持有，不写成第三种 resolution kind。
2. 既有 `memory_type` 枚举列只作为 `memory_tag` wire metadata 的 nullable 存储接缝。P1 output 可省略或显式给 null；有值时仍只能是既有枚举，但不得参与 semantic identity、同批去重、target resolution/thread CAS 或 current-reader eligibility。
3. forward migration 只对 claim/resolution 的 `memory_type` 执行 `DROP NOT NULL`，保留 enum 与 legacy 值；不得用 `event`、`unfinished_story` 或新 sentinel 为无 tag 的 Episode/Fact 补位。
4. 唯一性拆成互斥 partial indexes：legacy `semantic_kind IS NULL` 继续按 `project_id + memory_type + canonical_key (+ revision/status)`；P1 `semantic_kind IS NOT NULL` 按 `project_id + semantic_kind + canonical_key (+ revision/status)`。P1 tag 变化不创建新语义槽。
5. 新 runtime identity 使用 `memory-p1-v1.2:*`。数据库 namespace CHECK 必须同时允许既有 v1.1 与新 v1.2 Maintainer rows，并禁止 non-maintainer 使用任一前缀；历史 key 不迁移、不重写。
6. trigger 先按稳定顺序选出 cap 内本次 `new` eligible、未消费、final、trusted elder、`content_kind=conversation` segments，再对每段 effective text 统一 NFKC、移除 Unicode 空白、按 Unicode code point 计数并求和。overlap、interviewer、已消费、非 conversation、重复 ID 与 cap 外 segment 均不计入。
7. 只有 `(selected new count 达 batch threshold OR oldest selected new 达 time threshold OR session final flush) AND cumulative useful characters >= minimumUsefulCharacters` 才能 freeze Maintainer 调用。普通 batch/time 门禁不满足时不得创建 job、provider call、snapshot、claim/resolution 或 consumption。session final flush 累计不足时允许且必须通过既有 P6 seam 写一条 deterministic `MEMORY_UNJUDGED` terminal system job，使后续 opening 不会永久等待；它仍保存本次 gate 的 reference-only AiJob/maintenance `new` input rows、真实 session scope/input hash，但不得创建 provider call、snapshot、claim/resolution 或 consumption。
8. 每个 v1.2 `working_memory_maintain` Trace 必须以一对一 `decision_trace_memory_trigger_observation` 保存 `ai_job_id`、trigger identity/kind、observation/policy version、selected-new count、cumulative/minimum useful characters 与 selected membership manifest hash；`decision_trace_memory_trigger_segment_membership` 只保存有序 segment ID、text/speaker revision、effective-text digest、useful-character count 与 order。两表与 Trace 同事务写入，不保存完整 Transcript、Prompt、Context 或 provider 原文。
9. Reader 必须经由 Trace 的 `ai_job_id` 与 selected membership refs 重算 count/cumulative/manifest，并逐项校验 project/session、revision、digest、trusted elder conversation 与 source-job membership。任一 source 被修订、删除、过期、隐藏或不符合 authority 时失败关闭。normal job/source/Trace 与 low-content terminal job/source/Trace 都必须各自在同一事务提交；final-low identity suffix 等于 canonical full manifest hash 前 32 hex。startup 对 pre-fix missing-trace orphan 只能建立不可读的 `unavailable/recovered` Trace，不得用当前 config、当前 Transcript 或空 membership 伪造 observation。

正式 machine contract 见 `docs/contracts/memory-maintainer-v1.2.md`。本修订不实现 P2-B/C/D、P3/P4 新功能、真实 provider/data 或 UI。
## P2-A Memory Evolution Contract (formal candidate)

P2-A only defines append-only `MemoryEvolutionCheckpoint`, `CheckpointMember`, `MemoryLayerIdentity`, `MemoryLayerRevision`, `RevisionMember` and reference-only `LongJobProjection`; no Prisma table, enum, migration or runtime writer is introduced here. Resolution/Claim remains the sole value and semantic authority. See `docs/contracts/memory-evolution-v1.md`.

## 16. P2-A1 Semantic Envelope 与 committed authority bridge

P2-A1 仍不新增 Prisma model、字段、枚举或 migration。正式 transient Context 从冻结 checkpoint/final Mid manifest 临时解引用有界 `MemoryResolution/MemoryClaim` value，并把 transcript evidence 冻结为独立 ordered typed membership；Context、provider proposal 和进程内 `ValidatedMemoryMutationPlan` 均不持久化。

`MemoryClaim/MemoryResolution/MemoryEvidence` 继续唯一持有 semantic value、claim、semantic status 与 evidence。plan 不拥有 lifecycle、不预留 new-slot 最终 ID、不改变 source disposition；MemoryModule 后续 runtime 必须重读 authority 并 CAS，成功事务后才能形成 `CommittedSemanticProjection`。该前向 bridge 明确分开 `source_checkpoint_member_refs`、`committed_authority_ref`、`committed_evidence_manifest` 与 target layer identity/revision；旧 P2-A `source_resolution_id` 不得同时冒充输入和新归纳结果。automatic merge 不自动 supersede source authority。

同一 Context 内 source member 的 durable `resolution_id` 全局唯一，不得用不同 source ref 重复包装同一 Resolution。P1 v1.2 的 `canonical_key` 在 Context、Proposal 和 committed authority 中统一为 1-240 字符；241 及以上由 Schema 与 pure validator 双重拒绝。每个 proposal 的 source claim 必须由其声明的 source member 拥有，proposal/committed evidence 唯一键均为 `(proposal_claim_ref_id, evidence_ref_id)`，pair 集合完全相等；同一 evidence ref 可以支持多个不同 claims，但同一 transcript segment identity 在 Context membership 中只允许一个 evidence ref。new slot 的 committed Resolution revision 固定为 1 且 ID 不得命中任何 source Resolution；existing slot 必须属于 proposal source set、匹配 `semantic_kind + canonical_key`，plan 的 expected revision 绑定 source revision，commit 为同 ID 的下一 revision。committed authority 的 `semantic_kind/canonical_key/value_kind/resolution_kind/semantic_status` 必须与 proposal 方言逐字段一致，但不复制 value。

projection-wide 唯一性不得退化为 entry-local：Proposal 全局唯一 `proposal_id`、claim ref 与 target semantic slot；Plan 全局唯一 entry proposal、target slot 与 existing CAS authority，一个 authority 只能被一个 entry 写；Commit 全局唯一 entry proposal、committed Resolution、target slot、claim/evidence pair 与 durable MemoryEvidence ID。同 committed authority ID 出现不同 revision/dialect/slot metadata 同时构成 authority metadata conflict。合法 source ref 与 evidence ref 可跨 entries/claims 复用，不得误禁。

Decision Trace 只持久 proposal/plan/whole-commit digest、typed source membership 和 committed refs；Long/layer/Trace/log/provider receipt 不保存 value、正文、Prompt、Context 或 provider payload。正式 machine contract 见 `docs/contracts/memory-semantic-envelope-v1.md`。

## 17. P2-C forward-only persistence 与 runtime 数据目标

本节是 `MEMORY-T5-T8-P2-C-RUNTIME-001` 的正式实现前置，映射 `T5-T8/P2`、`T18-T19/P6`、`T0/Foundation-Observability`。P2-B machine contract 已接收，但其 `source_p1_final_job_id`/`mid_final`、必填 target revision、string policy version 和 v1.1-only snapshot 不能直接映射当前数据库。本节冻结兼容映射，不改写 accepted artifacts，也不表示 Prisma、migration、repository 或 runtime 已实现。所有数据库变更必须新增 forward migration；既有 migration、Schema/fixture 字节和历史数据不得改写。

字段约定：所有 `*_id` 为 UUID；revision/count/order/policy 数字为非负或正整数；digest 为 lowercase SHA-256 `CHAR(64)`；contract version 为有界 `VARCHAR`；时间为 `TIMESTAMPTZ`。nullable 只表示本节明确允许的 unavailable/未提交事实，Reader 不得猜值。

### 17.1 唯一 semantic authority 与稳定 Resolution identity

1. `memory_claim` 与 `memory_resolution` 是唯一 semantic value/claim/resolution/status authority。`memory_claim_evidence` 只是 claim-scoped evidence link；可跨 claim 复用的 evidence authority 必须是独立的 `memory_evidence_authority`。Checkpoint、layer、Long projection、retention、Decision Trace 均只存 typed reference、revision、status、digest 和 membership。
2. forward migration 新增可被真实 FK 引用的 `memory_resolution_authority` registry。现有 `memory_resolution.id` 保持 append-only revision-row 主键；物理 `memory_resolution.authority_id` FK 保持 nullable 以兼容 legacy，P2 新写入必须由 CHECK/trigger/reader gate 强制非空并 FK 到 registry。legacy NULL 只表示 unavailable，不得被 P2 reader/checkpoint 选中。同一 semantic slot 的完整 revision chain 共享同一 `authority_id`，并建立 `UNIQUE(authority_id, resolution_revision)`。A1/P2-B/P2-C wire 中的 `resolution_id` 映射 registry 的稳定 `authority_id`，不映射某一 revision-row ID。具体字段与 P2-B immutable view 的兼容规则见 `docs/contracts/memory-persistence-p2c-compatibility-v1.md`。
3. new-slot 提交同时分配不同的 `authority_id` 与首个 row `id`，revision=1，`supersedes_resolution_id=NULL`。existing-slot 必须锁定当前 row并验证 `(authority_id, expected_revision, project, semantic_kind, canonical_key, status=current)`；成功事务将旧 row置 superseded，插入新 row `id`、复用同一 `authority_id`、revision=`expected+1`、`supersedes_resolution_id=旧row.id`。禁止复用旧 row ID或原地覆盖 value。
4. 旧 resolution 只在 project、semantic slot、`supersedes_resolution_id` 单链、revision 连续性均可机械唯一证明时回填同一 `authority_id`。断链、分叉、冲突或缺必要 authority 字段的 legacy row 保持 `NULL`，P2 reader/checkpoint 对其返回 unavailable，禁止猜为 Mid/Long 或猜 revision 归属。
5. `memory_claim` 前向增加 `claim_revision INT NOT NULL DEFAULT 1 CHECK(claim_revision=1)`。V1 claim row不可变，既有 row 可确定性回填1；更正产生新 claim ID而非 claim revision。P2-B `claim_revision` 映射该值；未来若允许大于1必须新契约。
6. `memory_evidence_authority.authority_revision` 是唯一 evidence authority revision owner，首版为 `INT NOT NULL DEFAULT 1 CHECK(authority_revision=1)`。`memory_claim_evidence`/独立 bridge 只镜像并校验 `(claim_id,evidence_id,authority_revision)` pair/parity，不拥有、不推进 authority revision，也不得被称为 authority。P2-B evidence `source_revision` 只映射 owner 的 `memory_evidence_authority.authority_revision`，不映射允许为0的 transcript `text_revision`；实际 text/speaker revision继续从所引用 `ai_job_input_segment` 逐项校验。
7. `AiJobType` 前向增加 `mid_online`、`mid_final`、`long_session_end`，`AiJobStatus` 前向增加 `unavailable`。P2 stable trigger identity 必须使用版本化 `memory-p2-v1:*` namespace；非 P2 job 禁止占用该 namespace。

### 17.2 不可变 checkpoint 与成员

`memory_evolution_checkpoint` 至少包含：`id`、唯一 `root_identity`、`project_id`、`source_session_id`、`trigger_kind`、`trigger_identity`、`p2_producer_job_id`、`ai_policy_revision INT`、`retention_policy_version INT`、独立 contract identity `p2_policy_contract_revision/p2_retention_contract_version`、`deletion_scope_digest`、`deletion_scope_policy_revision`、`source_working_snapshot_id/source_working_snapshot_contract_version`、`source_resolution_manifest_hash`、`source_thread_manifest_hash`、`source_boundary_manifest_hash`、`source_thread_id/source_thread_revision_id/source_thread_revision/source_thread_status`、可空 `source_p1_terminal_job_id/source_p1_terminal_status/source_p1_terminal_outcome`、`source_set_kind`、`mid_expected_count/mid_manifest_hash`、`current_expected_count/current_manifest_hash`、`expected_member_count`、`member_manifest_hash`、`evidence_manifest_hash`、`manifest_algorithm_version`、`lifecycle_status`、`committed_at`。Checkpoint 不含 `p2_policy_revision/p2_retention_policy_version`，不提供第二个 P2 string policy source；legacy Int snapshot、contract identity 与 projection string policy 必须分域校验，禁止替代或 cast。freeze 成功后全部不可变；deletion scope digest 必须与 job projection 和 memory Trace source reference 逐字段一致并参与 CAS。

禁止持久化歧义字段 `source_p1_final_job_id`。P2-A wire 的该字段只映射 `source_p1_terminal_job_id`；P2-A `checkpoint.ai_job_id` 与 P2-B validator误当作 `mid_final` 的 job reference 均映射 `p2_producer_job_id`。两个 FK 必须不同 ownership：前者只指 P1 `working_memory_maintain` final lane terminal，后者只指 P2 `mid_online|mid_final` job。

| checkpoint kind | source Working snapshot | source thread revision | source P1 terminal | P2 producer |
|---|---|---|---|---|
| online `semantic_park` / `capacity_checkpoint` | required；历史v1.1或当前v1.2 | required；status按trigger为parked或active | 必须NULL | required `mid_online` |
| `session_final_flush` successful freeze | required；当前producer必须v1.2 | required；final authority revision | required terminal P1 final lane | required `mid_final` |
| final prerequisite unavailable | 不创建checkpoint | 不创建checkpoint | 保存于P2 job terminal provenance | `mid_final` terminal unavailable，零target |

successful final的`source_p1_terminal_job_id`必须指`AiJob.job_type=working_memory_maintain`、terminal succeeded、同project/session且属于P1 final trigger namespace；`source_p1_terminal_outcome`必须证明完整snapshot可读。final-low `MEMORY_UNJUDGED`、failed/unavailable或缺snapshot只可进入P2 job unavailable provenance，不可建立伪checkpoint。

Long 不创建第二个 Working checkpoint；`long_session_end` 通过 P2 job projection 引用已提交的 `source_final_mid_checkpoint_id`，并由该 checkpoint追溯真实 P1 terminal。

`memory_evolution_checkpoint_member` 至少包含：`checkpoint_id`、`resolution_row_id`、`resolution_authority_id`、`resolution_revision`、`semantic_status`、`claim_count`、`boundary_status`、`membership_digest`、`input_order`；`resolution_row_id` FK 精确绑定 row，且其 authority/revision必须等于成员快照。必须对 `(checkpoint_id,resolution_authority_id)` 与 `(checkpoint_id,input_order)` 唯一，count/order/digest/manifest 与 source authority机械重算完全一致。

### 17.3 layer identity、revision、member 与 Long projection

- `memory_layer_identity`：`id`、`project_id`、`origin_session_id`、必填 `origin_thread_id`、`origin_resolution_authority_id`、`identity_key_digest`、`created_at`。稳定 identity tuple 与 digest 各自唯一；A→B→A 必须恢复原 identity，不能创建第二条 identity。Long 沿用 source Mid identity 的 origin thread；无法证明时只能 unavailable。
- `memory_layer_revision`：`id`、`identity_id`、`layer(mid|long)`、`revision_no`、`lifecycle_status`、`project_id`、`source_session_id`、`source_checkpoint_id`、`source_job_id`、`resolution_row_id`、`resolution_authority_id`、`resolution_revision`、`semantic_status`、可空 `predecessor_revision_id`、`expected_member_count`、`member_manifest_hash`、`manifest_algorithm_version`、`created_at`。row FK 与 authority/revision必须一致；`(identity_id,revision_no)`唯一，同 identity同时最多一个current；非首版predecessor必须是同identity直接前版且不得被另一revision复用。
- `memory_layer_revision_member`：`revision_id`、`memory_claim_id`、`claim_revision`、`role`、`input_order`、`evidence_membership_digest`；对 claim authority 与 order 分别唯一，且集合必须等于对应 committed semantic projection。
- `memory_long_job_projection`：`id`、唯一 `ai_job_id`、唯一 `target_layer_revision_id`、`source_final_checkpoint_id`、`source_mid_manifest_hash`、`deletion_scope_digest`、`expected_source_count`、`source_manifest_hash`、`created_at`。
- `memory_long_job_projection_source`：`projection_id`、`source_session_id`、`source_mid_revision_id`、`membership_digest`、`input_order`；session/revision 和 order 均唯一。Long 只接受 P1 final、P2 final Mid 与显式 current resolution 两个分域的完整 manifest，禁止 P1 读取 Long 或在 write side 检索 Long。

`memory_p2_job_projection` 是现有 `ai_job` 的一对一 extension，不是第二 coordinator。它至少包含：`ai_job_id` PK/FK、`job_kind`、`trigger_identity_hash`、可空 `source_checkpoint_id/source_final_mid_checkpoint_id/source_p1_terminal_job_id/source_working_snapshot_id/source_thread_revision_id`、`source_revision_digest`、`deletion_scope_digest`、`deletion_scope_policy_revision`、`p2_policy_revision VARCHAR`、`p2_retention_policy_version VARCHAR`、`p2_policy_contract_revision`、`p2_retention_contract_version`、`target_slot_digest`、可空 `target_layer_identity_id/target_layer_revision_id/target_revision_digest/terminal_error_code`。现有 `AiJob.policyRevision` 与 `retentionPolicyVersion` 保持 Int且仅作为 legacy snapshot；P2-B string view 只能读取 projection 的两个专用 VARCHAR 字段，禁止从旧 Int 隐式 cast。P2 policy 字符串与 AiJob 整数快照分别参与各自 CAS。

pending/running/failed/cancelled/unavailable job 的 `target_layer_revision_id` 与 `target_revision_digest` 必须为NULL；new identity的 `target_layer_identity_id` 也必须为NULL。只有成功写回事务分配最终 revision/新 identity ID并同时补齐 projection。P2-B required target字段只在由 succeeded rows导出的 committed contract view中出现，不能反向要求durable未提交job预留ID。

### 17.4 Evidence authority、冻结输入与引用一致性

1. A1 的 `memory_evidence_id` 在 P2-C 映射独立 `memory_evidence_authority.evidence_id`；现有 `memory_claim_evidence.id` 仍是 claim/evidence link 或 pair ID，不是可跨 claim 复用的 authority，也不新建 `MemoryEvidence` value 表。
2. P2-C 首版只接受 `source_kind=transcript_segment`。每条 committed evidence 必须引用该 P2 job 冻结的 `ai_job_input_segment` 行，并校验 project/session、segment ID、text revision、speaker-role revision、effective-text digest、trusted elder conversation 和 input order；禁止保存或复制 transcript text。
3. checkpoint evidence manifest、A1 Context evidence membership、`memory_claim_evidence` 与 committed projection 的 `(claim_ref,evidence_ref)` pair 集合必须完全相等。同一 evidence 可支持不同 claims，但同一 pair、durable evidence ID 或 frozen segment identity 不得重复。
4. P2-B 中其他 source kind 未经新正式契约和 migration 不得由 P2-C 猜映射。

### 17.5 AiJob retention root、typed target 与删除 FK

不得新增`memory_persistence_retention_root`，也不得扩展只属于human-confirmed/system-migration的`memory_retention_root` ownership。所有自动P2 job、checkpoint、layer revision、Long projection、Trace、Claim/Resolution/Evidence link都继承其现有`AiJob` root的Int policy/version、expiry与唯一`RetentionState(active|hidden|purging|cleanup_failed)`。

新增`memory_p2_retention_target`只是AiJob-owned typed child，不是root：至少含`id`、`ai_job_id`、`target_kind(checkpoint|layer_revision|job|trace)`、四个互斥可空target FK、`input_order`、`created_at`；exactly-one-target、`(ai_job_id,target_kind,target_id)`与`(ai_job_id,input_order)`唯一。不得新增`long_projection` root kind；Long projection 只能通过其 owning job/layer revision target被保护。P2-B `retention_root` view由AiJob root + typed target投影得到；`root_kind=job`映射AiJob本身。

P2-B状态映射不得改旧enum：`expired`是`expires_at<=now()`但尚未完成hide的判定；`detached`是hidden/purging期间已完成跨root detach的cleanup阶段；`purged`是root已删除且只剩`ai_retention_cleanup_audit`，三者都不是`RetentionState`新值。`cleanup_failed`保持hidden/ineligible并按同cleanup identity重试，绝不回active。

typed FK inventory：所有 live authority/reference FK（AiJob→P2 projection/checkpoint/retention target/Trace、checkpoint→members、layer revision→members、Long projection→sources、Trace→typed children、跨root source/target authority）统一 `ON DELETE RESTRICT`；不得用数据库 `CASCADE`。cleanup前先隐藏依赖root，再由 purge repository 按固定锁序显式删除 owned children；唯一允许 `SET NULL` 的关系是 nullable cleanup/audit pointer，例如 cleanup job pointer。`memory_claim/memory_resolution/memory_claim_evidence` 始终禁止数据库级CASCADE。逐条 `name/from/to/nullable/onDelete/deferrable` 清单以 `docs/contracts/memory-persistence-p2c-physical-fk-v1.json` 为唯一机器依据，任何 NULL/detach都会使reader失败关闭。

### 17.6 memory Decision Trace typed child

`decision_trace` parent 前向支持 `trace_kind=memory_layer_evolve` 与 memory outcome；既有 parent 字段不得搬入 child，也不得用 NULL 规避现有非空。P2 memory row 的 parent 固定使用：真实 id/project/session/owner/request/generation/ai-job/timestamp/input-hash/retention facts；`trigger_type=trace_kind=memory_layer_evolve`；正式 `memory_outcome`；`decision_outcome='unavailable'`、`director_invoked=false`、`context_revision=0`、`stage_timings_json={}` neutral sentinel；真实 `status`；现有物理可空 `stage` 对 memory row 必须为 `frozen|proposed|validated|planned|committed|recovered|terminal`；现有物理可空 `error_code` 仅在 failed/unavailable terminal 写稳定 P2 code，其余为NULL；question-only `gate_reason/publication_outcome/working_revision/context_digest` 等可空列保持NULL。新增 `decision_trace_memory_semantic` 一对一 child 只保存非空 `trace_id/ai_job_id/source_manifest_hash/deletion_scope_digest/created_at` 与可空 `proposal_digest/plan_digest/commit_digest`，禁止复制 parent 的 trace/outcome/status/stage/error/sentinel 字段。另设 ordered source membership 与 committed authority reference child rows；五个 typed source FK 均物理 nullable，`source_kind` 必须闭合于 `checkpoint|job|input_segment|evidence|resolution`，并由对应 kind CHECK + `num_nonnulls(...)=1` 决定唯一 target。reader 必须联合校验 parent sentinel、child digest 与 source refs；物理列、非空、索引和正反自检以 `memory-persistence-p2c-physical-fk-v1.json` 为准。

freeze 事务必须同时写 running job、checkpoint、job-input references、running trace 与 trace source members。成功事务必须同时写 Claim/Resolution/Evidence、layer revision/members、projection、三个 digest、committed refs 和 terminal succeeded job/trace。unavailable/failure terminal trace 不得含 committed refs；Trace、普通日志和 provider receipt 永不保存 semantic value、Prompt、Context、Transcript 或 provider payload。

### 17.7 migration 与可见性

DDL由Prisma migration history持有；需要分批backfill的stable authority另建`memory_p2_migration_manifest`，至少含`manifest_id/schema_version/source_version/target_version/mode/status/predecessor_fingerprint/last_resolution_id/error_code/started_at/completed_at`。literal predecessor migration IDs/checksums、**恰好 26 条** migration 的 expected count、Prisma schema fingerprint、canonical predecessor fingerprint（当前值 `2b1a4ba4a0a20f2e986cec7de2c9863dd7a67673abb033406374517e4bafcea6`）、manifest unique key、advisory lock、fresh/upgrade detection、DDL/backfill transaction boundary 与 retry/no-op 规则以 `docs/contracts/memory-persistence-p2c-physical-fk-v1.json` 为唯一机器依据；不匹配即`P2_MIGRATION_UNAVAILABLE`。状态非completed时P2 reader/scanner/provider call全部失败关闭。

P1 snapshot兼容矩阵：历史`memory-maintainer-v1.1` snapshot只读，只有revision/scope/manifest/semantic authority完整时可供历史online/final重放验证；新checkpoint不得由v1.1 producer创建。当前producer只允许`memory-maintainer-v1.2`，保留optional tag、selected-new cumulative trigger与final-low provenance。P2 checkpoint显式保存版本，validator按版本分派；v1.1/v1.2不能互相伪装或改写。混合历史Mid可进入Long，但每个source checkpoint版本必须可追溯且各自通过对应reader。

升级为`upgrading|interrupted|unavailable`、legacy authority无法证明、member/manifest/FK不完整或retention非active时，P2 reader失败关闭。半完成checkpoint/revision、pending/running/failed/unavailable job的业务结果不可见；只有完整成功事务的current revision可读。
