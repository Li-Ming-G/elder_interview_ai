# 00｜项目说明与执行入口

## 1. 项目名称

AI 辅助长者访谈系统

## 2. 项目目标

本项目用于帮助倾听员与长者进行人生故事访谈。

系统负责：

- 保存原始录音；
- 实时语音转录；
- 区分长者与倾听员；
- 维护跨多次访谈的结构化记忆；
- 根据当前谈话生成可选追问；
- 保存重要原话、敏感边界和待确认事项；
- 在统一倾听员网页工作区完成新建、继续和已结束访谈的最小回顾。

系统不代替倾听员进行情绪判断，也不在当前阶段自动生成完整传记。

## 3. 当前阶段

当前处于：

> 探索期 MVP 核心纵向链路验证阶段

本轮目标是以虚构或脱敏数据建立能够验证核心产品假设的最小纵向链路，并逐步用真实实现替换测试夹具。

当前不对公网开放、不处理真实付费客户，不允许使用真实长者访谈资料。生产部署能力、未影响本地内部验证的加固项和完整独立审查覆盖应登记为后续门禁，不得成为边界清晰、可回退的核心原型前置阻塞。

探索期推进优先级为：核心产品假设验证 → 原始数据可靠保存 → 关键业务流程 → 使用体验 → 安全与可靠性加固 → 生产部署能力。原始录音/转录/授权不可覆盖、未授权不得开始、ASR/AI 失败不得损害录音、AI 结论必须回链确定态转录等核心数据安全底线不因探索期而降低。

## 4. 当前版本目标

MVP 只验证一条核心链路：

> 倾听员能够从统一网页工作区创建并完成一次真实访谈，录音和转录可靠保存，AI 能够记住上下文并提供有价值的追问，结束后可回顾记录并管理当前浏览器录音副本。

当前优先验证其中最短的一段，正式命名为“首次访谈最小纵向闭环”：

> 倾听员从一个已分配、已预创建的虚构项目进入访谈准备页，在转录优先的工作台中看到当前对话和一个最佳下一问，并能安全结束访谈。

DEV-005 的三页深链历史证据保持有效；当前下一切片由 SPEC-DEV-008A 冻结统一倾听员工作区、新建访谈完整入口、已结束访谈最小回顾和本机副本管理。它不依赖 DEV-007 聚合验收、正式题库、AI 问题历史或完整回顾。授权、权限、原始数据可靠保存和正式服务器删除治理本身没有被取消或降低。

三页闭环是产品路径，不代表三页都能由一个前端任务独立完成。SPEC-SESSION-END-001 与 DEV-005C 已提供 stop/recover 服务端事实；旧 DEV-005A/B/C 仍只是分别通过的历史切片，正式页面尚未把真实麦克风、本浏览器 archive、上传作业、实时 PCM 与结束 commitments 连成同一对象。当前由 SPEC-DEV-005R 与 DEV-005R1/2C/2/3/4 重建该纵向链路，CON-020 在真实 Chromium 验收前保持开放。

### 当前版本必须完成

1. 长者项目和访谈会话；
2. 服务时长与费用说明记录；
3. 录音、转录、AI 分析授权；
4. 麦克风采集和原始录音分片保存；
5. 实时 ASR 转录；
6. 长者与倾听员角色映射；
7. 原始转录与修订文本分离；
8. 项目级结构化记忆；
9. AI 追问建议和推荐原因；
10. 重要原话、敏感、禁止使用、待确认标记；
11. 多次访谈上下文继承；
12. 统一倾听员网页工作区、最小回顾和本机浏览器录音副本管理；
13. 正式服务器隐私删除治理；
14. 审计、异常降级和基础测试。

### 当前版本明确不做

- 在线支付、退款和发票；
- 老照片、证件和档案资料管理；
- AI 直接通过语音采访长者；
- 自动生成完整传记；
- 文学化润色和章节编排；
- 家属在线审稿；
- 电子书和印刷文件生成；
- 对接印刷厂；
- 树状人生经历展示；
- 完整人物关系图；
- 独立向量数据库；
- 微服务和复杂工作流平台；
- 倾听员派单平台。
- 倾听员端资料导出；未来其他角色确需导出时另立受控任务；
- PWA、Capacitor/WebView 封装和 Android App；当前只做响应式网页，未来 Android 软件在网页 MVP 经真实实践验证后另立产品阶段。

## 5. 文档体系

### 5.1 根目录规范文档

| 文件 | 主要回答的问题 | 主要读者 |
|---|---|---|
| `AGENTS.md` | Agent 能做什么、修改前读什么、哪些事情禁止做 | 所有 Agent、人类负责人 |
| `00-项目说明与执行入口.md` | 当前阶段、本轮任务和阅读入口 | 人、总控 Agent、所有执行 Agent |
| `01-产品需求文档.md` | 产品是什么、给谁用、要解决什么问题 | 产品负责人、设计、研发、Agent |
| `02-项目开发规范.md` | 全项目共同遵守的工程基线 | 全体开发人员和开发 Agent |
| `03-业务流程与交互规范.md` | 用户如何操作、状态如何流转 | 前端、后端、测试 Agent |
| `04-数据模型规范.md` | 数据对象、字段、关系和状态怎样保存 | 后端、数据库、测试 Agent |
| `05-API与实时事件契约.md` | 前后端如何通过 REST 与 WebSocket 通信 | 前端、后端、测试 Agent |
| `06-实时音频与转录规范.md` | 录音、ASR、说话人链路如何实现 | 音频、前端、后端 Agent |
| `07-AI访谈引擎规范.md` | AI 如何记忆、分析和推荐追问 | AI、后端、评测 Agent |
| `08-安全隐私与数据治理.md` | 授权、访问、保存、导出和删除如何处理 | 后端、安全、测试 Agent |
| `09-测试与验收规范.md` | 怎样证明系统可以交付 | 测试、审查、总控 Agent |
| `10-研发协作与交接规范.md` | 任务、分工、契约变更和交接遵循什么规则 | 总控 Agent、所有执行 Agent |

### 5.2 运行态协作文档

位于 `docs/agent/`，记录项目实际上如何推进：

- `README.md`：协作文档入口、各文件职责和当前阅读顺序；
- `00-task-board.md`：当前任务、负责人、状态、依赖和验收依据；
- `01-requirement-traceability.md`：需求、代码、接口、测试和审查的对应关系；
- `02-open-conflicts.md`：当前开放冲突的快速索引；
- `02-conflict-log.md`：文档矛盾、未决问题和最终处理结果的完整历史；
- `03-architecture-decisions.md`：重要技术决策及其原因；
- `04-review-report.md`：当前有效审查结论的索引；历史正文位于 `reviews/`；
- `05-handoff-log.md`：当前交接入口；历史正文位于 `handoffs/`；
- `tasks/`：已进入准备阶段的正式开发任务卡；任务状态仍只以 `00-task-board.md` 为准。

### 5.3 机器可读契约

位于 `docs/contracts/`。当前占位文件只有在明确标记为“正式”后，才能用于代码生成、自动校验或作为高于 Markdown 的实现依据。

## 6. 强制阅读顺序

所有 Agent 必须先完整读取：

1. `AGENTS.md`
2. `00-项目说明与执行入口.md`
3. `01-产品需求文档.md`
4. `02-项目开发规范.md`
5. `docs/agent/README.md`
6. `docs/agent/00-task-board.md`

随后按任务类型读取专项文档，不得只阅读任务描述就开始编码。接手既有任务时，还必须读取 `docs/agent/05-handoff-log.md` 中与该任务相关的最新交接。

## 7. 文档解释优先级

出现冲突时，按以下规则解释：

1. 当前阶段和执行范围以 `00` 为准；
2. 产品目标、用户行为和业务边界以 `01` 为准；
3. 业务流程与状态流转以 `03` 为准；
4. 全局技术基线以 `02` 为准；
5. 数据模型以 `04` 为准；
6. API 与实时事件以 `05` 为准；
7. 实时音频链路以 `06` 为准；
8. AI 行为以 `07` 为准；
9. 安全和数据治理以 `08` 为准；
10. 验收方式以 `09` 为准；
11. 协作方式以 `10` 为准；
12. 实际任务状态以 `docs/agent/00-task-board.md` 为准。

若文档冲突，不得自行选择方便实现的版本。应停止受影响的改动，在 `docs/agent/02-conflict-log.md` 登记，并等待有权角色决策。

## 8. 推荐研发顺序

```text
工程基础
  ↓
项目、服务信息与授权
  ↓
原始录音保存
  ↓
实时转录和说话人映射
  ↓
访谈工作台
  ↓
结构化长期记忆
  ↓
AI 追问
  ↓
回顾与本机副本管理
  ↓
真实访谈验证
```

不得先开发完整 AI 功能，再补录音保全。

## 9. 当前任务导航

文档与工程基线已经建立。DEV-001A 已通过独立验收；DEV-001B 已形成内部身份原型候选 `ab9628b`，但其真实部署安全复审与 CON-008 仍未闭合。

当前核心里程碑为：

> `MVP-V01｜最小纵向核心假设链路`

最小链路使用虚构倾听员与长者项目，分段证明项目/授权/会话、短时录音与原始分片、确定态测试转录、证据关联记忆、单个当前最佳追问和首次访谈页面闭环。历史切片曾以预创建项目深链作为内部验收入口；当前 `SPEC-DEV-008A` 将其纠偏为同一 authenticated home/app shell 下的“新建访谈 → 服务说明 → 正式有效授权 → 准备/设备检查 → 开始访谈”、项目/访谈列表、继续未完成访谈，以及已结束访谈的最小回顾与当前 origin 的 IndexedDB 录音副本管理。测试转录注入只能作为 local/test 内部夹具，不得形成公开产品接口。倾听员端不导出；正式服务器隐私删除由后续 `DEV-008D` 独立实现。当前只交付响应式网页，不做 PWA、Capacitor/WebView 或 Android App。完整任务、依赖、状态和验收依据以 `docs/agent/00-task-board.md` 为唯一动态来源。

## 10. 当前待确认参数

以下内容不阻塞研发，必须做成配置项：

- 基础套餐包含时长；
- 超时计价单位和价格；
- 录音分片时长；
- 原始录音保存期限；
- 转录保存期限；
- 导出文件保存期限；
- AI 自动触发字数和时间阈值；
- AI 最短调用间隔；
- 单次访谈最大时长；
- ASR 最终供应商；
- LLM 最终供应商；
- 是否允许使用境外模型；
- 商业化后的正式授权形式；
- 删除申请处理时限。

## 11. MVP 退出条件

只有同时满足以下条件，MVP 才能进入真实试点：

1. 完成一小时连续录音且无无法解释的缺失；
2. ASR 或 AI 故障时，原始录音仍然可靠保存；
3. 原始转录和修订文本严格分离；
4. AI 建议能够关联原始证据片段；
5. 已拒绝话题不会被再次自动推荐；
6. 两次访谈能够继承同一项目的长期记忆；
7. 倾听员可从统一网页工作区创建可依法开始的访谈，并回顾已结束访谈；
8. 授权、修改和正式服务器隐私删除均有审计记录；本机副本删除明确不冒充隐私删除；
9. `09` 中的发布门禁通过；
10. 项目负责人通过 GitHub 或独立验收 Agent 给出可追溯的通过结论。
